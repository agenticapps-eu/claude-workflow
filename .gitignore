
# Phase 14 — reviewer raw outputs (consolidated into 14-REVIEWS.md)
.planning/phases/*/.codex-review.md
.planning/phases/*/.gemini-review.md
.planning/phases/*/.claude-review.md
.planning/phases/*/.review-prompt.md

# Local state
.claude/
.planning/current-phase
.planning/skill-observations/

# Generated agent-instruction files
#
# CLAUDE.md is deliberately NOT listed here. It stopped being regenerable when it
# took on the spec §11 canonical block (agenticapps-workflow-core §11 MUSTs that
# block in the host's primary project-instruction file, and this is it), so it is
# authored and tracked. AGENTS.md carries no §11 obligation in a Claude host and
# is left untracked.
AGENTS.md

# understand-anything knowledge graph (regenerable: `/understand`)
.understand-anything/

# Ephemeral npm lockfiles produced by running template test suites
add-observability/templates/**/package-lock.json
add-observability/templates/**/node_modules/

# Local working notes
claude-workflow-update-prompt.md

# /cso security audit reports — keep local
.gstack/

# CI-only sibling checkout of agenticapps-workflow-core (canonical spec source
# for the mirror ↔ core-spec guard; see migrations/run-tests.sh)
.core-spec/
