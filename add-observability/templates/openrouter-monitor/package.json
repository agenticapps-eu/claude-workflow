{
  "name": "openrouter-monitor",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Cloudflare Worker that polls OpenRouter /api/v1/key every 15 min for proactive budget alerting. ADR-0030.",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "@sentry/cloudflare": "^8.0.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240909.0",
    "typescript": "^5.4.0",
    "vitest": "^2.0.0",
    "wrangler": "^3.0.0"
  }
}
