---
phase: 25
plan: 05
type: execute
wave: 4
depends_on:
  - 25-02
  - 25-04
files_modified:
  - templates/.claude/scripts/migrate-0019-sentry-crons-and-healthz.sh
  - migrations/0019-sentry-crons-and-healthz.md
  - migrations/0021-add-queue-monitor.md
  - templates/.claude/scripts/migrate-0021-add-queue-monitor.sh
  - skill/SKILL.md
  - add-observability/SKILL.md
  - CHANGELOG.md
  - add-observability/CHANGELOG.md
requirements:
  - SC1
  - SC6
  - SC7
  - D-02a
  - D-02b
  - D-11
  - D-13
  - D-14
autonomous: false
must_haves:
  truths:
    - "templates/.claude/scripts/migrate-0019-sentry-crons-and-healthz.sh apply_root function (D-11 expansion site lines 686-728) is extended so the cf-worker, cf-pages, supabase-edge case copies queue-monitor.ts alongside cron-monitor.ts and healthz-snippet.ts. go-fly-http case is unchanged (D-12 — no queue_monitor.go this phase). The pre-check `if [ ! -f \"$cm\" ] || [ ! -f \"$hz\" ]` extends to also check queue-monitor.ts presence — template files missing must still error."
    - "emit_refuse_artifacts_for (lines 566-591) is extended to also emit a would-be queue-monitor.ts for TS stacks (D-11) when the wrapper would refuse — keeps the refuse-artifact set complete."
    - "Fixtures 08-index-ts-anchored-worker and 09-index-ts-anchored-pages flip from PARTIAL-GREEN (cron-monitor + healthz only) to FULLY GREEN — verify.sh's `test -f \"$ROOT/queue-monitor.ts\"` now passes."
    - "Existing fixture 01-fresh-apply's verify.sh is amended to also require queue-monitor.ts (or 01-fresh-apply remains testing the legacy path AND a new variant 01b explicitly checks queue-monitor.ts presence — planner picks; recommend extending 01 since D-11 expands the canonical 0019 fresh-apply contract)."
    - "migrations/0019-sentry-crons-and-healthz.md gets D-02a amendments: (i) header note `Re-rev 2026-05-31 (Phase 25)` documenting D-11 file-set expansion and D-01 engine widening; (ii) new ## Recovery section documenting the manual recovery path for already-migrated v1.18.0+ projects (delete cron-monitor.ts + healthz-snippet.ts, downgrade skill/SKILL.md to 1.17.0, re-run engine — INFORMATIONAL ONLY; the supported path is D-02b's migration 0021); (iii) updated Files Copied list noting queue-monitor.ts addition. The Frontmatter `from_version: 1.17.0` is UNCHANGED."
    - "migrations/0021-add-queue-monitor.md exists as a new standalone spec mirroring 0019.md section structure (Frontmatter / Goal / Inputs / Apply / Verify / Idempotency / Recovery). Frontmatter: `from_version: 1.19.0`, `to_version: 1.20.0`, idempotency marker `queue-monitor.ts presence` (OQ-11 — standalone structure with delta-only content)."
    - "templates/.claude/scripts/migrate-0021-add-queue-monitor.sh exists as a new engine script — thinner than 0019 (no canonicalize_awk, no all-clean gate; additive single-file copy). Per OQ-8 resolution. Idempotency: `test -f <wrapper>/queue-monitor.ts` → SKIP. Discovery uses the SAME find + classify pattern as 0019 (re-use post-Plan-02 logic; can source-include or copy-adapt — planner recommends copy-adapt to keep migration scripts independent). Bumps SKILL.md to 1.20.0 on success."
    - "Migration 0021 fixture 0021/01-fresh-1.19.0-apply flips from RED (no engine) to GREEN (engine ships, queue-monitor.ts installed)."
    - "skill/SKILL.md frontmatter version 1.19.0 → 1.20.0 (D-14)."
    - "add-observability/SKILL.md frontmatter version 0.8.0 → 0.9.0 (D-13)."
    - "CHANGELOG.md (repo root) has a [1.20.0] entry summarising: Added (withQueueMonitor × 3 stacks, queue-monitor.test.ts × 3, migration 0021, ADRs 0031/0032/0033, fixture 10 strict-Env typecheck); Changed (0019 engine accepts index.ts canonical anchor, CronMonitorSchedule discriminated union, withCronMonitor generic narrowed, add-observability bumped 0.8.0→0.9.0, openrouter-monitor bundled cron-monitor.ts byte-symmetric refresh); Fixed (callbot acceptance items 1-4 from issue #56)."
    - "add-observability/CHANGELOG.md has a 0.9.0 entry covering the four template-surface changes (D-03, D-05, D-07, D-21)."
    - "Full migration test harness `bash migrations/run-tests.sh` passes — 0019 fixtures all GREEN, 0021 fixtures all GREEN, strict-env dispatcher GREEN. Net test surface grew ~21 cases (per RESEARCH.md §Validation Architecture estimate)."
    - "Full template test harness `bash add-observability/templates/run-template-tests.sh all` passes — all five stacks (cf-worker, cf-pages, supabase-edge, react-vite, go-fly-http + openrouter-monitor) green."
    - "Issue #56 has four linkback comments — one per finding (F1 → ADR-0031 + D-01 engine fix; F2 → D-03 + ADR-0032; F3 → D-05 + ADR-0032; F4 → D-07 + ADR-0033 + Migration 0021). Posted via `gh issue comment 56 -b \"...\"`."
  artifacts:
    - path: "templates/.claude/scripts/migrate-0019-sentry-crons-and-healthz.sh"
      provides: "Engine apply_root ships queue-monitor.ts for TS stacks (D-11)"
      contains: "queue-monitor.ts"
    - path: "migrations/0019-sentry-crons-and-healthz.md"
      provides: "D-02a docs amendment — Re-rev note + Recovery section + Files Copied update"
      contains: "Recovery"
    - path: "migrations/0021-add-queue-monitor.md"
      provides: "D-02b new migration spec — from_version 1.19.0 → to_version 1.20.0, additive"
      contains: "from_version: 1.19.0"
    - path: "templates/.claude/scripts/migrate-0021-add-queue-monitor.sh"
      provides: "D-02b new engine — additive single-file queue-monitor.ts copy"
      contains: "queue-monitor.ts"
    - path: "skill/SKILL.md"
      provides: "Frontmatter version 1.20.0 (D-14)"
      contains: "version: 1.20.0"
    - path: "add-observability/SKILL.md"
      provides: "Frontmatter version 0.9.0 (D-13)"
      contains: "version: 0.9.0"
    - path: "CHANGELOG.md"
      provides: "[1.20.0] entry for Phase 25"
      contains: "[1.20.0]"
    - path: "add-observability/CHANGELOG.md"
      provides: "## 0.9.0 entry"
      contains: "## 0.9.0"
  key_links:
    - from: "migrate-0021-add-queue-monitor.sh"
      to: "add-observability/templates/<stack>/queue-monitor.ts"
      via: "single-file additive copy on idempotency-marker-absent v1.19.0 projects"
      pattern: "cp.*queue-monitor.ts"
    - from: "issue #56 acceptance"
      to: "Phase 25 deliverables"
      via: "4 linkback comments via gh issue comment"
      pattern: "gh issue comment 56"
---

# Phase 25 — Plan 05 — Wave 4: Migration 0019 docs + apply-step + Migration 0021 + version bumps + issue #56 closure

> **Branch:** `feat/fix-0019-engine-and-cron-wrappers-v1.20.0`.
> **Wave:** 4. Depends on Plan 02 (engine D-01 fix) AND Plan 04 (queue-monitor.ts ships in templates).
> **Reads first:** `25-CONTEXT.md` (D-02a, D-02b, D-11, D-13, D-14 locked; OQ-8 + OQ-11 planner-resolved in Plan 01), `25-RESEARCH.md` §"D-11 fix site" (apply_root extension diff) + §"Common Pitfalls Pitfall 2" (migration runner contract — why 0021 is needed) + §"Refuse-artifact expansion", `templates/.claude/scripts/migrate-0019-sentry-crons-and-healthz.sh` (full file — apply_root lines 680-735, emit_refuse_artifacts_for lines 566-591), `migrations/0019-sentry-crons-and-healthz.md` (full file — model for 0021.md structure), `migrations/0017-axiom-destination.md` (thinner additive migration precedent), `templates/.claude/scripts/migrate-0017-axiom-destination.sh` (thinner engine precedent — read first 100 lines and the apply_root equivalent for shape), `migrations/run-tests.sh` lines 2071-2144 (test_migration_0019 dispatcher) and the test_migration_0021 stub from Plan 01, `docs/decisions/0031-*.md`/`0032-*.md`/`0033-*.md` (cite in PRs and commit bodies), `.planning/phases/24-openrouter-integration/PLAN.md` (Wave 2/4 — CHANGELOG + SKILL.md bump pattern), Issue #56 in full (https://github.com/agenticapps-eu/claude-workflow/issues/56).
>
> **NOTE on `autonomous: false`:** This plan ends with a `checkpoint:human-action` for the GitHub issue linkbacks (4 comments via `gh issue comment 56`). Everything BEFORE that checkpoint is fully autonomous.

<threat_model>
**Trust Boundaries**

| Boundary | Description |
|----------|-------------|
| Engine apply_root → wrapper directory file write | Engine writes queue-monitor.ts into the resolved wrapper root; same security profile as cron-monitor.ts. |
| Migration 0021 engine → wrapper directory | New engine surface; reuses 0019's pre-classify filter pattern. |
| Operator → `gh issue comment` | Public GitHub issue surface; no secrets posted. |

**STRIDE Threat Register:**

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-25-04 | T (Tampering) | Migration 0021 engine writes queue-monitor.ts to a wrapper dir — same risk as 0019 | mitigate | New 0021 engine inherits 0019's path-prune of `./node_modules`, `./.git`, `$SCAFFOLDER_TEMPLATES_REAL`; copies the same sibling-anchor filter from Plan 02. Fixture 0021/01 + a new 0021/02-stray-index-ts (optional — recommend not adding, 0021 is single-file additive so blast radius is smaller) cover the discovery surface. Severity: MEDIUM. |
| T-25-09 (NEW, plan-local) | I (Info Disclosure) | Issue #56 linkback comments could leak internal architectural detail | accept | Linkback content is ADR citations + public CHANGELOG; no operator data, no secrets. Severity: LOW. |

No HIGH-severity threats. ASVS L1 block-on-HIGH does not trigger.
</threat_model>

## Tasks

<task type="auto" tdd="true">
  <name>Task 5.1: Engine 0019 apply_root extension (D-11) — ship queue-monitor.ts for TS stacks</name>
  <files>templates/.claude/scripts/migrate-0019-sentry-crons-and-healthz.sh</files>
  <depends_on>Plan 02 Task 2.2 (engine resolve_anchor_files in place) AND Plan 04 Task 4.1 (queue-monitor.ts exists in template dirs)</depends_on>
  <read_first>
    - templates/.claude/scripts/migrate-0019-sentry-crons-and-healthz.sh lines 680-735 (apply_root, TS-stack case branch)
    - templates/.claude/scripts/migrate-0019-sentry-crons-and-healthz.sh lines 566-591 (emit_refuse_artifacts_for — refuse-artifact path)
    - 25-RESEARCH.md §"D-11 fix site — apply Files Copied" (verbatim diff sketch)
    - 25-RESEARCH.md §"Refuse-artifact expansion"
    - migrations/test-fixtures/0019/08-index-ts-anchored-worker/verify.sh (the contract requiring queue-monitor.ts at apply time)
  </read_first>
  <behavior>
    BEFORE: apply_root for TS stacks copies cron-monitor.ts + healthz-snippet.ts.
    AFTER: apply_root for TS stacks copies cron-monitor.ts + healthz-snippet.ts + queue-monitor.ts. Pre-check (`if [ ! -f "$cm" ] || [ ! -f "$hz" ]`) extends to also gate on queue-monitor.ts presence. Refuse artifact path also emits would-be queue-monitor.ts.
  </behavior>
  <action>
    **GitNexus pre-edit:**
    ```
    gitnexus_impact({target: "apply_root", direction: "upstream"})
    gitnexus_impact({target: "emit_refuse_artifacts_for", direction: "upstream"})
    ```

    **Edit 1 — `apply_root` TS-stack branch (lines 686-701).** From:
    ```bash
    ts-cloudflare-worker|ts-cloudflare-pages|ts-supabase-edge)
      local cm="$src/cron-monitor.ts" hz="$src/healthz-snippet.ts"
      if [ ! -f "$cm" ] || [ ! -f "$hz" ]; then
        warn "  ERROR: template files missing for stack '$stack' ($cm or $hz)"
        return 1
      fi
      if [ "$DRY_RUN" -eq 1 ]; then
        info "  (dry-run) would copy: $cm -> $dir/cron-monitor.ts"
        info "  (dry-run) would copy: $hz -> $dir/healthz-snippet.ts"
      else
        cp "$cm" "$dir/cron-monitor.ts"    || return 1
        cp "$hz" "$dir/healthz-snippet.ts" || return 1
        info "  migrated: $dir  (stack: $stack) — added cron-monitor.ts + healthz-snippet.ts"
      fi
      ;;
    ```

    To:
    ```bash
    ts-cloudflare-worker|ts-cloudflare-pages|ts-supabase-edge)
      local cm="$src/cron-monitor.ts" hz="$src/healthz-snippet.ts" qm="$src/queue-monitor.ts"
      if [ ! -f "$cm" ] || [ ! -f "$hz" ] || [ ! -f "$qm" ]; then
        warn "  ERROR: template files missing for stack '$stack' ($cm or $hz or $qm)"
        return 1
      fi
      if [ "$DRY_RUN" -eq 1 ]; then
        info "  (dry-run) would copy: $cm -> $dir/cron-monitor.ts"
        info "  (dry-run) would copy: $hz -> $dir/healthz-snippet.ts"
        info "  (dry-run) would copy: $qm -> $dir/queue-monitor.ts"
      else
        cp "$cm" "$dir/cron-monitor.ts"    || return 1
        cp "$hz" "$dir/healthz-snippet.ts" || return 1
        cp "$qm" "$dir/queue-monitor.ts"   || return 1
        info "  migrated: $dir  (stack: $stack) — added cron-monitor.ts + healthz-snippet.ts + queue-monitor.ts (Phase 25 D-11)"
      fi
      ;;
    ```

    **Edit 2 — `emit_refuse_artifacts_for` (lines 566-591).** Read the function to identify how it emits would-be files (it likely writes a `.observability-0019.patch` or similar describing what the engine WOULD have done). Extend the would-be file list to include queue-monitor.ts for the three TS stacks. Mirror the addition shape of cron-monitor.ts in that block (the function probably builds a list of file names then iterates).

    **Verify behaviour:**
    ```bash
    # 1. Fixtures 08, 09 now FULLY GREEN.
    bash migrations/run-tests.sh 0019
    # Expected: 08, 09 PASS — verify.sh's queue-monitor.ts assertion now satisfied.

    # 2. Existing fixture 01-fresh-apply: depending on whether its verify.sh asserts queue-monitor.ts,
    # it may need amending. Read migrations/test-fixtures/0019/01-fresh-apply/verify.sh:
    cat migrations/test-fixtures/0019/01-fresh-apply/verify.sh
    # If it does NOT assert queue-monitor.ts, AMEND it to add:
    #   test -f "$ROOT/queue-monitor.ts" || { echo "queue-monitor.ts not installed (D-11)"; exit 1; }
    # This makes 01 a regression test for D-11 at the canonical fresh-apply path.
    # If it already asserts queue-monitor.ts (was added in Plan 01), no change needed.

    # 3. Fixture 03-hand-modified-refuse: if its verify.sh asserts refuse artifacts,
    # check that queue-monitor.ts is in the expected refuse-artifact list. Amend if needed.

    # 4. All other existing fixtures (02-already-applied, 04, 05, 06, 07, 07-react-vite-only): re-run.
    # 02-already-applied: SKIP_ALREADY path — engine does NOT copy queue-monitor.ts.
    # This is OK — the v1.18.0 already-applied state pre-dates queue-monitor.ts, and migration 0021
    # is the proper path for v1.19.0 projects to pick up queue-monitor.ts.
    ```

    **CAVEAT — fixture 02 (already-applied):** This fixture seeds a project at v1.18.0 (post-0019 state). Engine hits `SKIP_ALREADY` and does NOT copy queue-monitor.ts. This is by design — re-revving 0019 cannot deliver queue-monitor.ts to already-migrated v1.18.0+ projects per researcher Pitfall 2. Migration 0021 (Task 5.4) is the supported path. The fixture's verify.sh must NOT assert queue-monitor.ts presence (read current state and confirm; if it incorrectly asserts, leave it untouched — fixture 02 verifies idempotency, not file-shipping).

    **GitNexus post-edit:** `gitnexus_detect_changes()`.

    Commit: `feat(25): GREEN — engine apply_root ships queue-monitor.ts for TS stacks (D-11)`.
  </action>
  <verify>
    <automated>grep -c "queue-monitor.ts" templates/.claude/scripts/migrate-0019-sentry-crons-and-healthz.sh | awk '{exit !($1>=3)}' && grep -q 'cp "\$qm" "\$dir/queue-monitor.ts"' templates/.claude/scripts/migrate-0019-sentry-crons-and-healthz.sh && bash migrations/run-tests.sh 0019 2>&1 | grep -E "PASS.*08-index-ts-anchored-worker" && bash migrations/run-tests.sh 0019 2>&1 | grep -E "PASS.*09-index-ts-anchored-pages"</automated>
  </verify>
  <acceptance_criteria>
    - Engine apply_root copies queue-monitor.ts for cf-worker, cf-pages, supabase-edge stacks.
    - Engine pre-check errors if template queue-monitor.ts is missing.
    - emit_refuse_artifacts_for extends would-be file list to include queue-monitor.ts.
    - Fixtures 08, 09 FULLY GREEN.
    - Fixtures 01-07 all still PASS (no regression).
    - go-fly-http branch untouched (D-12).
  </acceptance_criteria>
  <done>Engine 0019 ships queue-monitor.ts to fresh-apply v1.17.0 projects; fixtures 08/09 fully green; SC1 satisfied end-to-end.</done>
</task>

<task type="auto">
  <name>Task 5.2: Migration 0019.md D-02a amendments — Re-rev note + Recovery section + Files Copied update</name>
  <files>migrations/0019-sentry-crons-and-healthz.md</files>
  <depends_on>Task 5.1</depends_on>
  <read_first>
    - migrations/0019-sentry-crons-and-healthz.md (full file)
    - 25-CONTEXT.md D-02a (the amendment scope)
    - 25-RESEARCH.md §"Pitfall 2 option (c)" (manual recovery wording — informational only since D-02b is the supported path)
  </read_first>
  <behavior>
    BEFORE: 0019.md describes the v1.17.0 → v1.18.0 migration (cron-monitor.ts + healthz-snippet.ts shipped). No Recovery section. No mention of queue-monitor.ts. No re-rev note.

    AFTER: 0019.md has a re-rev note at top (Phase 25 / 2026-05-31), a new ## Recovery section near the end (informational only — D-02b is the supported path), and the Files Copied list amended to include queue-monitor.ts.
  </behavior>
  <action>
    **Edit 1 — Add re-rev note** near the top, immediately after the frontmatter block:

    ```markdown
    > **Re-rev 2026-05-31 (Phase 25):** This migration's apply engine was extended in Phase 25 to (1) accept `index.ts` as the canonical materialised filename for cf-worker / cf-pages wrappers (was looking only for `lib-observability.ts` — the template SOURCE filename — see [ADR-0031](../docs/decisions/0031-0019-engine-index-ts-anchor.md)); and (2) copy `queue-monitor.ts` alongside `cron-monitor.ts` and `healthz-snippet.ts` for fresh applies on v1.17.0 projects (D-11 — see [ADR-0033](../docs/decisions/0033-with-queue-monitor.md)). Already-migrated v1.18.0+ projects are unaffected by this re-rev (the migration runner uses exact `from_version` matching — see [migrations/README.md](README.md) §"Picking versions"). Such projects pick up `queue-monitor.ts` via [migration 0021](0021-add-queue-monitor.md). The Frontmatter `from_version: 1.17.0` is unchanged.
    ```

    **Edit 2 — Amend Files Copied list** (search for "Files Copied" section heading or equivalent — likely near the apply-engine docs). Extend the TS-stack list:
    ```
    Files Copied (TS stacks — cf-worker, cf-pages, supabase-edge):
    - cron-monitor.ts (Phase 22)
    - healthz-snippet.ts (Phase 22)
    - queue-monitor.ts (Phase 25 D-11 — Cloudflare Queue consumer wrapper, Guarded Shape A per ADR-0029 / ADR-0033)

    Files Copied (Go stack — go-fly-http):
    - cron_monitor.go (Phase 22)
    - healthz_snippet.go (Phase 22)
    - (no queue_monitor.go — out of scope per Phase 25 D-12; Fly HTTP workers have no Cloudflare Queue equivalent)
    ```

    **Edit 3 — Add ## Recovery section** near the end of the file, before any "## See Also" or footer:

    ```markdown
    ## Recovery (informational)

    **Already on v1.18.0+ and need the Phase 25 fixes (queue-monitor.ts + engine widening)?**

    The migration runner uses exact `from_version` matching — re-revving this migration cannot retrigger on projects already at v1.18.0 or higher. There are two paths:

    **Supported path (recommended):** Run [migration 0021](0021-add-queue-monitor.md). It is additive (single-file copy of `queue-monitor.ts`), idempotent (skipped if `queue-monitor.ts` already exists), and bumps `from_version: 1.19.0 → to_version: 1.20.0`. This is what `npx claude-workflow migrate` will run for you on a v1.19.0 project.

    **Manual path (only if migration 0021 cannot apply, e.g., custom version pin):** Delete the wrapper's `cron-monitor.{ts,go}` and `healthz-snippet.{ts,go}` files; downgrade `.claude/skills/agentic-apps-workflow/SKILL.md` `version:` field to `1.17.0`; re-run the engine with the new claude-workflow 1.20.0 install. The engine will treat the wrapper as a fresh-apply target and ship all three files (including `queue-monitor.ts`). NOTE: this is INFORMATIONAL — it works but is friction-heavy. Use migration 0021 unless you have a specific reason not to.

    There is no `--force` flag — adding one was considered and rejected per [ADR-0033](../docs/decisions/0033-with-queue-monitor.md) (semantically confusing; expands engine surface for one-off path).
    ```

    Commit: `docs(25): 0019.md D-02a — Re-rev note + Recovery section + Files Copied (queue-monitor.ts)`.
  </action>
  <verify>
    <automated>grep -q "Re-rev 2026-05-31" migrations/0019-sentry-crons-and-healthz.md && grep -q "## Recovery" migrations/0019-sentry-crons-and-healthz.md && grep -q "queue-monitor.ts" migrations/0019-sentry-crons-and-healthz.md && grep -q "0021" migrations/0019-sentry-crons-and-healthz.md && grep -q "ADR-0031" migrations/0019-sentry-crons-and-healthz.md && grep -q "ADR-0033" migrations/0019-sentry-crons-and-healthz.md</automated>
  </verify>
  <acceptance_criteria>
    - 0019.md has a re-rev note linking ADR-0031 + ADR-0033 + migration 0021.
    - Files Copied list mentions queue-monitor.ts (TS stacks) and explicitly carves out go-fly-http.
    - ## Recovery section explains supported path (migration 0021) AND informational manual path.
    - Frontmatter `from_version: 1.17.0` is unchanged (verify with grep).
  </acceptance_criteria>
  <done>0019.md amended with Phase 25 deltas; D-02a complete.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5.3: Migration 0021 spec + engine — additive queue-monitor.ts for v1.19.0 projects (D-02b, OQ-8, OQ-11)</name>
  <files>
    migrations/0021-add-queue-monitor.md
    templates/.claude/scripts/migrate-0021-add-queue-monitor.sh
  </files>
  <depends_on>Task 5.1 (engine 0019 D-11 in place — 0021 reuses the pattern)</depends_on>
  <read_first>
    - migrations/0019-sentry-crons-and-healthz.md post-Task-5.2 (structural model for 0021.md — OQ-11 standalone with delta-only content)
    - migrations/0017-axiom-destination.md (thinner additive migration precedent)
    - templates/.claude/scripts/migrate-0019-sentry-crons-and-healthz.sh post-Plan-02-and-Task-5.1 (discovery + classify + apply patterns; 0021 reuses these; canonicaliser NOT needed per OQ-8)
    - templates/.claude/scripts/migrate-0017-axiom-destination.sh (thinner engine precedent — read structure)
    - migrations/test-fixtures/0021/01-fresh-1.19.0-apply/verify.sh (the contract this engine must satisfy)
    - migrations/test-fixtures/0021/common-setup.sh (seed_v1_19_0_worker — what state 0021 engine starts from)
  </read_first>
  <behavior>
    Migration 0021 covers: discover wrappers (same find + classify pattern as 0019 post-Plan-02); skip if `queue-monitor.ts` already present (idempotency marker); else copy `queue-monitor.ts` from `add-observability/templates/<stack>/`; bump SKILL.md to 1.20.0. No canonicaliser (additive — no existing file to compare against). No all-clean gate (the migration is purely additive). No refuse path (additive cannot DIRTY-classify).
  </behavior>
  <action>
    **Edit 1 — Create `migrations/0021-add-queue-monitor.md`** following OQ-11 (standalone, mirrors 0019.md sections, delta-only content):

    ```markdown
    ---
    migration_id: "0021"
    from_version: "1.19.0"
    to_version: "1.20.0"
    type: "additive"
    idempotency_marker: "queue-monitor.ts presence"
    related: ["0019", "0028", "0029", "0033"]
    ---

    # Migration 0021 — Add `queue-monitor.ts` for Cloudflare Queue consumer wrappers

    > **Status:** Released 2026-05-31 (Phase 25)
    > **Architecture:** [ADR-0033](../docs/decisions/0033-with-queue-monitor.md)
    > **Related:** [Migration 0019](0019-sentry-crons-and-healthz.md) (the original wrapper migration; this migration delivers the new `queue-monitor.ts` to v1.19.0 projects that Migration 0019 cannot reach by virtue of the runner's exact `from_version` matching).

    ## Goal

    Deliver `queue-monitor.ts` (the Cloudflare Queue consumer wrapper introduced in Phase 25, [ADR-0033](../docs/decisions/0033-with-queue-monitor.md)) to projects already at v1.19.0 — which Migration 0019 cannot retrigger on per the runner's `from_version` contract.

    ## Inputs

    - **Source:** `add-observability/templates/<stack>/queue-monitor.ts` (one of `ts-cloudflare-worker`, `ts-cloudflare-pages`, `ts-supabase-edge`).
    - **Target wrapper directory:** discovered by reusing Migration 0019's classify_stack + index.ts/lib-observability.ts anchor logic (post-Phase-25 widening per ADR-0031).
    - **Project state:** SKILL.md at `version: 1.19.0`.

    ## Apply

    For each discovered wrapper root:
    1. Classify stack (cf-worker / cf-pages / supabase-edge / go-fly-http / unknown).
    2. If stack is `go-fly-http`, SKIP (no queue-monitor.go this phase — Phase 25 D-12; Fly HTTP workers have no Cloudflare Queue equivalent).
    3. If stack is `unknown`, SKIP_UNSUPPORTED.
    4. If `<wrapper>/queue-monitor.ts` already exists, SKIP_ALREADY (idempotent).
    5. Else `cp add-observability/templates/<stack>/queue-monitor.ts <wrapper>/queue-monitor.ts`.

    After all wrappers processed (or no wrappers found — pre-init projects):
    6. Bump SKILL.md `version: 1.19.0` → `version: 1.20.0`.

    ## Verify

    - `test -f <wrapper>/queue-monitor.ts` (per discovered wrapper).
    - `grep -q '^version: 1.20.0$' .claude/skills/agentic-apps-workflow/SKILL.md`.
    - Existing `cron-monitor.ts`, `healthz-snippet.ts`, and wrapper anchor files are untouched.

    ## Idempotency

    Idempotency marker: presence of `queue-monitor.ts` in the wrapper root. Re-running the engine on a wrapper that already has `queue-monitor.ts` SKIPs that wrapper. The migration is purely additive — no canonicalisation, no all-clean gate, no refuse path.

    Implication: an operator who hand-applied `queue-monitor.ts` (e.g., copied from this repo before 1.20.0 shipped) will hit SKIP_ALREADY. To force re-apply (e.g., to pick up a hot-fix), delete the file manually and re-run the engine.

    ## Recovery

    The migration has no recovery surface beyond the manual one: delete `<wrapper>/queue-monitor.ts`, downgrade SKILL.md to `1.19.0`, re-run. There is no destructive action this migration could perform that warrants a more elaborate rollback path.

    ## Why a new migration (not re-rev 0019)?

    See [ADR-0033](../docs/decisions/0033-with-queue-monitor.md) and [Migration 0019.md §Re-rev note](0019-sentry-crons-and-healthz.md). Short answer: the migration runner matches `from_version` exactly per [migrations/README.md §"Picking versions"](README.md). A v1.19.0 project cannot re-trigger Migration 0019 (`from_version: 1.17.0`) — it would never see the new `queue-monitor.ts`. Migration 0021's `from_version: 1.19.0` is the only path that reaches them.
    ```

    **Edit 2 — Create `templates/.claude/scripts/migrate-0021-add-queue-monitor.sh`** as a thinner engine per OQ-8.

    Approach: copy the discovery + classify_stack + resolve_anchor_files + _filter_index_ts_requires_co_anchor sections verbatim from migrate-0019-sentry-crons-and-healthz.sh (post-Plan-02), strip out the canonicaliser + all-clean gate + refuse path. Then write a thin apply step + version bump.

    Sketch (the executor will write the full file based on this skeleton — total ~250-350 LOC):

    ```bash
    #!/usr/bin/env bash
    # Migration 0021 apply engine — add queue-monitor.ts to v1.19.0 wrappers.
    # See migrations/0021-add-queue-monitor.md.
    # See docs/decisions/0033-with-queue-monitor.md.
    #
    # Shape vs 0019: additive single-file copy. No canonicaliser, no all-clean
    # gate, no refuse path. Idempotency by queue-monitor.ts presence.
    set -uo pipefail

    # ─── Boilerplate (copy from 0019: arg parsing for --templates-dir, --dry-run, --project-dir, --pause-between-passes; logging helpers info/warn) ─

    # ─── Discovery (copy from 0019 post-Plan-02: same find + _filter_index_ts_requires_co_anchor + ROOTS array + supabase-edge filter) ─

    # ─── classify_stack (copy from 0019 post-Plan-02 verbatim) ─

    # ─── resolve_anchor_files (copy from 0019 post-Plan-02 verbatim) ─

    # ─── version-bump helper (copy from 0019) ─
    bump_version() {
      # 1.19.0 → 1.20.0
      sed -i.bak -E 's/^(version:[[:space:]]*)1\.19\.0$/\11.20.0/' .claude/skills/agentic-apps-workflow/SKILL.md
      rm -f .claude/skills/agentic-apps-workflow/SKILL.md.bak
    }

    # ─── apply_root_021 (thinner — additive single-file copy) ─
    apply_root_021() {
      local dir="$1" stack="$2" src="$3"
      case "$stack" in
        ts-cloudflare-worker|ts-cloudflare-pages|ts-supabase-edge)
          local qm="$src/queue-monitor.ts"
          if [ ! -f "$qm" ]; then
            warn "  ERROR: template queue-monitor.ts missing for stack '$stack' ($qm)"
            return 1
          fi
          if [ -f "$dir/queue-monitor.ts" ]; then
            info "  SKIP_ALREADY: $dir already has queue-monitor.ts"
            return 0
          fi
          if [ "$DRY_RUN" -eq 1 ]; then
            info "  (dry-run) would copy: $qm -> $dir/queue-monitor.ts"
          else
            cp "$qm" "$dir/queue-monitor.ts" || return 1
            info "  migrated: $dir  (stack: $stack) — added queue-monitor.ts (Migration 0021)"
          fi
          ;;
        go-fly-http)
          info "  SKIP: go-fly-http has no queue_monitor.go in scope (Phase 25 D-12)"
          ;;
        *)
          info "  SKIP_UNSUPPORTED: $dir  (stack: $stack)"
          ;;
      esac
      return 0
    }

    # ─── Main loop ─
    # If no ROOTS, bump version anyway (pre-init projects on-track per 0019 precedent).
    if [ ${#ROOTS[@]} -eq 0 ]; then
      info "no materialised observability wrapper found — nothing to migrate."
      bump_version
      exit 0
    fi

    EXIT_CODE=0
    for dir in "${ROOTS[@]}"; do
      stack=$(classify_stack "$dir")
      src="$(stack_template_dir "$stack")"
      apply_root_021 "$dir" "$stack" "$src" || EXIT_CODE=1
    done

    if [ "$EXIT_CODE" -eq 0 ]; then
      bump_version
      info "Migration 0021 complete — version 1.20.0"
    else
      warn "Migration 0021 had errors; version NOT bumped"
    fi
    exit $EXIT_CODE
    ```

    Make executable: `chmod +x templates/.claude/scripts/migrate-0021-add-queue-monitor.sh`.

    **Verify behaviour:**
    ```bash
    bash migrations/run-tests.sh 0021
    # Expected: PASS 0021/01-fresh-1.19.0-apply (queue-monitor.ts installed, version 1.20.0).
    ```

    **CAVEAT — code duplication between 0019 and 0021 engines:** Per OQ-8 resolution, the planner chose copy-adapt (not source-include) — both engines maintain their own discovery + classify code. Justification: migration scripts are vendored; downstream projects copy the engine into `.claude/scripts/` and edits to claude-workflow's engines don't retroactively apply. Independent scripts are easier to reason about per-migration.

    Future maintenance burden: if Plan 02's `_filter_index_ts_requires_co_anchor` or `resolve_anchor_files` get bugfixes, BOTH engines must be patched. Document this in commit body.

    Commit: `feat(25): GREEN — Migration 0021 spec + engine (D-02b, OQ-8 thinner additive, OQ-11 standalone docs)`.
  </action>
  <verify>
    <automated>test -f migrations/0021-add-queue-monitor.md && grep -q "from_version: \"1.19.0\"" migrations/0021-add-queue-monitor.md && grep -q "to_version: \"1.20.0\"" migrations/0021-add-queue-monitor.md && grep -q "## Idempotency" migrations/0021-add-queue-monitor.md && test -x templates/.claude/scripts/migrate-0021-add-queue-monitor.sh && grep -q "queue-monitor.ts" templates/.claude/scripts/migrate-0021-add-queue-monitor.sh && bash migrations/run-tests.sh 0021 2>&1 | grep -E "PASS.*01-fresh-1.19.0-apply"</automated>
  </verify>
  <acceptance_criteria>
    - migrations/0021-add-queue-monitor.md exists with from_version: 1.19.0, to_version: 1.20.0.
    - Five mirroring sections: Goal / Inputs / Apply / Verify / Idempotency (+ Recovery, + Why a new migration).
    - templates/.claude/scripts/migrate-0021-add-queue-monitor.sh exists and is executable.
    - Engine reuses post-Plan-02 discovery + classify_stack + resolve_anchor_files patterns.
    - Engine does NOT include canonicalize_awk (OQ-8 — additive-only).
    - Fixture 0021/01-fresh-1.19.0-apply PASSES.
  </acceptance_criteria>
  <done>Migration 0021 ships; v1.19.0 projects now have a supported upgrade path; SC1+SC4 closed for downstream consumers.</done>
</task>

<task type="auto">
  <name>Task 5.4: Version bumps + CHANGELOG entries (D-13, D-14)</name>
  <files>
    skill/SKILL.md
    add-observability/SKILL.md
    CHANGELOG.md
    add-observability/CHANGELOG.md
  </files>
  <depends_on>Tasks 5.1, 5.2, 5.3 (all production changes landed before version bump)</depends_on>
  <read_first>
    - skill/SKILL.md (frontmatter version: 1.19.0 currently)
    - add-observability/SKILL.md (frontmatter version: 0.8.0 currently)
    - CHANGELOG.md (existing entries — match the format of [1.19.0] and [1.18.0] entries from Phases 23-24)
    - add-observability/CHANGELOG.md (existing entries — match 0.8.0 format)
    - .planning/phases/24-openrouter-integration/PLAN.md §Wave 4 (the version-bump task precedent)
  </read_first>
  <action>
    **Edit 1 — skill/SKILL.md.** Find the frontmatter line `version: 1.19.0` and change to `version: 1.20.0`.

    **Edit 2 — add-observability/SKILL.md.** Find the frontmatter line `version: 0.8.0` and change to `version: 0.9.0`.

    **Edit 3 — CHANGELOG.md (repo root).** Add a [1.20.0] entry above [1.19.0]:

    ```markdown
    ## [1.20.0] - 2026-05-31

    Phase 25 — fix 0019 engine + ship withQueueMonitor (issue #56).

    ### Added
    - `withQueueMonitor` export in three TS templates (ts-cloudflare-worker, ts-cloudflare-pages, ts-supabase-edge) — Sentry Crons heartbeat wrapper for Cloudflare Queue consumer handlers; Guarded Shape A per ADR-0029 (ADR-0033).
    - `queue-monitor.test.ts` × 3 stacks with 10 behavioural-parity assertions per stack.
    - Migration 0021 (`from_version: 1.19.0` → `to_version: 1.20.0`) — additive single-file copy of `queue-monitor.ts` for already-migrated v1.19.0 projects (ADR-0033).
    - Synthetic strict-Env typecheck fixture at `migrations/test-fixtures/0019/10-strict-env-typecheck/` (D-18) verifying SC5 acceptance — proves callbot-shape strict `Env` interface compiles against `withCronMonitor` and `withQueueMonitor` without consumer-side casts.
    - Three ADRs: 0031 (engine accepts index.ts canonical anchor), 0032 (cron-monitor generic narrowing), 0033 (withQueueMonitor + Migration 0021 rationale).
    - Migration test fixtures 08-index-ts-anchored-worker, 09-index-ts-anchored-pages, 11-stray-index-ts-no-co-anchor (Pitfall 1 negative case).

    ### Changed
    - Migration 0019 engine accepts `index.ts` as canonical materialised anchor for cf-worker / cf-pages (was looking only for the template SOURCE filename `lib-observability.ts`). Existing `lib-observability.ts`-seeded fixtures continue to work (legacy alias) (ADR-0031).
    - Migration 0019 engine apply step copies `queue-monitor.ts` alongside `cron-monitor.ts` and `healthz-snippet.ts` for fresh applies on v1.17.0 projects (D-11).
    - `CronMonitorSchedule` is now a discriminated union — `{ type: "crontab"; value: string } | { type: "interval"; value: number; unit: ... }` — structurally compatible with Sentry's `MonitorSchedule` (was an interface with `value: string` only, which broke interval-variant consumers) (D-03 / ADR-0032).
    - `withCronMonitor`'s generic constraint narrowed from `<E extends Record<string, unknown>>` to `<E extends { SENTRY_DSN?: string; SERVICE_NAME?: string }>` — accepts strict-typed `Env` interfaces without consumer-side casts or index-signature pollution (D-05 / ADR-0032).
    - `add-observability` skill bumped 0.8.0 → 0.9.0 (minor — three template surface changes: schedule type, generic narrowing, new queue-monitor.ts).
    - `add-observability/templates/openrouter-monitor/src/observability/cron-monitor.ts` (Phase 24 bundled subtree) receives D-03 + D-05 byte-symmetric refresh (D-21).
    - Migration 0019.md amended with Phase 25 re-rev note and new Recovery section documenting the supported (Migration 0021) and informational (manual delete + downgrade) upgrade paths (D-02a).

    ### Fixed
    - Issue #56 Finding 1 — engine misses wrappers anchored at `index.ts` (was matching the template source filename, not the materialised target filename).
    - Issue #56 Finding 2 — `CronMonitorSchedule` was structurally incompatible with Sentry's `MonitorSchedule` discriminated union.
    - Issue #56 Finding 3 — `withCronMonitor`'s `E extends Record<string, unknown>` generic forced strict-typed Env consumers to cast at every call site.
    - Issue #56 Finding 4 — no `withQueueMonitor` analog for Cloudflare Queue consumer handlers.
    - callbot v1.16.0 → v1.19.0 migration friction (the four hand-applied workarounds: 0019 marker, LOCAL-PATCH schedule cast, local withMonitor helper, env-as-Record cast) — all four can now be deleted in a downstream callbot PR.
    ```

    **Edit 4 — add-observability/CHANGELOG.md.** Add a 0.9.0 entry:

    ```markdown
    ## 0.9.0 - 2026-05-31

    Phase 25 — fix issue #56 template-surface deltas.

    ### Added
    - `withQueueMonitor` export across `ts-cloudflare-worker`, `ts-cloudflare-pages`, `ts-supabase-edge` templates (D-07/D-08/D-09/D-10 — see ADR-0033).
    - `queue-monitor.test.ts` × 3 stacks (D-17).

    ### Changed
    - `CronMonitorSchedule` is now a discriminated union matching Sentry's `MonitorSchedule` (D-03 — see ADR-0032). Type alias replaces interface.
    - `withCronMonitor`'s generic narrowed to accept strict-typed `Env` interfaces without index signature pollution (D-05 — see ADR-0032).
    - `cron-monitor.test.ts` × 3 stacks gains D-16 type-level firewall + strict-Env runtime test (Pitfall 4 mitigation — explicit `Sentry.MonitorConfig['schedule']` pin).
    - `openrouter-monitor/src/observability/cron-monitor.ts` byte-symmetric refresh (D-21).
    ```

    Commit: `chore(25): bump skill/SKILL.md 1.20.0 + add-observability/SKILL.md 0.9.0 + CHANGELOG entries`.
  </action>
  <verify>
    <automated>grep -q "^version: 1.20.0$" skill/SKILL.md && grep -q "^version: 0.9.0$" add-observability/SKILL.md && grep -q "^## \[1.20.0\]" CHANGELOG.md && grep -q "withQueueMonitor" CHANGELOG.md && grep -q "ADR-0031" CHANGELOG.md && grep -q "issue #56" CHANGELOG.md && grep -q "^## 0.9.0" add-observability/CHANGELOG.md && grep -q "withQueueMonitor" add-observability/CHANGELOG.md</automated>
  </verify>
  <acceptance_criteria>
    - skill/SKILL.md version → 1.20.0.
    - add-observability/SKILL.md version → 0.9.0.
    - CHANGELOG.md [1.20.0] entry has Added / Changed / Fixed subsections covering all five lockable decisions.
    - CHANGELOG entry cites issue #56 and ADRs 0031/0032/0033.
    - add-observability/CHANGELOG.md 0.9.0 entry covers D-03 / D-05 / D-07 / D-21.
  </acceptance_criteria>
  <done>Versions bumped; CHANGELOG narrates the four findings; release is shippable.</done>
</task>

<task type="auto">
  <name>Task 5.5: Full-suite verification + per-plan SUMMARY.md files</name>
  <files>
    .planning/phases/25-fix-0019-engine-and-cron-wrappers/25-05-SUMMARY.md
  </files>
  <depends_on>Tasks 5.1-5.4</depends_on>
  <read_first>none — this task is verification + write-up</read_first>
  <action>
    Run the full test suite end-to-end:
    ```bash
    bash migrations/run-tests.sh
    # Expected: ALL 0019 fixtures PASS (01-07 existing + 08, 09, 11 new) + 0021 fixture 01 PASS + strict-env GREEN. Net delta from baseline: ~5 new dispatcher entries, +21 new test cases.

    bash add-observability/templates/run-template-tests.sh all
    # Expected: all five stacks PASS (cf-worker, cf-pages, supabase-edge, react-vite, go-fly-http, openrouter-monitor). Net delta: +6 cron-monitor.test.ts cases × 3 stacks + 10 queue-monitor.test.ts cases × 3 stacks = +48 cases.
    ```

    Run a final GitNexus sweep:
    ```bash
    gitnexus_detect_changes()
    # Expected scope: engines (0019, 0021), templates (cron-monitor.ts × 4, queue-monitor.ts × 3, test files × 6), docs (3 ADRs, 2 migration docs, 2 CHANGELOGs, 2 SKILL.md files), fixtures (5 new fixture dirs + dispatcher edits). No surprise scope.
    ```

    Write `.planning/phases/25-fix-0019-engine-and-cron-wrappers/25-05-SUMMARY.md` recapping:
    - Plans 01-05 outcomes (one paragraph each).
    - Final test counts (existing + delta).
    - All seven Success Criteria mapped to evidence (specific tests that pass).
    - The four issue #56 findings → resolution citations (ADR + commit SHA + test fixture).
    - OQ-8/OQ-9/OQ-10/OQ-11 final outcomes.
    - Threat-model coverage (T-25-01 through T-25-09 — disposition + evidence).
    - Outstanding items (real callbot adoption PR is a separate downstream effort).

    Commit: `docs(25): final SUMMARY for Phase 25`.
  </action>
  <verify>
    <automated>test -f .planning/phases/25-fix-0019-engine-and-cron-wrappers/25-05-SUMMARY.md && bash migrations/run-tests.sh 2>&1 | grep -E "FAIL" | wc -l | awk '{exit !($1==0)}' && bash add-observability/templates/run-template-tests.sh all 2>&1 | grep -E "FAIL" | wc -l | awk '{exit !($1==0)}'</automated>
  </verify>
  <acceptance_criteria>
    - Both test suites GREEN with zero FAIL.
    - SUMMARY.md exists; documents Plans 01-05, SC mapping, issue resolution traceability.
    - GitNexus detect_changes scope matches expected file list (no surprise scope creep).
  </acceptance_criteria>
  <done>All automated checks GREEN; SUMMARY documents the phase; ready for human checkpoint.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Checkpoint 5.6: GitHub issue #56 linkback comments (SC7)</name>
  <files>(no project files — comments posted via gh CLI to https://github.com/agenticapps-eu/claude-workflow/issues/56)</files>
  <action>Post four linkback comments on issue #56 via the gh CLI. The four comment payloads are documented verbatim in the <how-to-verify> block below. This task is checkpoint:human-action because gh issue commenting writes to a public issue surface; Claude executes the commands but pauses for the user to confirm post-execution via the resume-signal.</action>
  <verify><automated>gh issue view 56 | grep -c "Phase 25" | awk "{exit !(>=4)}"</automated></verify>
  <done>Four linkback comments visible on issue #56; user confirms via resume-signal.</done>
  <what-built>
    Phase 25 delivers all four issue #56 acceptance items: (F1) engine accepts index.ts canonical anchor + Migration 0021 for v1.19.0 projects (ADR-0031, ADR-0033); (F2) CronMonitorSchedule discriminated union (D-03, ADR-0032); (F3) withCronMonitor generic narrowed (D-05, ADR-0032); (F4) withQueueMonitor implementation × 3 TS stacks (D-07, ADR-0033).

    All five plans complete and tested. The phase is feature-branch-merge-ready (`feat/fix-0019-engine-and-cron-wrappers-v1.20.0`).
  </what-built>
  <how-to-verify>
    Post four linkback comments on https://github.com/agenticapps-eu/claude-workflow/issues/56 via the `gh` CLI (per workflow-level CLAUDE.md feature-branch + PR-to-main rule, gh CLI is authorized).

    Comment 1 (Finding 1 — index.ts anchor):
    ```
    gh issue comment 56 -b "Finding 1 (engine misses index.ts-anchored wrappers) — resolved in Phase 25.

    - Engine fix: \`templates/.claude/scripts/migrate-0019-sentry-crons-and-healthz.sh\` now accepts \`index.ts\` as the canonical materialised anchor (with sibling \`middleware.ts\`/\`_middleware.ts\` co-anchor requirement). \`lib-observability.ts\` remains supported as a legacy/fixture alias.
    - Decision rationale: ADR-0031 (\`docs/decisions/0031-0019-engine-index-ts-anchor.md\`).
    - Test coverage: fixtures \`08-index-ts-anchored-worker\` + \`09-index-ts-anchored-pages\` (positive cases); \`11-stray-index-ts-no-co-anchor\` (Pitfall 1 negative case, asserts SKIP_UNSUPPORTED for bare \`dist/index.ts\`).
    - Project upgrade path: pre-1.18.0 projects re-run \`migrate\` (Migration 0019 picks them up); v1.19.0+ projects also get Migration 0021 ([0021-add-queue-monitor.md](../blob/main/migrations/0021-add-queue-monitor.md))."
    ```

    Comment 2 (Finding 2 — schedule type):
    ```
    gh issue comment 56 -b "Finding 2 (CronMonitorSchedule structurally incompatible with Sentry's MonitorSchedule) — resolved in Phase 25.

    - Type fix: \`CronMonitorSchedule\` is now a discriminated union: \`{ type: 'crontab'; value: string } | { type: 'interval'; value: number; unit: 'minute' | 'hour' | 'day' | 'week' | 'month' | 'year' }\`. Verified structurally compatible with \`@sentry/core@8.55.2\`'s \`MonitorSchedule\` (research-verified against installed SDK types).
    - Decision rationale: ADR-0032 (\`docs/decisions/0032-cron-monitor-generic-narrowing.md\`).
    - Test coverage: D-16 type-level firewall in \`cron-monitor.test.ts\` × 3 TS stacks — uses \`expectTypeOf\` + explicit \`const _check: Sentry.MonitorConfig['schedule'] = ourSchedule\` pin to defeat the test harness's skipLibCheck blind spot (Pitfall 4 mitigation).
    - Symmetric refresh: \`add-observability/templates/openrouter-monitor/src/observability/cron-monitor.ts\` (Phase 24 bundled subtree) also receives the fix byte-symmetrically (D-21)."
    ```

    Comment 3 (Finding 3 — generic narrowing):
    ```
    gh issue comment 56 -b "Finding 3 (withCronMonitor's Record<string, unknown> generic forces consumer-side casts) — resolved in Phase 25.

    - API fix: \`withCronMonitor<E extends { SENTRY_DSN?: string; SERVICE_NAME?: string }>\` — strict-typed \`Env\` interfaces (no index signature) now satisfy the constraint natively.
    - Decision rationale: ADR-0032.
    - Test coverage: D-16 strict-Env runtime test in \`cron-monitor.test.ts\` × 3 stacks (declares a CallbotEnv-shape interface without index signature and calls \`withCronMonitor<CallbotEnv>\`) + synthetic SC5 typecheck fixture (\`migrations/test-fixtures/0019/10-strict-env-typecheck/\`) verifying \`tsc --noEmit\` exits 0 against a callbot-shape Env."
    ```

    Comment 4 (Finding 4 — withQueueMonitor):
    ```
    gh issue comment 56 -b "Finding 4 (no withQueueMonitor analog for Cloudflare Queue consumer handlers) — resolved in Phase 25.

    - New export: \`withQueueMonitor<E, Msg>(handler, config?)\` in three TS templates (\`ts-cloudflare-worker\`, \`ts-cloudflare-pages\`, \`ts-supabase-edge\`). Handler signature: \`(batch: MessageBatch<Msg>, env: E, ctx: ExecutionContext) => void | Promise<void>\` (matches Cloudflare's canonical \`QueueHandler<Env, Msg, Props>\` shape verbatim).
    - Guarded Shape A (ADR-0029): handlerStarted flag distinguishes pre-callback transport failure (fall back to unmonitored — queue always runs) from post-callback errors (propagate).
    - Slug resolution: 3-source (explicit > env > auto). Env-key prefix \`SENTRY_CRON_MONITOR_SLUG_\` shared with cron (one operator-facing convention). Default handlerName: \`queue\` → env key \`SENTRY_CRON_MONITOR_SLUG_QUEUE\`. Auto-derive: \`\${SERVICE_NAME ?? 'service'}:queue:\${batch.queue}\`.
    - Multi-queue policy: handlers dispatching by \`batch.queue\` MUST set \`monitorSlug\` explicitly (silent + docs, mirrors Phase 22 D11). Doc-comment enforced in source.
    - Decision rationale: ADR-0033 (\`docs/decisions/0033-with-queue-monitor.md\`).
    - Distribution: pre-1.18.0 projects get \`queue-monitor.ts\` via Migration 0019 expansion (D-11); v1.19.0 projects via new Migration 0021.
    - Test coverage: \`queue-monitor.test.ts\` × 3 TS stacks with 10 behavioural-parity assertions per stack; SC5 acceptance verified via synthetic strict-Env fixture.

    All four findings closed. callbot can now drop the LOCAL-PATCH cast, local withMonitor helper, env-as-Record casts, and hand-applied 0019 marker — separate downstream callbot PR."
    ```

    After posting, verify:
    ```bash
    gh issue view 56 | tail -100 # should show four new comments authored by the workflow operator
    ```

    If `gh` is not authenticated (returns auth-required), Claude must authenticate via `gh auth login` and retry — this is an auth gate, not a checkpoint-blocker.
  </how-to-verify>
  <resume-signal>Type "approved" after the four comments are posted and `gh issue view 56` confirms them. Or describe issues if any comment failed to post.</resume-signal>
</task>

## Verification (Plan-level)

After all tasks land:

```bash
# 1. Engine 0019 D-11 in place
grep -c "queue-monitor.ts" templates/.claude/scripts/migrate-0019-sentry-crons-and-healthz.sh # >= 3

# 2. Migration 0019.md amended
grep -q "Re-rev 2026-05-31" migrations/0019-sentry-crons-and-healthz.md
grep -q "## Recovery" migrations/0019-sentry-crons-and-healthz.md

# 3. Migration 0021 exists
test -f migrations/0021-add-queue-monitor.md
test -x templates/.claude/scripts/migrate-0021-add-queue-monitor.sh
grep -q "from_version: \"1.19.0\"" migrations/0021-add-queue-monitor.md

# 4. Version bumps
grep -q "^version: 1.20.0$" skill/SKILL.md
grep -q "^version: 0.9.0$" add-observability/SKILL.md

# 5. CHANGELOG entries
grep -q "^## \[1.20.0\]" CHANGELOG.md
grep -q "^## 0.9.0" add-observability/CHANGELOG.md

# 6. Full suites GREEN
bash migrations/run-tests.sh 2>&1 | grep -E "FAIL" | wc -l # must equal 0
bash add-observability/templates/run-template-tests.sh all 2>&1 | grep -E "FAIL" | wc -l # must equal 0

# 7. Issue #56 has 4 linkback comments (post-checkpoint)
gh issue view 56 | grep -c "Phase 25" # >= 4
```

## Success Criteria mapping

- **SC1** (engine accepts index.ts) — GREEN end-to-end (fixtures 08, 09 full, 11 negative).
- **SC4** (withQueueMonitor) — GREEN (queue-monitor.test.ts × 3 + fixture 10 SC5).
- **SC6** (test surface extended) — GREEN (~+69 test cases per RESEARCH.md estimate).
- **SC7** (issue #56 closed) — GREEN after Checkpoint 5.6.

## Output

`.planning/phases/25-fix-0019-engine-and-cron-wrappers/25-05-SUMMARY.md` (Task 5.5) is the final phase summary; per-plan SUMMARYs (01, 02, 03, 04) land alongside their executions. After Checkpoint 5.6 resumes, the branch is PR-ready to main (`feat/fix-0019-engine-and-cron-wrappers-v1.20.0` → `main`).

**Workflow-level post-execute gates (NOT plan tasks):**
- `/review` on the full phase diff.
- `/cso` because the phase touches the Sentry SDK boundary (workflow CLAUDE.md condition).
- `/gsd-verify-work` against the SC1-SC7 success criteria before opening the PR.
