---
phase: 30-split-03-claude-workflow-2-0-0-follow-up
plan: 03
type: execute
wave: 3
depends_on: [30-01, 30-02]
files_modified:
  - README.md
  - install.sh
  - setup/SKILL.md
  - templates/config-hooks.json
  - docs/UPGRADING.md
  - CHANGELOG.md
autonomous: false
requirements: [D-04, D-05, D-06]

must_haves:
  truths:
    - "No add-observability references remain in the non-immutable forward-looking files (README, install.sh, setup/SKILL.md, templates/config-hooks.json)"
    - "templates/config-hooks.json line 97 reads observability:scan (not add-observability:scan)"
    - "docs/UPGRADING.md documents the 1.21.0 -> 2.0.0 transition and cross-references the obs repo's README + install.sh (not a non-existent INSTALLATION.md)"
    - "Immutable shipped migrations and historical records (.planning/, CHANGELOG history) are NOT rewritten"
    - "claude-workflow ships 2.0.0: CHANGELOG [2.0.0] section, git tag v2.0.0, breaking-change PR opened"
    - "Full suite green and drift PASS at ship time"
  artifacts:
    - path: "docs/UPGRADING.md"
      provides: "1.21.0 -> 2.0.0 upgrade story"
      min_lines: 20
    - path: "templates/config-hooks.json"
      provides: "observability:scan reference (repointed)"
      contains: "observability:scan"
    - path: "CHANGELOG.md"
      provides: "[2.0.0] breaking-change entry"
      contains: "2.0.0"
  key_links:
    - from: "docs/UPGRADING.md"
      to: "agenticapps-observability repo README + install.sh"
      via: "install instructions cross-reference"
      pattern: "agenticapps-observability"
    - from: "templates/config-hooks.json"
      to: "observability skill scan command"
      via: "skill field repoint"
      pattern: "observability:scan"
---

<objective>
Finish SPLIT-03: rewrite the forward-looking `add-observability` -> `observability` references in the
non-immutable files (D-05), write the downstream upgrade story (D-06), and ship claude-workflow 2.0.0
as a deliberate breaking change (D-04: CHANGELOG, tag, PR).

Reference cleanup scope is NON-immutable forward-looking files ONLY (RESEARCH §5.1): README.md,
install.sh, setup/SKILL.md, templates/config-hooks.json (line 97). Immutable shipped migrations keep
their old-name text — the obs repo's `add-observability` dual-symlink alias (retained through 0.12.0)
resolves them at runtime. Historical records (.planning/, CHANGELOG history entries) are left as-is.

The ship step (tag + PR) is a non-autonomous checkpoint: it requires a human to confirm the 2.0.0
breaking release and merge. (D-04)

Purpose: a clean public-facing claude-workflow that points exclusively at the new `observability`
skill, with a documented upgrade path and a tagged 2.0.0 release. (D-04/D-05/D-06)
Output: cleaned references, docs/UPGRADING.md, CHANGELOG [2.0.0], git tag v2.0.0, breaking PR.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-split-03-claude-workflow-2-0-0-follow-up/30-RESEARCH.md
@.planning/phases/30-split-03-claude-workflow-2-0-0-follow-up/30-CONTEXT.md

<interfaces>
<!-- D-05 forward-looking files with live add-observability counts (verified at plan time): -->
  README.md (3 refs), install.sh (4 refs), setup/SKILL.md (4 refs), templates/config-hooks.json (1 ref, line 97).
  update/SKILL.md = 0 refs (nothing to do). templates/claude-md-sections.md = 0 refs.

<!-- DO NOT rewrite (RESEARCH §5.2 / §5.3): -->
  - migrations/0002, 0011, 0020-* and any *.md migration (immutable; alias resolves)
  - CHANGELOG.md HISTORICAL entries (append-only audit; new [2.0.0] entry uses observability)
  - .planning/** (historical decision record)
  - docs/decisions/** that STAY (e.g. 0035)

<!-- config-hooks.json line 97 (verified): -->
  Current:  "skill": "add-observability:scan",
  Target:   "skill": "observability:scan",

<!-- obs repo install contract for UPGRADING.md cross-reference (verified live; NO docs/INSTALLATION.md in obs): -->
  git clone https://github.com/agenticapps-eu/agenticapps-observability \
    ~/.claude/skills/agenticapps-observability
  bash ~/.claude/skills/agenticapps-observability/install.sh
  # creates ~/.claude/skills/observability (canonical) + ~/.claude/skills/add-observability (alias)
  Reference the obs repo's README + install.sh, NOT a docs/INSTALLATION.md (it does not exist).

<!-- PR title (D-04, conventional-commits breaking): -->
  v2.0.0 chore!: extract observability to agenticapps-observability (SPLIT-03)
<!-- Current feature branch at plan time: plan-30-split-03 -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite add-observability -> observability in the 4 non-immutable forward-looking files</name>
  <read_first>
    - .planning/phases/30-split-03-claude-workflow-2-0-0-follow-up/30-RESEARCH.md §5.1 (rewrite table) and §5.2/§5.3 (do-NOT-touch list)
    - README.md, install.sh, setup/SKILL.md, templates/config-hooks.json (the actual add-observability lines)
    - The <interfaces> block above (file/count list, line 97 target, do-not-rewrite list)
  </read_first>
  <files>README.md, install.sh, setup/SKILL.md, templates/config-hooks.json</files>
  <action>
For EACH of README.md, install.sh, setup/SKILL.md, templates/config-hooks.json: rewrite
forward-looking `add-observability` references to `observability`. Read each ref in context — only
rewrite references describing the CURRENT install/skill name; if any ref is a historical narrative or
changelog-style line, leave it (none expected in these 4 forward-looking files, but verify).

Specifics:
- templates/config-hooks.json line 97: `"skill": "add-observability:scan"` -> `"skill": "observability:scan"`.
  Validate JSON after: `jq . templates/config-hooks.json >/dev/null`.
- install.sh: the install/symlink references to the skill name become `observability` (note the obs
  skill is now a SEPARATE install per D-03 — if install.sh previously linked/expected
  `add-observability` inside the scaffolder repo, update the reference to reflect the separate
  `observability` skill; do NOT add submodule/auto-install wiring).
- README.md + setup/SKILL.md: rename the skill in install/usage instructions.

DO NOT touch: any `migrations/*.md` file, CHANGELOG.md historical entries, anything under `.planning/`,
or docs/decisions ADRs that stay. The obs alias resolves the immutable-migration old-name refs.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && ! grep -q 'add-observability' README.md install.sh setup/SKILL.md && grep -q '"skill": "observability:scan"' templates/config-hooks.json && ! grep -q 'add-observability:scan' templates/config-hooks.json && jq . templates/config-hooks.json >/dev/null && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `! grep -q 'add-observability' README.md` (and install.sh, setup/SKILL.md)
    - `grep -q '"skill": "observability:scan"' templates/config-hooks.json` and `! grep -q 'add-observability:scan' templates/config-hooks.json`
    - `jq . templates/config-hooks.json` valid
    - No `migrations/*.md` modified by this task
    - `.planning/**` and CHANGELOG history NOT modified by this task
  </acceptance_criteria>
  <done>The 4 forward-looking files reference observability; config-hooks.json line 97 repointed; JSON valid; immutable + historical files untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Write docs/UPGRADING.md (1.21.0 -> 2.0.0) + CHANGELOG [2.0.0] entry + README link</name>
  <read_first>
    - .planning/phases/30-split-03-claude-workflow-2-0-0-follow-up/30-RESEARCH.md §7 (obs install contract — no INSTALLATION.md), §8.3 (UPGRADING.md location = docs/)
    - .planning/phases/30-split-03-claude-workflow-2-0-0-follow-up/30-CONTEXT.md D-06
    - CHANGELOG.md (head — current latest entry format, e.g. [1.21.0])
    - The <interfaces> block above (obs install commands, PR title)
  </read_first>
  <files>docs/UPGRADING.md, CHANGELOG.md, README.md</files>
  <action>
1. Create `docs/UPGRADING.md` documenting the 1.21.0 -> 2.0.0 transition. Content:
   - Why 2.0.0 is breaking: observability extracted to agenticapps-observability; claude-workflow no
     longer ships the observability skill.
   - What changed: `add-observability/` removed; migrations 0012/0013/0017/0018/0019/0020/0021
     tombstoned (chain stays contiguous); new migration 0022 repoints the install.
   - What downstream projects must do: install agenticapps-observability separately (two independent
     installs, D-03), using the exact obs install commands from the <interfaces> block. Cross-reference
     the obs repo's README + install.sh (NOT a docs/INSTALLATION.md — it does not exist, RESEARCH §7).
   - Note the `add-observability` alias still resolves (retained through obs 0.12.0) so existing
     immutable-migration references keep working.
   - How to upgrade: run `/update-agenticapps-workflow` (applies 0022 -> bumps to 2.0.0). The migration
     aborts with install instructions if the observability skill is absent.
2. Add a `## [2.0.0]` section to the TOP of CHANGELOG.md (above the latest existing entry) describing
   the breaking extraction, the tombstones, migration 0022, and the #58 deterministic Phase Sentinel
   hook. Do NOT rewrite older entries.
3. Add a one-line link to docs/UPGRADING.md from README.md (forward-looking; fine to edit README here).
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && test -f docs/UPGRADING.md && grep -q '2.0.0' docs/UPGRADING.md && grep -q 'agenticapps-observability' docs/UPGRADING.md && ! grep -q 'INSTALLATION.md' docs/UPGRADING.md && grep -q '2.0.0' CHANGELOG.md && grep -qi 'UPGRADING' README.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `test -f docs/UPGRADING.md` with the 1.21.0 -> 2.0.0 story (>= 20 lines)
    - `grep -q 'agenticapps-observability' docs/UPGRADING.md` (obs install cross-ref present)
    - `! grep -q 'INSTALLATION.md' docs/UPGRADING.md` (does not reference the non-existent file)
    - CHANGELOG.md has a `[2.0.0]` section at the top; older entries unchanged
    - README links to docs/UPGRADING.md
  </acceptance_criteria>
  <done>docs/UPGRADING.md written with the obs-repo cross-reference; CHANGELOG [2.0.0] entry added; README links UPGRADING.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Ship 2.0.0 — full suite green, /gsd-review on diff, tag v2.0.0, breaking PR</name>
  <files>CHANGELOG.md, skill/SKILL.md</files>
  <action>
Automate everything with a CLI/API BEFORE pausing for human approval (automation-first):
  1. Run the FULL suite: `bash migrations/run-tests.sh` — confirm `FAIL: 0` and the drift filter
     `test-skill-md-version-matches-latest-migration-to-version` PASS (0022 to_version 2.0.0 ==
     skill/SKILL.md 2.0.0).
  2. Run `/gsd-review` on the phase diff (codex + gemini, each invoked with `< /dev/null` per memory
     codex-exec-stdin-hang) and address any HIGH findings before opening the PR.
  3. Commit on the feature branch with a conventional-commits breaking message.
  4. Open the PR with `gh pr create` using title
     `v2.0.0 chore!: extract observability to agenticapps-observability (SPLIT-03)`, body linking
     SPLIT-00/01/02/03 + the obs repo + docs/UPGRADING.md.
  5. Create the annotated tag LOCALLY: `git tag -a v2.0.0 -m "claude-workflow 2.0.0 (SPLIT-03)"`.
     Do NOT push the tag or merge until the human approves at this checkpoint.
Then PAUSE for the human-verify checkpoint below.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && grep -q '^version: 2.0.0$' skill/SKILL.md && grep -q '2.0.0' CHANGELOG.md && bash migrations/run-tests.sh 2>&1 | tail -6 | grep -q 'FAIL: 0' && bash migrations/run-tests.sh test-skill-md-version-matches-latest-migration-to-version 2>&1 | grep -qi PASS && echo OK</automated>
  </verify>
  <what-built>
SPLIT-03 is complete on the feature branch: observability tree deleted (30-01), 7 tombstones, migration
0022 (repoint + 2.0.0 + #58 hook) and SKILL.md 2.0.0 (30-02), reference cleanup + docs/UPGRADING.md
(30-03 Tasks 1-2). The suite is green and the drift test passes at 2.0.0. Claude has run the suite,
run /gsd-review, committed, opened the PR, and created the local v2.0.0 tag — pending approval to merge + push.
  </what-built>
  <how-to-verify>
1. Confirm the suite output: `bash migrations/run-tests.sh 2>&1 | tail -6` shows `FAIL: 0` and the
   drift `test-skill-md-version-matches-latest-migration-to-version` PASS.
2. Confirm `grep -q '^version: 2.0.0$' skill/SKILL.md` and migration 0022 `to_version: 2.0.0`.
3. Review the PR on GitHub: title is the breaking `v2.0.0 chore!: ...`; body links the four SPLIT docs,
   the obs repo, and docs/UPGRADING.md.
4. Read docs/UPGRADING.md: the 1.21.0 -> 2.0.0 transition + the obs-repo install cross-reference read
   correctly (prose/UX judgement — D-06 manual verification per 30-VALIDATION.md).
5. Confirm no `add-observability` refs remain in the forward-looking files:
   `! grep -rn 'add-observability' README.md install.sh setup/SKILL.md templates/config-hooks.json`.
6. On approval: merge the PR, then `git push origin v2.0.0`. Then (per memory local-scaffolder-clone)
   `git -C ~/.claude/skills/agenticapps-workflow pull` to pick up the deletion/rename in the local
   scaffolder clone.
  </how-to-verify>
  <resume-signal>Type "approved" to merge + push the v2.0.0 tag, or describe required changes.</resume-signal>
  <done>Suite green + drift PASS at 2.0.0; /gsd-review clean; PR opened with the breaking title; v2.0.0 tag created locally; merge + tag push gated on human approval.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reference rewrite (sed/replace) | A too-broad replace could rewrite immutable-migration text and break content hashes |
| release tag + PR | A 2.0.0 breaking release affects all downstream consumers; mis-tagging or premature push is high-impact |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-10 | Tampering | add-observability rewrite scope | mitigate | Edits restricted to the 4 named forward-looking files; acceptance asserts no `migrations/*.md` changed and `.planning/**` / CHANGELOG history untouched; obs alias covers immutable-migration old-name refs |
| T-30-11 | Repudiation | 2.0.0 release tag/PR | mitigate | Ship is a blocking human-verify checkpoint; tag pushed only after explicit approval; conventional-commits breaking title makes the breaking change auditable |
| T-30-12 | Information Disclosure | docs/UPGRADING.md | accept | Public-facing upgrade doc references only the public obs repo URL + install commands; no secrets |
| T-30-13 | Denial of Service | downstream upgrade path | mitigate | UPGRADING.md + 0022 abort-if-absent give downstreams an explicit, fail-closed install path; alias retention prevents silent breakage of old-name references |
</threat_model>

<verification>
- `! grep -q 'add-observability' README.md install.sh setup/SKILL.md` and config-hooks.json reads `observability:scan`
- `test -f docs/UPGRADING.md` with obs-repo cross-reference; no INSTALLATION.md reference
- CHANGELOG [2.0.0] entry present; older entries unchanged
- Full suite `FAIL: 0`; drift PASS at 2.0.0
- /gsd-review run on the phase diff; HIGH findings resolved
- PR opened with the breaking title; tag v2.0.0 created (pushed on approval)
</verification>

<success_criteria>
- Forward-looking files reference `observability`; immutable + historical files untouched
- docs/UPGRADING.md documents 1.21.0 -> 2.0.0 with the obs-repo install cross-reference
- CHANGELOG [2.0.0] section added
- 2.0.0 shipped: suite green, drift PASS, /gsd-review clean, tag v2.0.0 + breaking PR (human-approved)
</success_criteria>

<output>
After completion, create `.planning/phases/30-split-03-claude-workflow-2-0-0-follow-up/30-03-SUMMARY.md`
(record: files rewritten; UPGRADING.md path; final suite baseline; /gsd-review outcome; PR URL + tag).
</output>
