---
phase: 30-split-03-claude-workflow-2-0-0-follow-up
plan: 03
type: execute
wave: 3
depends_on: [30-01, 30-02]
files_modified:
  - README.md
  - install.sh
  - setup/SKILL.md
  - templates/config-hooks.json
  - templates/.claude/hooks/observability-postphase-scan.sh
  - templates/.claude/claude-md/workflow.md
  - docs/UPGRADING.md
  - CHANGELOG.md
autonomous: false
requirements: [D-04, D-05, D-06]

must_haves:
  truths:
    - "No add-observability references remain in the non-immutable forward-looking files (README, install.sh, setup/SKILL.md, templates/config-hooks.json, templates/.claude/hooks/observability-postphase-scan.sh, templates/.claude/claude-md/workflow.md)"
    - "install.sh no longer ships an add-observability skill-pair: the LINKS array entry is REMOVED (not renamed) and no remaining skill-pair points at a non-existent scaffolder subdir; the /add-observability help line and the discovery grep hint no longer advertise a skill this repo no longer ships"
    - "templates/config-hooks.json line 97 reads observability:scan (not add-observability:scan)"
    - "docs/UPGRADING.md documents the 1.21.0 -> 2.0.0 transition (supported upgrade floor 1.21.0, the Phase 27 SPLIT-00 stable baseline) and cross-references the obs repo's README + install.sh (not a non-existent INSTALLATION.md)"
    - "Immutable shipped migrations and historical records (.planning/, CHANGELOG history) are NOT rewritten"
    - "claude-workflow ships 2.0.0: CHANGELOG [2.0.0] section, git tag v2.0.0, breaking-change PR opened"
    - "Full suite green and drift PASS at ship time"
  artifacts:
    - path: "docs/UPGRADING.md"
      provides: "1.21.0 -> 2.0.0 upgrade story"
      min_lines: 20
    - path: "templates/config-hooks.json"
      provides: "observability:scan reference (repointed)"
      contains: "observability:scan"
    - path: "CHANGELOG.md"
      provides: "[2.0.0] breaking-change entry"
      contains: "2.0.0"
  key_links:
    - from: "docs/UPGRADING.md"
      to: "agenticapps-observability repo README + install.sh"
      via: "install instructions cross-reference"
      pattern: "agenticapps-observability"
    - from: "templates/config-hooks.json"
      to: "observability skill scan command"
      via: "skill field repoint"
      pattern: "observability:scan"
    - from: "install.sh"
      to: "LINKS skill-pair array"
      via: "add-observability pair DROPPED (no dangling symlink target)"
      pattern: "! grep -qE '\"add-observability"
---

<objective>
Finish SPLIT-03: rewrite the forward-looking `add-observability` -> `observability` references in the
non-immutable files (D-05), drop the `add-observability` skill-pair from install.sh (its scaffolder
subdir no longer exists), write the downstream upgrade story (D-06), and ship claude-workflow 2.0.0
as a deliberate breaking change (D-04: CHANGELOG, tag, PR).

Reference cleanup scope is NON-immutable forward-looking files ONLY (RESEARCH §5.1): README.md,
install.sh, setup/SKILL.md, templates/config-hooks.json (line 97). Immutable shipped migrations keep
their old-name text — the obs repo's `add-observability` dual-symlink alias (retained through 0.12.0)
resolves them at runtime. Historical records (.planning/, CHANGELOG history entries) are left as-is.

install.sh is NOT a string rename: Phase 30-01 deletes the repo's `add-observability/` subdir, so the
`"add-observability add-observability"` LINKS pair must be REMOVED entirely (a textual rename to
`observability` would symlink an `observability/` subdir that ALSO does not exist — obs moved to the
sibling repo). claude-workflow no longer installs observability at all.

PATH NOTE (canonical forms — do NOT conflate; see 30-02 for the authoritative version-bump form):
  - The developer's local scaffolder CLONE is `~/.claude/skills/agenticapps-workflow` (NON-hyphenated,
    a full repo clone). The line 251 `git -C ~/.claude/skills/agenticapps-workflow pull` below refers
    to THIS clone — correct, leave non-hyphenated.
  - The project-local installed-skill version-bump file is `.claude/skills/agentic-apps-workflow/SKILL.md`
    (HYPHENATED, per install.sh:42 skill-name + 0011 applies_to). That path is owned by 30-02; this plan
    does not touch it.

The ship step (tag + PR) is a non-autonomous checkpoint: it requires a human to confirm the 2.0.0
breaking release and merge. (D-04)

Purpose: a clean public-facing claude-workflow that points exclusively at the new `observability`
skill (and installs none), with a documented upgrade path and a tagged 2.0.0 release. (D-04/D-05/D-06)
Output: cleaned references, install.sh skill-pair dropped, docs/UPGRADING.md, CHANGELOG [2.0.0], git tag v2.0.0, breaking PR.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-split-03-claude-workflow-2-0-0-follow-up/30-RESEARCH.md
@.planning/phases/30-split-03-claude-workflow-2-0-0-follow-up/30-CONTEXT.md
@install.sh
@migrations/0011-observability-enforcement.md

<interfaces>
<!-- D-05 forward-looking files with live add-observability counts (verified at plan time): -->
  README.md (3 refs), install.sh (skill-pair @line 45 + help line @114 + grep hint @116), setup/SKILL.md (4 refs), templates/config-hooks.json (1 ref, line 97).
  templates/.claude/hooks/observability-postphase-scan.sh (~7 refs: skill-name prose + `/add-observability` slash invocations).
  templates/.claude/claude-md/workflow.md (1 ref: `/add-observability scan-apply` slash invocation).
  update/SKILL.md = 0 refs (nothing to do). templates/claude-md-sections.md = 0 refs.
<!-- Slash-command form: the obs install.sh exposes BOTH `/observability` (canonical, 0.11.0+) AND -->
<!-- `/add-observability` (legacy alias, retained until 0.14.0). Rewrite `/add-observability` -> -->
<!-- `/observability` (canonical) and skill-name prose `add-observability` -> `observability`. The -->
<!-- `claude -p "/observability ..."` form resolves correctly. Forward-looking templates ship the -->
<!-- canonical name (D-03 forked-axis world: new projects use forward-looking names). -->

<!-- install.sh is a SKILL-PAIR DROP, not a rename (verified live against install.sh): -->
  - LINKS array (line ~41-46) currently contains the pair: `"add-observability add-observability"` (line 45).
    The symlink loop (line 59-99) iterates LINKS and symlinks `$SCAFFOLDER/add-observability` -> `~/.claude/skills/add-observability`.
    Phase 30-01 deletes `add-observability/`, so this pair would target a NON-EXISTENT subdir.
    FIX: DELETE the `"add-observability add-observability"` line from the LINKS array entirely. Do NOT
    rename it to `observability` — there is no `observability/` subdir in this repo either.
  - Help text line ~114: `echo "  /add-observability             greenfield observability scaffold (init) + brownfield scan + apply"`
    DELETE this echo line (claude-workflow no longer ships /add-observability).
  - Discovery grep hint line ~116: the trailing `grep -E '(agentic|setup|update)-?(apps-)?workflow|add-observability'`
    REMOVE the `|add-observability` alternation so the hint no longer advertises a skill this repo no longer ships.
  - The remaining three pairs (`skill agentic-apps-workflow`, `setup setup-agenticapps-workflow`,
    `update update-agenticapps-workflow`) all still have existing scaffolder subdirs — assert each loop
    target dir exists after the edit.

<!-- DO NOT rewrite (RESEARCH §5.2 / §5.3): -->
  - migrations/0002, 0011, 0020-* and any *.md migration (immutable; alias resolves)
  - CHANGELOG.md HISTORICAL entries (append-only audit; new [2.0.0] entry uses observability)
  - .planning/** (historical decision record)
  - docs/decisions/** that STAY (e.g. 0035)

<!-- config-hooks.json line 97 (verified): -->
  Current:  "skill": "add-observability:scan",
  Target:   "skill": "observability:scan",

<!-- obs repo install contract for UPGRADING.md cross-reference (verified live; NO docs/INSTALLATION.md in obs): -->
  git clone https://github.com/agenticapps-eu/agenticapps-observability \
    ~/.claude/skills/agenticapps-observability
  bash ~/.claude/skills/agenticapps-observability/install.sh
  # creates ~/.claude/skills/observability (canonical) + ~/.claude/skills/add-observability (alias)
  Reference the obs repo's README + install.sh, NOT a docs/INSTALLATION.md (it does not exist).

<!-- Supported upgrade floor (HARDENING 6 / dismissal of <1.20.0 replay): -->
  The supported upgrade floor is 1.21.0 -> 2.0.0 (the Phase 27 SPLIT-00 stable baseline at which all
  live downstreams are parked). Pre-baseline (<1.21.0) full-chain replay is out of support scope — the
  tombstones keep the chain contiguous, but no live consumer takes that path. State this floor in
  docs/UPGRADING.md.

<!-- PR title (D-04, conventional-commits breaking): -->
  v2.0.0 chore!: extract observability to agenticapps-observability (SPLIT-03)
<!-- Current feature branch at plan time: plan-30-split-03 -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Drop the add-observability skill-pair from install.sh + rewrite add-observability -> observability in the other 5 non-immutable forward-looking files</name>
  <read_first>
    - .planning/phases/30-split-03-claude-workflow-2-0-0-follow-up/30-RESEARCH.md §5.1 (rewrite table) and §5.2/§5.3 (do-NOT-touch list)
    - install.sh (the LINKS array @41-46, the symlink loop @59-99, the /add-observability help echo @114, the discovery grep hint @116)
    - README.md, setup/SKILL.md, templates/config-hooks.json (the actual add-observability lines)
    - templates/.claude/hooks/observability-postphase-scan.sh, templates/.claude/claude-md/workflow.md (the `/add-observability` slash-command + skill-name refs)
    - The <interfaces> block above (file/count list, install.sh skill-pair DROP rules, line 97 target, slash-command rewrite rule, do-not-rewrite list)
  </read_first>
  <files>README.md, install.sh, setup/SKILL.md, templates/config-hooks.json, templates/.claude/hooks/observability-postphase-scan.sh, templates/.claude/claude-md/workflow.md</files>
  <action>
install.sh — SKILL-PAIR DROP (NOT a rename). Phase 30-01 deleted the repo's `add-observability/` subdir,
so the install.sh LINKS pair that symlinks it must be removed entirely:
  1. DELETE the line `  "add-observability add-observability"` from the LINKS array (line ~45). Leave the
     other three pairs intact. A textual rename to `observability` is WRONG — there is no `observability/`
     subdir in this repo; the obs skill moved to the sibling repo and installs separately (D-03).
  2. DELETE the help echo line (~114) that advertises `/add-observability`
     (`echo "  /add-observability   ... scan + apply"`). claude-workflow no longer ships it.
  3. EDIT the trailing discovery grep hint (~116): remove the `|add-observability` alternation from the
     `grep -E '...'` pattern so it reads `grep -E '(agentic|setup|update)-?(apps-)?workflow'` (or equivalent
     without the add-observability branch).
  Do NOT add any submodule/auto-install wiring for the obs skill — claude-workflow installs no observability.

For EACH of README.md, setup/SKILL.md, templates/config-hooks.json,
templates/.claude/hooks/observability-postphase-scan.sh, and templates/.claude/claude-md/workflow.md:
rewrite forward-looking `add-observability` references to `observability`. Read each ref in context — only
rewrite references describing the CURRENT install/skill name; if any ref is a historical narrative or
changelog-style line, leave it (none expected in these forward-looking files, but verify).

Specifics:
- templates/config-hooks.json line 97: `"skill": "add-observability:scan"` -> `"skill": "observability:scan"`.
  Validate JSON after: `jq . templates/config-hooks.json >/dev/null`.
- README.md + setup/SKILL.md: rename the skill in install/usage instructions (and, in README, drop any
  claim that the scaffolder installs observability — it does not; reference the separate obs install instead).

- templates/.claude/hooks/observability-postphase-scan.sh: rewrite the `/add-observability` slash
  invocations (`claude -p "/add-observability scan ..."`, `scan-apply`, `--update-baseline`) to
  `/observability`, AND the skill-name prose in the header comments (e.g. "the add-observability delta
  scan", "(add-observability is at implements_spec ...)") to `observability`. The canonical
  `/observability` command is exposed by the obs install.sh (0.11.0+), so the `claude -p` calls resolve.
  Keep the script behaviour identical (advisory, exit 0); rename only the references.
- templates/.claude/claude-md/workflow.md: rewrite the single `/add-observability scan-apply
  --confidence high` reference (line ~123) to `/observability scan-apply --confidence high`.

WHY scope these in (D-05 covers "non-immutable files"): both are forward-looking templates that NEW
projects ship. In the forked-axis world (D-03), new projects should ship the canonical forward-looking
command name, not the legacy alias. The `/add-observability` alias is retained only until obs 0.14.0;
shipping `/observability` now avoids a future re-rename.

DO NOT touch: any `migrations/*.md` file, CHANGELOG.md historical entries, anything under `.planning/`,
or docs/decisions ADRs that stay. The obs alias resolves the immutable-migration old-name refs.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && ! grep -q 'add-observability' README.md install.sh setup/SKILL.md templates/.claude/hooks/observability-postphase-scan.sh templates/.claude/claude-md/workflow.md && ! grep -qE '"add-observability' install.sh && for pair in "skill agentic-apps-workflow" "setup setup-agenticapps-workflow" "update update-agenticapps-workflow"; do d="${pair%% *}"; test -d "$d" || { echo "MISSING loop target subdir: $d"; exit 1; }; done && grep -q '"skill": "observability:scan"' templates/config-hooks.json && ! grep -q 'add-observability:scan' templates/config-hooks.json && grep -q '/observability' templates/.claude/claude-md/workflow.md && jq . templates/config-hooks.json >/dev/null && bash -n templates/.claude/hooks/observability-postphase-scan.sh && bash -n install.sh && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `! grep -q 'add-observability' README.md` (and install.sh, setup/SKILL.md, templates/.claude/hooks/observability-postphase-scan.sh, templates/.claude/claude-md/workflow.md)
    - install.sh skill-pair DROPPED: `! grep -qE '"add-observability' install.sh` (the LINKS entry is gone, not renamed)
    - No dangling symlink target: every remaining LINKS pair's source subdir exists — `for p in skill setup update; do test -d "$p"; done` (the loop never points at a missing dir)
    - install.sh help text + discovery hint no longer advertise add-observability: `! grep -q 'add-observability' install.sh` (covers the @114 echo and @116 grep hint)
    - `bash -n install.sh` (still parses after the LINKS/echo/grep edits)
    - `grep -q '/observability' templates/.claude/claude-md/workflow.md` (slash command rewritten to canonical name)
    - `bash -n templates/.claude/hooks/observability-postphase-scan.sh` (post-phase scan still parses; behaviour unchanged, refs renamed)
    - `grep -q '"skill": "observability:scan"' templates/config-hooks.json` and `! grep -q 'add-observability:scan' templates/config-hooks.json`
    - `jq . templates/config-hooks.json` valid
    - No `migrations/*.md` modified by this task
    - `.planning/**` and CHANGELOG history NOT modified by this task
  </acceptance_criteria>
  <done>install.sh add-observability skill-pair + help line + grep-hint reference DROPPED (no dangling symlink target; every remaining pair's subdir exists); the other 5 forward-looking files reference observability (slash commands rewritten to canonical `/observability`); config-hooks.json line 97 repointed; JSON valid; install.sh + post-phase-scan.sh still parse; immutable + historical files untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Write docs/UPGRADING.md (1.21.0 -> 2.0.0, supported floor stated) + CHANGELOG [2.0.0] entry + README link</name>
  <read_first>
    - .planning/phases/30-split-03-claude-workflow-2-0-0-follow-up/30-RESEARCH.md §7 (obs install contract — no INSTALLATION.md), §8.3 (UPGRADING.md location = docs/)
    - NOTE (D-06 deviation): D-06 names the obs repo's `docs/INSTALLATION.md` as the cross-reference target, but RESEARCH §7 (verified live) confirmed that file does NOT exist in the obs repo. Reference the obs README + install.sh instead — this is a correct, intentional override of D-06's named target, not an oversight.
    - .planning/phases/30-split-03-claude-workflow-2-0-0-follow-up/30-CONTEXT.md D-06
    - CHANGELOG.md (head — current latest entry format, e.g. [1.21.0])
    - The <interfaces> block above (obs install commands, supported floor 1.21.0, PR title)
  </read_first>
  <files>docs/UPGRADING.md, CHANGELOG.md, README.md</files>
  <action>
1. Create `docs/UPGRADING.md` documenting the 1.21.0 -> 2.0.0 transition. Content:
   - Supported upgrade floor: state explicitly that the supported path is **1.21.0 -> 2.0.0** (the Phase 27
     SPLIT-00 stable baseline at which all live downstreams are parked). Pre-baseline (<1.21.0) full-chain
     replay is out of support scope — the tombstones keep the chain contiguous, but no live consumer takes
     that path. (This documents — without building a <1.20.0 replay test — that pre-baseline replay is
     unsupported.)
   - Why 2.0.0 is breaking: observability extracted to agenticapps-observability; claude-workflow no
     longer ships the observability skill.
   - What changed: `add-observability/` removed; migrations 0012/0013/0017/0018/0019/0020/0021
     tombstoned (chain stays contiguous); new migration 0022 repoints the install.
   - What downstream projects must do: install agenticapps-observability separately (two independent
     installs, D-03), using the exact obs install commands from the <interfaces> block. Cross-reference
     the obs repo's README + install.sh (NOT a docs/INSTALLATION.md — it does not exist, RESEARCH §7).
   - Note the `add-observability` alias still resolves (retained through obs 0.12.0) so existing
     immutable-migration references keep working.
   - How to upgrade: run `/update-agenticapps-workflow` (applies 0022 -> bumps to 2.0.0). The migration
     aborts with install instructions if the observability skill is absent.
2. Add a `## [2.0.0]` section to the TOP of CHANGELOG.md (above the latest existing entry) describing
   the breaking extraction, the tombstones, migration 0022, and the #58 deterministic Phase Sentinel
   hook. Do NOT rewrite older entries.
3. Add a one-line link to docs/UPGRADING.md from README.md (forward-looking; fine to edit README here).
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && test -f docs/UPGRADING.md && grep -q '2.0.0' docs/UPGRADING.md && grep -q '1.21.0' docs/UPGRADING.md && grep -q 'agenticapps-observability' docs/UPGRADING.md && ! grep -q 'INSTALLATION.md' docs/UPGRADING.md && grep -q '2.0.0' CHANGELOG.md && grep -qi 'UPGRADING' README.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `test -f docs/UPGRADING.md` with the 1.21.0 -> 2.0.0 story (>= 20 lines)
    - `grep -q '1.21.0' docs/UPGRADING.md` (supported upgrade floor stated)
    - `grep -q 'agenticapps-observability' docs/UPGRADING.md` (obs install cross-ref present)
    - `! grep -q 'INSTALLATION.md' docs/UPGRADING.md` (does not reference the non-existent file)
    - CHANGELOG.md has a `[2.0.0]` section at the top; older entries unchanged
    - README links to docs/UPGRADING.md
  </acceptance_criteria>
  <done>docs/UPGRADING.md written with the supported 1.21.0 floor + obs-repo cross-reference; CHANGELOG [2.0.0] entry added; README links UPGRADING.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Ship 2.0.0 — full suite green, /gsd-review on diff, tag v2.0.0, breaking PR</name>
  <files>CHANGELOG.md, skill/SKILL.md</files>
  <action>
Automate everything with a CLI/API BEFORE pausing for human approval (automation-first):
  1. Run the FULL suite: `bash migrations/run-tests.sh` — confirm `FAIL: 0` and the drift filter
     `test-skill-md-version-matches-latest-migration-to-version` PASS (0022 to_version 2.0.0 ==
     skill/SKILL.md 2.0.0).
  2. Run `/gsd-review` on the phase diff (codex + gemini, each invoked with `< /dev/null` per memory
     codex-exec-stdin-hang) and address any HIGH findings before opening the PR.
  3. Commit on the feature branch with a conventional-commits breaking message.
  4. Open the PR with `gh pr create` using title
     `v2.0.0 chore!: extract observability to agenticapps-observability (SPLIT-03)`, body linking
     SPLIT-00/01/02/03 + the obs repo + docs/UPGRADING.md.
  5. Create the annotated tag LOCALLY: `git tag -a v2.0.0 -m "claude-workflow 2.0.0 (SPLIT-03)"`.
     Do NOT push the tag or merge until the human approves at this checkpoint.
Then PAUSE for the human-verify checkpoint below.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && grep -q '^version: 2.0.0$' skill/SKILL.md && grep -q '2.0.0' CHANGELOG.md && bash migrations/run-tests.sh 2>&1 | tail -6 | grep -q 'FAIL: 0' && bash migrations/run-tests.sh test-skill-md-version-matches-latest-migration-to-version 2>&1 | grep -qi PASS && echo OK</automated>
  </verify>
  <what-built>
SPLIT-03 is complete on the feature branch: observability tree deleted (30-01), 7 tombstones, migration
0022 (repoint + 2.0.0 + #58 hook) and SKILL.md 2.0.0 (30-02), reference cleanup + install.sh skill-pair
drop + docs/UPGRADING.md (30-03 Tasks 1-2). The suite is green and the drift test passes at 2.0.0. Claude
has run the suite, run /gsd-review, committed, opened the PR, and created the local v2.0.0 tag — pending
approval to merge + push.
  </what-built>
  <how-to-verify>
1. Confirm the suite output: `bash migrations/run-tests.sh 2>&1 | tail -6` shows `FAIL: 0` and the
   drift `test-skill-md-version-matches-latest-migration-to-version` PASS.
2. Confirm `grep -q '^version: 2.0.0$' skill/SKILL.md` and migration 0022 `to_version: 2.0.0`.
3. Review the PR on GitHub: title is the breaking `v2.0.0 chore!: ...`; body links the four SPLIT docs,
   the obs repo, and docs/UPGRADING.md.
4. Read docs/UPGRADING.md: the 1.21.0 -> 2.0.0 transition + the supported-floor note + the obs-repo
   install cross-reference read correctly (prose/UX judgement — D-06 manual verification per 30-VALIDATION.md).
5. Confirm no `add-observability` refs remain in the forward-looking files AND the install.sh skill-pair is gone:
   `! grep -rn 'add-observability' README.md install.sh setup/SKILL.md templates/config-hooks.json templates/.claude/hooks/observability-postphase-scan.sh templates/.claude/claude-md/workflow.md`
   and `! grep -qE '"add-observability' install.sh`.
6. On approval: merge the PR, then `git push origin v2.0.0`. Then (per memory local-scaffolder-clone)
   `git -C ~/.claude/skills/agenticapps-workflow pull` to pick up the deletion/rename in the local
   scaffolder clone.
  </how-to-verify>
  <resume-signal>Type "approved" to merge + push the v2.0.0 tag, or describe required changes.</resume-signal>
  <done>Suite green + drift PASS at 2.0.0; /gsd-review clean; PR opened with the breaking title; v2.0.0 tag created locally; merge + tag push gated on human approval.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reference rewrite (sed/replace) | A too-broad replace could rewrite immutable-migration text and break content hashes |
| install.sh skill-linking loop | A LINKS pair pointing at a missing scaffolder subdir creates a dangling symlink target / silent skip |
| release tag + PR | A 2.0.0 breaking release affects all downstream consumers; mis-tagging or premature push is high-impact |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-10 | Tampering | add-observability rewrite scope | mitigate | Edits restricted to the named forward-looking files; acceptance asserts no `migrations/*.md` changed and `.planning/**` / CHANGELOG history untouched; obs alias covers immutable-migration old-name refs |
| T-30-11 | Repudiation | 2.0.0 release tag/PR | mitigate | Ship is a blocking human-verify checkpoint; tag pushed only after explicit approval; conventional-commits breaking title makes the breaking change auditable |
| T-30-12 | Information Disclosure | docs/UPGRADING.md | accept | Public-facing upgrade doc references only the public obs repo URL + install commands; no secrets |
| T-30-13 | Denial of Service | downstream upgrade path | mitigate | UPGRADING.md + 0022 abort-if-absent give downstreams an explicit, fail-closed install path; alias retention prevents silent breakage of old-name references |
| T-30-14 | Denial of Service | install.sh dangling skill-pair | mitigate | The add-observability LINKS pair is DROPPED (not renamed); acceptance asserts `! grep -qE '"add-observability' install.sh` AND that every remaining pair's source subdir exists, so the loop never symlinks a missing dir |
</threat_model>

<verification>
- `! grep -q 'add-observability' README.md install.sh setup/SKILL.md` and config-hooks.json reads `observability:scan`
- install.sh add-observability skill-pair DROPPED (`! grep -qE '"add-observability' install.sh`); every remaining LINKS pair's subdir exists; `bash -n install.sh` passes
- `test -f docs/UPGRADING.md` with the supported 1.21.0 floor + obs-repo cross-reference; no INSTALLATION.md reference
- CHANGELOG [2.0.0] entry present; older entries unchanged
- Full suite `FAIL: 0`; drift PASS at 2.0.0
- /gsd-review run on the phase diff; HIGH findings resolved
- PR opened with the breaking title; tag v2.0.0 created (pushed on approval)
</verification>

<success_criteria>
- Forward-looking files reference `observability`; install.sh add-observability skill-pair dropped (no dangling symlink target); immutable + historical files untouched
- docs/UPGRADING.md documents 1.21.0 -> 2.0.0 with the supported floor + the obs-repo install cross-reference
- CHANGELOG [2.0.0] section added
- 2.0.0 shipped: suite green, drift PASS, /gsd-review clean, tag v2.0.0 + breaking PR (human-approved)
</success_criteria>

<output>
After completion, create `.planning/phases/30-split-03-claude-workflow-2-0-0-follow-up/30-03-SUMMARY.md`
(record: files rewritten; install.sh skill-pair drop confirmation; UPGRADING.md path; final suite baseline; /gsd-review outcome; PR URL + tag).
</output>
</content>
</invoke>
