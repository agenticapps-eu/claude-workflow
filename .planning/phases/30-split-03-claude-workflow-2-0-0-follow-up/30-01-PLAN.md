---
phase: 30-split-03-claude-workflow-2-0-0-follow-up
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - add-observability/
  - migrations/0012-slash-discovery.md
  - migrations/0013-auto-init-and-stale-vendored-cleanup.md
  - migrations/0017-add-axiom-logs-destination.md
  - migrations/0018-postphase-observability-hook.md
  - migrations/0019-sentry-crons-and-healthz.md
  - migrations/0020-openrouter-integration.md
  - migrations/0021-with-cron-and-queue-updates.md
  - migrations/0012-slash-discovery-moved.md
  - migrations/0013-auto-init-moved.md
  - migrations/0017-add-axiom-logs-destination-moved.md
  - migrations/0018-postphase-observability-hook-moved.md
  - migrations/0019-sentry-crons-and-healthz-moved.md
  - migrations/0020-openrouter-integration-moved.md
  - migrations/0021-with-cron-and-queue-updates-moved.md
  - migrations/test-fixtures/0012/
  - migrations/test-fixtures/0013/
  - migrations/test-fixtures/0017/
  - migrations/test-fixtures/0018/
  - migrations/test-fixtures/0019/
  - migrations/test-fixtures/0021/
  - templates/.claude/scripts/migrate-0017-axiom-destination.sh
  - templates/.claude/scripts/migrate-0017-old-wrappers/
  - templates/.claude/scripts/migrate-0019-sentry-crons-and-healthz.sh
  - templates/.claude/scripts/migrate-0021-with-cron-and-queue-updates.sh
  - docs/decisions/0029-cron-monitor-sdk-composition.md
  - docs/decisions/0030-openrouter-integration-sdk-first.md
  - docs/decisions/0031-0019-engine-index-ts-anchor.md
  - docs/decisions/0032-cron-monitor-generic-narrowing-cf-worker-only.md
  - docs/decisions/0033-with-queue-monitor.md
  - docs/decisions/0034-observability-init-singleton-invariant.md
  - migrations/run-tests.sh
autonomous: true
requirements: [D-01]

must_haves:
  truths:
    - "add-observability/ directory no longer exists in claude-workflow"
    - "The seven moved migration .md files are gone; seven [TOMBSTONE] .md files take their slots carrying verbatim from_version/to_version"
    - "The migration chain stays contiguous 1.10.0 -> 2.0.0-precursor with no from_version gap"
    - "run-tests.sh contains no test body that references add-observability/ (8 bodies removed; test_migration_0011 scaffolder sanity check removed)"
    - "The full suite runs green at end of wave: FAIL=0 and drift test still PASS (latest migration to_version still 1.20.0 == SKILL.md 1.20.0)"
  artifacts:
    - path: "migrations/0012-slash-discovery-moved.md"
      provides: "Tombstone 1.10.0 -> 1.11.0"
      contains: "to_version: 1.11.0"
    - path: "migrations/0021-with-cron-and-queue-updates-moved.md"
      provides: "Tombstone 1.19.0 -> 1.20.0 (last surviving tombstone; drift anchor pre-0022)"
      contains: "to_version: 1.20.0"
    - path: "migrations/run-tests.sh"
      provides: "Harness with moved test bodies removed, 0011 sanity-check removed"
  key_links:
    - from: "migrations/0021-with-cron-and-queue-updates-moved.md"
      to: "vendor/agenticapps-shared/migrations/lib/drift-test.sh"
      via: "drift test reads to_version from alphabetically-last migration file"
      pattern: "to_version: 1.20.0"
    - from: "migrations/run-tests.sh test_migration_0011"
      to: "fixture sandbox SCAN.md"
      via: "stub SCAN.md instead of $REPO_ROOT/add-observability/scan/SCAN.md"
      pattern: "scaffolder_scan"
---

<objective>
Delete the moved observability tree from claude-workflow and tombstone the seven vacated
migration slots, leaving the migration chain contiguous and the test suite green.

This is the BREAKING deletion wave of SPLIT-03. Phase 29 already copied the observability tree
(with history) into agenticapps-observability v0.11.1 (live). This plan removes the now-duplicated
content from claude-workflow: the whole `add-observability/` directory, the seven moved migration
`.md` files (0012, 0013, 0017, 0018, 0019, 0020, 0021), their fixtures, the migrate-*.sh engine
scripts, the six observability ADRs, and the eight test bodies in run-tests.sh that depend on the
deleted tree. Each vacated migration slot is replaced by a no-op `[TOMBSTONE]` `.md` so a downstream
project replaying the chain gets an actionable "moved to agenticapps-observability" pointer instead
of a missing-number gap.

Purpose: claude-workflow becomes a clean scaffolder that no longer ships observability. (D-01)
Output: 7 tombstones; deleted tree; trimmed run-tests.sh; suite green (FAIL=0, drift PASS).

CRITICAL SEQUENCING (drift invariant, RESEARCH §2.3): Do NOT bump skill/SKILL.md in this wave.
The last surviving migration after deletion is the 0021 tombstone with `to_version: 1.20.0`, which
matches the unchanged SKILL.md `version: 1.20.0` — drift stays GREEN. The 2.0.0 bump + 0022 land
together in plan 30-02. Never leave the suite red between commits: delete-a-migration and
add-its-tombstone in the SAME commit so the chain is never gapped.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-split-03-claude-workflow-2-0-0-follow-up/30-RESEARCH.md
@.planning/phases/30-split-03-claude-workflow-2-0-0-follow-up/30-CONTEXT.md
@migrations/README.md

<interfaces>
<!-- Verbatim from/to versions for the seven tombstones (verified live via grep at plan time). -->
<!-- Tombstones MUST carry these EXACT values or the drift test / chain continuity breaks (RESEARCH §1.4, Pitfall 1). -->

| id   | slug-moved                                  | from_version | to_version | original file |
|------|---------------------------------------------|--------------|------------|---------------|
| 0012 | slash-discovery-moved                       | 1.10.0       | 1.11.0     | 0012-slash-discovery.md |
| 0013 | auto-init-moved                             | 1.11.0       | 1.12.0     | 0013-auto-init-and-stale-vendored-cleanup.md |
| 0017 | add-axiom-logs-destination-moved            | 1.15.0       | 1.16.0     | 0017-add-axiom-logs-destination.md |
| 0018 | postphase-observability-hook-moved          | 1.16.0       | 1.17.0     | 0018-postphase-observability-hook.md |
| 0019 | sentry-crons-and-healthz-moved              | 1.17.0       | 1.18.0     | 0019-sentry-crons-and-healthz.md |
| 0020 | openrouter-integration-moved                | 1.18.0       | 1.19.0     | 0020-openrouter-integration.md |
| 0021 | with-cron-and-queue-updates-moved           | 1.19.0       | 1.20.0     | 0021-with-cron-and-queue-updates.md |

NOTE: 0020 IS tombstoned. 29-CONTEXT line 150 ("0020 moves the .md ONLY") + D-01 CONTEXT list
(0012,0013,0017,0018,0019,0020,0021) are authoritative. RESEARCH §6.1's "0020 STAYS" row is an
internal contradiction the same RESEARCH corrects in Pitfall 4 ("0020 IS in the tombstone list").
Tombstone it. The OpenRouter ADR-0030 deletion is handled separately in Task 2 (subject-verified).

run-tests.sh test bodies to remove (line numbers verified live; will drift — re-grep at exec time):
  test_migration_0012()   @1254     test_migration_0013()   @1343
  test_migration_0017()   @1633     test_migration_0018()   @1907
  test_migration_0019()   @1984     test_migration_0021()   @2066
  test_meta_destinations_consistency() @1853 (+ _roles_from_adapter @1806, _roles_from_meta @1833)
  test_sigterm_mid_apply_preserves_state() @2157
Dispatcher entries (the `if [ -z "$FILTER" ] || [ "$FILTER" = "00NN" ]; then ... fi` blocks):
  @2373(0012) @2377(0013) @2393(0017) @2397(0018) @2401(0019) @2405(0021)
  @2413(destinations->test_meta_destinations_consistency) @2429(sigterm guard block)
test_migration_0011 scaffolder sanity check to remove: lines ~1173-1181 (the
  `local scaffolder_scan="$REPO_ROOT/add-observability/scan/SCAN.md"` block + the `if [ ! -f ... ]`
  FAIL guard). KEEP the rest of test_migration_0011. RESEARCH §1.5 / Pitfall 2.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete the moved observability tree (dir, 7 migration .md, fixtures, migrate scripts) + write 7 tombstones in the same commit</name>
  <read_first>
    - .planning/phases/30-split-03-claude-workflow-2-0-0-follow-up/30-RESEARCH.md §1.4 (canonical tombstone shape), §6.1 (deletion manifest)
    - migrations/README.md (frontmatter fields; engine ignores unknown fields; from_version matching)
    - migrations/0012-slash-discovery.md and migrations/0021-with-cron-and-queue-updates.md (confirm verbatim from/to before deleting)
    - The <interfaces> block above (the exact 7 from/to version pairs)
  </read_first>
  <files>
    add-observability/, migrations/0012-slash-discovery.md, migrations/0013-auto-init-and-stale-vendored-cleanup.md,
    migrations/0017-add-axiom-logs-destination.md, migrations/0018-postphase-observability-hook.md,
    migrations/0019-sentry-crons-and-healthz.md, migrations/0020-openrouter-integration.md,
    migrations/0021-with-cron-and-queue-updates.md, migrations/test-fixtures/{0012,0013,0017,0018,0019,0021}/,
    templates/.claude/scripts/migrate-0017-axiom-destination.sh, templates/.claude/scripts/migrate-0017-old-wrappers/,
    templates/.claude/scripts/migrate-0019-sentry-crons-and-healthz.sh, templates/.claude/scripts/migrate-0021-with-cron-and-queue-updates.sh,
    migrations/0012-slash-discovery-moved.md, migrations/0013-auto-init-moved.md, migrations/0017-add-axiom-logs-destination-moved.md,
    migrations/0018-postphase-observability-hook-moved.md, migrations/0019-sentry-crons-and-healthz-moved.md,
    migrations/0020-openrouter-integration-moved.md, migrations/0021-with-cron-and-queue-updates-moved.md
  </files>
  <action>
Use `git rm -r` for deletions so the index stays clean. Delete in one logical commit together with
the tombstone creation (chain never gapped).

1. Delete the whole observability directory: `git rm -r add-observability/`
2. Delete the seven moved migration source files:
   `git rm migrations/0012-slash-discovery.md migrations/0013-auto-init-and-stale-vendored-cleanup.md migrations/0017-add-axiom-logs-destination.md migrations/0018-postphase-observability-hook.md migrations/0019-sentry-crons-and-healthz.md migrations/0020-openrouter-integration.md migrations/0021-with-cron-and-queue-updates.md`
3. Delete their fixtures: `git rm -r migrations/test-fixtures/0012 migrations/test-fixtures/0013 migrations/test-fixtures/0017 migrations/test-fixtures/0018 migrations/test-fixtures/0019 migrations/test-fixtures/0021`
   (NOTE: no `migrations/test-fixtures/0020/` exists — 0020 has no fixtures. Do NOT touch 0011/0014/0015/0016 or init-* fixtures.)
4. Delete the engine scripts:
   `git rm templates/.claude/scripts/migrate-0017-axiom-destination.sh templates/.claude/scripts/migrate-0019-sentry-crons-and-healthz.sh templates/.claude/scripts/migrate-0021-with-cron-and-queue-updates.sh`
   `git rm -r templates/.claude/scripts/migrate-0017-old-wrappers`
   (KEEP install-wiki-compiler.sh, rollback-wiki-compiler.sh, install-gitnexus.sh, rollback-gitnexus.sh, index-family-repos.sh — those are migrations 0006/0007, not obs.)
5. Write seven tombstone files using the canonical shape from RESEARCH §1.4. Each is PURELY
   INFORMATIONAL — no Pre-flight / Step / Post-checks sections (those would mislead /update into
   executing non-existent actions). Use the EXACT from/to pairs from the <interfaces> table. Template
   (substitute id/slug/from/to/obs_migration per row):

```yaml
# --- (example frontmatter; indented to avoid confusing the plan parser)
id: 0012
slug: slash-discovery-moved
title: "[TOMBSTONE] slash-discovery — moved to agenticapps-observability"
from_version: 1.10.0
to_version: 1.11.0
applies_to: []
moved_to: agenticapps-eu/agenticapps-observability
obs_migration: "0012 (1.10.0 -> 1.11.0)"
# --- end example frontmatter

# Migration 0012 — [TOMBSTONE] Moved to agenticapps-observability

This migration was moved to the `agenticapps-eu/agenticapps-observability` repository
as part of `claude-workflow 2.0.0` (SPLIT-03). The observability skill is now a
separate installation:

\`\`\`bash
git clone https://github.com/agenticapps-eu/agenticapps-observability \
  ~/.claude/skills/agenticapps-observability
bash ~/.claude/skills/agenticapps-observability/install.sh
\`\`\`

Then run `/update-agenticapps-workflow` to apply any pending observability migrations
via the obs repo's own update chain.

This slot is a no-op tombstone. If your project is already past version 1.11.0, the
migration engine skips it automatically (it matches by `from_version`).
```

   The `obs_migration` value for each: 0012->"0012 (1.10.0 -> 1.11.0)", 0013->"0013 (1.11.0 -> 1.12.0)",
   0017->"0017 (1.15.0 -> 1.16.0)", 0018->"0018 (1.16.0 -> 1.17.0)", 0019->"0019 (1.17.0 -> 1.18.0)",
   0020->"0020 (1.18.0 -> 1.19.0)", 0021->"0021 (1.19.0 -> 1.20.0)". Set the `to_version`/`from_version`
   frontmatter to match each row's pair EXACTLY.
6. `git add` the seven new `*-moved.md` files.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && ! test -d add-observability && for s in 0012-slash-discovery-moved 0013-auto-init-moved 0017-add-axiom-logs-destination-moved 0018-postphase-observability-hook-moved 0019-sentry-crons-and-healthz-moved 0020-openrouter-integration-moved 0021-with-cron-and-queue-updates-moved; do test -f "migrations/${s}.md" || { echo "MISSING $s"; exit 1; }; done && grep -q '^to_version: 1.20.0$' migrations/0021-with-cron-and-queue-updates-moved.md && grep -q '^to_version: 1.11.0$' migrations/0012-slash-discovery-moved.md && test -f migrations/0020-openrouter-integration-moved.md && grep -q '^to_version: 1.19.0$' migrations/0020-openrouter-integration-moved.md && ! test -f migrations/0020-openrouter-integration.md && ! test -f migrations/0019-sentry-crons-and-healthz.md && ! test -f templates/.claude/scripts/migrate-0019-sentry-crons-and-healthz.sh && ! test -d migrations/test-fixtures/0019 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `! test -d add-observability`
    - All 7 `migrations/*-moved.md` tombstones exist
    - `grep -q '^to_version: 1.20.0$' migrations/0021-with-cron-and-queue-updates-moved.md`
    - `grep -q '^to_version: 1.11.0$' migrations/0012-slash-discovery-moved.md`
    - `test -f migrations/0020-openrouter-integration-moved.md` (0020 IS tombstoned — RESEARCH Pitfall 4 / D-01, overriding §6.1's "0020 STAYS" row)
    - `grep -q '^to_version: 1.19.0$' migrations/0020-openrouter-integration-moved.md` (verbatim 1.18.0 -> 1.19.0 pair)
    - `! test -f migrations/0020-openrouter-integration.md` (original 0020 source removed)
    - Each tombstone has `applies_to: []` and NO `## Pre-flight` / `## Steps` section: `! grep -lq '## Steps' migrations/*-moved.md`
    - Originals gone: `! ls migrations/0012-slash-discovery.md migrations/0019-sentry-crons-and-healthz.md 2>/dev/null`
    - migrate-*.sh scripts gone: `! test -f templates/.claude/scripts/migrate-0019-sentry-crons-and-healthz.sh`
    - Fixtures gone: `! test -d migrations/test-fixtures/0019` and `! test -d migrations/test-fixtures/0017`
    - Staying fixtures intact: `test -d migrations/test-fixtures/0011 && test -d migrations/test-fixtures/0016`
  </acceptance_criteria>
  <done>add-observability/ deleted; 7 originals + their fixtures + 3 migrate scripts + old-wrappers dir removed; 7 verbatim-versioned tombstones written with informational-only bodies.</done>
</task>

<task type="auto">
  <name>Task 2: Subject-verify then delete the six observability ADRs (guard the 0030 flag)</name>
  <read_first>
    - .planning/phases/30-split-03-claude-workflow-2-0-0-follow-up/30-RESEARCH.md §6.1 CONFLICT FLAG (ADR-0030)
    - .planning/phases/29-split-02-agenticapps-observability/29-CONTEXT.md lines ~176, ~232 (authoritative MOVE list: "obs ADRs 0029-0034 MOVE")
    - The first line (title) of each docs/decisions/0029..0034 file
  </read_first>
  <files>
    docs/decisions/0029-cron-monitor-sdk-composition.md, docs/decisions/0030-openrouter-integration-sdk-first.md,
    docs/decisions/0031-0019-engine-index-ts-anchor.md, docs/decisions/0032-cron-monitor-generic-narrowing-cf-worker-only.md,
    docs/decisions/0033-with-queue-monitor.md, docs/decisions/0034-observability-init-singleton-invariant.md
  </files>
  <action>
The 29-CONTEXT MOVE list is authoritative: ADRs 0029-0034 MOVE to the obs repo. VERIFIED at plan
time that the obs repo `~/Sourcecode/agenticapps/agenticapps-observability/docs/decisions/` already
contains 0029, 0030, 0031, 0032, 0033, 0034 (plus obs-only 0036) — so all six were already extracted
and the claude-workflow copies are now duplicates safe to delete.

GUARD STEP (do not blind-delete — RESEARCH §6.1 flagged 0030 as the "openrouter" ADR): before
deleting each, grep its title (first `# ` line) and confirm it is one of the known obs ADRs. The
0030 case: ADR-0030 is titled "OpenRouter integration: SDK-first" — its CONTEXT explicitly frames
OpenRouter as an OBSERVABILITY destination ("Without observability instrumentation, four signals
are invisible") and it was extracted to the obs repo (confirmed present there). So 0030 IS an obs
ADR for SPLIT purposes → delete the claude-workflow copy.

Do NOT delete docs/decisions/0035-shared-extraction-boundaries.md (it STAYS — it is the SPLIT-01
boundary ADR, not observability; and it is NOT in the obs repo).

For each of 0029, 0030, 0031, 0032, 0033, 0034:
1. Confirm the obs repo has a same-numbered ADR: `test -f ~/Sourcecode/agenticapps/agenticapps-observability/docs/decisions/00NN-*.md`
2. `git rm docs/decisions/00NN-*.md`

If any obs-repo counterpart is MISSING for a given number, STOP and do not delete that file — surface
it in the SUMMARY as a discrepancy rather than losing the only copy.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && ! ls docs/decisions/0029-*.md docs/decisions/0030-*.md docs/decisions/0031-*.md docs/decisions/0032-*.md docs/decisions/0033-*.md docs/decisions/0034-*.md 2>/dev/null && test -f docs/decisions/0035-shared-extraction-boundaries.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `! ls docs/decisions/0029-*.md docs/decisions/0034-*.md 2>/dev/null` (obs ADRs deleted)
    - `test -f docs/decisions/0035-shared-extraction-boundaries.md` (boundary ADR stays)
    - SUMMARY records that each deleted ADR was confirmed present in the obs repo before deletion
  </acceptance_criteria>
  <done>Six obs ADRs deleted after subject-verification + obs-repo presence check; ADR-0035 retained.</done>
</task>

<task type="auto">
  <name>Task 3: Remove the 8 add-observability-dependent test bodies + 0011 scaffolder sanity check from run-tests.sh; suite green</name>
  <read_first>
    - .planning/phases/30-split-03-claude-workflow-2-0-0-follow-up/30-RESEARCH.md §1.5, §6.2 (test-body deletion table), Pitfall 2
    - migrations/run-tests.sh (re-grep the function + dispatcher line numbers — they drift from the <interfaces> block)
    - The <interfaces> block above (function names, helper names, dispatcher anchors)
  </read_first>
  <files>migrations/run-tests.sh</files>
  <action>
The 4 pre-existing FAILs in the 186/4 baseline are test_migration_0017 fixtures — removing that body
drops FAIL to 0. There is NO hardcoded "must equal 186" assertion in run-tests.sh (RESEARCH §1.5);
the baseline is tracked manually. The new green baseline is recorded in this plan's SUMMARY.

Re-grep current line numbers first (they drift):
`grep -n 'test_migration_0012()\|test_migration_0013()\|test_migration_0017()\|test_migration_0018()\|test_migration_0019()\|test_migration_0021()\|test_meta_destinations_consistency()\|_roles_from_adapter()\|_roles_from_meta()\|test_sigterm_mid_apply_preserves_state()' migrations/run-tests.sh`

1. DELETE these eight function bodies in full (from their `name() {` to the matching closing `}`):
   - `test_migration_0012()`, `test_migration_0013()`, `test_migration_0017()`,
     `test_migration_0018()`, `test_migration_0019()`, `test_migration_0021()`
   - `test_meta_destinations_consistency()` AND its two helpers `_roles_from_adapter()` and
     `_roles_from_meta()` (both reference observability meta.yaml/adapter tables)
   - `test_sigterm_mid_apply_preserves_state()` (references add-observability/templates/ at ~line 2162)
2. DELETE their dispatcher blocks (the `if [ -z "$FILTER" ] || [ "$FILTER" = "00NN" ]; then\n  test_migration_00NN\nfi`
   stanzas for 0012/0013/0017/0018/0019/0021; the `destinations` stanza calling
   test_meta_destinations_consistency; the `test-sigterm-mid-apply-preserves-state` guarded stanza).
   Do NOT delete the 0014/0015/0016 dispatcher stanzas, the `preflight` stanza, or the
   `test-skill-md-version-matches-latest-migration-to-version` (drift) stanza.
3. In `test_migration_0011()` (STAYS): REMOVE the 9-line scaffolder sanity check block — the
   `local scaffolder_scan="$REPO_ROOT/add-observability/scan/SCAN.md"` declaration plus the
   `if [ ! -f "$scaffolder_scan" ]; then ... FAIL=$((FAIL+1)); return; fi` guard (RESEARCH §1.5 / Pitfall 2).
   Then fix `run_0011_fixture`: it `cp "$scaffolder_scan" ...` into the sandbox. Replace that copy with
   an inline stub so the 0011 fixture still has a SCAN.md to reference:
   `mkdir -p "$fake_home/.claude/skills/agenticapps-workflow/add-observability/scan" && printf '%s\n' '# SCAN (stub — observability moved to agenticapps-observability)' > "$fake_home/.claude/skills/agenticapps-workflow/add-observability/scan/SCAN.md"`
   (The 0011 migration's verify only checks project-local state, not the scaffolder source, so a stub
   is sufficient. KEEP everything else in test_migration_0011 and run_0011_fixture.)
4. Run the full suite. Expect FAIL=0 and the drift test PASS (drift still reads 0021-tombstone
   to_version 1.20.0 == SKILL.md 1.20.0 — SKILL.md is NOT bumped in this wave).
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && ! grep -q 'test_migration_0019()\|test_migration_0017()\|test_meta_destinations_consistency()\|test_sigterm_mid_apply_preserves_state()' migrations/run-tests.sh && grep -q 'test_migration_0011()' migrations/run-tests.sh && grep -q 'test_migration_0014' migrations/run-tests.sh && ! grep -q 'scaffolder source missing' migrations/run-tests.sh && bash migrations/run-tests.sh 2>&1 | tail -4 | grep -q 'FAIL: 0' && bash migrations/run-tests.sh test-skill-md-version-matches-latest-migration-to-version 2>&1 | grep -qi 'PASS' && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `! grep -q 'test_migration_0019()' migrations/run-tests.sh` (and 0012/0013/0017/0018/0021)
    - `! grep -q 'test_meta_destinations_consistency' migrations/run-tests.sh` and `! grep -q 'test_sigterm_mid_apply_preserves_state' migrations/run-tests.sh`
    - `grep -q 'test_migration_0011()' migrations/run-tests.sh` (0011 body retained)
    - `! grep -q 'scaffolder source missing' migrations/run-tests.sh` (sanity-check block gone)
    - `bash migrations/run-tests.sh` summary shows `FAIL: 0`
    - drift test PASSES: `bash migrations/run-tests.sh test-skill-md-version-matches-latest-migration-to-version` exits 0 / prints PASS
    - SUMMARY records the new PASS baseline integer (was 186/4; now FAIL=0, PASS=lower)
  </acceptance_criteria>
  <done>8 obs-dependent test bodies + helpers + dispatcher stanzas removed; 0011 scaffolder sanity check removed and fixture stubbed; full suite FAIL=0, drift PASS, SKILL.md untouched at 1.20.0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem deletion (`git rm`) | Destructive operation on the repo working tree; mis-targeted globs could delete staying migrations/fixtures |
| migration replay (downstream) | A downstream project replaying the chain reads tombstone .md content; malformed frontmatter could break its update run |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-01 | Tampering | `git rm` deletion globs | mitigate | Explicit per-file paths (no wildcards on the migrations dir except the named fixtures); acceptance_criteria assert staying files (0011/0014/0015/0016 fixtures, ADR-0035) still present after deletion |
| T-30-02 | Denial of Service | tombstone frontmatter | mitigate | Tombstones carry verbatim from_version/to_version (asserted by grep); informational-only body (no Steps) so /update never executes a phantom action; engine ignores unknown fields (README frontmatter table) |
| T-30-03 | Information Disclosure | obs ADR deletion | mitigate | Each ADR confirmed present in obs repo before deletion (no last-copy loss); ADR-0035 explicitly retained |
| T-30-04 | Repudiation | run-tests.sh body removal | accept | Removal of obs-dependent test bodies is the intended cleanup; git history preserves the removed bodies; new baseline recorded in SUMMARY |
</threat_model>

<verification>
- `bash migrations/run-tests.sh` → FAIL: 0; drift test PASS
- `! test -d add-observability`
- 7 `migrations/*-moved.md` tombstones present with verbatim to_version values
- obs ADRs 0029-0034 deleted; ADR-0035 retained
- No SKILL.md version change in this wave (stays 1.20.0; the 2.0.0 bump is plan 30-02)
</verification>

<success_criteria>
- add-observability/ tree, 7 moved migrations, their fixtures, 3 migrate scripts, old-wrappers dir, 6 obs ADRs all deleted
- 7 verbatim-versioned informational tombstones in place; chain contiguous
- run-tests.sh free of all 8 obs-dependent test bodies + the 0011 scaffolder sanity check
- Full suite green (FAIL=0); drift test GREEN (1.20.0 == 1.20.0)
</success_criteria>

<output>
After completion, create `.planning/phases/30-split-03-claude-workflow-2-0-0-follow-up/30-01-SUMMARY.md`
(record: new PASS/FAIL baseline integers; confirmation each obs ADR existed in obs repo before deletion; any discrepancy surfaced).
</output>
