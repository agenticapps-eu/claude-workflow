---
phase: 30-split-03-claude-workflow-2-0-0-follow-up
plan: 02
type: execute
wave: 2
depends_on: [30-01]
files_modified:
  - templates/.claude/hooks/phase-sentinel.sh
  - templates/claude-settings.json
  - migrations/0022-observability-repoint-phase-sentinel.md
  - migrations/test-fixtures/0022/
  - migrations/run-tests.sh
  - skill/SKILL.md
autonomous: true
requirements: [D-02, D-03, D-04, D-07]

must_haves:
  truths:
    - "templates/.claude/hooks/phase-sentinel.sh exists, is executable, and is a deterministic shell gate (no Haiku/prompt-type hook)"
    - "templates/claude-settings.json Stop block invokes phase-sentinel.sh via type:command (no prompt-type Hook 3)"
    - "migrations/0022 repoints the observability requires/verify from add-observability to the observability skill and aborts (exit 3) with an actionable install pointer when the skill is absent — no auto-install"
    - "migrations/0022 carries to_version: 2.0.0 and folds in the #58 phase-sentinel hook swap"
    - "skill/SKILL.md version is 2.0.0 and the drift test PASSES (0022 to_version 2.0.0 == SKILL.md 2.0.0)"
    - "test_migration_0022 and test_phase_sentinel bodies exist and the full suite is green"
  artifacts:
    - path: "templates/.claude/hooks/phase-sentinel.sh"
      provides: "Deterministic Stop hook (exit 0 allow / exit 2 block)"
      min_lines: 14
    - path: "migrations/0022-observability-repoint-phase-sentinel.md"
      provides: "Repoint + 2.0.0 + #58 migration"
      contains: "to_version: 2.0.0"
    - path: "skill/SKILL.md"
      provides: "Skill version 2.0.0"
      contains: "version: 2.0.0"
    - path: "migrations/test-fixtures/0022/"
      provides: "0022 fixtures (abort-if-absent, repoint, hook swap, version bump)"
  key_links:
    - from: "migrations/0022-observability-repoint-phase-sentinel.md"
      to: "~/.claude/skills/observability/SKILL.md"
      via: "requires.verify grep ^name: observability"
      pattern: "grep -q '\\^name: observability'"
    - from: "skill/SKILL.md"
      to: "vendor/agenticapps-shared/migrations/lib/drift-test.sh"
      via: "drift test: SKILL.md version == latest migration (0022) to_version"
      pattern: "version: 2.0.0"
    - from: "templates/claude-settings.json"
      to: "templates/.claude/hooks/phase-sentinel.sh"
      via: "Stop hook type:command path"
      pattern: "phase-sentinel.sh"
---

<objective>
Add migration 0022 — the single migration that (a) repoints the observability install from the
renamed `observability` skill, (b) bumps claude-workflow to 2.0.0, and (c) folds in the #58 fix
(deterministic Phase Sentinel hook) — plus the new template hook and the test bodies that cover all
three deliverables. Bump skill/SKILL.md to 2.0.0 in the SAME wave so the drift test stays green.

Phase 29's supersede pattern (a new 0022 superseding immutable 0021) is reused here: 0011 is NOT
mutated (immutability contract). 0022 chains off the current chain endpoint (from_version: 1.20.0),
not off 0011. (D-02, D-04)

The #58 fix replaces the Haiku prompt-type Stop hook with a 28-line deterministic shell script,
delivered BOTH as a template (new projects) and via the 0022 migration step (existing projects on
/update). (D-07)

CRITICAL SEQUENCING (RESEARCH §2.3, Pitfall 3): create 0022 (to_version: 2.0.0) AND bump
skill/SKILL.md to 2.0.0 in the SAME commit. Splitting them leaves the drift test red between commits.

Purpose: repoint the install to the extracted skill, resolve the 1.20.0/1.21.0 skew via a real
2.0.0 migration, and ship the deterministic hook. (D-02/D-03/D-04/D-07)
Output: phase-sentinel.sh template, 0022 migration, 0022 fixtures + 2 test bodies, SKILL.md 2.0.0,
suite green with drift PASS.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-split-03-claude-workflow-2-0-0-follow-up/30-RESEARCH.md
@.planning/phases/30-split-03-claude-workflow-2-0-0-follow-up/30-VALIDATION.md
@migrations/README.md
@migrations/0011-observability-enforcement.md

<interfaces>
<!-- Concrete shapes copied from RESEARCH so the executor needs no re-derivation. -->

#### phase-sentinel.sh (verbatim from GH #58, RESEARCH §4.2):
```bash
#!/usr/bin/env bash
# phase-sentinel.sh — deterministic Stop hook.
# Allows stop unless .planning/current-phase/checklist.md exists AND
# contains unchecked `- [ ]` items.

set -euo pipefail

checklist="${CLAUDE_PROJECT_DIR:-$PWD}/.planning/current-phase/checklist.md"

[ -f "$checklist" ] || exit 0

unchecked=$(grep -cE '^[[:space:]]*-[[:space:]]*\[[[:space:]]\]' "$checklist" || true)
[ "${unchecked:-0}" -eq 0 ] && exit 0

echo "Phase Sentinel: $unchecked unchecked item(s) remain in $checklist:" >&2
grep -E '^[[:space:]]*-[[:space:]]*\[[[:space:]]\]' "$checklist" | head -5 >&2
exit 2
```

#### claude-settings.json Stop block replacement (RESEARCH §4.3) — replaces current lines ~53-65:
```json
"Stop": [
  {
    "_hook": "Hook 3 — Phase Sentinel (deterministic shell)",
    "hooks": [
      {
        "type": "command",
        "command": "$CLAUDE_PROJECT_DIR/.claude/hooks/phase-sentinel.sh",
        "timeout": 5000
      }
    ]
  }
]
```
The current Hook 3 (to remove) is the `"type": "prompt"`, `"model": "claude-haiku-4-5-20251001"`
entry whose prompt contains the unique substring `.planning/current-phase/checklist.md` (line ~61).

#### 0022 frontmatter (RESEARCH §8.2). from_version is the CURRENT chain endpoint 1.20.0, NOT 1.9.3:
```yaml
# --- (example 0022 frontmatter; commented to avoid confusing the plan parser)
id: 0022
slug: observability-repoint-phase-sentinel
title: Repoint observability to agenticapps-observability; replace Phase Sentinel hook (v1.20.0 -> 2.0.0)
from_version: 1.20.0
to_version: 2.0.0
applies_to:
  - .claude/settings.json
  - .claude/hooks/phase-sentinel.sh
  - .claude/skills/agentic-apps-workflow/SKILL.md
requires:
  - skill: observability
    install: |
      git clone https://github.com/agenticapps-eu/agenticapps-observability \
        ~/.claude/skills/agenticapps-observability && \
      bash ~/.claude/skills/agenticapps-observability/install.sh
    verify: "test -f ~/.claude/skills/observability/SKILL.md && grep -q '^name: observability' ~/.claude/skills/observability/SKILL.md"
# --- end example 0022 frontmatter
```

#### Pre-flight abort (no auto-install — D-03; RESEARCH §3.1):
```bash
test -f "$HOME/.claude/skills/observability/SKILL.md" || {
  echo "ABORT: The 'observability' skill is not installed."
  echo "Install agenticapps-observability separately:"
  echo ""
  echo "  git clone https://github.com/agenticapps-eu/agenticapps-observability \\"
  echo "    ~/.claude/skills/agenticapps-observability"
  echo "  bash ~/.claude/skills/agenticapps-observability/install.sh"
  echo ""
  echo "Then re-run /update-agenticapps-workflow."
  exit 3
}
```

#### 0022 step idempotency anchors (RESEARCH §8.2):
  - hook write: `test -f .claude/hooks/phase-sentinel.sh && grep -q 'phase-sentinel.sh' .claude/settings.json`
  - settings patch: `! grep -q 'current-phase/checklist.md' .claude/settings.json` (old prompt hook gone)
  - version bump: `grep -q '^version: 2.0.0$' .claude/skills/agentic-apps-workflow/SKILL.md`

#### obs install.sh contract (verified live):
  Creates `~/.claude/skills/observability -> $REPO` (canonical) + `~/.claude/skills/add-observability -> $REPO/legacy` (alias).
  obs SKILL.md line 1: `name: observability` (version 0.11.1). So the verify grep above resolves.

#### Existing hook-script pattern (templates/.claude/hooks/): all `#!/usr/bin/env bash`, chmod +x, exit 0 allow / exit 2 block.
#### Drift test fn name in run-tests.sh: `test_skill_md_version_matches_latest_migration_to_version` (filter: test-skill-md-version-matches-latest-migration-to-version).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1 (Wave 0): Write phase-sentinel.sh template + swap claude-settings.json Stop block</name>
  <read_first>
    - .planning/phases/30-split-03-claude-workflow-2-0-0-follow-up/30-RESEARCH.md §4 (#58 fix shape, verbatim script + settings block)
    - templates/claude-settings.json (current Hook 3 prompt-type block, lines ~53-65)
    - templates/.claude/hooks/session-bootstrap.sh (existing hook-script pattern reference)
    - The <interfaces> block above (verbatim phase-sentinel.sh + Stop block JSON)
  </read_first>
  <files>templates/.claude/hooks/phase-sentinel.sh, templates/claude-settings.json</files>
  <action>
1. Create `templates/.claude/hooks/phase-sentinel.sh` with the VERBATIM 28-line script from the
   <interfaces> block (GH #58). `chmod +x templates/.claude/hooks/phase-sentinel.sh` and
   `git add` it (preserve the executable bit — verify with `git ls-files -s` showing mode 100755).
2. In `templates/claude-settings.json`, replace the entire current Hook 3 Stop entry (the
   `"_hook": "Hook 3 — Phase Sentinel (Haiku, prompt-type)"` object containing the `"type": "prompt"`
   / `"model": "claude-haiku-4-5-20251001"` / `prompt` with the `.planning/current-phase/checklist.md`
   substring) with the deterministic Stop block from the <interfaces> block. Keep valid JSON.
3. Validate JSON: `jq . templates/claude-settings.json >/dev/null`.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && test -x templates/.claude/hooks/phase-sentinel.sh && grep -q 'deterministic Stop hook' templates/.claude/hooks/phase-sentinel.sh && jq -e '.. | objects | select(.type? == "command" and (.command? // "" | test("phase-sentinel.sh")))' templates/claude-settings.json >/dev/null && ! grep -q 'claude-haiku-4-5' templates/claude-settings.json && ! grep -q 'current-phase/checklist.md' templates/claude-settings.json && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `test -x templates/.claude/hooks/phase-sentinel.sh`
    - `grep -q 'set -euo pipefail' templates/.claude/hooks/phase-sentinel.sh` and `grep -q 'exit 2' templates/.claude/hooks/phase-sentinel.sh`
    - `jq . templates/claude-settings.json` is valid
    - `! grep -q 'claude-haiku-4-5' templates/claude-settings.json` (prompt-type hook gone)
    - `! grep -q 'current-phase/checklist.md' templates/claude-settings.json` (old prompt text gone)
    - Stop block now references `phase-sentinel.sh` via `type:command`
  </acceptance_criteria>
  <done>Deterministic phase-sentinel.sh shipped as a template (executable); claude-settings.json Stop block swapped from Haiku prompt to type:command; valid JSON.</done>
</task>

<task type="auto">
  <name>Task 2: Author migration 0022 (repoint + abort-if-absent + #58 hook step) and bump skill/SKILL.md to 2.0.0 in the same commit</name>
  <read_first>
    - .planning/phases/30-split-03-claude-workflow-2-0-0-follow-up/30-RESEARCH.md §3 (0011 requires block + 0022 requires shape), §8.2 (0022 skeleton + steps + idempotency anchors), Pitfall 3 (atomic SKILL.md bump)
    - migrations/0011-observability-enforcement.md (the requires/verify + pre-flight shape being superseded)
    - migrations/0014-inject-spec-11-coding-discipline.md (a recent multi-step migration for Step/Idempotency/Apply/Rollback formatting reference)
    - skill/SKILL.md (current version: 1.20.0)
    - The <interfaces> block above (0022 frontmatter, pre-flight abort, step anchors)
  </read_first>
  <files>migrations/0022-observability-repoint-phase-sentinel.md, skill/SKILL.md</files>
  <action>
Create `migrations/0022-observability-repoint-phase-sentinel.md` using the frontmatter from the
<interfaces> block EXACTLY (id 0022, from_version 1.20.0, to_version 2.0.0, the `observability`
requires entry with the canonical verify grep). 0011 is NOT touched.

Pre-flight: include the no-auto-install abort (exit 3) from the <interfaces> block — verifies the
observability skill is present and prints the separate-install pointer if absent (D-03).

Steps (each with Idempotency check / Pre-condition / Apply / Rollback per migrations/README.md format):

- Step 1 — Repoint observability requires in the project: update any `add-observability` reference in
  the project's CLAUDE.md observability cross-reference / Skills line to `observability`. Idempotency:
  `! grep -q 'add-observability' CLAUDE.md || grep -q 'skill: observability' CLAUDE.md`. Pre-condition:
  `grep -q '^observability:' CLAUDE.md`. Apply: sed/replace the `add-observability` skill name to
  `observability` in the observability metadata block / Skills line only (NOT historical prose).
  Rollback: revert the line.

- Step 2 — Install the deterministic Phase Sentinel hook (D-07/#58): copy phase-sentinel.sh into the
  project's `.claude/hooks/` and chmod +x. Idempotency: `test -f .claude/hooks/phase-sentinel.sh`.
  Apply: write the verbatim phase-sentinel.sh body (same as the template) into
  `.claude/hooks/phase-sentinel.sh`; `chmod +x`. Rollback: `rm .claude/hooks/phase-sentinel.sh`.

- Step 3 — Swap the Stop hook in `.claude/settings.json` (D-07/#58): replace the prompt-type Hook 3
  with the `type:command` entry pointing at phase-sentinel.sh. Idempotency:
  `! grep -q 'current-phase/checklist.md' .claude/settings.json`. Pre-condition:
  `test -f .claude/settings.json`. Apply: use `jq` to reconstruct the `.Stop` array — drop the entry
  whose hook has `type=="prompt"` and whose prompt contains `current-phase/checklist.md`, and append
  the deterministic `{type:"command", command:"$CLAUDE_PROJECT_DIR/.claude/hooks/phase-sentinel.sh",
  timeout:5000}` entry (wrapped in the `_hook` object shape). Provide the exact jq filter in the Apply
  block. Rollback: restore the prior Stop array from git.

- Step 4 — Bump installed version: set `version: 2.0.0` in
  `.claude/skills/agentic-apps-workflow/SKILL.md`. Idempotency:
  `grep -q '^version: 2.0.0$' .claude/skills/agentic-apps-workflow/SKILL.md`. Apply: rewrite the
  version line. Rollback: restore prior version.

Post-checks: all idempotency greps pass; `jq .` validates `.claude/settings.json`.

THEN, in the SAME commit (Pitfall 3): bump `skill/SKILL.md` frontmatter `version: 1.20.0` -> `version: 2.0.0`.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && test -f migrations/0022-observability-repoint-phase-sentinel.md && grep -q '^from_version: 1.20.0$' migrations/0022-observability-repoint-phase-sentinel.md && grep -q '^to_version: 2.0.0$' migrations/0022-observability-repoint-phase-sentinel.md && grep -q "skill: observability" migrations/0022-observability-repoint-phase-sentinel.md && grep -q "name: observability" migrations/0022-observability-repoint-phase-sentinel.md && grep -q 'ABORT: The .observability. skill is not installed' migrations/0022-observability-repoint-phase-sentinel.md && grep -q 'phase-sentinel.sh' migrations/0022-observability-repoint-phase-sentinel.md && grep -q '^version: 2.0.0$' skill/SKILL.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `test -f migrations/0022-observability-repoint-phase-sentinel.md`
    - `grep -q '^to_version: 2.0.0$' migrations/0022-*.md` and `grep -q '^from_version: 1.20.0$' migrations/0022-*.md`
    - 0022 requires entry: `grep -q 'skill: observability' migrations/0022-*.md` AND verify greps `^name: observability`
    - abort-if-absent present (no auto-install): `grep -q 'ABORT: The .observability. skill is not installed' migrations/0022-*.md` and `grep -q 'exit 3' migrations/0022-*.md`
    - #58 hook folded in: `grep -q 'phase-sentinel.sh' migrations/0022-*.md`
    - 0011 NOT mutated: `git diff --quiet migrations/0011-observability-enforcement.md`
    - `grep -q '^version: 2.0.0$' skill/SKILL.md`
  </acceptance_criteria>
  <done>0022 migration authored (repoint to observability skill, exit-3 abort if absent, #58 hook swap steps, to_version 2.0.0); 0011 untouched; skill/SKILL.md bumped to 2.0.0 in the same commit.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add 0022 fixtures + test_migration_0022 + test_phase_sentinel bodies; full suite green with drift PASS</name>
  <read_first>
    - .planning/phases/30-split-03-claude-workflow-2-0-0-follow-up/30-VALIDATION.md (Wave 0 requirements: test_phase_sentinel + test_migration_0022 bodies; drift guard)
    - .planning/phases/30-split-03-claude-workflow-2-0-0-follow-up/30-RESEARCH.md §Validation Architecture (test->command map; 3 phase-sentinel cases)
    - migrations/run-tests.sh (a retained fixture-based test e.g. test_migration_0014 for harness shape + dispatcher registration pattern; the drift dispatcher stanza with declare -F guard)
    - migrations/test-fixtures/README.md (fixture contract)
  </read_first>
  <behavior>
    - phase-sentinel: no checklist.md -> exit 0 (allow)
    - phase-sentinel: checklist.md with all items checked -> exit 0 (allow)
    - phase-sentinel: checklist.md with >=1 unchecked `- [ ]` -> exit 2 (block) and prints the unchecked item(s)
    - 0022 migration: applied to a fixture project at 1.20.0 -> CLAUDE.md observability ref says `observability` (not add-observability); .claude/hooks/phase-sentinel.sh present; .claude/settings.json Stop block has no `current-phase/checklist.md` prompt; SKILL.md version 2.0.0
    - 0022 idempotent reapply: second run is a no-op (idempotency greps short-circuit)
    - drift: SKILL.md 2.0.0 == latest migration (0022) to_version 2.0.0 -> PASS
  </behavior>
  <files>migrations/test-fixtures/0022/, migrations/run-tests.sh</files>
  <action>
RED first: add the two test bodies and assert they fail before the implementation is wired, then make
them GREEN. (phase-sentinel.sh + 0022 migration already exist from Tasks 1-2, so RED here means the
fixtures/dispatcher are absent — author the fixtures and bodies, run, confirm GREEN.)

1. Create `migrations/test-fixtures/0022/` fixture set mirroring the retained-fixture pattern (e.g.
   how 0014 fixtures are laid out): a before-state project at version 1.20.0 with a prompt-type Stop
   hook and an `observability:` CLAUDE.md block, plus expected-after assertions. Include at least:
   - `repoint-and-hookswap/` — before: 1.20.0 project; after: SKILL.md 2.0.0, phase-sentinel.sh present,
     Stop block swapped, observability ref repointed.
   - `abort-when-skill-absent/` — asserts the pre-flight exit-3 path message when
     `~/.claude/skills/observability/SKILL.md` is absent in the sandbox HOME.
   - `idempotent-reapply/` — second apply is a no-op.
2. Add `test_migration_0022()` to run-tests.sh (model it on `test_migration_0014`'s fixture-runner
   shape) and register a dispatcher stanza:
   `if [ -z "$FILTER" ] || [ "$FILTER" = "0022" ]; then test_migration_0022; fi`
3. Add `test_phase_sentinel()` — an inline test (no fixture dir needed) that runs
   `templates/.claude/hooks/phase-sentinel.sh` under a temp `CLAUDE_PROJECT_DIR` across the 3 cases
   in <behavior> and asserts exit codes 0/0/2. Register a dispatcher stanza
   `if [ -z "$FILTER" ] || [ "$FILTER" = "phase-sentinel" ]; then test_phase_sentinel; fi`.
4. Run `bash migrations/run-tests.sh 0022`, `bash migrations/run-tests.sh phase-sentinel`, and the
   drift filter; then the full suite. Expect FAIL=0 and drift PASS (2.0.0 == 2.0.0).
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && test -d migrations/test-fixtures/0022 && grep -q 'test_migration_0022()' migrations/run-tests.sh && grep -q 'test_phase_sentinel' migrations/run-tests.sh && bash migrations/run-tests.sh phase-sentinel 2>&1 | grep -qi PASS && bash migrations/run-tests.sh 0022 2>&1 | grep -qi PASS && bash migrations/run-tests.sh test-skill-md-version-matches-latest-migration-to-version 2>&1 | grep -qi PASS && bash migrations/run-tests.sh 2>&1 | tail -4 | grep -q 'FAIL: 0' && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `test -d migrations/test-fixtures/0022`
    - `grep -q 'test_migration_0022()' migrations/run-tests.sh` with a registered dispatcher stanza
    - `grep -q 'test_phase_sentinel' migrations/run-tests.sh` covering the 3 exit-code cases (0/0/2)
    - `bash migrations/run-tests.sh phase-sentinel` PASS; `bash migrations/run-tests.sh 0022` PASS
    - drift: `bash migrations/run-tests.sh test-skill-md-version-matches-latest-migration-to-version` PASS (2.0.0 == 2.0.0)
    - full suite `FAIL: 0`
    - SUMMARY records the new green PASS baseline integer
  </acceptance_criteria>
  <done>0022 fixtures + test_migration_0022 + test_phase_sentinel registered and green; drift test GREEN at 2.0.0; full suite FAIL=0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| /update applying 0022 on a downstream project | Migration mutates `.claude/settings.json` (a config that gates Stop) and installs an executable hook |
| Stop hook execution | phase-sentinel.sh runs on every Stop with CLAUDE_PROJECT_DIR-derived path |
| obs-skill presence check | Pre-flight gates the install on a host-side skill path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-05 | Elevation of Privilege | phase-sentinel.sh | mitigate | Script is deterministic, `set -euo pipefail`, only reads checklist.md and greps; no eval, no network, no write; path is `${CLAUDE_PROJECT_DIR:-$PWD}`-anchored (no arbitrary path injection) |
| T-30-06 | Tampering | 0022 jq patch of .claude/settings.json | mitigate | jq selector narrowly targets the `type=="prompt"` entry with the `current-phase/checklist.md` substring (Pitfall 5); idempotency anchor prevents double-apply; post-check validates JSON with `jq .` |
| T-30-07 | Denial of Service | abort-if-absent pre-flight | mitigate | Exit 3 with actionable install pointer (no auto-install per D-03) — fails closed; downstream is told exactly how to install the observability skill |
| T-30-08 | Spoofing | observability skill verify | mitigate | verify greps `^name: observability` in the resolved SKILL.md (canonical symlink target), not just file presence |
| T-30-09 | Tampering | immutable 0011 | mitigate | 0022 is a NEW migration; acceptance asserts `git diff --quiet migrations/0011-*.md` (0011 byte-unchanged) |
</threat_model>

<verification>
- `bash migrations/run-tests.sh` → FAIL: 0
- drift test PASS: latest migration 0022 to_version 2.0.0 == skill/SKILL.md 2.0.0
- `bash migrations/run-tests.sh phase-sentinel` PASS (3 cases); `bash migrations/run-tests.sh 0022` PASS
- 0011 unchanged (`git diff --quiet migrations/0011-observability-enforcement.md`)
- templates/claude-settings.json has no Haiku/prompt Hook 3
</verification>

<success_criteria>
- phase-sentinel.sh template (executable) + swapped Stop block shipped (new projects)
- 0022 migration repoints to the observability skill, aborts (exit 3) if absent with no auto-install, folds in the #58 hook swap, carries to_version 2.0.0
- 0011 not mutated; skill/SKILL.md == 2.0.0
- 0022 + phase-sentinel test bodies green; drift test GREEN; full suite FAIL=0
</success_criteria>

<output>
After completion, create `.planning/phases/30-split-03-claude-workflow-2-0-0-follow-up/30-02-SUMMARY.md`
(record: new PASS baseline; drift before/after; confirmation 0011 byte-unchanged; jq filter used for the Stop swap).
</output>
