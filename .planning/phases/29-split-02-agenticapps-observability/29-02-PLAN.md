---
phase: 29-split-02-agenticapps-observability
plan: 02
type: execute
wave: 2
depends_on: [29-01]
files_modified:
  - /tmp/cw-scratch-for-obs (scratch clone — disposable)
  - ~/Sourcecode/agenticapps/agenticapps-observability/SKILL.md
  - ~/Sourcecode/agenticapps/agenticapps-observability/CHANGELOG.md
  - ~/Sourcecode/agenticapps/agenticapps-observability/init/
  - ~/Sourcecode/agenticapps/agenticapps-observability/scan/
  - ~/Sourcecode/agenticapps/agenticapps-observability/templates/
  - ~/Sourcecode/agenticapps/agenticapps-observability/enforcement/
  - ~/Sourcecode/agenticapps/agenticapps-observability/migrations/
  - ~/Sourcecode/agenticapps/agenticapps-observability/docs/decisions/
autonomous: false
requirements: []
tags: [repo-split, git-filter-repo, history-preservation, migration-ownership]
user_setup: []

must_haves:
  truths:
    - "The observability tree (add-observability/) lives at the obs repo root WITH git history"
    - "git log --follow works on SKILL.md, a migration script, and a fixture (3+ moved files)"
    - "Migrations 0012, 0013, 0017, 0018, 0019, 0020, 0021 + their scripts + 0017/0019/0021 fixtures + ADRs 0029-0034 are present"
    - "0011 (install migration) was NOT moved (stays in claude-workflow)"
    - "claude-workflow's own working tree and baseline are unchanged (filter-repo ran on a scratch clone only)"
  artifacts:
    - path: "~/Sourcecode/agenticapps/agenticapps-observability/SKILL.md"
      provides: "Observability skill manifest (still name: add-observability until 29-03)"
      contains: "name: add-observability"
    - path: "~/Sourcecode/agenticapps/agenticapps-observability/migrations/scripts/migrate-0019.sh"
      provides: "0019 engine (renamed path)"
    - path: "~/Sourcecode/agenticapps/agenticapps-observability/migrations/test-fixtures/0019"
      provides: "13 fixtures for 0019"
    - path: "~/Sourcecode/agenticapps/agenticapps-observability/docs/decisions/0033-with-queue-monitor.md"
      provides: "Moved observability ADR"
  key_links:
    - from: "obs repo SKILL.md"
      to: "claude-workflow add-observability/SKILL.md history"
      via: "git filter-repo --path-rename add-observability/:"
      pattern: "add-observability/SKILL.md"
---

<objective>
Phase B of SPLIT-02. Extract the observability tree from claude-workflow into the obs repo
WITH full `git log --follow` lineage, using `git filter-repo --path-rename` against a
DISPOSABLE scratch clone, then push the filtered history into the obs repo created in 29-01.

The migration-ownership audit (29-RESEARCH § Migration Ownership Audit) is authoritative:
8 migrations MOVE (0012, 0013, 0017, 0018, 0019, 0020, 0021 + 0022-new-in-29-04); 0011 STAYS.
0017 MOVES (overriding the SPLIT-02 doc line 122 "stays") because its engine sources
`add-observability/templates/` — observability owns the failure mode. The 4 known-failing
test_migration_0017 fixtures travel with 0017 (documented as obs follow-up, NOT fixed here).

Purpose: History-preserving relocation so blame/log survive the split.
Output: Populated obs repo tree (still named add-observability internally — rename is 29-03).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/29-split-02-agenticapps-observability/29-CONTEXT.md
@.planning/phases/29-split-02-agenticapps-observability/29-RESEARCH.md
@SPLIT-02-agenticapps-observability.md

<migration_ownership_table>
AUTHORITATIVE (29-RESEARCH § Migration Ownership Audit). MOVE = goes to obs repo.

| Migration | Disposition | Note |
|-----------|-------------|------|
| 0011 observability-enforcement | STAYS | bootstraps skill INTO consumers; claude-workflow failure mode; Phase 30 repoints |
| 0012 slash-discovery | MOVE | applies_to add-observability skill symlink |
| 0013 auto-init-and-stale-vendored-cleanup | MOVE | obs skill stale-copy cleanup |
| 0017 add-axiom-logs-destination | MOVE | engine sources add-observability/templates/; 4 known-fail fixtures travel |
| 0018 postphase-observability-hook | MOVE | installs obs scan hook |
| 0019 sentry-crons-and-healthz | MOVE | ships cron-monitor.ts |
| 0020 openrouter-integration | MOVE | obs OpenRouter kit |
| 0021 cron-monitor-shape-and-queues | MOVE | ships queue-monitor.ts |

ADRs MOVE: 0029, 0030, 0031, 0032, 0033, 0034 (all observability-scoped, verified present).
ADR-0035 (shared-extraction-boundaries) STAYS in claude-workflow.
</migration_ownership_table>

<filter_repo_command>
EXACT path mappings (29-RESEARCH § Pattern 1, with full migration set per the audit).
Verify `--path-rename` syntax with `git-filter-repo --help` first (Assumption A1). Use a
DRY-RUN check before the destructive run.

Paths to KEEP + rename (source : target):
- add-observability/                                              → (repo root)
- migrations/0012-slash-discovery.md                             → migrations/0012-slash-discovery.md
- migrations/0013-auto-init-and-stale-vendored-cleanup.md        → (same)
- migrations/0017-add-axiom-logs-destination.md                  → (same)
- migrations/0018-postphase-observability-hook.md                → (same)
- migrations/0019-sentry-crons-and-healthz.md                    → (same)
- migrations/0020-openrouter-integration.md                      → (same)
- migrations/0021-cron-monitor-shape-and-queues.md               → (same)  [VERIFY exact filename in source]
- templates/.claude/scripts/migrate-0017-axiom-destination.sh    → migrations/scripts/migrate-0017.sh
- templates/.claude/scripts/migrate-0017-old-wrappers/           → migrations/scripts/migrate-0017-old-wrappers/
- templates/.claude/scripts/migrate-0019-sentry-crons-and-healthz.sh → migrations/scripts/migrate-0019.sh
- templates/.claude/scripts/migrate-0021-with-cron-and-queue-updates.sh → migrations/scripts/migrate-0021.sh
- migrations/test-fixtures/0017/                                 → migrations/test-fixtures/0017/
- migrations/test-fixtures/0019/                                 → migrations/test-fixtures/0019/
- migrations/test-fixtures/0021/                                 → migrations/test-fixtures/0021/
- docs/decisions/0029-cron-monitor-sdk-composition.md            → (same)
- docs/decisions/0030-openrouter-integration-sdk-first.md        → (same)
- docs/decisions/0031-0019-engine-index-ts-anchor.md             → (same)
- docs/decisions/0032-cron-monitor-generic-narrowing-cf-worker-only.md → (same)
- docs/decisions/0033-with-queue-monitor.md                      → (same)
- docs/decisions/0034-observability-init-singleton-invariant.md  → (same)

NOTE on 0012/0013/0018/0020: these may NOT have observability-specific migrate-*.sh scripts
(they wire skill discovery/hooks, not template content). Move the .md only; if a corresponding
migrate-00NN script exists in templates/.claude/scripts/ AND is observability-coupled, move it
too. The executor MUST `ls templates/.claude/scripts/` in the scratch clone and confirm which
scripts exist before finalizing the --path set.

DO NOT MOVE: migrations/0011-*.md (STAYS), ADR-0035, any framework migrate-*.sh that is generic.
</filter_repo_command>

<cross_repo_note>
filter-repo is DESTRUCTIVE to the clone it runs on. ALWAYS run on `/tmp/cw-scratch-for-obs`
(a fresh `git clone`), NEVER on `~/Sourcecode/agenticapps/claude-workflow`. Idempotency:
re-running requires a FRESH scratch clone (discard + re-clone on failure). The push into the
obs repo merges the filtered history onto the 29-01 skeleton — this requires care (see Task 3).
This plan is autonomous:false (the push is outward-facing).
</cross_repo_note>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fresh scratch clone + confirm exact source paths exist (pre-flight, non-destructive)</name>
  <read_first>
    - 29-RESEARCH.md § "Pattern 1: git filter-repo Whole-File Extraction" (scratch-clone discipline)
    - <filter_repo_command> block above (the path set to validate)
  </read_first>
  <files>/tmp/cw-scratch-for-obs</files>
  <action>
    1. `rm -rf /tmp/cw-scratch-for-obs` then
       `git clone https://github.com/agenticapps-eu/claude-workflow /tmp/cw-scratch-for-obs`
       (use the REMOTE, not the local working tree — guarantees no uncommitted noise travels).
    2. `cd /tmp/cw-scratch-for-obs` and enumerate the actual source paths so the filter-repo
       command in Task 2 has a verified path list. Run and capture:
       - `ls add-observability/`
       - `ls templates/.claude/scripts/ | grep -E "migrate-001[2378]|migrate-0019|migrate-002[01]"`
       - `ls migrations/ | grep -E "^001[2-9]|^002[01]"`  (confirm exact filenames, esp. 0021)
       - `ls migrations/test-fixtures/ | grep -E "^001[79]|^0021"`
       - `ls docs/decisions/ | grep -E "002[9]|003[0-4]"`
    3. Confirm migration 0011 .md is PRESENT in source (so we can confirm we are intentionally
       NOT moving it). Record the exact 0021 markdown filename (it may be
       `0021-cron-monitor-shape-and-queues.md` or `0021-with-cron-and-queue-updates.md` —
       use whatever `ls` reports).
    4. Run `git-filter-repo --help | grep -A2 path-rename` to confirm `--path-rename src:dst`
       syntax (Assumption A1). If the flag differs, record the correct form for Task 2.
    Output a verified path manifest to the SUMMARY (do not run filter-repo yet).
  </action>
  <verify>
    <automated>test -d /tmp/cw-scratch-for-obs/.git && test -d /tmp/cw-scratch-for-obs/add-observability && test -f /tmp/cw-scratch-for-obs/templates/.claude/scripts/migrate-0019-sentry-crons-and-healthz.sh && echo PREFLIGHT_OK</automated>
  </verify>
  <acceptance_criteria>
    - `test -d /tmp/cw-scratch-for-obs/add-observability` exits 0
    - The exact 0021 markdown filename is recorded (from `ls migrations/`)
    - `migrate-0017-axiom-destination.sh`, `migrate-0019-sentry-crons-and-healthz.sh`, `migrate-0021-with-cron-and-queue-updates.sh` all confirmed present
    - All six ADRs 0029-0034 confirmed present in `docs/decisions/`
    - `--path-rename src:dst` syntax confirmed (or corrected form recorded)
  </acceptance_criteria>
  <done>Fresh scratch clone exists; verified path manifest captured; filter-repo flag syntax confirmed.</done>
</task>

<task type="auto">
  <name>Task 2: Run git filter-repo (DRY-RUN then destructive) on the scratch clone</name>
  <read_first>
    - 29-RESEARCH.md § "Pattern 1" + "Pitfall 1: filter-repo path-rename order sensitivity"
    - The verified path manifest from Task 1
    - <filter_repo_command> block above
  </read_first>
  <files>/tmp/cw-scratch-for-obs</files>
  <action>
    In `/tmp/cw-scratch-for-obs`, run filter-repo with the full path set from the
    <filter_repo_command> block, using the EXACT 0021 filename verified in Task 1. Pair every
    `--path <src>` with a matching `--path-rename <src>:<dst>` (Pitfall 1). The
    `--path-rename add-observability/:` (empty target) hoists the obs tree to repo root.

    Run order:
    1. First, a non-destructive analysis: `git filter-repo --analyze` then inspect
       `.git/filter-repo/analysis/path-all-sizes.txt` to confirm the intended paths exist.
    2. Then the destructive run with ALL `--path` + `--path-rename` flags from the block,
       substituting the verified 0021 filename. For 0012/0013/0018/0020: include their
       migrate-*.sh ONLY if Task 1 confirmed an observability-coupled script exists.
    3. filter-repo will refuse on a non-fresh repo unless `--force`; since this is the first
       run on a fresh clone, no `--force` needed. If it errors "expected freshly packed repo",
       re-clone (Task 1) and retry — do NOT pass `--force` blindly.

    After the run, the scratch clone's HEAD tree should show: SKILL.md at root, init/, scan/,
    scan-apply/, enforcement/, templates/, migrations/{scripts,test-fixtures,00NN.md},
    docs/decisions/0029-0034. NOTHING else from claude-workflow.

    Confirm 0011 is ABSENT: `test ! -f migrations/0011-*.md` (it must NOT have been moved).
    Confirm ADR-0035 is ABSENT.
  </action>
  <verify>
    <automated>cd /tmp/cw-scratch-for-obs && test -f SKILL.md && test -f migrations/scripts/migrate-0019.sh && test -d migrations/test-fixtures/0019 && test -d docs/decisions && ! ls migrations/0011-* >/dev/null 2>&1 && echo FILTER_OK</automated>
  </verify>
  <acceptance_criteria>
    - `test -f /tmp/cw-scratch-for-obs/SKILL.md` exits 0 (obs tree hoisted to root)
    - `test -f /tmp/cw-scratch-for-obs/migrations/scripts/migrate-0019.sh` exits 0 (renamed)
    - `test -f /tmp/cw-scratch-for-obs/migrations/scripts/migrate-0017.sh` exits 0
    - `test -d /tmp/cw-scratch-for-obs/migrations/test-fixtures/0017` AND `/0019` AND `/0021` all exit 0
    - `ls /tmp/cw-scratch-for-obs/migrations/0011-* 2>/dev/null` returns NOTHING (0011 stayed)
    - `ls /tmp/cw-scratch-for-obs/docs/decisions/0035-* 2>/dev/null` returns NOTHING (ADR-0035 stayed)
    - `git -C /tmp/cw-scratch-for-obs log --follow --oneline -- SKILL.md | head -5` shows pre-extraction commits (history preserved)
  </acceptance_criteria>
  <done>Scratch clone rewritten to obs-only tree at correct paths; 0011 + ADR-0035 confirmed absent; git log --follow works.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Merge filtered history into the obs repo + push (HUMAN-GATED — outward-facing)</name>
  <read_first>
    - 29-RESEARCH.md § "Pattern 1" step 3 (push into new repo)
    - 29-01-SUMMARY.md (the skeleton commit already on obs main)
  </read_first>
  <action>
    Add the scratch clone as a `filtered` remote, `git merge filtered/main --allow-unrelated-histories`, resolve CHANGELOG by keeping the 29-01 root version, then `git push origin main` (plain — NO --force). Clean up /tmp/cw-scratch-for-obs. See <what-built> for exact commands. Outward-facing — pause for human verify after.
  </action>
  <what-built>
    The obs repo (29-01) already has a skeleton commit + submodule on `main`. The scratch
    clone has the filtered observability history on its own `main`. These are unrelated
    histories. Merge approach (preserves BOTH the skeleton's submodule AND the filtered history):

    ```bash
    cd ~/Sourcecode/agenticapps/agenticapps-observability
    git remote add filtered /tmp/cw-scratch-for-obs
    git fetch filtered
    # Merge the filtered (unrelated) history, keeping our skeleton's submodule + metadata.
    git merge filtered/main --allow-unrelated-histories --no-edit
    git remote remove filtered
    git push origin main      # plain push — NO --force
    ```

    If the merge conflicts on CHANGELOG.md (both sides have one — skeleton's vs the moved
    add-observability/CHANGELOG.md): keep the obs-root CHANGELOG.md authored in 29-01 (the
    0.11.0 continuation); the moved add-observability/CHANGELOG.md history is preserved via
    git but the canonical file is the 29-01 one. Resolve by keeping the 29-01 version, then
    `git add CHANGELOG.md && git commit --no-edit`.

    NEVER `--force`. The push is plain `git push origin main`.

    After push, clean up: `rm -rf /tmp/cw-scratch-for-obs`.
  </what-built>
  <how-to-verify>
    1. `git -C ~/Sourcecode/agenticapps/agenticapps-observability log --follow --oneline -- SKILL.md | head -5` shows pre-extraction history.
    2. `git -C ~/Sourcecode/agenticapps/agenticapps-observability log --follow --oneline -- migrations/scripts/migrate-0019.sh | head -3` shows history.
    3. Submodule still pinned: `git -C ~/Sourcecode/agenticapps/agenticapps-observability ls-files -s vendor/agenticapps-shared` == 1f5d543.
    4. Remote HEAD updated: `gh repo view agenticapps-eu/agenticapps-observability`.
    5. claude-workflow untouched: `cd ~/Sourcecode/agenticapps/claude-workflow && git status` clean of unexpected changes (only the .planning/ plan docs).
  </how-to-verify>
  <verify>
    <automated>cd ~/Sourcecode/agenticapps/agenticapps-observability && [ "$(git log --follow --oneline -- SKILL.md | wc -l)" -ge 3 ] && git ls-files -s vendor/agenticapps-shared | awk '{print $2}' | grep -q 1f5d543bc6ca080ab6e3ba188df33cf3d193e3d4 && echo MERGE_OK</automated>
  </verify>
  <done>Filtered history merged + pushed; git log --follow works on 3+ files; submodule pin intact; claude-workflow untouched.</done>
  <acceptance_criteria>
    - `git -C ~/Sourcecode/agenticapps/agenticapps-observability log --follow --oneline -- SKILL.md | wc -l` ≥ 3
    - `git -C ~/Sourcecode/agenticapps/agenticapps-observability log --follow --oneline -- migrations/scripts/migrate-0019.sh | wc -l` ≥ 1
    - `git -C ~/Sourcecode/agenticapps/agenticapps-observability ls-files -s vendor/agenticapps-shared | awk '{print $2}'` == `1f5d543bc6ca080ab6e3ba188df33cf3d193e3d4`
    - `test -f ~/Sourcecode/agenticapps/agenticapps-observability/templates/ts-cloudflare-worker/cron-monitor.ts` exits 0 (template tree present)
    - claude-workflow `git -C ~/Sourcecode/agenticapps/claude-workflow status --short add-observability/` shows NO modifications
  </acceptance_criteria>
  <resume-signal>Type "approved" once filtered history is merged + pushed and git log --follow works on 3+ files, or describe issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live claude-workflow → scratch clone | filter-repo destructive rewrite must never touch the live repo |
| scratch clone → obs repo push | outward-facing merge + push of rewritten history |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-05 | Tampering | filter-repo target | mitigate | Run ONLY on `/tmp/cw-scratch-for-obs` (fresh `git clone` of remote); never on `~/Sourcecode/.../claude-workflow`; verify claude-workflow `git status` clean (Task 3 acceptance) |
| T-29-06 | Elevation | push to obs main | mitigate | Plain `git push origin main`; NEVER `--force`; Task 3 is autonomous:false human-gated |
| T-29-07 | Tampering | wrong migration moved (0011) | mitigate | Explicit acceptance check `ls migrations/0011-*` returns nothing; audit table authoritative |
| T-29-08 | Repudiation | history lineage lost | mitigate | `git log --follow` verified on 3+ files (SKILL.md, migrate-0019.sh, a fixture) before approve |
| T-29-09 | Tampering | submodule clobbered by merge | mitigate | `--allow-unrelated-histories` merge keeps skeleton submodule; gitlink SHA re-verified post-merge |
</threat_model>

<verification>
- git log --follow works on SKILL.md, migrate-0019.sh, and a fixture (Task 3)
- 0011 + ADR-0035 absent from obs repo (Task 2)
- Submodule pin intact after merge (Task 3)
- claude-workflow working tree unmodified (Task 3)
</verification>

<success_criteria>
- Obs repo tree populated with full observability content at correct paths
- History preserved (git log --follow on 3+ moved files)
- 8 migrations moved per audit; 0011 stayed; ADRs 0029-0034 moved, 0035 stayed
- Submodule still pinned at 1f5d543; claude-workflow untouched
</success_criteria>

<output>
After completion, create `.planning/phases/29-split-02-agenticapps-observability/29-02-SUMMARY.md`
recording: the exact filter-repo command run, the verified moved-file list, git log --follow
evidence on 3+ files, and confirmation 0011/ADR-0035 stayed and the 4 known-fail 0017 fixtures travelled.
</output>
