---
phase: 29-split-02-agenticapps-observability
plan: 03
type: execute
wave: 3
depends_on: [29-02]
files_modified:
  - ~/Sourcecode/agenticapps/agenticapps-observability/SKILL.md
  - ~/Sourcecode/agenticapps/agenticapps-observability/init/INIT.md
  - ~/Sourcecode/agenticapps/agenticapps-observability/scan/SCAN.md
  - ~/Sourcecode/agenticapps/agenticapps-observability/README.md
  - ~/Sourcecode/agenticapps/agenticapps-observability/legacy/SKILL.md
  - ~/Sourcecode/agenticapps/agenticapps-observability/install.sh
  - ~/Sourcecode/agenticapps/agenticapps-observability/migrations/run-tests.sh
autonomous: true
requirements: []
tags: [repo-split, skill-rename, dual-symlink-alias, source-and-keep, drift-test]
user_setup: []

must_haves:
  truths:
    - "Plans 03+04 commit on the obs-repo feature branch split-02-rename-and-0022 (NOT main; PR merged before the v0.11.0 tag in 29-05)"
    - "SKILL.md is named observability at version 0.11.0"
    - "A legacy/SKILL.md add-observability alias exists with a deprecation banner"
    - "install.sh creates BOTH symlinks (observability canonical + add-observability legacy) with clobber-guard"
    - "/observability and /add-observability both resolve to a SKILL.md after install.sh"
    - "migrations/run-tests.sh sources the shared lib and calls run_drift_test with REPO_ROOT/SKILL.md (root, not skill/SKILL.md)"
  artifacts:
    - path: "~/Sourcecode/agenticapps/agenticapps-observability/SKILL.md"
      provides: "Renamed skill manifest"
      contains: "name: observability"
    - path: "~/Sourcecode/agenticapps/agenticapps-observability/legacy/SKILL.md"
      provides: "add-observability deprecation alias"
      contains: "name: add-observability"
    - path: "~/Sourcecode/agenticapps/agenticapps-observability/install.sh"
      provides: "Dual-symlink installer with clobber-guard"
      contains: "observability"
    - path: "~/Sourcecode/agenticapps/agenticapps-observability/migrations/run-tests.sh"
      provides: "Source-and-keep migration runner shim (obs SKILL.md path at repo root)"
      contains: "vendor/agenticapps-shared/migrations/lib"
  key_links:
    - from: "obs migrations/run-tests.sh"
      to: "vendor/agenticapps-shared/migrations/lib/*.sh"
      via: "BASH_SOURCE[0]-relative source"
      pattern: "source .*agenticapps-shared/migrations/lib"
    - from: "obs run-tests.sh drift policy"
      to: "$REPO_ROOT/SKILL.md"
      via: "run_drift_test with explicit root path (NOT skill/SKILL.md)"
      pattern: "run_drift_test .*REPO_ROOT/SKILL.md"
---

<objective>
Phase C of SPLIT-02 (new-repo side only). Rename the skill `add-observability` → `observability`
(version 0.10.0 → 0.11.0), scaffold the Option A dual-symlink legacy alias, write the obs
`install.sh` (dual symlink + clobber-guard + submodule sync), and author the obs
`migrations/run-tests.sh` as a source-and-keep shim that sources the shared lib and runs the
existing WORKFLOW per-migration test bodies + the drift policy with the obs-correct SKILL.md
path (repo ROOT, not `skill/SKILL.md`).

Phase 30 owns the claude-workflow side (deleting add-observability/, alias window management).
This plan touches ONLY the obs repo.

FEATURE-BRANCH POLICY (per global CLAUDE.md "never commit directly to main"): the substantive
development in plans 03 + 04 (skill rename, install.sh, run-tests.sh shim, migration 0022 +
ADR-0036) lands on a feature branch IN the obs repo — `split-02-rename-and-0022` — NOT on `main`.
Task 0 below creates + checks out that branch. Plan 04 continues committing to it. Plan 05 opens
a PR for the branch and merges it to main BEFORE tagging v0.11.0. (Plans 01–02 bootstrapped the
fresh repo directly on main — no pre-existing branch to PR against; SPLIT-01 precedent.)

Purpose: Make the obs skill discoverable under both names and runnable via the shared runner.
Output: Renamed + installable skill with a working test harness shim (0019/0021 green; 0022 comes in 29-04), all committed on the split-02-rename-and-0022 feature branch.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/29-split-02-agenticapps-observability/29-CONTEXT.md
@.planning/phases/29-split-02-agenticapps-observability/29-RESEARCH.md
@install.sh
@migrations/run-tests.sh

<interfaces>
Shared lib API the obs run-tests.sh sources (from vendor/agenticapps-shared/migrations/lib/, v1.0.0):
- helpers.sh        — _runtests_do_cleanup, color vars (GREEN/RED/RESET), extract_to deps
- fixture-runner.sh — extract_to(ref, src, dst); run_fixture machinery
- preflight.sh      — run_preflight_verify_paths (reads ${STRICT_PREFLIGHT:-0} internally; set -u safe)
- drift-test.sh     — run_drift_test(skill_md_path, migrations_dir) → returns 0 (match) / 1 (mismatch);
                      NO PASS/FAIL mutation (policy wrapper owns counting — D-28d separation)

claude-workflow run-tests.sh (the reference): lines 35-66 = BASH_SOURCE resolution + fail-closed
guard + 4 source lines (copy VERBATIM). Lines 68-77 = traps + REPO_ROOT. The obs shim mirrors
this EXACTLY, except the drift policy wrapper passes "$REPO_ROOT/SKILL.md" (obs root) instead
of "$REPO_ROOT/skill/SKILL.md" (claude-workflow nesting).
</interfaces>

<key_pitfalls>
- Pitfall 2 (29-RESEARCH): obs SKILL.md is at REPO ROOT. The drift policy MUST call
  `run_drift_test "$REPO_ROOT/SKILL.md" "$REPO_ROOT/migrations"` — copying claude-workflow's
  `skill/SKILL.md` path makes the drift test silently no-op or always-fail.
- Pitfall 3 (29-RESEARCH): drift checks the LATEST migration's to_version against SKILL.md version.
  At end of THIS plan the latest moved migration (0021) has to_version: 1.20.0, but SKILL.md is
  0.11.0 → drift would FAIL. RESOLUTION: 29-04 adds migration 0022 with to_version: 0.11.0 (obs
  version) as the latest, making drift PASS. THEREFORE: in THIS plan, the drift test is EXPECTED
  to fail until 29-04 lands. Wire the policy correctly now; full-suite drift-green is a 29-05 gate.
- Pitfall 6: moved migration .md files reference scripts at the OLD claude-workflow path
  (templates/.claude/scripts/migrate-00NN-*.sh). Task 3 repoints these to migrations/scripts/migrate-00NN.sh.
- Option A alias (Pattern 3): the add-observability symlink points at the legacy/ SUBDIR
  (legacy/SKILL.md), which routes sub-skills via relative ../init/INIT.md paths.
</key_pitfalls>
</context>

<tasks>

<task type="auto">
  <name>Task 0: Create + checkout the obs-repo feature branch split-02-rename-and-0022</name>
  <read_first>
    - 29-CONTEXT.md § "Cross-Repo Execution Constraints" (feature-branch policy: plans 03–05 use split-02-rename-and-0022)
  </read_first>
  <files>~/Sourcecode/agenticapps/agenticapps-observability (git branch)</files>
  <action>
    BEFORE any rename/install/shim edits, create and check out the feature branch in the obs repo so
    every commit in plans 03 + 04 lands on it (NOT main). Per global CLAUDE.md "never commit directly
    to main"; plan 05 opens the PR + merges before the v0.11.0 tag.

    ```bash
    cd ~/Sourcecode/agenticapps/agenticapps-observability
    # Ensure we branch from the post-29-02 main (skeleton + filtered history + submodule).
    git checkout main
    git pull --ff-only origin main 2>/dev/null || true
    # Idempotent: reuse the branch if it already exists, else create it.
    git checkout split-02-rename-and-0022 2>/dev/null || git checkout -b split-02-rename-and-0022
    git rev-parse --abbrev-ref HEAD   # must print split-02-rename-and-0022
    ```

    Do NOT push the branch here (the push happens via the PR in plan 05). Subsequent Tasks 1–3 in this
    plan, and all of plan 04, commit onto this branch. NEVER commit to main from plans 03/04.
  </action>
  <verify>
    <automated>cd ~/Sourcecode/agenticapps/agenticapps-observability && git rev-parse --abbrev-ref HEAD | grep -qx split-02-rename-and-0022 && echo BRANCH_OK</automated>
  </verify>
  <acceptance_criteria>
    - `git -C ~/Sourcecode/agenticapps/agenticapps-observability rev-parse --abbrev-ref HEAD` outputs `split-02-rename-and-0022`
    - The branch is local (not yet pushed — PR is opened in plan 05)
  </acceptance_criteria>
  <done>Feature branch split-02-rename-and-0022 is checked out in the obs repo; plans 03+04 will commit to it, not main.</done>
</task>

<task type="auto">
  <name>Task 1: Rename skill in SKILL.md + internal references; scaffold legacy/SKILL.md alias</name>
  <read_first>
    - ~/Sourcecode/agenticapps/agenticapps-observability/SKILL.md (current, name: add-observability, version: 0.10.0)
    - 29-RESEARCH.md § "Code Examples / SKILL.md Frontmatter Rename" + "Pattern 3: Option A Dual-Symlink Alias" (legacy/SKILL.md body)
    - SPLIT-02-agenticapps-observability.md § "SKILL.md changes" + "Skill rename: backward compat"
  </read_first>
  <files>
    ~/Sourcecode/agenticapps/agenticapps-observability/SKILL.md,
    ~/Sourcecode/agenticapps/agenticapps-observability/init/INIT.md,
    ~/Sourcecode/agenticapps/agenticapps-observability/scan/SCAN.md,
    ~/Sourcecode/agenticapps/agenticapps-observability/README.md,
    ~/Sourcecode/agenticapps/agenticapps-observability/legacy/SKILL.md
  </files>
  <action>
    Commit the work in this task onto the split-02-rename-and-0022 branch (created in Task 0), NOT main.

    1. In `SKILL.md` (repo root): change frontmatter `name: add-observability` → `name: observability`
       and `version: 0.10.0` → `version: 0.11.0`. Keep `implements_spec: 0.3.2`. Update the
       description's subcommand references from `/add-observability` to `/observability` where they
       appear in prose. Do NOT rewrite the whole description — change only the name/version/slash-prefix.
    2. Update internal slash-command references in `init/INIT.md`, `scan/SCAN.md`, and `README.md`:
       grep for `add-observability` (as a slash command `/add-observability` or skill-name reference)
       and change to `observability`. PRESERVE any historical/provenance mentions and the alias
       explanation. Do NOT touch CHANGELOG provenance text.
    3. Create `legacy/SKILL.md` with the deprecation-banner body from 29-RESEARCH Pattern 3:
       frontmatter `name: add-observability`, `version: 0.11.0`; body explains the rename to
       `observability`, the alias window (0.11.0 + 0.12.0, warning 0.13.0, removed 0.14.0), and a
       sub-skill routing table pointing at `../init/INIT.md`, `../scan/SCAN.md`, `../scan-apply/APPLY.md`.
    Use `gitnexus_rename` ONLY if renaming code symbols — here it's markdown frontmatter + prose,
    so direct Edit is correct (no call-graph symbol involved).
  </action>
  <verify>
    <automated>cd ~/Sourcecode/agenticapps/agenticapps-observability && grep -q "^name: observability" SKILL.md && grep -q "^version: 0.11.0" SKILL.md && grep -q "^name: add-observability" legacy/SKILL.md && echo RENAME_OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "^name: observability" ~/Sourcecode/agenticapps/agenticapps-observability/SKILL.md` exits 0
    - `grep -q "^version: 0.11.0" ~/Sourcecode/agenticapps/agenticapps-observability/SKILL.md` exits 0
    - `grep -q "^name: add-observability" ~/Sourcecode/agenticapps/agenticapps-observability/legacy/SKILL.md` exits 0
    - `grep -q "observability" ~/Sourcecode/agenticapps/agenticapps-observability/legacy/SKILL.md` (mentions canonical name) exits 0
    - `grep -q "0.14.0" ~/Sourcecode/agenticapps/agenticapps-observability/legacy/SKILL.md` (removal window documented) exits 0
    - Commit landed on `split-02-rename-and-0022` (not main)
  </acceptance_criteria>
  <done>SKILL.md renamed to observability 0.11.0; internal refs updated; legacy/SKILL.md alias with deprecation banner + routing table exists; committed on the feature branch.</done>
</task>

<task type="auto">
  <name>Task 2: Write obs install.sh (dual symlink + clobber-guard + submodule sync)</name>
  <read_first>
    - install.sh (claude-workflow — lines 17-37 submodule sync, lines 79-98 clobber-guard + link loop)
    - 29-RESEARCH.md § "Pattern 3: Option A Dual-Symlink Alias" + "Security Domain / install.sh symlink clobbering"
  </read_first>
  <files>~/Sourcecode/agenticapps/agenticapps-observability/install.sh</files>
  <action>
    Commit on the split-02-rename-and-0022 branch (not main).

    Replace the 29-01 install.sh stub with a real installer modeled on claude-workflow's install.sh:

    1. `set -euo pipefail`; resolve `REPO="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"`,
       `SKILLS_DIR="$HOME/.claude/skills"`, `mkdir -p "$SKILLS_DIR"`.
    2. Submodule sync block — copy claude-workflow install.sh lines 30-37 VERBATIM (guard on
       `.gitmodules` AND a real `.git` dir-or-file; non-fatal warning on failure so skill linking
       still proceeds), substituting `$REPO` for `$SCAFFOLDER`.
    3. Two symlinks via a loop with the clobber-guard from claude-workflow install.sh lines 79-94:
       - canonical: `observability` → `$REPO` (the repo root, where SKILL.md lives)
       - legacy alias: `add-observability` → `$REPO/legacy` (the legacy/ subdir, where the alias SKILL.md lives)
       For EACH link: if it's a symlink to the right target, report already-linked; if symlink to
       elsewhere, replace; if a real (non-symlink) path exists, REFUSE to clobber (mirror lines 89-94)
       and increment FAILED. Verify `$src/SKILL.md` exists before linking (mirror lines 73-77).
    4. Print a summary (linked/skipped/failed); exit 1 if FAILED>0.
    `chmod +x install.sh`.

    NEVER hardcode `/Users/...` paths (codex F1 pattern) — use `$HOME` and `$REPO` only. Quote all vars.
  </action>
  <verify>
    <automated>cd ~/Sourcecode/agenticapps/agenticapps-observability && bash -n install.sh && grep -q 'SKILLS_DIR="\$HOME/.claude/skills"' install.sh && grep -q 'observability' install.sh && grep -q 'legacy' install.sh && ! grep -q '/Users/' install.sh && echo INSTALL_OK</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n ~/Sourcecode/agenticapps/agenticapps-observability/install.sh` (syntax) exits 0
    - `grep -q 'add-observability' ~/Sourcecode/agenticapps/agenticapps-observability/install.sh` (legacy link) exits 0
    - `grep -q 'NOT a symlink' ~/Sourcecode/agenticapps/agenticapps-observability/install.sh` (clobber-guard) exits 0
    - `grep -c '/Users/' ~/Sourcecode/agenticapps/agenticapps-observability/install.sh` outputs `0` (no hardcoded home)
    - Running `bash install.sh` then `test -L ~/.claude/skills/observability && test -f ~/.claude/skills/observability/SKILL.md` exits 0
    - `test -L ~/.claude/skills/add-observability && test -f ~/.claude/skills/add-observability/SKILL.md` exits 0
  </acceptance_criteria>
  <done>install.sh creates both symlinks with clobber-guard + submodule sync; no hardcoded paths; both /observability and /add-observability resolve; committed on the feature branch.</done>
</task>

<task type="auto">
  <name>Task 3: Author obs migrations/run-tests.sh source-and-keep shim + repoint moved migration script paths</name>
  <read_first>
    - migrations/run-tests.sh (claude-workflow — lines 1-136: BASH_SOURCE resolution, fail-closed guard, 4 source lines, traps, REPO_ROOT, the WORKFLOW per-migration test body pattern)
    - 29-RESEARCH.md § "Pattern 2: Source-and-Keep Migration Runner Shim" + "Pitfall 2 (drift path)" + "Pitfall 6 (script paths after rename)"
    - vendor/agenticapps-shared/migrations/lib/drift-test.sh (run_drift_test signature)
    - The moved migration .md files in the obs repo (0017, 0019, 0021) — for the internal-path repoint
  </read_first>
  <files>
    ~/Sourcecode/agenticapps/agenticapps-observability/migrations/run-tests.sh,
    ~/Sourcecode/agenticapps/agenticapps-observability/migrations/0017-add-axiom-logs-destination.md,
    ~/Sourcecode/agenticapps/agenticapps-observability/migrations/0019-sentry-crons-and-healthz.md,
    ~/Sourcecode/agenticapps/agenticapps-observability/migrations/0021-cron-monitor-shape-and-queues.md
  </files>
  <action>
    Commit on the split-02-rename-and-0022 branch (not main).

    1. Create `migrations/run-tests.sh` as a source-and-keep shim:
       - Copy the BASH_SOURCE[0] resolution block + fail-closed `$_SHARED_LIB` guard + the four
         `source "$_SHARED_LIB/{helpers,fixture-runner,preflight,drift-test}.sh"` lines VERBATIM
         from claude-workflow run-tests.sh lines 35-66.
       - Copy the trap block + `REPO_ROOT="$(git rev-parse --show-toplevel)"` + flag/filter parsing
         (lines 68-104).
       - Bring over ONLY the WORKFLOW per-migration test bodies for the MOVED observability
         migrations that have test fixtures: `test_migration_0017`, `test_migration_0019`,
         `test_migration_0021`. These bodies move from claude-workflow's run-tests.sh — extract
         them from the source (they call run_fixture / extract_to against the obs migrate-00NN.sh
         engines and the test-fixtures/00NN/ dirs). Update any engine path references inside these
         bodies from `templates/.claude/scripts/migrate-00NN-*.sh` to `migrations/scripts/migrate-00NN.sh`.
       - Do NOT bring over claude-workflow-only WORKFLOW bodies (0001, setup_fixture, etc.) — those STAY.
       - Drift policy wrapper (Pitfall 2): call EXACTLY
         `if run_drift_test "$REPO_ROOT/SKILL.md" "$REPO_ROOT/migrations"; then ...PASS... else ...FAIL... fi`
         — note `$REPO_ROOT/SKILL.md` (obs ROOT), NOT `skill/SKILL.md`.
       - Honor the `<filter>` positional so `bash migrations/run-tests.sh 0019` runs only 0019.
       - Accumulate PASS/FAIL and exit non-zero if FAIL>0 (matching claude-workflow's contract).
    2. Repoint internal script paths in the moved migration .md files (Pitfall 6): in 0017/0019/0021
       markdown, replace `ENGINE=...templates/.claude/scripts/migrate-00NN-*.sh` references with the
       obs path `migrations/scripts/migrate-00NN.sh`. Grep each .md for `templates/.claude/scripts`
       and `migrate-00NN-` and fix to the renamed obs path.
    3. Create `tests/run-tests.sh` as a one-line entrypoint that execs `migrations/run-tests.sh "$@"`.

    EXPECTED at end of this plan: `bash migrations/run-tests.sh 0019` → PASS=13;
    `bash migrations/run-tests.sh 0021` → PASS=4; `bash migrations/run-tests.sh 0017` → PASS=7 FAIL=4
    (the 4 known FIX-0017 failures, documented, NOT fixed here). The DRIFT test will FAIL until
    29-04 adds 0022 (to_version 0.11.0) — that is expected and gated in 29-05, not here.
    Quote all shell variables; never eval migration markdown content.
  </action>
  <verify>
    <automated>cd ~/Sourcecode/agenticapps/agenticapps-observability && bash -n migrations/run-tests.sh && grep -q 'run_drift_test "\$REPO_ROOT/SKILL.md"' migrations/run-tests.sh && bash migrations/run-tests.sh 0019 2>&1 | grep -qE "PASS[= ]*13|13 PASS" && echo SHIM_OK</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n ~/Sourcecode/agenticapps/agenticapps-observability/migrations/run-tests.sh` exits 0
    - `grep -q 'source .*agenticapps-shared/migrations/lib/helpers.sh' migrations/run-tests.sh` exits 0 (all four sourced)
    - `grep -q 'run_drift_test "\$REPO_ROOT/SKILL.md"' migrations/run-tests.sh` exits 0 (obs ROOT path, NOT skill/SKILL.md)
    - `bash migrations/run-tests.sh 0019` reports PASS=13
    - `bash migrations/run-tests.sh 0021` reports PASS=4
    - `bash migrations/run-tests.sh 0017` reports PASS=7 FAIL=4 (the 4 known FIX-0017 failures)
    - `grep -rL "templates/.claude/scripts" migrations/0019-*.md` confirms internal engine paths repointed (no stale claude-workflow path)
  </acceptance_criteria>
  <done>Source-and-keep shim runs 0017/0019/0021 with correct counts; drift policy uses obs root SKILL.md path; moved migration .md engine paths repointed; committed on the feature branch.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| install.sh → ~/.claude/skills/ | Writes symlinks into the user's real home skill dir |
| run-tests.sh → migration markdown content | Migration .md is data the runner reads, not code to eval |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-10 | Elevation | install.sh symlink clobbering | mitigate | Mirror claude-workflow install.sh:89-94 clobber-guard (refuse non-symlink targets); acceptance greps for "NOT a symlink" |
| T-29-11 | Tampering | hardcoded /Users path (sandbox escape) | mitigate | install.sh uses $HOME/$REPO only; acceptance `grep -c '/Users/'` == 0 |
| T-29-12 | Tampering | command injection via migration markdown | mitigate | run-tests.sh quotes all shell vars; never eval markdown content (copies the quoted SHARED-lib pattern verbatim) |
| T-29-13 | Tampering | drift test silent no-op (wrong SKILL.md path) | mitigate | Explicit `run_drift_test "$REPO_ROOT/SKILL.md"`; acceptance greps the exact root path string |
| T-29-14 | Elevation | submodule refresh fatal aborts install | accept | Non-fatal warning (copied from claude-workflow install.sh:32-36) lets skill linking proceed |
| T-29-24 | Repudiation | commit-to-main bypasses review (CLAUDE.md policy) | mitigate | Task 0 creates split-02-rename-and-0022 feature branch; plans 03/04 commit to it; PR merged before tag (plan 05) |
</threat_model>

<verification>
- Feature branch split-02-rename-and-0022 checked out; plans 03/04 commit to it, not main (Task 0)
- SKILL.md renamed to observability 0.11.0; legacy alias present (Task 1)
- install.sh dual-symlink with clobber-guard, no hardcoded paths, both names resolve (Task 2)
- run-tests.sh shim: 0019=13, 0021=4, 0017=7/4; drift uses obs root path; engine paths repointed (Task 3)
</verification>

<success_criteria>
- All plan 03 work committed on the split-02-rename-and-0022 feature branch (not main)
- Skill discoverable as both /observability (canonical) and /add-observability (alias)
- 0019 (13) + 0021 (4) green via the shared runner; 0017 at 7/4 (known failures documented)
- Drift policy correctly wired to $REPO_ROOT/SKILL.md (full-suite drift-green deferred to 29-04/05)
</success_criteria>

<output>
After completion, create `.planning/phases/29-split-02-agenticapps-observability/29-03-SUMMARY.md`
recording: the feature branch name (split-02-rename-and-0022), rename confirmation, both symlinks
resolving, the 0017/0019/0021 fixture counts, and a note that drift is expected RED until 29-04
lands migration 0022 (to_version 0.11.0).
</output>
</content>
</invoke>
