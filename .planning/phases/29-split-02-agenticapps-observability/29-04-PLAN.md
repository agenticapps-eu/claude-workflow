---
phase: 29-split-02-agenticapps-observability
plan: 04
type: tdd
wave: 4
depends_on: [29-03]
files_modified:
  - ~/Sourcecode/agenticapps/agenticapps-observability/templates/ts-cloudflare-worker/cron-monitor.ts
  - ~/Sourcecode/agenticapps/agenticapps-observability/templates/ts-cloudflare-worker/cron-monitor.test.ts
  - ~/Sourcecode/agenticapps/agenticapps-observability/templates/ts-cloudflare-pages/cron-monitor.ts
  - ~/Sourcecode/agenticapps/agenticapps-observability/templates/ts-cloudflare-pages/cron-monitor.test.ts
  - ~/Sourcecode/agenticapps/agenticapps-observability/templates/ts-supabase-edge/cron-monitor.ts
  - ~/Sourcecode/agenticapps/agenticapps-observability/templates/ts-cloudflare-worker/queue-monitor.ts
  - ~/Sourcecode/agenticapps/agenticapps-observability/templates/ts-cloudflare-pages/queue-monitor.ts
  - ~/Sourcecode/agenticapps/agenticapps-observability/migrations/0022-explicit-flush-and-monitor-config.md
  - ~/Sourcecode/agenticapps/agenticapps-observability/migrations/scripts/migrate-0022.sh
  - ~/Sourcecode/agenticapps/agenticapps-observability/migrations/test-fixtures/0022/
  - ~/Sourcecode/agenticapps/agenticapps-observability/docs/decisions/0036-explicit-per-checkin-flush.md
autonomous: true
requirements: []
tags: [observability, cron-monitor, sentry-flush, migration, tdd, fxsa-workers-6]
user_setup: []

must_haves:
  truths:
    - "withCronMonitor flushes the in_progress heartbeat immediately via ctx.waitUntil(Sentry.flush) BEFORE the handler resolves"
    - "The narrowed strict-Env generic E extends { SENTRY_DSN?: string; SERVICE_NAME?: string } is preserved (NOT Record<string, unknown>)"
    - "monitorConfig is forwarded on every check-in (in_progress/ok/error)"
    - "queue-monitor.ts (cf-worker + cf-pages) gets the same explicit-flush treatment"
    - "Migration 0022 has to_version: 0.11.0 (obs version) so the obs drift test passes"
    - "0022 supersedes 0021 (NOT a 0021 re-issue); recognizes the FXSA-WORKERS-6 marker as reconcilable"
    - "The #61 fixture types.d.ts uses the real @sentry/cloudflare MonitorConfig + captureCheckIn + flush shape"
  artifacts:
    - path: "~/Sourcecode/agenticapps/agenticapps-observability/templates/ts-cloudflare-worker/cron-monitor.ts"
      provides: "Explicit-flush withCronMonitor (canonical draft body)"
      contains: "ctx.waitUntil(Sentry.flush(FLUSH_TIMEOUT_MS))"
    - path: "~/Sourcecode/agenticapps/agenticapps-observability/migrations/0022-explicit-flush-and-monitor-config.md"
      provides: "Deferred-fix migration doc superseding 0021"
      contains: "to_version: 0.11.0"
    - path: "~/Sourcecode/agenticapps/agenticapps-observability/docs/decisions/0036-explicit-per-checkin-flush.md"
      provides: "ADR superseding ADR-0033's flush point"
      contains: "FX-SIGNALS-WORKERS-6"
  key_links:
    - from: "withCronMonitor in_progress check-in"
      to: "Sentry.flush via ctx.waitUntil"
      via: "immediate flush before handler awaits"
      pattern: "captureCheckIn.*in_progress[\\s\\S]*waitUntil.*flush"
    - from: "migration 0022 engine"
      to: "FXSA-WORKERS-6 LOCAL-PATCH marker"
      via: "marker recognition as reconcilable divergence"
      pattern: "FXSA-WORKERS-6"
---

<objective>
The deferred-fix migration (0022) in the obs repo. Bundle three observability correctness fixes
that touch the same cron/queue templates, ship as a NEW migration that SUPERSEDES 0021 (0021 is
Released/immutable — do NOT mutate it), preserving the narrowed strict-Env generic (ADR-0032/SC5):

1. cron-flush backport — re-rev cron-monitor.ts (cf-worker + cf-pages + supabase-edge) to the
   explicit-per-check-in-flush body from RESEARCH-cron-monitor-flush-fxsa.md (FX-SIGNALS-WORKERS-6).
2. #61 buildMonitorConfig / fixture fix — replace the relaxed MonitorConfig stub in
   test-fixtures/0021/04 types.d.ts with the real @sentry/cloudflare shape; forward monitorConfig
   on every check-in.
3. queue-monitor.ts race audit — apply the same explicit-flush treatment to queue-monitor.ts
   (cf-worker + cf-pages only — supabase-edge has no queue-monitor, VERIFIED).

Plus: a new ADR (0036) superseding ADR-0033's flush point, and a test-suite rewrite of the ~11
withMonitor-contract cases to the captureCheckIn+flush contract + a NEW immediate-flush regression
test (RESEARCH-cron Q3) — all preserving the narrowed generic.

DECISIONS LOCKED BY PLANNER (resolving 29-RESEARCH Open Questions):
- 0022 to_version: 0.11.0 (obs version) — only the latest migration's to_version matters for the
  obs drift test; this makes drift PASS against SKILL.md version 0.11.0 (Pitfall 3 Option B).
- Flush fix stacks: cf-worker + cf-pages (full); supabase-edge cron-monitor gets the flush body
  but documented as audited (no queue-monitor there). queue-monitor flush = cf-worker + cf-pages only.
- fxsa CronMonitorConfigInput function-form: LEAVE OUT (default per CONTEXT — separable enhancement).

Purpose: Close the confirmed missed-checkin race + #61 in the obs repo's first migration.
Output: Migration 0022 + engine + fixtures + ADR-0036 + rewritten test suites, all green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/29-split-02-agenticapps-observability/29-CONTEXT.md
@.planning/phases/29-split-02-agenticapps-observability/29-RESEARCH.md
@RESEARCH-cron-monitor-flush-fxsa.md

<authoritative_body>
The canonical withCronMonitor body is RESEARCH-cron-monitor-flush-fxsa.md lines 123-166 (also
mirrored in 29-RESEARCH § "Canonical withCronMonitor Body"). COPY IT — do not redesign. Key invariants:
- `const FLUSH_TIMEOUT_MS = 2000;`
- Generic: `export function withCronMonitor<E extends { SENTRY_DSN?: string; SERVICE_NAME?: string }>(...)`
  — NARROWED, NOT Record<string, unknown>. This is the SC5/ADR-0032 firewall.
- in_progress check-in → `ctx.waitUntil(Sentry.flush(FLUSH_TIMEOUT_MS))` IMMEDIATELY (the fix).
- monitorConfig forwarded on in_progress AND ok AND error.
- terminal `finally { ctx.waitUntil(Sentry.flush(FLUSH_TIMEOUT_MS)); }`.
- drops the `handlerStarted` flag (two try/catch scopes separate pre/post-handler naturally).
- Keeps isConfigured / resolveSlug / buildMonitorConfig unchanged.
</authoritative_body>

<types_d_ts_fix>
#61 fix — fixture 0021/04 types.d.ts. Current stub declares a relaxed MonitorConfig + withMonitor.
The new shape (29-RESEARCH § "Updated types.d.ts") must declare the REAL @sentry/cloudflare surface:
- MonitorConfig with discriminated `schedule: {type:"crontab";value:string} | {type:"interval";value:number;unit:string}` + checkinMargin/maxRuntime/timezone
- `captureCheckIn(checkIn:{monitorSlug;status:"in_progress"|"ok"|"error";checkInId?}, upsertMonitorConfig?:MonitorConfig): string`
- `flush(timeout?:number): Promise<boolean>`
- `captureException(error:unknown): string`
The mock module surface changes from `{ withMonitor }` to `{ captureCheckIn, flush }`.
</types_d_ts_fix>

<test_rewrite_scope>
RESEARCH-cron Q3 (cron-monitor.test.ts, 262 lines, 22 cases):
- REWRITE ~11 behavioural cases asserting the withMonitor contract → the new contract:
  captureCheckIn({status:"in_progress"}, monitorConfig) → ctx.waitUntil(flush) → handler →
  captureCheckIn({status:"ok"|"error"}, monitorConfig) → terminal flush. Mock surface
  { withMonitor } → { captureCheckIn, flush }. fakeCtx already mocks waitUntil (no harness change).
- PRESERVE unchanged: the CronMonitorSchedule D-16 assignability tests AND the D-05 strict-Env
  generic narrowing test — PROVIDED the narrowed generic is kept (Pitfall 4).
- ADD: a NEW regression test that the in_progress flush is scheduled BEFORE the handler resolves
  (block the handler on a deferred promise; assert ctx.waitUntil(Sentry.flush(...)) already fired).
</test_rewrite_scope>

<queue_audit>
queue-monitor.ts exists ONLY in cf-worker + cf-pages (VERIFIED — supabase-edge has none). Both use
Guarded Shape A via Sentry.withMonitor → same buffered-flush race. Apply ctx.waitUntil(Sentry.flush)
immediately after the in_progress-equivalent check-in. queue-monitor uses withMonitor which calls
captureCheckIn internally; the flush treatment is structurally the same as cron-monitor.
</queue_audit>

<migration_engine_pattern>
Clone migration 0021's engine (migrations/scripts/migrate-0021.sh, now in the obs repo via 29-02)
as migrate-0022.sh: same all-clean-gate + dirty-detection. ADD the FXSA-WORKERS-6 marker recognition
(29-RESEARCH § Pattern 4): if `grep -q "FXSA-WORKERS-6" "$wrapper/cron-monitor.ts"`, treat as a
known-reconcilable divergence (INFO message, proceed to apply; operator removes marker post-apply,
new canonical hash matches). Migration .md frontmatter: migration_id "0022", from_version 1.20.0,
to_version 0.11.0 (obs — see DECISIONS), type re-rev-with-dirty-detection, related ["0021",
"FXSA-WORKERS-6","ADR-0033","ADR-0036"]. Include a Recovery section (RESEARCH-cron OQ-3 two-step).
</migration_engine_pattern>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — rewrite cron-monitor.test.ts to the captureCheckIn+flush contract + add immediate-flush regression test</name>
  <read_first>
    - ~/Sourcecode/agenticapps/agenticapps-observability/templates/ts-cloudflare-worker/cron-monitor.test.ts (current 22-case suite, mocks { withMonitor })
    - RESEARCH-cron-monitor-flush-fxsa.md § "Q3 — test impact of the backport" (which cases rewrite, which survive, what to add)
    - <test_rewrite_scope> + <authoritative_body> above
  </read_first>
  <files>
    ~/Sourcecode/agenticapps/agenticapps-observability/templates/ts-cloudflare-worker/cron-monitor.test.ts,
    ~/Sourcecode/agenticapps/agenticapps-observability/templates/ts-cloudflare-pages/cron-monitor.test.ts
  </files>
  <behavior>
    - Mock surface for `@sentry/cloudflare` is `{ captureCheckIn, flush, captureException }` (was `{ withMonitor }`).
    - Happy path: handler runs → captureCheckIn called with {status:"in_progress"} + monitorConfig, then {status:"ok", checkInId} + monitorConfig.
    - Error path: handler throws → captureCheckIn {status:"error", checkInId} + monitorConfig, then rethrows.
    - DSN-unset no-op: isConfigured false → captureCheckIn NOT called, handler runs unchanged.
    - Immediate-flush regression (NEW): handler blocked on a deferred promise; assert ctx.waitUntil(flush) ALREADY fired before the handler resolves (this pins the FX-SIGNALS-WORKERS-6 fix).
    - monitorConfig forwarded on every check-in (in_progress/ok/error).
    - PRESERVED: CronMonitorSchedule D-16 assignability + D-05 strict-Env narrowing (compile against narrowed generic — must still typecheck).
  </behavior>
  <action>
    Rewrite the ~11 withMonitor-contract test cases per <test_rewrite_scope>. Change the mock module
    surface to `{ captureCheckIn, flush, captureException }`. Keep the D-16 type-assignability tests
    and the D-05 strict-Env generic narrowing test EXACTLY (they must compile against the narrowed
    generic — do NOT relax to Record<string, unknown>). Add the NEW immediate-flush regression test
    (deferred-promise handler, assert waitUntil(flush) fired pre-resolution). Mirror the rewrite into
    cf-pages cron-monitor.test.ts. Run the suite — it MUST FAIL (RED) because cron-monitor.ts still
    has the old withMonitor body. Commit RED:
    `test(29-04): rewrite cron-monitor tests to captureCheckIn+flush contract + immediate-flush regression`
  </action>
  <verify>
    <automated>cd ~/Sourcecode/agenticapps/agenticapps-observability/templates/ts-cloudflare-worker && npm install >/dev/null 2>&1; npx vitest run cron-monitor.test.ts 2>&1 | grep -qiE "fail" && echo RED_CONFIRMED</automated>
  </verify>
  <acceptance_criteria>
    - cron-monitor.test.ts (cf-worker) mocks `captureCheckIn` + `flush` (grep for `captureCheckIn` in the mock block) exits 0
    - A test case references the deferred/immediate-flush regression (grep for `waitUntil` + `flush` assertion in a "before handler" / "immediate" / "deferred" case) exits 0
    - `npx vitest run cron-monitor.test.ts` FAILS (RED) against the unmodified cron-monitor.ts
    - The D-05 strict-Env narrowing test is still present (grep for the SC5/strict-Env test name)
  </acceptance_criteria>
  <done>Both cron-monitor.test.ts suites assert the new flush contract + immediate-flush regression; suites RED; strict-Env + schedule type tests preserved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — apply the explicit-flush withCronMonitor body (cf-worker + cf-pages + supabase-edge) + queue-monitor flush (cf-worker + cf-pages)</name>
  <read_first>
    - RESEARCH-cron-monitor-flush-fxsa.md lines 123-166 (the copy-ready body)
    - <authoritative_body> + <queue_audit> above
    - ~/Sourcecode/agenticapps/agenticapps-observability/templates/ts-cloudflare-worker/cron-monitor.ts (current Guarded Shape A body to replace)
    - ~/Sourcecode/agenticapps/agenticapps-observability/templates/ts-cloudflare-worker/queue-monitor.ts (current withMonitor body)
  </read_first>
  <files>
    ~/Sourcecode/agenticapps/agenticapps-observability/templates/ts-cloudflare-worker/cron-monitor.ts,
    ~/Sourcecode/agenticapps/agenticapps-observability/templates/ts-cloudflare-pages/cron-monitor.ts,
    ~/Sourcecode/agenticapps/agenticapps-observability/templates/ts-supabase-edge/cron-monitor.ts,
    ~/Sourcecode/agenticapps/agenticapps-observability/templates/ts-cloudflare-worker/queue-monitor.ts,
    ~/Sourcecode/agenticapps/agenticapps-observability/templates/ts-cloudflare-pages/queue-monitor.ts
  </files>
  <action>
    1. Replace the `withCronMonitor` body in cf-worker cron-monitor.ts with the authoritative draft
       (RESEARCH-cron lines 123-166): `FLUSH_TIMEOUT_MS = 2000`; narrowed generic
       `E extends { SENTRY_DSN?: string; SERVICE_NAME?: string }`; in_progress captureCheckIn +
       immediate `ctx.waitUntil(Sentry.flush(FLUSH_TIMEOUT_MS))`; ok/error captureCheckIn with
       monitorConfig forwarded; terminal `finally { ctx.waitUntil(Sentry.flush(FLUSH_TIMEOUT_MS)); }`;
       drop the handlerStarted flag. Keep isConfigured/resolveSlug/buildMonitorConfig unchanged.
    2. Apply the SAME body to cf-pages cron-monitor.ts (if byte-symmetry contract applies, keep them
       symmetric per the existing snapshot mechanism). Apply to supabase-edge cron-monitor.ts too
       (the body is runtime-agnostic; document in the migration that supabase-edge's isolate model
       was audited and the explicit flush is correct/safe there).
    3. queue-monitor.ts (cf-worker + cf-pages ONLY): add `ctx.waitUntil(Sentry.flush(FLUSH_TIMEOUT_MS))`
       immediately after the in_progress-equivalent check-in (same treatment). Keep the existing
       Guarded Shape A structure otherwise.
    Run cron-monitor.test.ts (both stacks) — MUST now pass (GREEN), including the new immediate-flush
    regression and the preserved strict-Env narrowing test. Commit GREEN:
    `feat(29-04): explicit per-checkin flush in cron-monitor + queue-monitor (FX-SIGNALS-WORKERS-6)`
  </action>
  <verify>
    <automated>cd ~/Sourcecode/agenticapps/agenticapps-observability/templates/ts-cloudflare-worker && npx vitest run cron-monitor.test.ts 2>&1 | grep -qiE "pass" && grep -q "ctx.waitUntil(Sentry.flush(FLUSH_TIMEOUT_MS))" cron-monitor.ts && grep -q "SENTRY_DSN?: string; SERVICE_NAME?: string" cron-monitor.ts && echo GREEN_OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "ctx.waitUntil(Sentry.flush(FLUSH_TIMEOUT_MS))" templates/ts-cloudflare-worker/cron-monitor.ts` exits 0
    - `grep -q "E extends { SENTRY_DSN?: string; SERVICE_NAME?: string }" templates/ts-cloudflare-worker/cron-monitor.ts` exits 0 (narrowed generic preserved, NOT Record<string, unknown>)
    - `! grep -q "Record<string, unknown>" templates/ts-cloudflare-worker/cron-monitor.ts` (no regression to fxsa's form)
    - `! grep -q "handlerStarted" templates/ts-cloudflare-worker/cron-monitor.ts` (flag dropped)
    - `grep -q "Sentry.flush" templates/ts-cloudflare-worker/queue-monitor.ts` exits 0 AND same for cf-pages
    - `npx vitest run cron-monitor.test.ts` PASSES (GREEN) in cf-worker AND cf-pages
  </acceptance_criteria>
  <done>cron-monitor.ts (3 stacks) + queue-monitor.ts (2 stacks) carry explicit flush; narrowed generic preserved; both cron-monitor.test.ts suites GREEN.</done>
</task>

<task type="auto">
  <name>Task 3: Author migration 0022 (doc + engine + fixtures incl. #61 types.d.ts) + ADR-0036</name>
  <read_first>
    - ~/Sourcecode/agenticapps/agenticapps-observability/migrations/scripts/migrate-0021.sh (the engine to clone)
    - ~/Sourcecode/agenticapps/agenticapps-observability/migrations/test-fixtures/0021/04-callbot-shape-strict-env-typecheck/types.d.ts (the relaxed stub to fix — #61)
    - 29-RESEARCH.md § "Pattern 4: Deferred-Fix Migration Shape" + "Updated types.d.ts" + § Pitfall 3 (to_version)
    - RESEARCH-cron-monitor-flush-fxsa.md § "Migration + ADR changes needed" + ADR-0033 (the one superseded)
    - <migration_engine_pattern> + <types_d_ts_fix> above
  </read_first>
  <files>
    ~/Sourcecode/agenticapps/agenticapps-observability/migrations/0022-explicit-flush-and-monitor-config.md,
    ~/Sourcecode/agenticapps/agenticapps-observability/migrations/scripts/migrate-0022.sh,
    ~/Sourcecode/agenticapps/agenticapps-observability/migrations/test-fixtures/0022/,
    ~/Sourcecode/agenticapps/agenticapps-observability/docs/decisions/0036-explicit-per-checkin-flush.md
  </files>
  <action>
    1. ADR-0036 (`docs/decisions/0036-explicit-per-checkin-flush.md`): supersedes ADR-0033's flush
       point. Title: "Guarded Shape A + explicit per-check-in flush". Body: the @sentry/cloudflare
       buffered-flush model loses in_progress heartbeats for long-running handlers (FX-SIGNALS-WORKERS-6);
       flush each check-in via ctx.waitUntil. Reference RESEARCH-cron doc, the fxsa ADR, ADR-0034, and
       mark ADR-0033 as superseded-in-part (flush point only). Status: Accepted.
    2. Migration doc `0022-explicit-flush-and-monitor-config.md`: frontmatter per <migration_engine_pattern>
       (migration_id "0022", from_version 1.20.0, to_version 0.11.0, type re-rev-with-dirty-detection,
       related list incl. FXSA-WORKERS-6 + ADR-0036). Body: what it does (the 3 bundled fixes), the
       all-clean-gate + dirty-detection, the FXSA-WORKERS-6 reconcilable-marker behavior, and a
       Recovery section (RESEARCH-cron OQ-3 two-step: migration detects marker → operator removes
       marker + .observability-0021.patch refusal artifact → re-run yields SKIP_ALREADY).
    3. Engine `migrate-0022.sh`: clone migrate-0021.sh's all-clean-gate + dirty-detection structure;
       re-rev cron-monitor.ts (cf-worker + cf-pages + supabase-edge) + queue-monitor.ts (cf-worker +
       cf-pages) to the new bodies from Task 2; ADD the FXSA-WORKERS-6 marker recognition block
       (grep marker → INFO + treat as CLEAN). Quote all vars; never eval content; $REPO_ROOT-anchored
       paths only; no hardcoded /Users paths.
    4. Fixtures `migrations/test-fixtures/0022/`: mirror 0021's fixture structure (common-setup.sh,
       baselines/, fresh-apply, dirty-refuse, already-applied, strict-env-typecheck). The strict-env
       fixture's `types.d.ts` MUST use the REAL @sentry/cloudflare shape from <types_d_ts_fix>
       (captureCheckIn + flush + discriminated MonitorConfig) — this is the #61 fix. Include a fixture
       that exercises the FXSA-WORKERS-6 marker reconciliation (marker present → applies clean).
    Run `bash migrations/run-tests.sh 0022` — MUST be GREEN. Commit:
    `feat(29-04): migration 0022 + engine + fixtures + ADR-0036 (#61 types.d.ts real shape)`
  </action>
  <verify>
    <automated>cd ~/Sourcecode/agenticapps/agenticapps-observability && grep -q "to_version: 0.11.0" migrations/0022-explicit-flush-and-monitor-config.md && grep -q "FXSA-WORKERS-6" migrations/scripts/migrate-0022.sh && bash migrations/run-tests.sh 0022 2>&1 | grep -qE "PASS" && ! grep -q "/Users/" migrations/scripts/migrate-0022.sh && echo MIGRATION_OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "to_version: 0.11.0" migrations/0022-explicit-flush-and-monitor-config.md` exits 0 (obs version — drift will pass)
    - `grep -q "from_version: 1.20.0" migrations/0022-explicit-flush-and-monitor-config.md` exits 0 (supersedes 0021's target)
    - `grep -q "FXSA-WORKERS-6" migrations/scripts/migrate-0022.sh` exits 0 (marker recognition)
    - `grep -q "captureCheckIn" migrations/test-fixtures/0022/*/types.d.ts` exits 0 (#61 real shape; NOT the relaxed withMonitor stub)
    - `! grep -q "/Users/" migrations/scripts/migrate-0022.sh` (no hardcoded home)
    - `grep -q "FX-SIGNALS-WORKERS-6" docs/decisions/0036-explicit-per-checkin-flush.md` exits 0
    - `grep -qi "supersede" docs/decisions/0036-explicit-per-checkin-flush.md` (references ADR-0033) exits 0
    - `bash migrations/run-tests.sh 0022` reports PASS (no FAIL)
    - `0021-*.md` is UNMODIFIED (`git -C ~/Sourcecode/agenticapps/agenticapps-observability diff --name-only` does NOT list migrations/0021-*.md) — 0021 stays immutable
  </acceptance_criteria>
  <done>Migration 0022 (doc + engine + fixtures) GREEN with to_version 0.11.0; ADR-0036 supersedes 0033; #61 types.d.ts uses real shape; 0021 untouched; no hardcoded paths.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migrate-0022.sh → consumer wrapper dir | Engine rewrites cron/queue-monitor.ts in a target project |
| migration markdown / fixture content → shell | Engine reads fixture + wrapper content as data, not code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-15 | Tampering | path traversal in fixture dirs | mitigate | $REPO_ROOT-anchored paths only; never eval user-supplied paths (clone 0021 engine's anchoring) |
| T-29-16 | Elevation | sandbox escape (write to real $HOME) | mitigate | acceptance `! grep -q "/Users/" migrate-0022.sh`; fixtures run in tmpdir via shared run_fixture |
| T-29-17 | Tampering | command injection via wrapper content | mitigate | quote all shell vars; never eval cron-monitor.ts content; marker check uses `grep -q` (no eval) |
| T-29-18 | Tampering | strict-Env generic regression (security of type contract) | mitigate | acceptance `! grep Record<string, unknown>` + the preserved D-05 narrowing test must compile (Task 1/2) |
| T-29-19 | Repudiation | mutating immutable 0021 (breaks downstream hash contract) | mitigate | 0022 is a NEW migration; acceptance asserts 0021-*.md UNMODIFIED in git diff |
</threat_model>

<verification>
- cron-monitor.test.ts RED then GREEN with immediate-flush regression (Tasks 1-2)
- Narrowed generic preserved across all 3 cron stacks; queue flush on 2 stacks (Task 2)
- Migration 0022 to_version 0.11.0, FXSA marker recognition, #61 real types.d.ts, 0021 immutable (Task 3)
- `bash migrations/run-tests.sh 0022` GREEN
</verification>

<success_criteria>
- withCronMonitor flushes in_progress immediately; monitorConfig on every check-in; narrowed generic intact
- queue-monitor.ts (cf-worker + cf-pages) gets explicit flush
- Migration 0022 supersedes 0021 (0021 untouched); to_version 0.11.0 makes obs drift pass
- ADR-0036 supersedes ADR-0033's flush point; #61 types.d.ts uses real @sentry/cloudflare shape
- 0022 fixture suite GREEN; no hardcoded paths; marker reconciliation works
</success_criteria>

<output>
After completion, create `.planning/phases/29-split-02-agenticapps-observability/29-04-SUMMARY.md`
recording: the test-suite rewrite (cases changed/preserved/added), the 3-stack cron + 2-stack queue
flush, migration 0022 frontmatter (to_version 0.11.0 rationale), ADR-0036, the #61 fix, and
confirmation 0021 was not mutated.
</output>
