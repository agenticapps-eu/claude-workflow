---
phase: 28-split-01-agenticapps-shared
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - "~/Sourcecode/agenticapps/agenticapps-shared/migrations/lib/helpers.sh"
  - "~/Sourcecode/agenticapps/agenticapps-shared/migrations/lib/fixture-runner.sh"
  - "~/Sourcecode/agenticapps/agenticapps-shared/migrations/lib/preflight.sh"
  - "~/Sourcecode/agenticapps/agenticapps-shared/migrations/lib/drift-test.sh"
  - "docs/decisions/0035-shared-extraction-boundaries.md"
  - "migrations/run-tests.sh"
autonomous: true
requirements: [SC-1, SC-4, SC-5, D-28b, D-28c, D-28d]
must_haves:
  truths:
    - "The four shared lib files exist in agenticapps-shared/migrations/lib/ with the carved SHARED functions"
    - "helpers.sh defines run_check, assert_check, _runtests_do_cleanup, colors, and counters with an idempotency guard"
    - "fixture-runner.sh defines ONLY extract_to (the truly-generic git-ref primitive) — setup_fixture is NOT carved to shared (A1)"
    - "drift-test.sh defines run_drift_test(skill_md, migrations_dir) that returns 0/1 and does NOT touch PASS/FAIL"
    - "preflight.sh defines run_preflight_verify_paths(migrations_dir) parameterized off migrations_dir and reading ${STRICT_PREFLIGHT:-0} internally (set -u safe, A5)"
    - "No lib file calls 'set -e' and no lib file sources another lib file"
    - "No observability-specific or GSD-specific code appears in any lib file"
    - "ADR-0035 SHARED/WORKFLOW boundary is amended: setup_fixture moves to WORKFLOW (claude-workflow wrapper); extract_to remains SHARED (A1)"
  artifacts:
    - path: "~/Sourcecode/agenticapps/agenticapps-shared/migrations/lib/helpers.sh"
      provides: "Color vars, PASS/FAIL/SKIP/RAN_AUDIT counters, _runtests_do_cleanup, run_check, assert_check, reset_counters"
      contains: "assert_check()"
    - path: "~/Sourcecode/agenticapps/agenticapps-shared/migrations/lib/fixture-runner.sh"
      provides: "extract_to ONLY — the generic git-ref extraction primitive (A1)"
      contains: "extract_to()"
    - path: "~/Sourcecode/agenticapps/agenticapps-shared/migrations/lib/preflight.sh"
      provides: "run_preflight_verify_paths parameterized verify-path auditor (set -u safe)"
      contains: "run_preflight_verify_paths()"
    - path: "~/Sourcecode/agenticapps/agenticapps-shared/migrations/lib/drift-test.sh"
      provides: "run_drift_test policy-agnostic version-drift mechanism"
      contains: "run_drift_test()"
    - path: "docs/decisions/0035-shared-extraction-boundaries.md"
      provides: "Amended SHARED/WORKFLOW boundary — setup_fixture demoted to WORKFLOW wrapper (A1)"
      contains: "setup_fixture"
  key_links:
    - from: "fixture-runner.sh:extract_to"
      to: "claude-workflow:run-tests.sh:setup_fixture wrapper (added in 28-03)"
      via: "the workflow wrapper calls shared extract_to and layers template paths + the 1.3.0 special-case on top"
      pattern: "extract_to"
    - from: "drift-test.sh:run_drift_test"
      to: "no PASS/FAIL increment"
      via: "return 0/1 only — policy separation per D-28d"
      pattern: "return 1"
    - from: "preflight.sh:run_preflight_verify_paths"
      to: "${STRICT_PREFLIGHT:-0}"
      via: "reads the flag with a default so a set -u caller never explodes (A5)"
      pattern: "STRICT_PREFLIGHT:-0"
---

<objective>
Carve the truly-SHARED migration-harness functions out of claude-workflow's `migrations/run-tests.sh`
(per the ADR-0035 line-level annotations, AS AMENDED by A1) into four NEW clean lib files inside the
`agenticapps-shared` working copy at `~/Sourcecode/agenticapps/agenticapps-shared`. This is
a refactor-into-new-files operation (D-28b: provenance-by-note, NOT git filter-repo — the
functions are intermingled in one file and cannot be carved by whole-file history surgery).

Purpose: produce the policy-agnostic shared infrastructure layer that both claude-workflow
(this phase) and agenticapps-observability (SPLIT-02) will source as a submodule.
Output: `migrations/lib/{helpers,fixture-runner,preflight,drift-test}.sh` in agenticapps-shared,
each sourceable in isolation, with provenance headers citing the claude-workflow source SHA; PLUS
an amendment to ADR-0035 demoting `setup_fixture` to a claude-workflow WORKFLOW wrapper (A1).

REVIEW-DRIVEN CHANGE (A1 — codex HIGH, USER-LOCKED): `setup_fixture` is NOT shareable. It hardcodes
claude-workflow template paths (run-tests.sh:112-114) AND a workflow-only 1.3.0 ADR special-case
(run-tests.sh:131-133). Parameterizing only the skill name leaves hidden consumer coupling, so it
does NOT go to agenticapps-shared. Instead: agenticapps-shared exports ONLY the generic primitive
`extract_to`; `setup_fixture` STAYS in claude-workflow's run-tests.sh as a thin WRAPPER that calls
shared `extract_to` and layers the workflow template paths + the 1.3.0 special-case on top (the
wrapper itself is built in 28-03). This plan therefore does NOT carve setup_fixture, and it amends
ADR-0035 to record the boundary change.

IMPORTANT — this plan operates on the agenticapps-shared working copy
(`~/Sourcecode/agenticapps/agenticapps-shared`, branch `main`, currently skeleton `d136c96`,
tag `v1.0.0-pre.0`) for the lib files, AND on claude-workflow for the ADR-0035 amendment +
the annotation-only edit to run-tests.sh's `# SHARED`/`# WORKFLOW` line on setup_fixture. It does
NOT otherwise modify claude-workflow's run-tests.sh behavior — the consumer refactor happens in 28-03.
The carve here is a clean copy-and-generalize; the claude-workflow source SHA at carve time is
`5aff1b1` (v1.21.0 baseline).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-split-01-agenticapps-shared/28-CONTEXT.md
@.planning/phases/28-split-01-agenticapps-shared/28-RESEARCH.md
@docs/decisions/0035-shared-extraction-boundaries.md

# Carve source — the annotated boundary. Function line ranges per 28-RESEARCH CRQ 1:
#   _runtests_do_cleanup  77–85   | extract_to  96–104  | setup_fixture  110–134 (NOT carved — A1)
#   run_check  139–143    | assert_check  151–170  | test_preflight_verify_paths  1767–1851
#   drift runner  2247–2269  | color/counter preamble  29–42
@migrations/run-tests.sh

<interfaces>
<!-- Contracts the carved lib MUST expose. Downstream (28-02 standalone test, 28-03 consumer)
     sources these — keep signatures stable. -->

# helpers.sh exports (sourced first — initializes shared globals)
#   RED GREEN YELLOW RESET            (tty-conditional color strings)
#   PASS FAIL SKIP RAN_AUDIT          (counters, initialized to 0 at source time)
#   _runtests_cleanup_fired           (idempotency flag, init 0)
#   _runtests_do_cleanup()            (trap handler body; idempotent; does NOT set traps)
#   run_check(fixture, check) -> exit code of check
#   assert_check(label, check, fixture, expected:applied|not-applied) -> increments PASS/FAIL
#   reset_counters()                  (NEW — sets PASS=FAIL=SKIP=RAN_AUDIT=0 for isolated reruns)

# fixture-runner.sh exports (sourced after helpers)
#   extract_to(ref, path, out) -> 0 on git-show success, 1 otherwise   [A1: this is the ONLY export]
#   (setup_fixture is NOT here — it is a claude-workflow WORKFLOW wrapper, built in 28-03)

# preflight.sh exports
#   run_preflight_verify_paths(migrations_dir) -> walks frontmatter requires[*].verify;
#     informational; sets RAN_AUDIT=1; reads ${STRICT_PREFLIGHT:-0} internally (A5) for
#     global FAIL in strict mode

# drift-test.sh exports
#   run_drift_test(skill_md_path, migrations_dir) -> return 0 if version match, 1 if mismatch/missing;
#     does NOT touch PASS/FAIL (POLICY stays in consumer — D-28d)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write helpers.sh (cleanup trap + run_check + assert_check + colors + counters)</name>
  <read_first>
    - migrations/run-tests.sh lines 29-42 (colors + counter init), 76-85 (_runtests_do_cleanup), 139-143 (run_check), 151-170 (assert_check)
    - .planning/phases/28-split-01-agenticapps-shared/28-RESEARCH.md CRQ 3 (shared global state table), CRQ 9 Risks 1/4/5/7, "helpers.sh skeleton" code example
  </read_first>
  <files>~/Sourcecode/agenticapps/agenticapps-shared/migrations/lib/helpers.sh</files>
  <action>
    Run GitNexus impact analysis first (project CLAUDE.md requirement): attempt
    `gitnexus_impact({target: "assert_check", direction: "upstream"})` and
    `gitnexus_impact({target: "run_check", direction: "upstream"})`. Note in the SUMMARY that
    bash functions have limited GitNexus coverage; fall back to the call-graph in 28-RESEARCH
    CRQ 2 (WORKFLOW test_migration_* functions call assert_check/run_check). This is a NEW
    file, not an in-place edit of run-tests.sh, so blast radius is on the future consumer only.

    Create `~/Sourcecode/agenticapps/agenticapps-shared/migrations/lib/helpers.sh` with:
    1. Shebang `#!/usr/bin/env bash` and a provenance header block:
       `# agenticapps-shared :: migrations/lib/helpers.sh`
       `# Provenance (D-28b): carved from claude-workflow migrations/run-tests.sh @ 5aff1b1 (v1.21.0)`
       `#   lines 29-42 (colors/counters), 76-85 (_runtests_do_cleanup), 139-143 (run_check), 151-170 (assert_check)`
    2. Idempotency guard at top (Risk 7):
       `[ -n "${_AGENTICAPPS_HELPERS_LOADED:-}" ] && return 0`
       `_AGENTICAPPS_HELPERS_LOADED=1`
    3. The ENTIRE tty-conditional color block from lines 30-34 verbatim (Pitfall 1 — keep the
       `if [ -t 1 ]` check; do NOT hardcode colors).
    4. Counter initialization at file level (runs once at source): `PASS=0; FAIL=0; SKIP=0; RAN_AUDIT=0`
    5. `_runtests_cleanup_fired=0` then `_runtests_do_cleanup()` body verbatim from lines 77-82.
       Do NOT include the `trap` calls (lines 83-85) — traps are set by the consumer AFTER
       sourcing (Risk 2).
    6. `run_check()` verbatim from lines 139-143.
    7. `assert_check()` verbatim from lines 151-170.
    8. NEW `reset_counters()` function (Research OQ-2): `reset_counters() { PASS=0; FAIL=0; SKIP=0; RAN_AUDIT=0; }`.
    Do NOT call `set -e` anywhere (Risk 5). Do NOT source any other lib file (Risk 7).
    All counter mutation must be inside function bodies, never at file level except the single
    init at step 4 (Risk 4).
  </action>
  <verify>
    <automated>bash -c 'set -uo pipefail; source ~/Sourcecode/agenticapps/agenticapps-shared/migrations/lib/helpers.sh; declare -F _runtests_do_cleanup && declare -F run_check && declare -F assert_check && declare -F reset_counters && [ "$PASS" = "0" ] && [ "$FAIL" = "0" ] && echo HELPERS_OK'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'assert_check()' ~/Sourcecode/agenticapps/agenticapps-shared/migrations/lib/helpers.sh`
    - `grep -q 'run_check()' ~/Sourcecode/agenticapps/agenticapps-shared/migrations/lib/helpers.sh`
    - `grep -q '_AGENTICAPPS_HELPERS_LOADED' ~/Sourcecode/agenticapps/agenticapps-shared/migrations/lib/helpers.sh`
    - `grep -q 'reset_counters()' ~/Sourcecode/agenticapps/agenticapps-shared/migrations/lib/helpers.sh`
    - `! grep -qE '^\s*set -e\b|^\s*set -[a-z]*e[a-z]*\b' ~/Sourcecode/agenticapps/agenticapps-shared/migrations/lib/helpers.sh` (no set -e)
    - `! grep -q 'source.*lib/' ~/Sourcecode/agenticapps/agenticapps-shared/migrations/lib/helpers.sh` (no inter-lib sourcing)
    - The verify `bash -c` smoke test prints `HELPERS_OK`
  </acceptance_criteria>
  <done>helpers.sh sources cleanly under `set -u`, defines all five functions, initializes counters to 0, contains the idempotency guard, has no set -e and no inter-lib source.</done>
</task>

<task type="auto">
  <name>Task 2: Write fixture-runner.sh — extract_to ONLY (A1: setup_fixture is NOT carved)</name>
  <read_first>
    - migrations/run-tests.sh lines 96-104 (extract_to — the ONLY function carved here)
    - migrations/run-tests.sh lines 110-134 (setup_fixture — DO NOT CARVE; it stays in claude-workflow as a wrapper, built in 28-03. Read it only to confirm extract_to is its sole generic dependency)
    - 28-REVIEWS.md A1 (the locked decision: share only extract_to)
    - .planning/phases/28-split-01-agenticapps-shared/28-RESEARCH.md Pitfall 2 (positional arg / set -u default)
  </read_first>
  <files>~/Sourcecode/agenticapps/agenticapps-shared/migrations/lib/fixture-runner.sh</files>
  <action>
    Run `gitnexus_impact({target: "extract_to", direction: "upstream"})`; record limited-coverage
    note + manual caller list from 28-RESEARCH CRQ 2. Note that setup_fixture (the historical caller
    of extract_to) is NOT being carved per A1 — it remains a claude-workflow function.

    Create `~/Sourcecode/agenticapps/agenticapps-shared/migrations/lib/fixture-runner.sh` with:
    1. Shebang `#!/usr/bin/env bash` + provenance header citing claude-workflow `5aff1b1`,
       lines 96-104 (extract_to). Add a header NOTE explaining A1:
       `# A1 (codex HIGH, user-locked): only extract_to — the truly-generic git-ref primitive — is`
       `# shared. setup_fixture stays in claude-workflow as a WORKFLOW wrapper (hardcoded template`
       `# paths + the 1.3.0 ADR special-case make it non-generic). See ADR-0035 (amended) WORKFLOW set.`
    2. Idempotency guard:
       `[ -n "${_AGENTICAPPS_FIXTURE_RUNNER_LOADED:-}" ] && return 0`
       `_AGENTICAPPS_FIXTURE_RUNNER_LOADED=1`
    3. `extract_to()` verbatim from lines 96-104. No changes — it is already generic and set -u clean.
    DO NOT define setup_fixture in this file. DO NOT call `set -e`. DO NOT source helpers.sh.
  </action>
  <verify>
    <automated>bash -c 'set -uo pipefail; source ~/Sourcecode/agenticapps/agenticapps-shared/migrations/lib/fixture-runner.sh; declare -F extract_to && ! declare -F setup_fixture && echo FIXTURE_OK'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'extract_to()' ~/Sourcecode/agenticapps/agenticapps-shared/migrations/lib/fixture-runner.sh`
    - `! grep -q 'setup_fixture' ~/Sourcecode/agenticapps/agenticapps-shared/migrations/lib/fixture-runner.sh` (A1: setup_fixture NOT carved to shared)
    - `! grep -q 'agentic-apps-workflow' ~/Sourcecode/agenticapps/agenticapps-shared/migrations/lib/fixture-runner.sh` (no workflow template/skill paths leaked into shared)
    - `! grep -q '1.3.0' ~/Sourcecode/agenticapps/agenticapps-shared/migrations/lib/fixture-runner.sh` (no workflow ADR special-case in shared)
    - `! grep -qE '^\s*set -e\b' ~/Sourcecode/agenticapps/agenticapps-shared/migrations/lib/fixture-runner.sh`
    - The verify smoke test prints `FIXTURE_OK` (extract_to defined AND setup_fixture NOT defined)
  </acceptance_criteria>
  <done>fixture-runner.sh sources cleanly under set -u and exports ONLY extract_to (verbatim, generic). It defines NO setup_fixture and contains zero workflow template paths or the 1.3.0 special-case (A1). 3-arg extract_to(ref, path, out) is the sole shared fixture primitive.</done>
</task>

<task type="auto">
  <name>Task 3: Write preflight.sh + drift-test.sh (both parameterized, policy-agnostic, set -u safe)</name>
  <read_first>
    - migrations/run-tests.sh lines 46 (STRICT_PREFLIGHT init — note it is set in the CONSUMER, NOT inside the function; A5 requires the shared function to default it itself), 1755-1851 (test_preflight_verify_paths — uses `$REPO_ROOT/migrations` at 1795 and reads STRICT_PREFLIGHT at 1772/1786/1841), 2237-2269 (drift runner — uses relative `skill/SKILL.md` + `migrations/...` globs at 2255-2257)
    - 28-REVIEWS.md A5 (preflight must read ${STRICT_PREFLIGHT:-0} internally — set -u safety)
    - .planning/phases/28-split-01-agenticapps-shared/28-RESEARCH.md CRQ 5 (drift mechanism/policy split — copy the proposed `run_drift_test` signature verbatim), Pattern 3 (preflight parameterization), CRQ 9 Risk 3 (path validation)
  </read_first>
  <files>~/Sourcecode/agenticapps/agenticapps-shared/migrations/lib/preflight.sh, ~/Sourcecode/agenticapps/agenticapps-shared/migrations/lib/drift-test.sh</files>
  <action>
    Run `gitnexus_impact({target: "test_preflight_verify_paths", direction: "upstream"})`;
    record limited-coverage note (called only from the dispatcher at line 2534).

    A) Create `preflight.sh` with shebang + provenance header (claude-workflow `5aff1b1`,
       lines 1767-1851) + an idempotency guard
       (`[ -n "${_AGENTICAPPS_PREFLIGHT_LOADED:-}" ] && return 0; _AGENTICAPPS_PREFLIGHT_LOADED=1`).
       Carve `test_preflight_verify_paths` into a parameterized
       `run_preflight_verify_paths(migrations_dir)`:
       - Signature: `run_preflight_verify_paths() { local migrations_dir="$1"; ... }`
       - Replace the loop header at line 1795 `for migration in "$REPO_ROOT/migrations"/[0-9]*.md; do`
         with `for migration in "${migrations_dir}"/[0-9]*.md; do`
       - A5 (set -u safety, codex MEDIUM): the source line 46 sets STRICT_PREFLIGHT in the CONSUMER,
         not the function. The shared function MUST NOT assume the caller pre-initialized it. At the
         TOP of run_preflight_verify_paths add: `local strict="${STRICT_PREFLIGHT:-0}"` and replace
         EVERY `[ "$STRICT_PREFLIGHT" = "1" ]` test in the body (lines 1772, 1786, 1841) with
         `[ "$strict" = "1" ]`. This way a `set -u` caller that never exported STRICT_PREFLIGHT does
         not crash on the unbound-variable read.
       - Keep ALL other logic verbatim (RAN_AUDIT=1, python3/pyyaml graceful-skip, the embedded
         python heredoc, informational-only counting). It reads RED/GREEN/YELLOW/RESET, FAIL,
         RAN_AUDIT from the sourced shell — those are owned by helpers.sh + consumer (do NOT
         redefine them here).
       - Do NOT source helpers.sh.

    B) Create `drift-test.sh` with shebang + provenance header (claude-workflow `5aff1b1`,
       lines 2247-2269 + the POLICY NOTE comment block 2237-2244) + an idempotency guard
       (`[ -n "${_AGENTICAPPS_DRIFT_TEST_LOADED:-}" ] && return 0; _AGENTICAPPS_DRIFT_TEST_LOADED=1`).
       Carve ONLY the MECHANISM into `run_drift_test(skill_md, migrations_dir)` — copy the exact
       signature from 28-RESEARCH CRQ 5 "Proposed SHARED runner signature":
         `run_drift_test() { local skill_md="$1" migrations_dir="$2"; ... }`
       - Validate `[ -f "$skill_md" ]` at entry; return 1 with a stderr note if missing (Risk 3).
       - `skill_version=$(grep ^version: "$skill_md" | awk '{print $2}')`
       - `latest_migration_file=$(ls "${migrations_dir}"/[0-9][0-9][0-9][0-9]-*.md 2>/dev/null | sort | tail -1)`;
         return 1 if empty.
       - `migration_to_version=$(grep ^to_version: "$latest_migration_file" | awk '{print $2}')`
       - `[ "$skill_version" = "$migration_to_version" ]` → return 0; else emit a stderr
         "drift mismatch" line and return 1.
       - CRITICAL (D-28d): run_drift_test MUST NOT increment PASS or FAIL and MUST NOT echo the
         GREEN PASS / RED FAIL policy lines. Those stay in the claude-workflow POLICY wrapper
         (added in 28-03). Keep the D-04 minimal-grep-parser NOTE comment from lines 2248-2251.
       - Do NOT source helpers.sh.
    Neither file calls `set -e`.
  </action>
  <verify>
    <automated>bash -c 'set -uo pipefail; source ~/Sourcecode/agenticapps/agenticapps-shared/migrations/lib/helpers.sh; source ~/Sourcecode/agenticapps/agenticapps-shared/migrations/lib/preflight.sh; source ~/Sourcecode/agenticapps/agenticapps-shared/migrations/lib/drift-test.sh; declare -F run_preflight_verify_paths && declare -F run_drift_test && echo PREFLIGHT_DRIFT_OK'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'run_preflight_verify_paths()' ~/Sourcecode/agenticapps/agenticapps-shared/migrations/lib/preflight.sh`
    - `grep -q 'local migrations_dir="\$1"' ~/Sourcecode/agenticapps/agenticapps-shared/migrations/lib/preflight.sh`
    - `! grep -q 'REPO_ROOT' ~/Sourcecode/agenticapps/agenticapps-shared/migrations/lib/preflight.sh` (REPO_ROOT eliminated — fully parameterized)
    - `grep -q 'STRICT_PREFLIGHT:-0' ~/Sourcecode/agenticapps/agenticapps-shared/migrations/lib/preflight.sh` (A5: defaults the flag internally)
    - `! grep -q '"\$STRICT_PREFLIGHT"' ~/Sourcecode/agenticapps/agenticapps-shared/migrations/lib/preflight.sh` (no bare unbound read of STRICT_PREFLIGHT — A5)
    - `grep -q 'run_drift_test()' ~/Sourcecode/agenticapps/agenticapps-shared/migrations/lib/drift-test.sh`
    - `! grep -qE 'PASS=\$\(\(PASS|FAIL=\$\(\(FAIL' ~/Sourcecode/agenticapps/agenticapps-shared/migrations/lib/drift-test.sh` (no counter mutation — policy stays in consumer)
    - `! grep -qE '^\s*set -e\b' ~/Sourcecode/agenticapps/agenticapps-shared/migrations/lib/preflight.sh ~/Sourcecode/agenticapps/agenticapps-shared/migrations/lib/drift-test.sh`
    - A5 set -u proof: `bash -c 'set -u; source ~/Sourcecode/agenticapps/agenticapps-shared/migrations/lib/helpers.sh; source ~/Sourcecode/agenticapps/agenticapps-shared/migrations/lib/preflight.sh; unset STRICT_PREFLIGHT 2>/dev/null; run_preflight_verify_paths /nonexistent-dir; echo PREFLIGHT_SETU_OK'` prints `PREFLIGHT_SETU_OK` (no unbound-variable crash even with STRICT_PREFLIGHT never set)
    - The verify smoke test prints `PREFLIGHT_DRIFT_OK`
  </acceptance_criteria>
  <done>preflight.sh exposes run_preflight_verify_paths(migrations_dir) with no REPO_ROOT reference and reads ${STRICT_PREFLIGHT:-0} internally (runs under a bare `set -u` caller that never set the flag — A5); drift-test.sh exposes run_drift_test(skill_md, migrations_dir) that returns 0/1 only and never mutates PASS/FAIL. Both source cleanly alongside helpers.sh under set -u.</done>
</task>

<task type="auto">
  <name>Task 4: Amend ADR-0035 — demote setup_fixture to WORKFLOW; extract_to stays SHARED (A1)</name>
  <read_first>
    - docs/decisions/0035-shared-extraction-boundaries.md (the SHARED set table ~L60-74, the WORKFLOW set table ~L76-87)
    - migrations/run-tests.sh line 109 (the `# SHARED — generic fixture-runner harness...` annotation on setup_fixture)
    - 28-REVIEWS.md A1 (the locked boundary change)
    - .planning/phases/28-split-01-agenticapps-shared/28-CONTEXT.md D-28c (the "9 SHARED" count to update)
  </read_first>
  <files>docs/decisions/0035-shared-extraction-boundaries.md, migrations/run-tests.sh</files>
  <action>
    This is a documentation + annotation-only edit on claude-workflow (NO run-tests.sh behavior
    change — the consumer refactor is 28-03). Record the A1 boundary correction so ADR-0035 stays
    canonical.

    A) Edit `docs/decisions/0035-shared-extraction-boundaries.md`:
       1. In the SHARED set table (the "### SHARED set" table), REMOVE the `setup_fixture()` row.
          Keep the `extract_to()` row (it stays SHARED).
       2. In the WORKFLOW set table (the "### WORKFLOW set" table), ADD a `setup_fixture()` row with
          rationale: "Carved review (A1, codex HIGH): hardcodes claude-workflow template paths
          (templates/workflow-config.md, config-hooks.json, claude-md-sections.md) and a workflow-only
          1.3.0 ADR special-case. Not reusable by agenticapps-observability as-is. Stays in
          claude-workflow as a thin WRAPPER that calls shared extract_to and layers the workflow
          specifics on top."
       3. Add a new dated subsection at the end of the Decision section (or a short `### A1 amendment
          (2026-06-02): setup_fixture demoted to WORKFLOW wrapper` block) summarizing: the SHARED
          count drops from 9 to 8 SHARED elements; extract_to remains the generic fixture primitive;
          setup_fixture becomes a claude-workflow wrapper. Cite the cross-AI review (28-REVIEWS.md A1)
          as the trigger. Note the file annotation (step B) is the canonical line-level reference and
          this ADR table now matches it.
       4. Update any narrative "9 SHARED / 20 WORKFLOW" count to "8 SHARED / 21 WORKFLOW" if such a
          count appears (D-28c says the file annotations are canonical; keep ADR consistent).
    B) Edit `migrations/run-tests.sh` line 109 — the annotation ABOVE setup_fixture. Change the
       `# SHARED — generic fixture-runner harness; agenticapps-observability needs this...` line to:
       `# WORKFLOW — claude-workflow wrapper (A1): hardcodes template paths + the 1.3.0 special-case.`
       `# Calls SHARED extract_to (vendor/agenticapps-shared) and layers workflow specifics on top.`
       This is an ANNOTATION-ONLY edit (a comment line) — the setup_fixture BODY is unchanged here
       (its rewrite-into-a-wrapper happens in 28-03). The 186/4 baseline MUST be unaffected.
    C) Confirm baseline unchanged by the annotation edit:
       run `bash migrations/run-tests.sh` and confirm PASS=186 FAIL=4 (a comment edit must not move it).
  </action>
  <verify>
    <automated>cd /Users/donald/Sourcecode/agenticapps/claude-workflow && grep -A3 'WORKFLOW set' docs/decisions/0035-shared-extraction-boundaries.md | grep -q 'setup_fixture' ; SF_IN_WF=$? ; grep -B0 -A1 'setup_fixture()' migrations/run-tests.sh | grep -qi 'WORKFLOW' ; bash migrations/run-tests.sh > /tmp/28-01-baseline.out 2>&1 ; grep -qE 'PASS.*: 186' /tmp/28-01-baseline.out && grep -qE 'FAIL.*: 4' /tmp/28-01-baseline.out && echo ADR_AMENDED_BASELINE_OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'setup_fixture' docs/decisions/0035-shared-extraction-boundaries.md` AND it appears in the WORKFLOW set context (demoted): `awk '/WORKFLOW set/,/Rejected alternatives/' docs/decisions/0035-shared-extraction-boundaries.md | grep -q 'setup_fixture'`
    - The SHARED set no longer lists setup_fixture: `awk '/SHARED set/,/WORKFLOW set/' docs/decisions/0035-shared-extraction-boundaries.md | grep -vq 'setup_fixture' || ! awk '/SHARED set/,/WORKFLOW set/' docs/decisions/0035-shared-extraction-boundaries.md | grep -q '| .setup_fixture'`
    - `grep -q 'A1' docs/decisions/0035-shared-extraction-boundaries.md` (amendment recorded with provenance to the review)
    - The run-tests.sh annotation above setup_fixture now says WORKFLOW: `grep -B2 'setup_fixture()' migrations/run-tests.sh | grep -qi 'WORKFLOW'`
    - Baseline unchanged: `bash migrations/run-tests.sh` prints `PASS: 186` and `FAIL: 4` (annotation-only edit; verify smoke prints ADR_AMENDED_BASELINE_OK)
    - `! grep -qE '^\s*set -e\b' migrations/run-tests.sh` is NOT asserted here (run-tests.sh already has set -uo pipefail; only confirm no behavior change)
  </acceptance_criteria>
  <done>ADR-0035 SHARED set no longer contains setup_fixture; the WORKFLOW set documents it as a claude-workflow wrapper with the A1 rationale; the SHARED count is updated; run-tests.sh's annotation on setup_fixture reads WORKFLOW (annotation-only, body unchanged); and `bash migrations/run-tests.sh` still prints PASS=186 FAIL=4 (no behavior change from the comment edits).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| run-tests.sh source → new shared lib | Code is copied/generalized into a new repo; risk is semantic drift (wrong carve), not untrusted input |
| sourced lib executing in test harness | Lib functions run with the harness's `set -uo pipefail`; they execute `eval`/`grep`/`git show` on repo-local paths only |
| ADR + annotation edit on claude-workflow | Documentation/comment-only change; risk is an accidental behavior change resetting the 186/4 baseline |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-01 | Tampering | carved lib semantically diverges from source (wrong behavior) | mitigate | Carve verbatim except the documented parameterizations; 28-02 standalone test + 28-03 186/4 invariant prove equivalence |
| T-28-02 | Elevation | `run_check` uses `eval` on check strings | accept | Check strings are repo-authored test fixtures, not external input; pre-existing pattern unchanged by the carve |
| T-28-03 | Info disclosure | provenance header leaks internal paths | accept | Header cites public git SHA + relative paths only; repo is org-private; INFO at ASVS L1 |
| T-28-04 | Tampering | `set -e` accidentally introduced into a sourced lib leaks into WORKFLOW tests | mitigate | Acceptance criteria grep-block any `set -e` in every lib file (Risk 5) |
| T-28-14 | Tampering | shared code reads an unbound STRICT_PREFLIGHT under a `set -u` caller → crash (A5) | mitigate | preflight reads `${STRICT_PREFLIGHT:-0}` internally; Task 3 acceptance includes a bare-`set -u` proof |
| T-28-15 | Tampering | ADR/annotation edit accidentally changes setup_fixture behavior → resets 186/4 | mitigate | Task 4 is annotation-only (comment lines + ADR markdown); acceptance re-runs the suite and asserts PASS=186 FAIL=4 |

No HIGH-severity threats. Surface is supply-chain integrity of a carved bash lib, not app auth.
</threat_model>

<verification>
1. Each lib file sources cleanly in isolation under `set -uo pipefail` (the smoke tests above).
2. No lib file contains `set -e` or sources another lib file (grep gates).
3. fixture-runner.sh exports ONLY extract_to; setup_fixture is NOT present (A1 grep gate).
4. preflight reads `${STRICT_PREFLIGHT:-0}` and survives a bare `set -u` caller (A5 proof).
5. drift-test.sh `run_drift_test` does not mutate PASS/FAIL (D-28d separation verified by grep).
6. No observability/GSD code present:
   `! grep -rqiE 'observability|sentry|cron|queue|gsd-|migrate-0019|migrate-0021|destinations' ~/Sourcecode/agenticapps/agenticapps-shared/migrations/lib/`
7. ADR-0035 amended (setup_fixture in WORKFLOW set; A1 recorded); run-tests.sh annotation says WORKFLOW; baseline still 186/4.
8. Run GitNexus `gitnexus_detect_changes()` after the run-tests.sh annotation edit; confirm only the comment line + ADR moved (no symbol-body change); record in SUMMARY.
</verification>

<success_criteria>
- Four lib files exist in agenticapps-shared/migrations/lib/ with carved + parameterized SHARED functions (SC-1).
- fixture-runner.sh exports ONLY extract_to; setup_fixture is NOT carved to shared (A1 / SC-5).
- run_drift_test is policy-agnostic — mechanism shared, policy NOT carved (D-28d / SC-4).
- run_preflight_verify_paths is parameterized off migrations_dir, no REPO_ROOT, and reads ${STRICT_PREFLIGHT:-0} (A5).
- Zero observability/GSD code in the lib (SC-5).
- Provenance recorded in each file header citing claude-workflow 5aff1b1 (D-28b).
- ADR-0035 amended: setup_fixture → WORKFLOW, extract_to stays SHARED; run-tests.sh annotation updated; 186/4 baseline unchanged (A1).
</success_criteria>

<output>
After completion, create `.planning/phases/28-split-01-agenticapps-shared/28-01-SUMMARY.md`.
Do NOT commit/tag agenticapps-shared in this plan — 28-02 writes the standalone test, updates
CHANGELOG/VERSION, and cuts the single v1.0.0 commit + tag. Leave the lib files staged-but-uncommitted
or note their working-copy state for 28-02 to fold into one provenance commit. The ADR-0035 amendment
+ run-tests.sh annotation edit are on claude-workflow and are committed by 28-03 on the feature branch
(note their working-copy state in the SUMMARY so 28-03 folds them into its commit).
</output>
