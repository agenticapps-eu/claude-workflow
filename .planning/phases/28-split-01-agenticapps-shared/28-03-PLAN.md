---
phase: 28-split-01-agenticapps-shared
plan: 03
type: execute
wave: 2
depends_on: [28-02]
files_modified:
  - .gitmodules
  - migrations/run-tests.sh
  - install.sh
  - CHANGELOG.md
autonomous: false
requirements: [SC-2, SC-3, SC-4, SC-6, SC-7, SC-8, D-28a, D-28d, D-28e]
must_haves:
  truths:
    - "claude-workflow vendors agenticapps-shared as a git submodule at vendor/agenticapps-shared pinned to tag v1.0.0"
    - "migrations/run-tests.sh sources the four shared lib files from the submodule via BASH_SOURCE[0] dirname"
    - "run-tests.sh retains ALL WORKFLOW per-migration test bodies + the drift POLICY wrapper that increments PASS/FAIL"
    - "The suite baseline is preserved EXACTLY at PASS=186 FAIL=4 (the 4 pre-existing test_migration_0017 failures unchanged)"
    - "The drift test still PASSES (SKILL.md version == latest migration to_version) via run_drift_test mechanism + claude-workflow policy wrapper"
    - "install.sh initializes the submodule on fresh clones"
    - "GSD commands (/gsd-progress, /gsd-stats, /gsd-help) produce unchanged output (no code touched there)"
    - "CHANGELOG records the extraction; a PR is opened to claude-workflow main"
  artifacts:
    - path: ".gitmodules"
      provides: "Submodule declaration for vendor/agenticapps-shared"
      contains: "vendor/agenticapps-shared"
    - path: "migrations/run-tests.sh"
      provides: "Consumer runner that sources shared lib + keeps WORKFLOW bodies + drift policy"
      contains: "vendor/agenticapps-shared/migrations/lib"
    - path: "install.sh"
      provides: "Submodule init on fresh clone"
      contains: "submodule update --init"
  key_links:
    - from: "migrations/run-tests.sh"
      to: "vendor/agenticapps-shared/migrations/lib/*.sh"
      via: "source via _SHARED_LIB BASH_SOURCE dirname"
      pattern: "source .*_SHARED_LIB"
    - from: "run-tests.sh:test_skill_md_version_matches_latest_migration_to_version (policy wrapper)"
      to: "drift-test.sh:run_drift_test (mechanism)"
      via: "if run_drift_test ...; then PASS++ else FAIL++"
      pattern: "run_drift_test"
    - from: ".gitmodules"
      to: "tag v1.0.0"
      via: "submodule pinned to immutable tag, verified by git describe"
      pattern: "v1.0.0"
---

<objective>
Wire claude-workflow to consume agenticapps-shared as a git submodule at `vendor/agenticapps-shared`
pinned to tag `v1.0.0`, refactor `migrations/run-tests.sh` to SOURCE the four shared lib files
(D-28e source-and-keep: delete the now-duplicated SHARED function bodies, keep ALL WORKFLOW
per-migration bodies + the drift POLICY wrapper), update `install.sh` to init the submodule, record
the extraction in CHANGELOG, prove the `PASS=186 FAIL=4` baseline is preserved EXACTLY, then open a PR.

Purpose: complete the consumer side of SPLIT-01 with zero behavior change to the test suite — the
shared functions move out, the per-migration logic and version-coupling policy stay put.
Output: vendored submodule, slimmed consumer run-tests.sh, install.sh submodule init, CHANGELOG
entry, and an open PR to claude-workflow main.

IMPORTANT — operates on the claude-workflow repo. Depends on 28-02 having tagged agenticapps-shared
v1.0.0 (the submodule pins to it). ALL work on a FEATURE BRANCH (project CLAUDE.md: never commit to
main). Source SHA baseline = 5aff1b1 (v1.21.0). Version bump of claude-workflow is OUT OF SCOPE
(decided at SPLIT-02 ship time) — do NOT bump VERSION or SKILL.md here.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-split-01-agenticapps-shared/28-CONTEXT.md
@.planning/phases/28-split-01-agenticapps-shared/28-RESEARCH.md
@.planning/phases/28-split-01-agenticapps-shared/28-02-SUMMARY.md
@docs/decisions/0035-shared-extraction-boundaries.md
@migrations/run-tests.sh
@install.sh

<interfaces>
<!-- The consumer sourcing block (28-RESEARCH CRQ 4 / Code Examples, exact snippet) -->
# _SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
# _SHARED_LIB="$_SCRIPT_DIR/../vendor/agenticapps-shared/migrations/lib"
# if [ ! -d "$_SHARED_LIB" ]; then echo "ERROR: shared lib not found ..."; echo "Fix: git submodule update --init --recursive"; exit 1; fi
# source "$_SHARED_LIB/helpers.sh"; source "$_SHARED_LIB/fixture-runner.sh"; source "$_SHARED_LIB/preflight.sh"; source "$_SHARED_LIB/drift-test.sh"

# Functions REMOVED from run-tests.sh (now sourced from lib):
#   _runtests_do_cleanup, extract_to, setup_fixture, run_check, assert_check
#   plus their preamble: color block L30-34, counter init L39-42
# Functions REPLACED by thin policy wrappers:
#   test_preflight_verify_paths  -> calls run_preflight_verify_paths "$REPO_ROOT/migrations"
#   test_skill_md_version_matches_latest_migration_to_version -> calls run_drift_test, owns PASS/FAIL
# Functions KEPT verbatim (WORKFLOW):
#   test_migration_0001..0021, test_meta_destinations_consistency, _roles_from_adapter,
#   _roles_from_meta, test_sigterm_mid_apply_preserves_state, dispatcher block, summary block
# KEPT preamble: REPO_ROOT (L36-37), flag/FILTER parsing (L44-69), trap setup (moved AFTER sourcing)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Feature branch + add agenticapps-shared submodule pinned to v1.0.0 + install.sh init</name>
  <read_first>
    - install.sh (full — find the SCAFFOLDER/repo-root setup; the submodule init block goes after it, before the LINKS array)
    - .planning/phases/28-split-01-agenticapps-shared/28-RESEARCH.md CRQ 8 (install.sh block snippet, no CI exists, vendor/.gitmodules absent), Assumption A3 (verify remote URL first)
    - .planning/phases/28-split-01-agenticapps-shared/28-02-SUMMARY.md (the v1.0.0 commit SHA to confirm the pin)
  </read_first>
  <files>.gitmodules, install.sh</files>
  <action>
    A) Create the feature branch (project CLAUDE.md — never commit to main):
       `git checkout -b split-01-agenticapps-shared`
    B) Verify the remote URL before adding (Assumption A3):
       `gh repo view agenticapps-eu/agenticapps-shared --json url,visibility`
    C) Add the submodule (D-28a) over canonical HTTPS:
       `git submodule add https://github.com/agenticapps-eu/agenticapps-shared vendor/agenticapps-shared`
       `git submodule update --init --recursive`
    D) Pin to the immutable tag v1.0.0 (D-28a; T-28-05 — tag not branch):
       `git -C vendor/agenticapps-shared fetch --tags`
       `git -C vendor/agenticapps-shared checkout v1.0.0`
       Then stage the submodule pin from the superproject: `git add vendor/agenticapps-shared .gitmodules`
       Confirm `.gitmodules` URL is exactly `https://github.com/agenticapps-eu/agenticapps-shared`
       (canonical org repo, HTTPS — security gate).
    E) Update install.sh: add the submodule-init block from 28-RESEARCH CRQ 8, placed after the
       repo-root/SCAFFOLDER setup and before the LINKS array. Read install.sh first and match its
       actual repo-root variable name:
       ```
       # Init git submodule on fresh clone (vendor/agenticapps-shared)
       if [ -f "$SCAFFOLDER/.gitmodules" ] && [ ! -f "$SCAFFOLDER/vendor/agenticapps-shared/VERSION" ]; then
         echo "Initialising git submodule vendor/agenticapps-shared..."
         git -C "$SCAFFOLDER" submodule update --init --recursive
       fi
       ```
       (Replace `$SCAFFOLDER` with the variable install.sh actually uses for the repo root if different.)
    F) Do NOT commit yet (Task 3 commits everything once the refactor + baseline pass). Leave staged.
  </action>
  <verify>
    <automated>cd /Users/donald/Sourcecode/agenticapps/claude-workflow && git rev-parse --abbrev-ref HEAD | grep -qx split-01-agenticapps-shared && grep -q 'vendor/agenticapps-shared' .gitmodules && grep -q 'https://github.com/agenticapps-eu/agenticapps-shared' .gitmodules && git -C vendor/agenticapps-shared describe --tags --exact-match HEAD 2>/dev/null | grep -qx v1.0.0 && [ -f vendor/agenticapps-shared/migrations/lib/helpers.sh ] && grep -q 'submodule update --init' install.sh && echo SUBMODULE_PINNED_OK</automated>
  </verify>
  <acceptance_criteria>
    - `git rev-parse --abbrev-ref HEAD` is `split-01-agenticapps-shared` (on feature branch)
    - `grep -q 'vendor/agenticapps-shared' .gitmodules`
    - `.gitmodules` URL is exactly `https://github.com/agenticapps-eu/agenticapps-shared` (HTTPS, canonical org)
    - `git -C vendor/agenticapps-shared describe --tags --exact-match HEAD` prints `v1.0.0` (immutable tag, not a branch)
    - `[ -f vendor/agenticapps-shared/migrations/lib/helpers.sh ]` (submodule content present)
    - `grep -q 'submodule update --init' install.sh`
  </acceptance_criteria>
  <done>On feature branch; submodule added at vendor/agenticapps-shared over canonical HTTPS, pinned to tag v1.0.0 (verified via git describe), lib files present; install.sh has the fresh-clone init block. Nothing committed yet.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor run-tests.sh to source shared lib + keep WORKFLOW bodies + drift/preflight policy wrappers (186/4 invariant)</name>
  <read_first>
    - migrations/run-tests.sh (full — you are removing SHARED bodies and rewiring; this is the load-bearing edit)
    - .planning/phases/28-split-01-agenticapps-shared/28-RESEARCH.md "Code Examples" (consumer sourcing pattern verbatim), Pattern 2 (drift policy wrapper), Pattern 3 (preflight wrapper), CRQ 3 (trap-after-source, counter ownership), CRQ 9 Risks 2/4/5
    - docs/decisions/0035-shared-extraction-boundaries.md (MECHANISM vs POLICY — what stays)
  </read_first>
  <files>migrations/run-tests.sh</files>
  <action>
    Project CLAUDE.md: run `gitnexus_impact` on each SHARED symbol you remove
    (`_runtests_do_cleanup`, `extract_to`, `setup_fixture`, `run_check`, `assert_check`,
    `test_preflight_verify_paths`) BEFORE editing; report blast radius. Note bash has limited
    GitNexus coverage — cross-check callers against 28-RESEARCH CRQ 2 (all callers are WORKFLOW
    test_migration_* functions, which stay). Warn the user only if HIGH/CRITICAL is reported.

    Edit migrations/run-tests.sh to become a CONSUMER (D-28e source-and-keep):
    1. KEEP the shebang + usage header + `set -uo pipefail` (lines 1-27).
    2. After `set -uo pipefail`, INSERT the shared-lib sourcing block (28-RESEARCH Code Examples,
       verbatim): resolve `_SCRIPT_DIR` via `${BASH_SOURCE[0]}`, set
       `_SHARED_LIB="$_SCRIPT_DIR/../vendor/agenticapps-shared/migrations/lib"`, guard with the
       `[ ! -d "$_SHARED_LIB" ]` error+exit-1 block (Risk 6), then source helpers.sh, fixture-runner.sh,
       preflight.sh, drift-test.sh (helpers FIRST — Risk 1/ordering).
    3. REMOVE the now-duplicated SHARED preamble + bodies (sourced from lib now):
       - color block lines 30-34 (now in helpers.sh)
       - counter init lines 39-42 (now in helpers.sh) — KEEP REPO_ROOT (36-37) and flag/FILTER
         parsing (44-69), which stay WORKFLOW-side.
       - `_runtests_cleanup_fired` + `_runtests_do_cleanup()` lines 76-82 (now in helpers.sh)
       - the entire Helpers section bodies: `extract_to` 96-104, `setup_fixture` 110-134,
         `run_check` 139-143, `assert_check` 151-170 (all now in lib).
    4. KEEP the trap setup (lines 83-85) but place it AFTER the sourcing block (Risk 2 — traps set in
       consumer after helpers.sh defines _runtests_do_cleanup).
    5. REPLACE `test_preflight_verify_paths()` body (1767-1851) with a thin POLICY wrapper (Pattern 3):
       `test_preflight_verify_paths() { run_preflight_verify_paths "$REPO_ROOT/migrations"; }`
    6. REPLACE the drift runner `test_skill_md_version_matches_latest_migration_to_version()`
       (2247-2269) with the POLICY wrapper (Pattern 2 / D-28d) — it OWNS PASS/FAIL:
       ```
       test_skill_md_version_matches_latest_migration_to_version() {
         if run_drift_test "$REPO_ROOT/skill/SKILL.md" "$REPO_ROOT/migrations"; then
           echo "  ${GREEN}PASS${RESET}: test-skill-md-version-matches-latest-migration-to-version"
           PASS=$((PASS+1))
         else
           echo "  ${RED}FAIL${RESET}: SKILL.md version does not match latest migration to_version"
           FAIL=$((FAIL+1))
         fi
       }
       ```
    7. KEEP verbatim ALL WORKFLOW functions: test_migration_0001..0021, test_meta_destinations_consistency,
       _roles_from_adapter, _roles_from_meta, test_sigterm_mid_apply_preserves_state.
    8. KEEP the dispatcher block (2462-2560) and Summary block (2562-2579) verbatim — the dispatcher
       references the policy wrappers by name (declare -F guards already present, harmless).
    9. Do NOT introduce `set -e` (Risk 5). No counter increment at file level (Risk 4).
    Then run the suite and confirm the EXACT baseline. If PASS != 186 or FAIL != 4, STOP and diff
    against the baseline — do not "fix" the 0017 failures (they are FIX-0017 scope) and do not let
    the carve regress them.
  </action>
  <verify>
    <automated>cd /Users/donald/Sourcecode/agenticapps/claude-workflow && bash migrations/run-tests.sh > /tmp/28-03-suite.out 2>&1; grep -qE 'PASS.*: 186' /tmp/28-03-suite.out && grep -qE 'FAIL.*: 4' /tmp/28-03-suite.out && grep -q 'PASS.*test-skill-md-version-matches-latest-migration-to-version' /tmp/28-03-suite.out && echo BASELINE_186_4_PRESERVED || (echo BASELINE_REGRESSION; tail -30 /tmp/28-03-suite.out; false)</automated>
  </verify>
  <acceptance_criteria>
    - `bash migrations/run-tests.sh` prints `PASS: 186` and `FAIL: 4` (exact baseline; verify smoke prints BASELINE_186_4_PRESERVED)
    - `grep -q 'vendor/agenticapps-shared/migrations/lib' migrations/run-tests.sh` (sources the submodule lib)
    - `grep -q 'run_drift_test' migrations/run-tests.sh` (drift policy wrapper calls shared mechanism)
    - `grep -q 'run_preflight_verify_paths' migrations/run-tests.sh` (preflight wrapper calls shared mechanism)
    - `! grep -qE '^assert_check\(\)|^run_check\(\)|^extract_to\(\)|^setup_fixture\(\)' migrations/run-tests.sh` (SHARED bodies removed, not redefined)
    - `grep -q 'test_migration_0001' migrations/run-tests.sh` and `grep -q 'test_migration_0021' migrations/run-tests.sh` (WORKFLOW bodies kept)
    - `! grep -qE '^\s*set -e\b' migrations/run-tests.sh` (no set -e introduced)
    - Drift test line shows PASS (the SKILL.md-vs-migration policy still passes)
  </acceptance_criteria>
  <done>run-tests.sh sources the four lib files from the submodule, removed the SHARED bodies, kept all WORKFLOW per-migration bodies + dispatcher + summary, and replaced the drift + preflight functions with thin policy wrappers that call the shared mechanism. The suite prints PASS=186 FAIL=4 exactly, with the drift test PASSing.</done>
</task>

<task type="auto">
  <name>Task 3: GSD-regression check, CHANGELOG entry, commit, detect-changes, push, open PR</name>
  <read_first>
    - CHANGELOG.md (top — add the new entry above the most recent section; do NOT add a version-numbered header, this is unreleased extraction work; use an `## [Unreleased] — SPLIT-01` section)
    - .planning/phases/28-split-01-agenticapps-shared/28-RESEARCH.md CRQ 8 (no CI; run-tests.sh path unchanged so no external refs need updating)
    - SPLIT-01-agenticapps-shared.md Phase F (PR title/body conventions)
  </read_first>
  <files>CHANGELOG.md</files>
  <action>
    A) GSD-regression smoke (SC-6): confirm GSD commands still work. These call the GSD framework
       at `~/.claude/get-shit-done/bin/gsd-tools.cjs` (NOT in this repo — untouched), but verify
       no claude-workflow change broke them. Run a non-mutating subset, e.g.:
       `node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" --help` (or the project's documented
       progress/stats invocation) and confirm exit 0 + non-empty output. Record the commands +
       outputs in the SUMMARY. (No claude-workflow code under bin/ was touched, so this is a sanity
       check, not an expected-change site.)
    B) CHANGELOG.md: add an `## [Unreleased] — SPLIT-01: extract shared migration infrastructure`
       section at the top of the changelog body. Record: agenticapps-shared added as a git submodule
       at `vendor/agenticapps-shared` pinned to v1.0.0; `migrations/run-tests.sh` now sources the
       shared lib (helpers/fixture-runner/preflight/drift-test) and retains all per-migration
       WORKFLOW bodies + the version-coupling drift POLICY; suite baseline preserved at PASS=186
       FAIL=4 (the 4 pre-existing test_migration_0017 failures are FIX-0017 scope, unchanged);
       install.sh now inits the submodule on fresh clone. Note: claude-workflow VERSION/SKILL.md
       intentionally NOT bumped (decided at SPLIT-02 ship time).
    C) Run `gitnexus_detect_changes()` (project CLAUDE.md) before committing; confirm only the
       expected files/flows are affected; record output in SUMMARY (note bash/markdown limited coverage).
    D) Commit on the feature branch:
       `git add .gitmodules vendor/agenticapps-shared migrations/run-tests.sh install.sh CHANGELOG.md`
       `git commit -m "chore(split-01): consume agenticapps-shared as submodule; run-tests.sh sources shared lib" -m "Adds vendor/agenticapps-shared submodule pinned to v1.0.0. migrations/run-tests.sh refactored to source helpers/fixture-runner/preflight/drift-test from the submodule (D-28e source-and-keep); keeps all WORKFLOW per-migration bodies + drift POLICY wrapper. Baseline preserved: PASS=186 FAIL=4. install.sh inits submodule on fresh clone. Source baseline 5aff1b1 (v1.21.0)." -m "Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"`
    E) Push and open the PR (SC-8):
       `git push -u origin split-01-agenticapps-shared`
       `gh pr create --base main --head split-01-agenticapps-shared --title "chore: extract shared migration infrastructure to agenticapps-shared (SPLIT-01)" --body "<body>"`
       PR body MUST: link SPLIT-00 / SPLIT-01 docs + ADR-0035 + the new agenticapps-shared repo +
       its v1.0.0 tag; state the 186/4 baseline-preserved invariant; note provenance-by-note (D-28b)
       and that no claude-workflow version bump happens here. End the body with the
       `🤖 Generated with [Claude Code](https://claude.com/claude-code)` line.
       If push/PR hits an auth gate, create a checkpoint requesting `gh auth` and retry.
  </action>
  <verify>
    <automated>cd /Users/donald/Sourcecode/agenticapps/claude-workflow && grep -q 'SPLIT-01' CHANGELOG.md && grep -q 'PASS=186' CHANGELOG.md && git log -1 --pretty=%s | grep -qi 'split-01' && gh pr list --head split-01-agenticapps-shared --json number,title | grep -qi 'agenticapps-shared' && echo PR_OPEN_OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'SPLIT-01' CHANGELOG.md` and `grep -q 'PASS=186' CHANGELOG.md`
    - `git log -1 --pretty=%s` contains `split-01` (commit landed on feature branch)
    - `git ls-remote --heads origin split-01-agenticapps-shared` is non-empty (branch pushed)
    - `gh pr list --head split-01-agenticapps-shared` shows an open PR to main (SC-8)
    - SUMMARY records GSD command outputs proving no regression (SC-6) and the gitnexus_detect_changes scope
  </acceptance_criteria>
  <done>GSD commands verified non-regressed; CHANGELOG records the extraction + 186/4 invariant; commit landed on the feature branch with submodule + run-tests.sh + install.sh + CHANGELOG; branch pushed; PR opened to main linking the split docs, ADR-0035, and the shared repo v1.0.0 tag.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify fresh-clone submodule + 186/4 baseline, then /gsd-review + merge PR</name>
  <action>This is a human-verification checkpoint (no autonomous action). Claude has opened the PR; the human verifies fresh-clone submodule consumption, re-confirms the PASS=186 FAIL=4 baseline, runs /gsd-review (codex cross-AI, D-28f) and /review on the diff, then merges.</action>
  <what-built>
    claude-workflow now consumes agenticapps-shared as a v1.0.0-pinned submodule; run-tests.sh
    sources the shared lib and preserves the PASS=186 FAIL=4 baseline; install.sh inits the
    submodule; CHANGELOG updated; PR opened to main. Per project CLAUDE.md the human owns merge +
    the cross-AI /gsd-review (D-28f) and /review on the diff.
  </what-built>
  <how-to-verify>
    1. Fresh-clone submodule check (SC-2): in a scratch dir run
       `git clone --recurse-submodules <claude-workflow-remote> /tmp/cw-fresh && [ -f /tmp/cw-fresh/vendor/agenticapps-shared/migrations/lib/helpers.sh ] && echo FRESH_CLONE_OK`
       (or `git clone <remote> /tmp/cw-fresh2 && cd /tmp/cw-fresh2 && bash install.sh` and confirm
       the submodule init message + lib presence).
    2. Re-run `bash migrations/run-tests.sh` in the fresh clone and confirm `PASS: 186` / `FAIL: 4`.
    3. Review the PR diff (`gh pr diff`); confirm: only SHARED bodies removed, all WORKFLOW bodies
       intact, drift test still PASS, no claude-workflow version bump.
    4. Run /gsd-review (codex cross-AI, D-28f) and /review on the phase diff per project CLAUDE.md.
    5. Merge the PR when reviews approve.
  </how-to-verify>
  <resume-signal>Type "approved" once the PR is reviewed/merged, or describe issues to address.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| claude-workflow superproject → submodule pin | A pin to an external repo; integrity hinges on it being an immutable tag over canonical HTTPS |
| submodule lib `source`d in test harness | Shared bash executes in the local test process; inputs are repo-local fixture paths only |
| fresh-clone consumer | A developer cloning must fetch the submodule; missing-submodule must fail loud, not silent |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-09 | Tampering | submodule pinned to a moving branch (mutable upstream code executes in harness) | mitigate | Pin to immutable tag v1.0.0; Task 1 verifies via `git -C vendor/... describe --tags --exact-match` == v1.0.0 |
| T-28-10 | Spoofing | `.gitmodules` points at a non-canonical / typo'd / non-HTTPS URL | mitigate | Task 1 asserts URL == `https://github.com/agenticapps-eu/agenticapps-shared`; `gh repo view` confirms canonical org before add |
| T-28-11 | Tampering | sourced lib executes untrusted input | accept | Lib only `source`s vendored, version-pinned bash + runs repo-authored fixtures; no network/user input path; pre-existing harness pattern |
| T-28-12 | DoS | missing submodule → cryptic source failure blocks all tests | mitigate | Guard block in run-tests.sh prints actionable `git submodule update --init` error + exit 1 (Risk 6); install.sh auto-inits |
| T-28-13 | Elevation | org repo access scope expands via new submodule | accept | Repo is org-private; submodule read uses existing member auth; no new credential surface; INFO at ASVS L1 |

No HIGH-severity threats. Surface is supply-chain integrity of a tag-pinned submodule; mitigations are tag-pinning + canonical-URL assertion + loud-fail guard.
</threat_model>

<verification>
1. Submodule present and pinned to immutable tag v1.0.0 over canonical HTTPS (Task 1 grep/describe gates).
2. `bash migrations/run-tests.sh` → PASS=186 FAIL=4 exactly, drift test PASS (SC-3, SC-4).
3. SHARED bodies removed from run-tests.sh; WORKFLOW bodies + dispatcher + summary intact (grep gates).
4. install.sh inits submodule on fresh clone (SC-2); fresh-clone `--recurse-submodules` verified at checkpoint.
5. GSD commands non-regressed (SC-6); `gitnexus_detect_changes()` scope reviewed.
6. CHANGELOG records extraction (SC-7); PR opened to main (SC-8).
</verification>

<success_criteria>
- claude-workflow consumes agenticapps-shared as a v1.0.0-pinned submodule (SC-2, D-28a).
- run-tests.sh sources the shared lib + keeps WORKFLOW bodies + drift POLICY (D-28d, D-28e).
- Suite baseline preserved EXACTLY: PASS=186 FAIL=4 (SC-3).
- Drift test still PASSES (SC-4).
- install.sh inits the submodule on fresh clone (SC-2).
- No GSD command regression (SC-6); CHANGELOG records the extraction (SC-7); PR opened (SC-8).
</success_criteria>

<output>
After completion, create `.planning/phases/28-split-01-agenticapps-shared/28-03-SUMMARY.md`.
Record: the submodule pin SHA/tag, the exact suite tail (PASS/FAIL), the GSD-command outputs,
and the PR number/URL. Note that merge + /gsd-review + /review are owned by the human checkpoint (D-28f).
</output>
