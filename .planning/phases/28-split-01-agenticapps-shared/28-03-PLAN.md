---
phase: 28-split-01-agenticapps-shared
plan: 03
type: execute
wave: 2
depends_on: [28-02]
files_modified:
  - .gitmodules
  - migrations/run-tests.sh
  - install.sh
  - CHANGELOG.md
  - docs/decisions/0035-shared-extraction-boundaries.md
autonomous: false
requirements: [SC-2, SC-3, SC-4, SC-6, SC-7, SC-8, D-28a, D-28d, D-28e]
must_haves:
  truths:
    - "claude-workflow vendors agenticapps-shared as a git submodule at vendor/agenticapps-shared; the superproject gitlink SHA equals 28-02's recorded release SHA (A4)"
    - "migrations/run-tests.sh sources the four shared lib files from the submodule via BASH_SOURCE[0] dirname"
    - "setup_fixture is rebuilt in run-tests.sh as a WORKFLOW wrapper that calls shared extract_to and layers the template paths + the 1.3.0 special-case (A1)"
    - "run-tests.sh retains ALL WORKFLOW per-migration test bodies + the drift POLICY wrapper that increments PASS/FAIL"
    - "The suite baseline is preserved EXACTLY at PASS=186 FAIL=4 (the 4 pre-existing test_migration_0017 failures unchanged)"
    - "The drift test still PASSES (SKILL.md version == latest migration to_version) via run_drift_test mechanism + claude-workflow policy wrapper"
    - "install.sh ALWAYS advances the submodule when .gitmodules exists (sync + update --init --recursive), idempotent on existing clones (A3)"
    - "GSD command outputs (/gsd-progress, /gsd-stats, /gsd-help equivalents) are captured before and diffed identical after the refactor (A6)"
    - "CHANGELOG records the extraction; the ADR-0035 amendment from 28-01 is committed; a PR with a concrete body is opened to claude-workflow main (A7)"
  artifacts:
    - path: ".gitmodules"
      provides: "Submodule declaration for vendor/agenticapps-shared over canonical HTTPS"
      contains: "vendor/agenticapps-shared"
    - path: "migrations/run-tests.sh"
      provides: "Consumer runner: sources shared lib + setup_fixture WORKFLOW wrapper + WORKFLOW bodies + drift/preflight policy"
      contains: "vendor/agenticapps-shared/migrations/lib"
    - path: "install.sh"
      provides: "Submodule sync+update on every run when .gitmodules exists (A3)"
      contains: "submodule update --init"
  key_links:
    - from: "migrations/run-tests.sh"
      to: "vendor/agenticapps-shared/migrations/lib/*.sh"
      via: "source via _SHARED_LIB BASH_SOURCE dirname"
      pattern: "source .*_SHARED_LIB"
    - from: "run-tests.sh:setup_fixture (WORKFLOW wrapper)"
      to: "drift-test.sh:extract_to (shared primitive)"
      via: "wrapper calls shared extract_to + layers template paths + 1.3.0 special-case (A1)"
      pattern: "extract_to"
    - from: "run-tests.sh:test_skill_md_version_matches_latest_migration_to_version (policy wrapper)"
      to: "drift-test.sh:run_drift_test (mechanism)"
      via: "if run_drift_test ...; then PASS++ else FAIL++"
      pattern: "run_drift_test"
    - from: "superproject gitlink (git ls-tree HEAD vendor/agenticapps-shared)"
      to: "28-02 recorded release SHA"
      via: "gitlink SHA == recorded SHA (A4); tag is provenance only"
      pattern: "[0-9a-f]{40}"
---

<objective>
Wire claude-workflow to consume agenticapps-shared as a git submodule at `vendor/agenticapps-shared`,
verify the SUPERPROJECT GITLINK SHA equals 28-02's recorded release SHA (A4), refactor
`migrations/run-tests.sh` to SOURCE the four shared lib files AND rebuild `setup_fixture` as a
WORKFLOW wrapper over shared `extract_to` (A1), keep ALL WORKFLOW per-migration bodies + the drift
POLICY wrapper, fold in the ADR-0035 amendment + annotation edit produced in 28-01, update
`install.sh` to advance the submodule on every run (A3), record the extraction in CHANGELOG, prove
the `PASS=186 FAIL=4` baseline is preserved EXACTLY, prove GSD commands are byte-identical before/after
(A6), then open a PR with a concrete body (A7).

Purpose: complete the consumer side of SPLIT-01 with zero behavior change to the test suite — the
shared primitives move out, the workflow-specific fixture composition + per-migration logic +
version-coupling policy stay put.
Output: vendored submodule (gitlink-SHA-verified), refactored consumer run-tests.sh (with the
setup_fixture wrapper), install.sh submodule advance, CHANGELOG + committed ADR amendment, and an
open PR to claude-workflow main.

REVIEW-DRIVEN CHANGES:
- A1 (codex HIGH, user-locked): setup_fixture is NOT in shared. This plan REBUILDS it in run-tests.sh
  as a thin WORKFLOW wrapper that calls shared `extract_to` and layers the workflow template paths +
  the 1.3.0 ADR special-case. It also commits the ADR-0035 amendment + the setup_fixture annotation
  edit that 28-01 staged on this repo.
- A3 (codex HIGH): install.sh must advance the submodule on EXISTING clones, not just first init.
  When `.gitmodules` exists, ALWAYS run sync + update (idempotent).
- A4 (codex MEDIUM): the pin contract is the gitlink SHA. The acceptance criterion verifies the
  superproject gitlink SHA (`git ls-tree HEAD vendor/agenticapps-shared`) equals 28-02's recorded
  SHA, NOT `git describe --tags --exact-match HEAD`.
- A6 (codex MEDIUM): SC-6 GSD-regression proof is byte-diff of captured command outputs, not `--help`.
- A7 (gemini LOW): the PR body is a concrete template, not a placeholder.

IMPORTANT — operates on the claude-workflow repo. Depends on 28-02 having tagged agenticapps-shared
v1.0.0 AND recorded the canonical release SHA (the submodule gitlink pins to it). ALL work on a
FEATURE BRANCH (project CLAUDE.md: never commit to main). Source SHA baseline = 5aff1b1 (v1.21.0).
Version bump of claude-workflow is OUT OF SCOPE (decided at SPLIT-02 ship time) — do NOT bump VERSION
or SKILL.md here.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-split-01-agenticapps-shared/28-CONTEXT.md
@.planning/phases/28-split-01-agenticapps-shared/28-RESEARCH.md
@.planning/phases/28-split-01-agenticapps-shared/28-02-SUMMARY.md
@docs/decisions/0035-shared-extraction-boundaries.md
@migrations/run-tests.sh
@install.sh

<interfaces>
<!-- The consumer sourcing block (28-RESEARCH CRQ 4 / Code Examples, exact snippet) -->
# _SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
# _SHARED_LIB="$_SCRIPT_DIR/../vendor/agenticapps-shared/migrations/lib"
# if [ ! -d "$_SHARED_LIB" ]; then echo "ERROR: shared lib not found ..."; echo "Fix: git submodule update --init --recursive"; exit 1; fi
# source "$_SHARED_LIB/helpers.sh"; source "$_SHARED_LIB/fixture-runner.sh"; source "$_SHARED_LIB/preflight.sh"; source "$_SHARED_LIB/drift-test.sh"

# Functions REMOVED from run-tests.sh (now sourced from lib):
#   _runtests_do_cleanup, extract_to, run_check, assert_check
#   plus their preamble: color block L30-34, counter init L39-42
# Functions REBUILT as a WORKFLOW wrapper in run-tests.sh (A1 — NOT sourced from shared):
#   setup_fixture(tmpdir, ref, version) -> calls shared extract_to for the 3 workflow template paths,
#     synthesizes the agentic-apps-workflow SKILL.md, and keeps the 1.3.0 ADR special-case
# Functions REPLACED by thin policy wrappers:
#   test_preflight_verify_paths  -> calls run_preflight_verify_paths "$REPO_ROOT/migrations"
#   test_skill_md_version_matches_latest_migration_to_version -> calls run_drift_test, owns PASS/FAIL
# Functions KEPT verbatim (WORKFLOW):
#   test_migration_0001..0021, test_meta_destinations_consistency, _roles_from_adapter,
#   _roles_from_meta, test_sigterm_mid_apply_preserves_state, dispatcher block, summary block
# KEPT preamble: REPO_ROOT (L36-37), STRICT_PREFLIGHT init (L46), flag/FILTER parsing (L44-69), trap setup (moved AFTER sourcing)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Feature branch + add submodule + verify gitlink SHA == 28-02 recorded SHA (A4) + install.sh advance-on-every-run (A3)</name>
  <read_first>
    - install.sh (full — find the SCAFFOLDER/repo-root setup; the submodule block goes after it, before the LINKS array. Note install.sh runs `set -euo pipefail` at L17 and SCAFFOLDER is set at L20)
    - .planning/phases/28-split-01-agenticapps-shared/28-RESEARCH.md CRQ 8 (install.sh block snippet, no CI exists, vendor/.gitmodules absent), Assumption A3 (verify remote URL first)
    - .planning/phases/28-split-01-agenticapps-shared/28-02-SUMMARY.md (the CANONICAL RELEASE SHA — the gitlink must equal this; A4)
    - 28-REVIEWS.md A3 (install.sh advance-on-existing-clones) and A4 (gitlink SHA pin contract)
  </read_first>
  <files>.gitmodules, install.sh</files>
  <action>
    A) Create the feature branch (project CLAUDE.md — never commit to main):
       `git checkout -b split-01-agenticapps-shared`
    B) Verify the remote URL before adding (Assumption A3):
       `gh repo view agenticapps-eu/agenticapps-shared --json url,visibility`
    C) Add the submodule (D-28a) over canonical HTTPS and pin to the recorded release SHA (A4):
       `git submodule add https://github.com/agenticapps-eu/agenticapps-shared vendor/agenticapps-shared`
       `git submodule update --init --recursive`
       Read the canonical release SHA from 28-02-SUMMARY.md (call it `$REL_SHA`). Check the submodule
       out at that exact commit so the gitlink pins to the SHA (A4 — the SHA is canonical, the tag is
       provenance):
       `git -C vendor/agenticapps-shared fetch --tags origin`
       `git -C vendor/agenticapps-shared checkout "$REL_SHA"`
       (Confirm `$REL_SHA` is the same commit v1.0.0 points at: `git -C vendor/agenticapps-shared rev-parse v1.0.0` == `$REL_SHA`. If they differ, STOP — 28-02's tag and recorded SHA disagree.)
       Then stage the submodule pin from the superproject: `git add vendor/agenticapps-shared .gitmodules`
       Confirm `.gitmodules` URL is exactly `https://github.com/agenticapps-eu/agenticapps-shared`
       (canonical org repo, HTTPS — security gate).
    D) A4 — verify the SUPERPROJECT GITLINK SHA equals the recorded release SHA. The gitlink (what the
       superproject persists) is read with `git ls-tree`:
       `GITLINK_SHA=$(git ls-tree HEAD vendor/agenticapps-shared 2>/dev/null | awk '{print $3}')`
       (before commit the gitlink lives in the index — use `git ls-files -s vendor/agenticapps-shared
       | awk '{print $2}'` pre-commit; after Task 3 commits, `git ls-tree HEAD ...` is authoritative).
       Assert it equals `$REL_SHA`. Do NOT use `git describe --tags --exact-match HEAD` for the pin
       check (environment-local; fails on a fresh clone where submodule tags weren't fetched — A4).
    E) Update install.sh — A3: the submodule must advance on EXISTING clones too, not just first init.
       Place this block after the SCAFFOLDER setup (L20) and before the LINKS array (L25). Note
       install.sh uses `$SCAFFOLDER` as the repo-root var:
       ```
       # Advance the agenticapps-shared submodule (idempotent: safe on fresh AND existing clones).
       # A3: do NOT guard on VERSION-missing — after a `git pull` an existing clone must move to the
       # new gitlink SHA. sync picks up any .gitmodules URL change; update --init advances/initialises.
       if [ -f "$SCAFFOLDER/.gitmodules" ]; then
         echo "Syncing git submodule(s) vendor/agenticapps-shared..."
         git -C "$SCAFFOLDER" submodule sync --recursive
         git -C "$SCAFFOLDER" submodule update --init --recursive
       fi
       ```
       (Match `$SCAFFOLDER` to install.sh's actual repo-root variable if it differs.)
    F) Do NOT commit yet (Task 3 commits everything once the refactor + baseline + GSD-diff pass).
       Leave staged.
  </action>
  <verify>
    <automated>cd /Users/donald/Sourcecode/agenticapps/claude-workflow && git rev-parse --abbrev-ref HEAD | grep -qx split-01-agenticapps-shared && grep -q 'vendor/agenticapps-shared' .gitmodules && grep -q 'https://github.com/agenticapps-eu/agenticapps-shared' .gitmodules && REL_SHA=$(grep -oE '[0-9a-f]{40}' .planning/phases/28-split-01-agenticapps-shared/28-02-SUMMARY.md | head -1) && GITLINK=$(git ls-files -s vendor/agenticapps-shared | awk '{print $2}') && [ -n "$REL_SHA" ] && [ "$GITLINK" = "$REL_SHA" ] && [ -f vendor/agenticapps-shared/migrations/lib/helpers.sh ] && grep -q 'submodule sync --recursive' install.sh && grep -q 'submodule update --init' install.sh && echo "SUBMODULE_PINNED_OK gitlink=$GITLINK"</automated>
  </verify>
  <acceptance_criteria>
    - `git rev-parse --abbrev-ref HEAD` is `split-01-agenticapps-shared` (on feature branch)
    - `grep -q 'vendor/agenticapps-shared' .gitmodules`
    - `.gitmodules` URL is exactly `https://github.com/agenticapps-eu/agenticapps-shared` (HTTPS, canonical org — kept per constraint)
    - A4 — the superproject gitlink SHA equals 28-02's recorded release SHA: `[ "$(git ls-files -s vendor/agenticapps-shared | awk '{print $2}')" = "$(grep -oE '[0-9a-f]{40}' .planning/phases/28-split-01-agenticapps-shared/28-02-SUMMARY.md | head -1)" ]` (NOT `git describe --tags`)
    - `[ -f vendor/agenticapps-shared/migrations/lib/helpers.sh ]` (submodule content present)
    - A3 — install.sh runs sync+update whenever .gitmodules exists (no VERSION-missing guard): `grep -q 'submodule sync --recursive' install.sh` AND `grep -q 'submodule update --init' install.sh` AND `! grep -q 'vendor/agenticapps-shared/VERSION' install.sh` (the old missing-VERSION guard is gone)
    - A3 stale-gitlink advance proof: in a throwaway worktree/copy, rewind the submodule to an older commit (`git -C vendor/agenticapps-shared checkout HEAD~1` if available, else simulate a stale gitlink), then `bash install.sh` and assert `git -C vendor/agenticapps-shared rev-parse HEAD` returns to the pinned `$REL_SHA` (update advanced it). Document the exact commands run in the SUMMARY.
  </acceptance_criteria>
  <done>On feature branch; submodule added at vendor/agenticapps-shared over canonical HTTPS, pinned so the superproject gitlink SHA equals 28-02's recorded release SHA (A4, verified via git ls-files/ls-tree NOT git describe); install.sh advances the submodule on every run via sync+update when .gitmodules exists, and a stale gitlink is demonstrably advanced by rerunning install.sh (A3). Nothing committed yet.</done>
</task>

<task type="auto">
  <name>Task 2: Capture GSD baseline (A6), refactor run-tests.sh — source shared lib + setup_fixture WORKFLOW wrapper (A1) + policy wrappers + 186/4 invariant</name>
  <read_first>
    - migrations/run-tests.sh (full — you are removing SHARED bodies, rebuilding setup_fixture as a wrapper, and rewiring; this is the load-bearing edit)
    - migrations/run-tests.sh lines 106-134 (the current setup_fixture body — you rebuild it as a wrapper that still calls extract_to but now sources extract_to from shared) and line 109 (the WORKFLOW annotation 28-01 already changed)
    - .planning/phases/28-split-01-agenticapps-shared/28-RESEARCH.md "Code Examples" (consumer sourcing pattern verbatim), Pattern 2 (drift policy wrapper), Pattern 3 (preflight wrapper), CRQ 3 (trap-after-source, counter ownership), CRQ 9 Risks 2/4/5
    - 28-REVIEWS.md A1 (setup_fixture is a workflow wrapper) and A6 (GSD byte-diff baseline)
    - docs/decisions/0035-shared-extraction-boundaries.md (MECHANISM vs POLICY — what stays; the A1-amended SHARED/WORKFLOW sets)
  </read_first>
  <files>migrations/run-tests.sh</files>
  <action>
    Project CLAUDE.md: run `gitnexus_impact` on each SHARED symbol you remove
    (`_runtests_do_cleanup`, `extract_to`, `run_check`, `assert_check`,
    `test_preflight_verify_paths`) BEFORE editing; report blast radius. Note bash has limited
    GitNexus coverage — cross-check callers against 28-RESEARCH CRQ 2 (all callers are WORKFLOW
    test_migration_* functions, which stay). Warn the user only if HIGH/CRITICAL is reported.

    A6 — CAPTURE GSD BASELINE BEFORE EDITING (codex MEDIUM). Capture the output of the GSD status
    commands the user actually relies on, to a baseline file, while run-tests.sh is still unmodified.
    These run against the GSD framework at `~/.claude/get-shit-done/bin/gsd-tools.cjs` (NOT this repo),
    so they should be byte-identical after a pure run-tests.sh refactor. Capture the gsd-tools.cjs
    equivalents of `/gsd-progress`, `/gsd-stats`, `/gsd-help` (discover the exact subcommands from the
    GSD CLI, e.g. `node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" progress`, `... stats`,
    `... --help`/`help`), redirecting stdout+stderr, into `/tmp/28-03-gsd-before.txt`. Run each from
    the repo root on the current branch BEFORE step 1. If a command is non-deterministic (timestamps,
    elapsed time, in-progress counters), narrow it: capture only the stable lines (e.g. `grep -v` the
    volatile fields) OR drop that command and document in the SUMMARY which specific invariant SC-6
    is protecting and why exact-diff is impractical for it. Record the exact commands used.

    Edit migrations/run-tests.sh to become a CONSUMER (D-28e source-and-keep):
    1. KEEP the shebang + usage header + `set -uo pipefail` (lines 1-27).
    2. After `set -uo pipefail`, INSERT the shared-lib sourcing block (28-RESEARCH Code Examples,
       verbatim): resolve `_SCRIPT_DIR` via `${BASH_SOURCE[0]}`, set
       `_SHARED_LIB="$_SCRIPT_DIR/../vendor/agenticapps-shared/migrations/lib"`, guard with the
       `[ ! -d "$_SHARED_LIB" ]` error+exit-1 block (Risk 6), then source helpers.sh, fixture-runner.sh
       (provides extract_to), preflight.sh, drift-test.sh (helpers FIRST — Risk 1/ordering).
    3. REMOVE the now-duplicated SHARED preamble + bodies (sourced from lib now):
       - color block lines 30-34 (now in helpers.sh)
       - counter init lines 39-42 (now in helpers.sh) — KEEP REPO_ROOT (36-37), STRICT_PREFLIGHT init
         (46), and flag/FILTER parsing (44-69), which stay WORKFLOW-side.
       - `_runtests_cleanup_fired` + `_runtests_do_cleanup()` lines 76-82 (now in helpers.sh)
       - the SHARED bodies: `extract_to` 96-104 (now in fixture-runner.sh), `run_check` 139-143,
         `assert_check` 151-170 (now in helpers.sh).
    4. A1 — REBUILD `setup_fixture` IN PLACE as a WORKFLOW wrapper (do NOT remove it; it is no longer
       carved to shared). It keeps its current body EXCEPT extract_to is now the sourced shared
       primitive. Concretely, setup_fixture stays as the function from lines 110-134, unchanged in
       behavior — it calls `extract_to` (now resolved from fixture-runner.sh), lays down the three
       template paths (`templates/workflow-config.md`, `config-hooks.json`, `claude-md-sections.md`),
       synthesizes the `agentic-apps-workflow` SKILL.md, and keeps the 1.3.0 ADR special-case
       (lines 131-133). Update its header comment to read WORKFLOW (28-01 already changed the
       annotation; confirm it persisted). The point of A1: the workflow-specific composition lives
       here; only the generic git-show primitive (`extract_to`) is shared.
    5. KEEP the trap setup (lines 83-85) but place it AFTER the sourcing block (Risk 2 — traps set in
       consumer after helpers.sh defines _runtests_do_cleanup).
    6. REPLACE `test_preflight_verify_paths()` body (1767-1851) with a thin POLICY wrapper (Pattern 3):
       `test_preflight_verify_paths() { run_preflight_verify_paths "$REPO_ROOT/migrations"; }`
    7. REPLACE the drift runner `test_skill_md_version_matches_latest_migration_to_version()`
       (2247-2269) with the POLICY wrapper (Pattern 2 / D-28d) — it OWNS PASS/FAIL:
       ```
       test_skill_md_version_matches_latest_migration_to_version() {
         if run_drift_test "$REPO_ROOT/skill/SKILL.md" "$REPO_ROOT/migrations"; then
           echo "  ${GREEN}PASS${RESET}: test-skill-md-version-matches-latest-migration-to-version"
           PASS=$((PASS+1))
         else
           echo "  ${RED}FAIL${RESET}: SKILL.md version does not match latest migration to_version"
           FAIL=$((FAIL+1))
         fi
       }
       ```
    8. KEEP verbatim ALL WORKFLOW functions: test_migration_0001..0021, test_meta_destinations_consistency,
       _roles_from_adapter, _roles_from_meta, test_sigterm_mid_apply_preserves_state.
    9. KEEP the dispatcher block (2462-2560) and Summary block (2562-2579) verbatim.
    10. Do NOT introduce `set -e` (Risk 5). No counter increment at file level (Risk 4).
    Then run the suite and confirm the EXACT baseline. If PASS != 186 or FAIL != 4, STOP and diff
    against the baseline — do not "fix" the 0017 failures (they are FIX-0017 scope) and do not let
    the carve regress them.

    A6 — AFTER the refactor + green baseline, re-capture the same GSD commands to
    `/tmp/28-03-gsd-after.txt` and `diff /tmp/28-03-gsd-before.txt /tmp/28-03-gsd-after.txt`; assert
    the diff is EMPTY (identical). If a command was narrowed in the capture step, diff the narrowed
    form. Record before/after + diff result in the SUMMARY.
  </action>
  <verify>
    <automated>cd /Users/donald/Sourcecode/agenticapps/claude-workflow && bash migrations/run-tests.sh > /tmp/28-03-suite.out 2>&1; grep -qE 'PASS.*: 186' /tmp/28-03-suite.out && grep -qE 'FAIL.*: 4' /tmp/28-03-suite.out && grep -q 'PASS.*test-skill-md-version-matches-latest-migration-to-version' /tmp/28-03-suite.out && diff /tmp/28-03-gsd-before.txt /tmp/28-03-gsd-after.txt && echo BASELINE_186_4_AND_GSD_IDENTICAL || (echo REGRESSION; tail -30 /tmp/28-03-suite.out; diff /tmp/28-03-gsd-before.txt /tmp/28-03-gsd-after.txt; false)</automated>
  </verify>
  <acceptance_criteria>
    - `bash migrations/run-tests.sh` prints `PASS: 186` and `FAIL: 4` (exact baseline; verify smoke prints BASELINE_186_4_AND_GSD_IDENTICAL)
    - `grep -q 'vendor/agenticapps-shared/migrations/lib' migrations/run-tests.sh` (sources the submodule lib)
    - A1 — setup_fixture is KEPT in run-tests.sh as a WORKFLOW wrapper: `grep -q 'setup_fixture()' migrations/run-tests.sh` AND `grep -q 'templates/workflow-config.md' migrations/run-tests.sh` (workflow template paths live here, not in shared) AND `grep -q '1.3.0' migrations/run-tests.sh` (the special-case stays here)
    - A1 — setup_fixture calls the shared primitive: `grep -q 'extract_to' migrations/run-tests.sh` (the wrapper invokes sourced extract_to) AND `! grep -qE '^extract_to\(\)' migrations/run-tests.sh` (extract_to BODY removed — now sourced from shared)
    - `grep -q 'run_drift_test' migrations/run-tests.sh` (drift policy wrapper calls shared mechanism)
    - `grep -q 'run_preflight_verify_paths' migrations/run-tests.sh` (preflight wrapper calls shared mechanism)
    - `! grep -qE '^assert_check\(\)|^run_check\(\)' migrations/run-tests.sh` (assert_check/run_check bodies removed, not redefined)
    - `grep -q 'test_migration_0001' migrations/run-tests.sh` and `grep -q 'test_migration_0021' migrations/run-tests.sh` (WORKFLOW bodies kept)
    - `! grep -qE '^\s*set -e\b' migrations/run-tests.sh` (no set -e introduced)
    - A6 — GSD outputs identical: `diff /tmp/28-03-gsd-before.txt /tmp/28-03-gsd-after.txt` is empty (or the narrowed-form diff is empty, with the narrowing documented in SUMMARY)
    - Drift test line shows PASS (the SKILL.md-vs-migration policy still passes)
  </acceptance_criteria>
  <done>run-tests.sh sources the four lib files from the submodule, removed the extract_to/run_check/assert_check/cleanup SHARED bodies, REBUILT setup_fixture in place as a WORKFLOW wrapper that calls the shared extract_to and keeps the workflow template paths + 1.3.0 special-case (A1), kept all WORKFLOW per-migration bodies + dispatcher + summary, and replaced drift + preflight with thin policy wrappers. The suite prints PASS=186 FAIL=4 exactly with the drift test PASSing, and the captured GSD command outputs are byte-identical before/after (A6).</done>
</task>

<task type="auto">
  <name>Task 3: Commit ADR amendment + CHANGELOG + submodule + refactor, detect-changes, push, open PR with concrete body (A7)</name>
  <read_first>
    - CHANGELOG.md (top — add the new entry above the most recent section; use an `## [Unreleased] — SPLIT-01` section, no version-numbered header — this is unreleased extraction work)
    - docs/decisions/0035-shared-extraction-boundaries.md (the A1 amendment 28-01 staged on this repo — confirm it is present in the working tree and stage it)
    - .planning/phases/28-split-01-agenticapps-shared/28-RESEARCH.md CRQ 8 (no CI; run-tests.sh path unchanged)
    - SPLIT-01-agenticapps-shared.md Phase F (PR title/body conventions); SPLIT-00-PREREQUISITES.md (link target)
    - .planning/phases/28-split-01-agenticapps-shared/28-02-SUMMARY.md (the release SHA + v1.0.0 — link both in the PR body, A7)
    - 28-REVIEWS.md A7 (concrete PR body)
  </read_first>
  <files>CHANGELOG.md, docs/decisions/0035-shared-extraction-boundaries.md</files>
  <action>
    A) CHANGELOG.md: add an `## [Unreleased] — SPLIT-01: extract shared migration infrastructure`
       section at the top of the changelog body. Record: agenticapps-shared added as a git submodule
       at `vendor/agenticapps-shared` pinned by gitlink SHA (cite the SHA from 28-02-SUMMARY; note
       v1.0.0 is provenance) (A4); `migrations/run-tests.sh` now sources the shared lib
       (helpers/fixture-runner/preflight/drift-test); `setup_fixture` rebuilt as a WORKFLOW wrapper
       over shared `extract_to` (A1); all per-migration WORKFLOW bodies + the version-coupling drift
       POLICY retained; suite baseline preserved at PASS=186 FAIL=4 (the 4 pre-existing
       test_migration_0017 failures are FIX-0017 scope, unchanged); install.sh now advances the
       submodule on every run (A3); ADR-0035 amended (setup_fixture demoted to WORKFLOW). Note:
       claude-workflow VERSION/SKILL.md intentionally NOT bumped (decided at SPLIT-02 ship time).
    B) Confirm the ADR-0035 amendment + the run-tests.sh annotation edit from 28-01 are present in the
       working tree (28-01 left them staged-but-uncommitted on this repo). They are committed here.
       (If for any reason the ADR amendment is absent — e.g. 28-01 ran against a different checkout —
       apply it now per 28-01 Task 4: move setup_fixture to the WORKFLOW set, keep extract_to SHARED,
       record the A1 amendment.)
    C) Run `gitnexus_detect_changes()` (project CLAUDE.md) before committing; confirm only the
       expected files/flows are affected; record output in SUMMARY (note bash/markdown limited coverage).
    D) Commit on the feature branch:
       `git add .gitmodules vendor/agenticapps-shared migrations/run-tests.sh install.sh CHANGELOG.md docs/decisions/0035-shared-extraction-boundaries.md`
       `git commit -m "chore(split-01): consume agenticapps-shared as submodule; run-tests.sh sources shared lib" -m "Adds vendor/agenticapps-shared submodule pinned by gitlink SHA (28-02 release SHA; v1.0.0 is provenance). migrations/run-tests.sh refactored to source helpers/fixture-runner/preflight/drift-test (D-28e source-and-keep); setup_fixture rebuilt as a WORKFLOW wrapper over shared extract_to (A1); keeps all WORKFLOW per-migration bodies + drift POLICY wrapper. Baseline preserved: PASS=186 FAIL=4. GSD command outputs byte-identical before/after (A6). install.sh advances the submodule on every run (A3). ADR-0035 amended: setup_fixture -> WORKFLOW. Source baseline 5aff1b1 (v1.21.0)." -m "Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"`
    E) Push and open the PR (SC-8) with a CONCRETE body (A7). Write the body to a temp file and pass
       it via `--body-file` (avoids quoting issues). The body MUST be the following template, with the
       `<RELEASE_SHA>` placeholder replaced by the actual SHA read from 28-02-SUMMARY.md:
       ```
       ## SPLIT-01: extract shared migration infrastructure → agenticapps-shared

       Consumer side of the carve. Vendors `agenticapps-eu/agenticapps-shared` as a git submodule and
       refactors `migrations/run-tests.sh` to source the shared lib while keeping all workflow-specific
       behavior local.

       ### What changed
       - **Submodule:** `vendor/agenticapps-shared` pinned by gitlink SHA `<RELEASE_SHA>` (tag `v1.0.0`
         is provenance only — the gitlink SHA is the pin contract, A4).
       - **run-tests.sh:** sources `helpers.sh / fixture-runner.sh / preflight.sh / drift-test.sh` from
         the submodule (D-28e source-and-keep). `setup_fixture` rebuilt as a **workflow wrapper** over
         shared `extract_to` (A1 — workflow template paths + the 1.3.0 special-case are not generic).
         Drift + preflight became thin policy wrappers calling the shared mechanism (D-28d).
       - **Baseline preserved EXACTLY:** `PASS=186 FAIL=4`. The 4 failures are pre-existing
         `test_migration_0017` / FIX-0017 scope — unchanged, not regressed, not "fixed" here.
       - **install.sh:** advances the submodule on every run (sync + update --init), idempotent on
         existing clones (A3).
       - **GSD non-regression:** `/gsd-progress`, `/gsd-stats`, `/gsd-help` outputs byte-identical
         before/after (A6).

       ### Links
       - SPLIT-00 prerequisites: `SPLIT-00-PREREQUISITES.md`
       - SPLIT-01 plan: `SPLIT-01-agenticapps-shared.md`
       - Boundary decision (amended): `docs/decisions/0035-shared-extraction-boundaries.md` (setup_fixture → WORKFLOW)
       - New repo: https://github.com/agenticapps-eu/agenticapps-shared (release `v1.0.0`, commit `<RELEASE_SHA>`)

       ### Out of scope
       - claude-workflow version bump (SPLIT-02 ship time).
       - add-observability / obs-specific tests (SPLIT-02).

       🤖 Generated with [Claude Code](https://claude.com/claude-code)
       ```
       Then:
       `git push -u origin split-01-agenticapps-shared`
       `gh pr create --base main --head split-01-agenticapps-shared --title "chore: extract shared migration infrastructure to agenticapps-shared (SPLIT-01)" --body-file /tmp/28-03-pr-body.md`
       If push/PR hits an auth gate, create a checkpoint requesting `gh auth` and retry.
  </action>
  <verify>
    <automated>cd /Users/donald/Sourcecode/agenticapps/claude-workflow && grep -q 'SPLIT-01' CHANGELOG.md && grep -q 'PASS=186' CHANGELOG.md && awk '/WORKFLOW set/,/Rejected alternatives/' docs/decisions/0035-shared-extraction-boundaries.md | grep -q 'setup_fixture' && git log -1 --pretty=%s | grep -qi 'split-01' && gh pr list --head split-01-agenticapps-shared --json number,title,body | grep -qi 'agenticapps-shared' && echo PR_OPEN_OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'SPLIT-01' CHANGELOG.md` and `grep -q 'PASS=186' CHANGELOG.md`
    - ADR-0035 amendment committed: `awk '/WORKFLOW set/,/Rejected alternatives/' docs/decisions/0035-shared-extraction-boundaries.md | grep -q 'setup_fixture'` (setup_fixture in WORKFLOW set)
    - `git log -1 --pretty=%s` contains `split-01` (commit landed on feature branch)
    - `git ls-remote --heads origin split-01-agenticapps-shared` is non-empty (branch pushed)
    - `gh pr list --head split-01-agenticapps-shared` shows an open PR to main (SC-8)
    - A7 — the PR body is concrete (not a placeholder): `gh pr view --json body -q .body` contains the real release SHA, links to SPLIT-00, SPLIT-01, ADR-0035, and the agenticapps-shared repo URL, and the `🤖 Generated with` line
    - SUMMARY records the GSD before/after diff result (A6) and the gitnexus_detect_changes scope
  </acceptance_criteria>
  <done>GSD outputs proven byte-identical (A6); CHANGELOG records the extraction + 186/4 invariant + A1 wrapper + A3 install.sh; the ADR-0035 amendment is committed; commit landed on the feature branch with submodule + run-tests.sh + install.sh + CHANGELOG + ADR; branch pushed; PR opened to main with a concrete body (A7) linking the split docs, ADR-0035, and the shared repo release SHA.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify fresh-clone submodule + gitlink SHA + 186/4 baseline, then /gsd-review + merge PR</name>
  <action>This is a human-verification checkpoint (no autonomous action). Claude has opened the PR; the human verifies fresh-clone submodule consumption, confirms the superproject gitlink SHA equals the recorded release SHA (A4), re-confirms the PASS=186 FAIL=4 baseline, runs /gsd-review (codex cross-AI, D-28f) and /review on the diff, then merges.</action>
  <what-built>
    claude-workflow now consumes agenticapps-shared as a submodule whose superproject gitlink SHA
    equals 28-02's recorded release SHA (A4); run-tests.sh sources the shared lib, rebuilds
    setup_fixture as a workflow wrapper (A1), and preserves PASS=186 FAIL=4; install.sh advances the
    submodule on every run (A3); GSD outputs byte-identical (A6); CHANGELOG + ADR amendment committed;
    PR opened to main with a concrete body (A7). Per project CLAUDE.md the human owns merge + the
    cross-AI /gsd-review (D-28f) and /review on the diff.
  </what-built>
  <how-to-verify>
    1. Fresh-clone submodule check (SC-2): in a scratch dir run
       `git clone --recurse-submodules <claude-workflow-remote> /tmp/cw-fresh && [ -f /tmp/cw-fresh/vendor/agenticapps-shared/migrations/lib/helpers.sh ] && echo FRESH_CLONE_OK`
       (or `git clone <remote> /tmp/cw-fresh2 && cd /tmp/cw-fresh2 && bash install.sh` and confirm
       the submodule sync/update message + lib presence — A3).
    2. A4 — confirm the superproject gitlink SHA matches the recorded release SHA:
       `git -C /tmp/cw-fresh ls-tree HEAD vendor/agenticapps-shared` → the 3rd field equals the SHA in
       28-02-SUMMARY.md. (Do NOT rely on `git describe --tags`.)
    3. Re-run `bash migrations/run-tests.sh` in the fresh clone and confirm `PASS: 186` / `FAIL: 4`.
    4. Review the PR diff (`gh pr diff`); confirm: extract_to/run_check/assert_check bodies removed,
       setup_fixture kept as a workflow wrapper, all WORKFLOW bodies intact, drift test still PASS,
       ADR-0035 amended (setup_fixture → WORKFLOW), no claude-workflow version bump.
    5. Run /gsd-review (codex cross-AI, D-28f) and /review on the phase diff per project CLAUDE.md.
    6. Merge the PR when reviews approve.
  </how-to-verify>
  <resume-signal>Type "approved" once the PR is reviewed/merged, or describe issues to address.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| claude-workflow superproject → submodule pin | A pin to an external repo; integrity hinges on the gitlink SHA being the recorded immutable commit over canonical HTTPS |
| submodule lib `source`d in test harness | Shared bash executes in the local test process; inputs are repo-local fixture paths only |
| fresh-clone consumer | A developer cloning must fetch the submodule; missing-submodule must fail loud, not silent |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-09 | Tampering | submodule pinned to a moving branch (mutable upstream code executes in harness) | mitigate | Pin the gitlink to the recorded immutable release SHA; Task 1 verifies `git ls-files/ls-tree` gitlink SHA == 28-02 recorded SHA (A4 — not git describe) |
| T-28-10 | Spoofing | `.gitmodules` points at a non-canonical / typo'd / non-HTTPS URL | mitigate | Task 1 asserts URL == `https://github.com/agenticapps-eu/agenticapps-shared`; `gh repo view` confirms canonical org before add |
| T-28-11 | Tampering | sourced lib executes untrusted input | accept | Lib only `source`s vendored, SHA-pinned bash + runs repo-authored fixtures; no network/user input path; pre-existing harness pattern |
| T-28-12 | DoS | missing submodule → cryptic source failure blocks all tests | mitigate | Guard block in run-tests.sh prints actionable `git submodule update --init` error + exit 1 (Risk 6); install.sh auto-syncs+updates on every run (A3) |
| T-28-13 | Elevation | org repo access scope expands via new submodule | accept | Repo is org-private; submodule read uses existing member auth; no new credential surface; INFO at ASVS L1 |
| T-28-17 | Tampering | install.sh fails to advance a stale gitlink after `git pull` → stale shared code runs (A3) | mitigate | install.sh runs sync + update --init on every run when .gitmodules exists; Task 1 proves a stale gitlink is advanced back to the pinned SHA |
| T-28-18 | Tampering | a non-run-tests.sh change silently alters GSD command behavior | mitigate | A6 captures GSD outputs before/after and asserts an empty diff |

No HIGH-severity threats. Surface is supply-chain integrity of a SHA-pinned submodule; mitigations are gitlink-SHA pinning + canonical-URL assertion + loud-fail guard + advance-on-every-run install.
</threat_model>

<verification>
1. Submodule present; superproject gitlink SHA == 28-02 recorded release SHA over canonical HTTPS (Task 1 grep/ls-tree gates; A4).
2. `bash migrations/run-tests.sh` → PASS=186 FAIL=4 exactly, drift test PASS (SC-3, SC-4).
3. SHARED bodies (extract_to/run_check/assert_check/cleanup) removed; setup_fixture kept as a workflow wrapper (A1); WORKFLOW bodies + dispatcher + summary intact (grep gates).
4. install.sh advances the submodule on every run; stale gitlink demonstrably advanced (A3); fresh-clone `--recurse-submodules` verified at checkpoint (SC-2).
5. GSD command outputs byte-identical before/after (A6, SC-6); `gitnexus_detect_changes()` scope reviewed.
6. ADR-0035 amendment committed (setup_fixture → WORKFLOW); CHANGELOG records extraction (SC-7); PR opened to main with a concrete body (SC-8, A7).
</verification>

<success_criteria>
- claude-workflow consumes agenticapps-shared as a submodule whose gitlink SHA == 28-02's recorded release SHA (SC-2, D-28a, A4).
- run-tests.sh sources the shared lib; setup_fixture is a WORKFLOW wrapper over shared extract_to (A1); WORKFLOW bodies + drift POLICY kept (D-28d, D-28e).
- Suite baseline preserved EXACTLY: PASS=186 FAIL=4 (SC-3).
- Drift test still PASSES (SC-4).
- install.sh advances the submodule on every run, idempotent on existing clones (SC-2, A3).
- GSD command outputs byte-identical before/after (SC-6, A6); CHANGELOG + ADR amendment recorded (SC-7); PR opened with a concrete body (SC-8, A7).
</success_criteria>

<output>
After completion, create `.planning/phases/28-split-01-agenticapps-shared/28-03-SUMMARY.md`.
Record: the superproject gitlink SHA and confirmation it equals 28-02's recorded release SHA (A4),
the exact suite tail (PASS/FAIL), the GSD before/after diff result (A6), the install.sh stale-gitlink
advance proof (A3), and the PR number/URL. Note that merge + /gsd-review + /review are owned by the
human checkpoint (D-28f).
</output>
