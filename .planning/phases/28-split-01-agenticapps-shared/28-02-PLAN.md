---
phase: 28-split-01-agenticapps-shared
plan: 02
type: execute
wave: 1
depends_on: [28-01]
files_modified:
  - "~/Sourcecode/agenticapps/agenticapps-shared/tests/run-tests.sh"
  - "~/Sourcecode/agenticapps/agenticapps-shared/migrations/test-fixtures/_example/setup.sh"
  - "~/Sourcecode/agenticapps/agenticapps-shared/migrations/test-fixtures/_example/verify.sh"
  - "~/Sourcecode/agenticapps/agenticapps-shared/migrations/test-fixtures/_example/expected-exit"
  - "~/Sourcecode/agenticapps/agenticapps-shared/CHANGELOG.md"
  - "~/Sourcecode/agenticapps/agenticapps-shared/README.md"
  - "~/Sourcecode/agenticapps/agenticapps-shared/VERSION"
autonomous: true
requirements: [SC-1, SC-7, D-28b]
must_haves:
  truths:
    - "agenticapps-shared has its own GREEN standalone test suite that exercises the carved lib in isolation"
    - "tests/run-tests.sh sources all four lib files via the BASH_SOURCE[0] dirname pattern and exits 0"
    - "The suite proves extract_to does real git-ref extraction in a throwaway temp repo (A2)"
    - "The suite proves run_preflight_verify_paths works in BOTH strict (STRICT_PREFLIGHT=1) and non-strict (unset/0) modes (A2)"
    - "The suite includes a set -u safety assertion: preflight runs with STRICT_PREFLIGHT never set (A5)"
    - "assert_check counter + run_drift_test GREEN/RED coverage stays"
    - "_example fixture skeleton exists (setup.sh, verify.sh, expected-exit) for consumers to copy"
    - "CHANGELOG.md [1.0.0] section records the carve with a Migration provenance subsection citing claude-workflow 5aff1b1"
    - "A single commit lands the lib (from 28-01) + tests + fixture + docs; tag v1.0.0 is created and pushed; the exact release commit SHA is recorded as the canonical pin artifact (A4)"
  artifacts:
    - path: "~/Sourcecode/agenticapps/agenticapps-shared/tests/run-tests.sh"
      provides: "Standalone broad-surface suite for the shared lib (assert_check + drift + extract_to + preflight strict/non-strict + set -u)"
      contains: "run_drift_test"
    - path: "~/Sourcecode/agenticapps/agenticapps-shared/CHANGELOG.md"
      provides: "1.0.0 release notes + Migration provenance (D-28b) + the canonical release commit SHA (A4)"
      contains: "Migration provenance"
    - path: "~/Sourcecode/agenticapps/agenticapps-shared/migrations/test-fixtures/_example/expected-exit"
      provides: "Example fixture exit-code contract"
  key_links:
    - from: "tests/run-tests.sh"
      to: "../migrations/lib/*.sh"
      via: "source via BASH_SOURCE[0] dirname"
      pattern: "BASH_SOURCE"
    - from: "the carve commit SHA"
      to: "28-03 submodule gitlink"
      via: "the recorded commit SHA is the canonical pin artifact (A4); v1.0.0 tag is provenance only"
      pattern: "[0-9a-f]{7,40}"
---

<objective>
Give agenticapps-shared its own green test suite that proves the carved lib (from 28-01) is
loadable AND that its fragile shared surfaces actually work in isolation (no claude-workflow
context), create the `_example` fixture skeleton, finalize 1.0.0 metadata (CHANGELOG provenance per
D-28b, README consumption pattern, VERSION already 1.0.0), then land ONE provenance commit, cut the
immutable `v1.0.0` tag, and RECORD THE EXACT RELEASE COMMIT SHA as the canonical pin artifact that
28-03's submodule gitlink must equal.

Purpose: agenticapps-shared must be independently verifiable and must have an immutable pin BEFORE
claude-workflow consumes it (wave ordering). Output: green `tests/run-tests.sh` (broad surface),
`_example/` skeleton, 1.0.0 CHANGELOG/README, pushed tag `v1.0.0`, and the recorded release SHA.

REVIEW-DRIVEN CHANGES:
- A2 (codex HIGH): the tag MUST NOT be cut until the fragile shared surfaces are proven. The
  standalone suite is broadened to prove, in a temp git repo against real refs: extract_to (real
  ref extraction); plus synthetic run_preflight_verify_paths tests for BOTH strict and non-strict
  modes. The tag task DEPENDS on these passing.
- A4 (codex MEDIUM): the pin contract is the gitlink commit SHA, not the tag. This plan records the
  exact release commit SHA (in the SUMMARY + CHANGELOG) as the canonical pin artifact; v1.0.0 is
  provenance only.
- A1 (codex HIGH, knock-on): setup_fixture is NOT in shared (it stayed in claude-workflow as a
  wrapper). The standalone suite must NOT smoke-test setup_fixture; it tests extract_to directly.

IMPORTANT — operates on the agenticapps-shared working copy
(`~/Sourcecode/agenticapps/agenticapps-shared`, branch `main`). Depends on 28-01 having written
the four lib files. The single commit here includes BOTH the 28-01 lib files and this plan's
test/fixture/docs, with a provenance commit message citing claude-workflow `5aff1b1`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-split-01-agenticapps-shared/28-CONTEXT.md
@.planning/phases/28-split-01-agenticapps-shared/28-RESEARCH.md
@.planning/phases/28-split-01-agenticapps-shared/28-01-PLAN.md

# Sourcing pattern for the standalone test (28-RESEARCH CRQ 4):
#   _SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
#   _SHARED_LIB="$_SCRIPT_DIR/../migrations/lib"
# Standalone-test content recommendation: 28-RESEARCH OQ-3 (smoke tests for run_check,
#   assert_check counter, run_drift_test with synthetic data) — A2 broadens this to extract_to
#   real-ref + preflight strict/non-strict + set -u.
# CHANGELOG/README skeletons already exist in the agenticapps-shared skeleton (SPLIT-01 §Phase A.2).
</context>

<tasks>

<task type="auto">
  <name>Task 1: _example fixture skeleton + broad standalone tests/run-tests.sh (assert_check, drift, extract_to real-ref, preflight strict/non-strict, set -u) — GREEN (A2, A5)</name>
  <read_first>
    - 28-REVIEWS.md A2 (the broader shared-surface validation gate) and A5 (set -u safety)
    - .planning/phases/28-split-01-agenticapps-shared/28-RESEARCH.md CRQ 4 (standalone sourcing pattern), OQ-3 (smoke-test content), Pitfall 4 (agenticapps-shared runs its OWN dispatcher/tests, NOT test_migration_*)
    - .planning/phases/28-split-01-agenticapps-shared/28-01-PLAN.md <interfaces> block (lib signatures — NOTE: fixture-runner exports ONLY extract_to per A1; setup_fixture is NOT in shared)
    - ~/Sourcecode/agenticapps/agenticapps-shared/migrations/lib/helpers.sh, fixture-runner.sh, preflight.sh, drift-test.sh (the carved functions to exercise)
  </read_first>
  <files>~/Sourcecode/agenticapps/agenticapps-shared/tests/run-tests.sh, ~/Sourcecode/agenticapps/agenticapps-shared/migrations/test-fixtures/_example/setup.sh, ~/Sourcecode/agenticapps/agenticapps-shared/migrations/test-fixtures/_example/verify.sh, ~/Sourcecode/agenticapps/agenticapps-shared/migrations/test-fixtures/_example/expected-exit</files>
  <action>
    A) Create the `_example` fixture skeleton (replaces the `.gitkeep`):
       - `migrations/test-fixtures/_example/setup.sh`: minimal `#!/usr/bin/env bash` that creates a
         trivial marker file in `$1` (the fixture dir), e.g. `mkdir -p "$1" && echo applied > "$1/marker"`.
         Add a header comment: this is the copy-me skeleton for consumer repos (the
         `test-fixtures/NNNN/MM-name/{setup.sh, verify.sh, expected-exit}` convention).
       - `migrations/test-fixtures/_example/verify.sh`: `#!/usr/bin/env bash` that checks the marker:
         `[ -f "$1/marker" ]` — exit 0 if present (idempotency "applied"), non-zero otherwise.
       - `migrations/test-fixtures/_example/expected-exit`: file containing `0`.
       Make setup.sh and verify.sh executable (`chmod +x`).

    B) Create `tests/run-tests.sh` — agenticapps-shared's OWN standalone suite. It sources the lib
       and tests the lib functions themselves (NOT migration content — Pitfall 4):
       - `#!/usr/bin/env bash`, `set -uo pipefail`, provenance header.
       - Resolve lib via BASH_SOURCE pattern (28-RESEARCH CRQ 4 standalone variant):
         `_SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"`
         `_SHARED_LIB="$_SCRIPT_DIR/../migrations/lib"`
       - Source all four: helpers.sh, fixture-runner.sh, preflight.sh, drift-test.sh (helpers first).
       - Set the cleanup traps after sourcing (consumer responsibility per Risk 2):
         `trap '_runtests_do_cleanup' EXIT; trap '_runtests_do_cleanup; exit 130' INT; trap '_runtests_do_cleanup; exit 143' TERM`
       - Smoke/surface tests (OQ-3 + A2 + A5), each incrementing PASS/FAIL via assert_check or direct counter:
         1. `assert_check` via the _example fixture: build a temp fixture dir, run the _example
            setup.sh into it, then `assert_check "_example fixture applies" "[ -f marker ]" "$tmpdir" applied`.
         2. `assert_check` increments PASS: capture PASS before/after a known-pass assert; verify it grew by 1.
         3. `run_drift_test` GREEN case: write a synthetic SKILL.md (`version: 9.9.9`) + a synthetic
            migrations dir with `0001-x.md` (`to_version: 9.9.9`); assert `run_drift_test "$skill" "$migdir"`
            returns 0; PASS++ on success else FAIL++.
         4. `run_drift_test` RED case: same but mismatched versions; assert it returns non-zero; PASS++/FAIL++.
         5. A2 — `extract_to` REAL git-ref extraction: create a THROWAWAY git repo in a temp dir
            (`git init`, set a throwaway user.name/user.email locally), write a file (e.g.
            `hello.txt` with known content), `git add` + `git commit`, capture the commit ref
            (`HEAD` or its SHA). Then `extract_to "$ref" hello.txt "$out"` into a temp output path and
            assert the extracted content equals the committed content (PASS++); also assert
            `extract_to "$ref" does-not-exist.txt "$out2"` returns non-zero (PASS++ on the expected
            failure). Clean up the throwaway repo.
         6. A2 — `run_preflight_verify_paths` NON-STRICT mode: build a synthetic migrations dir with a
            migration `.md` carrying a `requires:` frontmatter `verify:` line that is guaranteed to
            FAIL (e.g. `verify: test -f /nonexistent/path/xyz`). With STRICT_PREFLIGHT unset (or =0),
            capture FAIL before/after `run_preflight_verify_paths "$migdir"`; assert FAIL did NOT grow
            (informational only in non-strict) and RAN_AUDIT==1 (PASS++). Skip-gate: if `python3 -c
            'import yaml'` is unavailable, count this as SKIP (the function self-skips) and still PASS
            the "did not crash / RAN_AUDIT set appropriately" assertion.
         7. A2 — `run_preflight_verify_paths` STRICT mode: same synthetic dir, set `STRICT_PREFLIGHT=1`,
            capture FAIL before/after; assert FAIL DID grow by the failing-verify count (strict rolls
            audit failures into global FAIL) — when pyyaml is present. If pyyaml is absent, assert the
            strict-skip path instead (it counts a FAIL for missing PyYAML in strict) and document the
            branch. Use `reset_counters` (or save/restore PASS/FAIL) between modes so the suite's own
            tally is not polluted by these probe assertions.
         8. A5 — set -u safety probe: in a subshell with `set -u` and STRICT_PREFLIGHT explicitly
            UNSET, source helpers+preflight and call `run_preflight_verify_paths` on a nonexistent dir;
            assert it does NOT crash with an unbound-variable error (capture exit + stderr; grep that
            stderr does NOT contain `STRICT_PREFLIGHT: unbound variable`); PASS++.
       - Append the standard Summary block (PASS/FAIL/SKIP echo + exit logic): exit 1 if FAIL>0,
         else exit 0. Reuse the shape from claude-workflow run-tests.sh lines 2566-2579.
       - Clean up all temp dirs (throwaway repo, synthetic migrations dirs, fixtures).
       - `chmod +x tests/run-tests.sh`.
    The suite MUST exit 0 (all surface tests GREEN). It MUST NOT reference setup_fixture (A1 — not in
    shared) and MUST NOT reference test_migration_* (Pitfall 4).
  </action>
  <verify>
    <automated>bash ~/Sourcecode/agenticapps/agenticapps-shared/tests/run-tests.sh; test $? -eq 0 && echo SHARED_SUITE_GREEN</automated>
  </verify>
  <acceptance_criteria>
    - `bash ~/Sourcecode/agenticapps/agenticapps-shared/tests/run-tests.sh` exits 0
    - The run prints at least one `PASS` and the suite exits 0 (no `FAIL:` >0)
    - `grep -q 'BASH_SOURCE' ~/Sourcecode/agenticapps/agenticapps-shared/tests/run-tests.sh`
    - `grep -q 'run_drift_test' ~/Sourcecode/agenticapps/agenticapps-shared/tests/run-tests.sh`
    - A2 extract_to coverage: `grep -q 'extract_to' ~/Sourcecode/agenticapps/agenticapps-shared/tests/run-tests.sh` AND `grep -qE 'git init|git -C .* init' ~/Sourcecode/agenticapps/agenticapps-shared/tests/run-tests.sh` (real throwaway repo)
    - A2 preflight coverage: `grep -q 'run_preflight_verify_paths' ~/Sourcecode/agenticapps/agenticapps-shared/tests/run-tests.sh` AND `grep -q 'STRICT_PREFLIGHT=1' ~/Sourcecode/agenticapps/agenticapps-shared/tests/run-tests.sh` (strict mode exercised)
    - A5 set -u probe present: `grep -qE 'set -u' ~/Sourcecode/agenticapps/agenticapps-shared/tests/run-tests.sh`
    - `! grep -q 'setup_fixture' ~/Sourcecode/agenticapps/agenticapps-shared/tests/run-tests.sh` (A1 — setup_fixture not in shared, suite must not call it)
    - `! grep -q 'test_migration_' ~/Sourcecode/agenticapps/agenticapps-shared/tests/run-tests.sh` (no claude-workflow per-migration calls — Pitfall 4)
    - `[ -f ~/Sourcecode/agenticapps/agenticapps-shared/migrations/test-fixtures/_example/expected-exit ]` and its content is `0`
    - `[ -x ~/Sourcecode/agenticapps/agenticapps-shared/tests/run-tests.sh ]`
  </acceptance_criteria>
  <done>agenticapps-shared's standalone suite sources the lib via BASH_SOURCE and proves the fragile shared surfaces: assert_check counter, run_drift_test GREEN+RED, extract_to real git-ref extraction in a throwaway repo, run_preflight_verify_paths in BOTH strict and non-strict modes, and a bare set -u safety probe (A2+A5). It exits 0, references neither setup_fixture (A1) nor test_migration_*. The _example fixture triad exists and is wired into the assert_check test.</done>
</task>

<task type="auto">
  <name>Task 2: Finalize 1.0.0 metadata, commit the carve, tag v1.0.0, record the canonical release SHA (A4) — GATED on Task 1 GREEN (A2)</name>
  <read_first>
    - ~/Sourcecode/agenticapps/agenticapps-shared/CHANGELOG.md (skeleton from Phase A — has a "Migration provenance" stub referencing filter-repo; that stub is WRONG for D-28b and must be rewritten)
    - ~/Sourcecode/agenticapps/agenticapps-shared/README.md (skeleton — verify the submodule consumption block is present)
    - .planning/phases/28-split-01-agenticapps-shared/28-CONTEXT.md D-28b (provenance-by-note wording)
    - 28-REVIEWS.md A2 (do not tag until the broad suite passes) and A4 (record the gitlink SHA as the pin artifact)
  </read_first>
  <files>~/Sourcecode/agenticapps/agenticapps-shared/CHANGELOG.md, ~/Sourcecode/agenticapps/agenticapps-shared/README.md, ~/Sourcecode/agenticapps/agenticapps-shared/VERSION</files>
  <action>
    A2 GATE (codex HIGH): BEFORE any commit/tag, re-run Task 1's suite and confirm it exits 0:
       `bash ~/Sourcecode/agenticapps/agenticapps-shared/tests/run-tests.sh || { echo "BLOCK: broad surface suite RED — do not tag"; exit 1; }`
    Do NOT proceed to the commit/tag steps unless the suite is GREEN.

    A) Rewrite the CHANGELOG `[1.0.0]` section. The Phase-A skeleton's "Migration provenance"
       paragraph claims "Full per-file history preserved via git filter-repo; use git log --follow" —
       this is FALSE under D-28b and MUST be corrected. Replace it with:
       - Set the `## [1.0.0] - YYYY-MM-DD` header to today's date.
       - `### Added`: "Shared migration infrastructure carved from claude-workflow:
         `migrations/lib/{helpers,fixture-runner,preflight,drift-test}.sh` — pass/fail helpers +
         cleanup trap, the generic `extract_to` git-ref primitive, parameterized preflight
         verify-path auditor, and the policy-agnostic drift-test runner mechanism. Standalone
         `tests/run-tests.sh` (proves extract_to + preflight strict/non-strict + drift + set -u) +
         `migrations/test-fixtures/_example/` skeleton."
       - `### Migration provenance (D-28b)`: state explicitly that these lib files were
         **refactored out of** claude-workflow's single `migrations/run-tests.sh` (one ~2579-line
         file with intermingled SHARED/WORKFLOW functions). Because `git filter-repo` is whole-file
         granularity, function-level carving does NOT preserve `git log --follow` lineage; provenance
         is recorded by note: source = claude-workflow commit `5aff1b1` (v1.21.0). Cite the source
         line ranges (helpers 29-42/76-85/139-170, fixture-runner extract_to 96-104, preflight
         1767-1851, drift-test 2237-2269). Note the A1 boundary: `setup_fixture` was NOT carved
         (it stays a claude-workflow wrapper over `extract_to`); `run_drift_test` /
         `run_preflight_verify_paths` were parameterized (paths passed as args, no REPO_ROOT) and
         preflight reads `${STRICT_PREFLIGHT:-0}` internally (set -u safe).
    B) Verify README has the submodule consumption block (from Phase A skeleton it should:
       `git submodule add https://github.com/agenticapps-eu/agenticapps-shared vendor/agenticapps-shared`).
       If missing, add it. Add a short "Running the shared test suite" line: `bash tests/run-tests.sh`.
    C) Confirm `VERSION` is `1.0.0` (Phase A set it; leave as-is — do not bump).
    D) Stage and commit EVERYTHING (the 28-01 lib files + this plan's tests/fixture/docs) in ONE commit:
       `cd ~/Sourcecode/agenticapps/agenticapps-shared`
       `git add -A`
       `git commit -m "feat: carve shared migration infrastructure from claude-workflow (SPLIT-01)" -m "Refactors SHARED harness functions out of claude-workflow migrations/run-tests.sh @ 5aff1b1 (v1.21.0) into migrations/lib/{helpers,fixture-runner,preflight,drift-test}.sh. Provenance-by-note per D-28b (filter-repo cannot carve intermingled functions). Adds standalone tests/run-tests.sh (extract_to real-ref + preflight strict/non-strict + drift + set -u) + _example fixture. A1: only extract_to is shared; setup_fixture stays a claude-workflow wrapper. drift/preflight parameterized off args; preflight set -u safe." -m "Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"`
    E) A4 — RECORD THE CANONICAL PIN ARTIFACT: capture the exact release commit SHA NOW:
       `RELEASE_SHA="$(git -C ~/Sourcecode/agenticapps/agenticapps-shared rev-parse HEAD)"`
       Append to the CHANGELOG `[1.0.0]` section a line:
       `### Release commit (canonical pin artifact — A4)` followed by the full 40-char SHA, and a
       note: "claude-workflow's submodule gitlink MUST equal this SHA; the v1.0.0 tag is provenance
       only." (Amend the commit to include this CHANGELOG line, OR write the SHA line before
       committing by committing once, reading HEAD, editing CHANGELOG, then `git commit --amend
       --no-edit` so the recorded SHA is the FINAL HEAD — prefer the amend approach so the recorded
       SHA equals the tagged commit. Re-read HEAD after the amend and confirm the CHANGELOG SHA
       matches the final HEAD.)
    F) Cut the immutable tag on the FINAL commit and push:
       `git tag -a v1.0.0 -m "agenticapps-shared 1.0.0 — initial shared infra carve (SPLIT-01)"`
       `git push origin main`
       `git push origin v1.0.0`
       If push fails on auth, create a checkpoint requesting `gh auth` (auth gate is dynamic).
    Verify the tag points at the carve commit (not the skeleton), the CHANGELOG-recorded SHA equals
    HEAD, and the working tree is clean. The 28-02 SUMMARY MUST record this SHA as the pin artifact.
  </action>
  <verify>
    <automated>cd ~/Sourcecode/agenticapps/agenticapps-shared && bash tests/run-tests.sh >/dev/null 2>&1 || { echo SUITE_RED; exit 1; }; HEAD_SHA=$(git rev-parse HEAD); git describe --tags --exact-match HEAD 2>/dev/null | grep -qx v1.0.0 && [ -z "$(git status --porcelain)" ] && grep -q 'Migration provenance' CHANGELOG.md && grep -q '5aff1b1' CHANGELOG.md && grep -q "$HEAD_SHA" CHANGELOG.md && echo "SHARED_V1_TAGGED SHA=$HEAD_SHA"</automated>
  </verify>
  <acceptance_criteria>
    - A2 gate: `bash ~/Sourcecode/agenticapps/agenticapps-shared/tests/run-tests.sh` exits 0 (suite GREEN before tag)
    - `cd ~/Sourcecode/agenticapps/agenticapps-shared && git describe --tags --exact-match HEAD` prints `v1.0.0`
    - `cd ~/Sourcecode/agenticapps/agenticapps-shared && git status --porcelain` is empty (clean tree)
    - `grep -q 'Migration provenance' ~/Sourcecode/agenticapps/agenticapps-shared/CHANGELOG.md`
    - `grep -q '5aff1b1' ~/Sourcecode/agenticapps/agenticapps-shared/CHANGELOG.md` (source SHA cited)
    - A4: the CHANGELOG records the release commit SHA AND it equals HEAD: `grep -q "$(git -C ~/Sourcecode/agenticapps/agenticapps-shared rev-parse HEAD)" ~/Sourcecode/agenticapps/agenticapps-shared/CHANGELOG.md`
    - `! grep -q 'git log --follow' ~/Sourcecode/agenticapps/agenticapps-shared/CHANGELOG.md` OR the phrase appears only in a "NOT preserved" negation (the false filter-repo claim is removed)
    - `cat ~/Sourcecode/agenticapps/agenticapps-shared/VERSION` is `1.0.0`
    - `git -C ~/Sourcecode/agenticapps/agenticapps-shared ls-remote --tags origin | grep -q 'refs/tags/v1.0.0$'` (tag pushed)
    - The four `migrations/lib/*.sh` files are tracked in the commit: `git -C ~/Sourcecode/agenticapps/agenticapps-shared ls-files migrations/lib/ | grep -c '\.sh$'` returns `4`
  </acceptance_criteria>
  <done>The broad surface suite is GREEN (A2 gate satisfied); agenticapps-shared has one provenance commit containing the carved lib + standalone suite + _example + corrected CHANGELOG (no false filter-repo claim, source SHA 5aff1b1 cited, A1 boundary noted); the exact release commit SHA is recorded in the CHANGELOG as the canonical pin artifact and equals HEAD (A4); tagged `v1.0.0` and pushed to origin main + tags; working tree clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local repo → GitHub origin (push) | Pushing tag + commit to org-private agenticapps-eu repo over HTTPS |
| immutable tag v1.0.0 + recorded gitlink SHA | The pin 28-03 consumes — the SHA is canonical (A4); the tag is provenance |
| broad standalone suite | Proves fragile shared surfaces before the tag is cut (A2 release-rigor gate) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-05 | Tampering | submodule later pinned to a moving ref instead of immutable commit | mitigate | Cut an annotated tag `v1.0.0` AND record the exact commit SHA (A4); 28-03 pins + verifies the gitlink SHA equals the recorded SHA |
| T-28-06 | Repudiation | provenance lost (no link from carved code to source) | mitigate | CHANGELOG Migration provenance + commit message both cite claude-workflow 5aff1b1 (D-28b) |
| T-28-07 | Spoofing | push to wrong/typo remote | accept | origin already verified = canonical `https://github.com/agenticapps-eu/agenticapps-shared.git`; HTTPS over org auth |
| T-28-08 | Info disclosure | CHANGELOG exposes internal line ranges | accept | Repo is org-private; line ranges are non-sensitive INFO at ASVS L1 |
| T-28-16 | Tampering | v1.0.0 cut before the fragile shared surfaces are proven → broken shared code shipped (A2) | mitigate | Task 2 hard-gates the commit/tag on Task 1's broad suite exiting 0 (extract_to real-ref + preflight strict/non-strict + set -u) |

No HIGH-severity threats.
</threat_model>

<verification>
1. `bash ~/Sourcecode/agenticapps/agenticapps-shared/tests/run-tests.sh` exits 0 — broad surface GREEN (A2): assert_check, drift GREEN/RED, extract_to real-ref, preflight strict + non-strict, set -u probe.
2. The tag is gated on (1): no commit/tag if the suite is RED.
3. `git describe --tags --exact-match HEAD` == `v1.0.0` and tree is clean.
4. CHANGELOG provenance corrected (no false filter-repo claim), cites source SHA 5aff1b1, notes A1 boundary, and records the canonical release commit SHA == HEAD (A4).
5. Tag pushed to origin (`git ls-remote --tags`).
6. No observability/GSD leakage in committed lib (re-run the 28-01 grep gate over committed tree).
</verification>

<success_criteria>
- agenticapps-shared has an independent GREEN test suite proving extract_to, preflight (both modes), drift, and set -u safety (SC-1 / A2 / A5).
- v1.0.0 tag exists, is annotated, points at the carve commit, and is pushed; the exact release SHA is recorded as the canonical pin artifact (A4).
- The tag is cut ONLY after the broad surface suite passes (A2 release gate).
- CHANGELOG records the extraction with accurate D-28b provenance + the A1 boundary + the release SHA (SC-7).
- README documents the submodule consumption pattern (SC-7).
</success_criteria>

<output>
After completion, create `.planning/phases/28-split-01-agenticapps-shared/28-02-SUMMARY.md`.
Record the EXACT v1.0.0 release commit SHA prominently as the CANONICAL PIN ARTIFACT (A4) — 28-03
verifies its superproject gitlink SHA equals this value (NOT via `git describe --tags`). Also note
the broad-suite coverage proven (extract_to real-ref, preflight strict/non-strict, set -u) so the
re-check can confirm A2.
</output>
