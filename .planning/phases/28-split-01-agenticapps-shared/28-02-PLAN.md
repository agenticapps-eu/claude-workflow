---
phase: 28-split-01-agenticapps-shared
plan: 02
type: execute
wave: 1
depends_on: [28-01]
files_modified:
  - "~/Sourcecode/agenticapps/agenticapps-shared/tests/run-tests.sh"
  - "~/Sourcecode/agenticapps/agenticapps-shared/migrations/test-fixtures/_example/setup.sh"
  - "~/Sourcecode/agenticapps/agenticapps-shared/migrations/test-fixtures/_example/verify.sh"
  - "~/Sourcecode/agenticapps/agenticapps-shared/migrations/test-fixtures/_example/expected-exit"
  - "~/Sourcecode/agenticapps/agenticapps-shared/CHANGELOG.md"
  - "~/Sourcecode/agenticapps/agenticapps-shared/README.md"
  - "~/Sourcecode/agenticapps/agenticapps-shared/VERSION"
autonomous: true
requirements: [SC-1, SC-7, D-28b]
must_haves:
  truths:
    - "agenticapps-shared has its own GREEN standalone test suite that exercises the carved lib in isolation"
    - "tests/run-tests.sh sources all four lib files via the BASH_SOURCE[0] dirname pattern and exits 0"
    - "_example fixture skeleton exists (setup.sh, verify.sh, expected-exit) for consumers to copy"
    - "CHANGELOG.md [1.0.0] section records the carve with a Migration provenance subsection citing claude-workflow 5aff1b1"
    - "A single commit lands the lib (from 28-01) + tests + fixture + docs, and tag v1.0.0 is created and pushed"
  artifacts:
    - path: "~/Sourcecode/agenticapps/agenticapps-shared/tests/run-tests.sh"
      provides: "Standalone smoke suite for the shared lib"
      contains: "run_drift_test"
    - path: "~/Sourcecode/agenticapps/agenticapps-shared/CHANGELOG.md"
      provides: "1.0.0 release notes + Migration provenance (D-28b)"
      contains: "Migration provenance"
    - path: "~/Sourcecode/agenticapps/agenticapps-shared/migrations/test-fixtures/_example/expected-exit"
      provides: "Example fixture exit-code contract"
  key_links:
    - from: "tests/run-tests.sh"
      to: "../migrations/lib/*.sh"
      via: "source via BASH_SOURCE[0] dirname"
      pattern: "BASH_SOURCE"
    - from: "git tag v1.0.0"
      to: "the carve commit"
      via: "annotated tag pushed to origin — the pin 28-03 consumes"
      pattern: "v1.0.0"
---

<objective>
Give agenticapps-shared its own green test suite that proves the carved lib (from 28-01) is
loadable and functional in isolation (no claude-workflow context), create the `_example` fixture
skeleton, finalize 1.0.0 metadata (CHANGELOG provenance per D-28b, README consumption pattern,
VERSION already 1.0.0), then land ONE provenance commit and cut the immutable `v1.0.0` tag that
28-03 will pin the submodule to.

Purpose: agenticapps-shared must be independently verifiable and must have an immutable v1.0.0
pin BEFORE claude-workflow consumes it (wave ordering). Output: green `tests/run-tests.sh`,
`_example/` skeleton, 1.0.0 CHANGELOG/README, and pushed tag `v1.0.0`.

IMPORTANT — operates on the agenticapps-shared working copy
(`~/Sourcecode/agenticapps/agenticapps-shared`, branch `main`). Depends on 28-01 having written
the four lib files. The single commit here includes BOTH the 28-01 lib files and this plan's
test/fixture/docs, with a provenance commit message citing claude-workflow `5aff1b1`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-split-01-agenticapps-shared/28-CONTEXT.md
@.planning/phases/28-split-01-agenticapps-shared/28-RESEARCH.md
@.planning/phases/28-split-01-agenticapps-shared/28-01-PLAN.md

# Sourcing pattern for the standalone test (28-RESEARCH CRQ 4):
#   _SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
#   _SHARED_LIB="$_SCRIPT_DIR/../migrations/lib"
# Standalone-test content recommendation: 28-RESEARCH OQ-3 (smoke tests for run_check,
#   assert_check counter, run_drift_test with synthetic data).
# CHANGELOG/README skeletons already exist in the agenticapps-shared skeleton (SPLIT-01 §Phase A.2).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the _example fixture skeleton + standalone tests/run-tests.sh (smoke suite, GREEN)</name>
  <read_first>
    - .planning/phases/28-split-01-agenticapps-shared/28-RESEARCH.md CRQ 4 (standalone sourcing pattern), OQ-3 (what the smoke tests should contain), Pitfall 4 (agenticapps-shared runs its OWN dispatcher/tests, NOT test_migration_*)
    - .planning/phases/28-split-01-agenticapps-shared/28-01-PLAN.md <interfaces> block (lib signatures)
    - ~/Sourcecode/agenticapps/agenticapps-shared/migrations/lib/helpers.sh, fixture-runner.sh, drift-test.sh (the carved functions to smoke-test)
  </read_first>
  <files>~/Sourcecode/agenticapps/agenticapps-shared/tests/run-tests.sh, ~/Sourcecode/agenticapps/agenticapps-shared/migrations/test-fixtures/_example/setup.sh, ~/Sourcecode/agenticapps/agenticapps-shared/migrations/test-fixtures/_example/verify.sh, ~/Sourcecode/agenticapps/agenticapps-shared/migrations/test-fixtures/_example/expected-exit</files>
  <action>
    A) Create the `_example` fixture skeleton (replaces the `.gitkeep`):
       - `migrations/test-fixtures/_example/setup.sh`: minimal `#!/usr/bin/env bash` that creates a
         trivial marker file in `$1` (the fixture dir), e.g. `mkdir -p "$1" && echo applied > "$1/marker"`.
         Add a header comment: this is the copy-me skeleton for consumer repos (the
         `test-fixtures/NNNN/MM-name/{setup.sh, verify.sh, expected-exit}` convention).
       - `migrations/test-fixtures/_example/verify.sh`: `#!/usr/bin/env bash` that checks the marker:
         `[ -f "$1/marker" ]` — exit 0 if present (idempotency "applied"), non-zero otherwise.
       - `migrations/test-fixtures/_example/expected-exit`: file containing `0`.
       Make setup.sh and verify.sh executable (`chmod +x`).

    B) Create `tests/run-tests.sh` — agenticapps-shared's OWN standalone suite. It sources the lib
       and tests the lib functions themselves (NOT migration content — Pitfall 4):
       - `#!/usr/bin/env bash`, `set -uo pipefail`, provenance header.
       - Resolve lib via BASH_SOURCE pattern (28-RESEARCH CRQ 4 standalone variant):
         `_SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"`
         `_SHARED_LIB="$_SCRIPT_DIR/../migrations/lib"`
       - Source all four: helpers.sh, fixture-runner.sh, preflight.sh, drift-test.sh (helpers first).
       - Set the cleanup traps after sourcing (consumer responsibility per Risk 2):
         `trap '_runtests_do_cleanup' EXIT; trap '_runtests_do_cleanup; exit 130' INT; trap '_runtests_do_cleanup; exit 143' TERM`
       - Smoke tests (OQ-3), each incrementing PASS/FAIL via assert_check or direct counter:
         1. `run_check` with a trivial passing check: build a temp fixture dir, run the _example
            setup.sh into it, then `assert_check "_example fixture applies" "[ -f marker ]" "$tmpdir" applied`.
         2. `assert_check` increments PASS: capture PASS before/after a known-pass assert; verify it grew by 1.
         3. `run_drift_test` GREEN case: write a synthetic SKILL.md (`version: 9.9.9`) + a synthetic
            migrations dir with `0001-x.md` (`to_version: 9.9.9`); assert `run_drift_test "$skill" "$migdir"`
            returns 0; increment PASS on success else FAIL.
         4. `run_drift_test` RED case: same but mismatched versions; assert it returns non-zero.
       - Append the standard Summary block (PASS/FAIL/SKIP echo + exit logic): exit 1 if FAIL>0,
         else exit 0. Reuse the shape from claude-workflow run-tests.sh lines 2566-2579.
       - Clean up all temp dirs.
       - `chmod +x tests/run-tests.sh`.
    The suite MUST exit 0 (all smoke tests GREEN).
  </action>
  <verify>
    <automated>bash ~/Sourcecode/agenticapps/agenticapps-shared/tests/run-tests.sh; test $? -eq 0 && echo SHARED_SUITE_GREEN</automated>
  </verify>
  <acceptance_criteria>
    - `bash ~/Sourcecode/agenticapps/agenticapps-shared/tests/run-tests.sh` exits 0
    - The run prints `FAIL: 0` is NOT shown (FAIL line only prints when >0) AND prints at least one `PASS`
    - `grep -q 'BASH_SOURCE' ~/Sourcecode/agenticapps/agenticapps-shared/tests/run-tests.sh`
    - `grep -q 'run_drift_test' ~/Sourcecode/agenticapps/agenticapps-shared/tests/run-tests.sh`
    - `! grep -q 'test_migration_' ~/Sourcecode/agenticapps/agenticapps-shared/tests/run-tests.sh` (no claude-workflow per-migration calls — Pitfall 4)
    - `[ -f ~/Sourcecode/agenticapps/agenticapps-shared/migrations/test-fixtures/_example/expected-exit ]` and its content is `0`
    - `[ -x ~/Sourcecode/agenticapps/agenticapps-shared/tests/run-tests.sh ]`
  </acceptance_criteria>
  <done>agenticapps-shared's standalone suite sources the lib via BASH_SOURCE, runs 4 smoke tests covering run_check/assert_check/run_drift_test (GREEN+RED), exits 0, and references no test_migration_* functions. The _example fixture triad exists and is wired into smoke test 1.</done>
</task>

<task type="auto">
  <name>Task 2: Finalize 1.0.0 metadata (CHANGELOG provenance, README consumption), commit the carve, tag + push v1.0.0</name>
  <read_first>
    - ~/Sourcecode/agenticapps/agenticapps-shared/CHANGELOG.md (skeleton from Phase A — has a "Migration provenance" stub referencing filter-repo; that stub is WRONG for D-28b and must be rewritten)
    - ~/Sourcecode/agenticapps/agenticapps-shared/README.md (skeleton — verify the submodule consumption block is present)
    - .planning/phases/28-split-01-agenticapps-shared/28-CONTEXT.md D-28b (provenance-by-note wording)
  </read_first>
  <files>~/Sourcecode/agenticapps/agenticapps-shared/CHANGELOG.md, ~/Sourcecode/agenticapps/agenticapps-shared/README.md, ~/Sourcecode/agenticapps/agenticapps-shared/VERSION</files>
  <action>
    A) Rewrite the CHANGELOG `[1.0.0]` section. The Phase-A skeleton's "Migration provenance"
       paragraph claims "Full per-file history preserved via git filter-repo; use git log --follow" —
       this is FALSE under D-28b and MUST be corrected. Replace it with:
       - Set the `## [1.0.0] - YYYY-MM-DD` header to today's date.
       - `### Added`: "Shared migration infrastructure carved from claude-workflow:
         `migrations/lib/{helpers,fixture-runner,preflight,drift-test}.sh` — the generic
         fixture-runner harness, pass/fail helpers, parameterized preflight verify-path auditor,
         and the policy-agnostic drift-test runner mechanism. Standalone `tests/run-tests.sh` +
         `migrations/test-fixtures/_example/` skeleton."
       - `### Migration provenance (D-28b)`: state explicitly that these lib files were
         **refactored out of** claude-workflow's single `migrations/run-tests.sh` (one 2579-line
         file with intermingled SHARED/WORKFLOW functions). Because `git filter-repo` is whole-file
         granularity, function-level carving does NOT preserve `git log --follow` lineage; provenance
         is recorded by note: source = claude-workflow commit `5aff1b1` (v1.21.0). Cite the source
         line ranges (helpers 29-42/76-85/139-170, fixture-runner 96-134, preflight 1767-1851,
         drift-test 2237-2269). Note the one functional change: `setup_fixture` gained a 4th
         backward-compatible `skill_name` arg; `run_drift_test`/`run_preflight_verify_paths` were
         parameterized (paths passed as args, no REPO_ROOT).
    B) Verify README has the submodule consumption block (from Phase A skeleton it should:
       `git submodule add https://github.com/agenticapps-eu/agenticapps-shared vendor/agenticapps-shared`).
       If missing, add it. Add a short "Running the shared test suite" line: `bash tests/run-tests.sh`.
    C) Confirm `VERSION` is `1.0.0` (Phase A set it; leave as-is — do not bump).
    D) Stage and commit EVERYTHING (the 28-01 lib files + this plan's tests/fixture/docs) in ONE commit:
       `cd ~/Sourcecode/agenticapps/agenticapps-shared`
       `git add -A`
       `git commit -m "feat: carve shared migration infrastructure from claude-workflow (SPLIT-01)" -m "Refactors SHARED harness functions out of claude-workflow migrations/run-tests.sh @ 5aff1b1 (v1.21.0) into migrations/lib/{helpers,fixture-runner,preflight,drift-test}.sh. Provenance-by-note per D-28b (filter-repo cannot carve intermingled functions). Adds standalone tests/run-tests.sh + _example fixture. setup_fixture parameterized with 4th skill_name arg; drift/preflight parameterized off args." -m "Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"`
    E) Cut the immutable tag and push:
       `git tag -a v1.0.0 -m "agenticapps-shared 1.0.0 — initial shared infra carve (SPLIT-01)"`
       `git push origin main`
       `git push origin v1.0.0`
       If push fails on auth, create a checkpoint requesting `gh auth` (auth gate is dynamic).
    Verify the tag points at the carve commit (not the skeleton) and the working tree is clean.
  </action>
  <verify>
    <automated>cd ~/Sourcecode/agenticapps/agenticapps-shared && git describe --tags --exact-match HEAD 2>/dev/null | grep -qx v1.0.0 && [ -z "$(git status --porcelain)" ] && grep -q 'Migration provenance' CHANGELOG.md && grep -q '5aff1b1' CHANGELOG.md && echo SHARED_V1_TAGGED</automated>
  </verify>
  <acceptance_criteria>
    - `cd ~/Sourcecode/agenticapps/agenticapps-shared && git describe --tags --exact-match HEAD` prints `v1.0.0`
    - `cd ~/Sourcecode/agenticapps/agenticapps-shared && git status --porcelain` is empty (clean tree)
    - `grep -q 'Migration provenance' ~/Sourcecode/agenticapps/agenticapps-shared/CHANGELOG.md`
    - `grep -q '5aff1b1' ~/Sourcecode/agenticapps/agenticapps-shared/CHANGELOG.md` (source SHA cited)
    - `! grep -q 'git log --follow' ~/Sourcecode/agenticapps/agenticapps-shared/CHANGELOG.md` OR the phrase appears only in a "NOT preserved" negation (the false filter-repo claim is removed)
    - `cat ~/Sourcecode/agenticapps/agenticapps-shared/VERSION` is `1.0.0`
    - `git -C ~/Sourcecode/agenticapps/agenticapps-shared ls-remote --tags origin | grep -q 'refs/tags/v1.0.0$'` (tag pushed)
    - The four `migrations/lib/*.sh` files are tracked in the commit: `git -C ~/Sourcecode/agenticapps/agenticapps-shared ls-files migrations/lib/ | grep -c '\.sh$'` returns `4`
  </acceptance_criteria>
  <done>agenticapps-shared has one provenance commit containing the carved lib + standalone suite + _example + corrected CHANGELOG (no false filter-repo claim, source SHA 5aff1b1 cited), tagged `v1.0.0` and pushed to origin main + tags. Working tree clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local repo → GitHub origin (push) | Pushing tag + commit to org-private agenticapps-eu repo over HTTPS |
| immutable tag v1.0.0 | The pin 28-03 consumes — must be an annotated tag, not a moving branch |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-05 | Tampering | submodule later pinned to a moving ref instead of immutable tag | mitigate | Cut an annotated tag `v1.0.0`; 28-03 pins to the tag (immutable), verified by `git describe` |
| T-28-06 | Repudiation | provenance lost (no link from carved code to source) | mitigate | CHANGELOG Migration provenance + commit message both cite claude-workflow 5aff1b1 (D-28b) |
| T-28-07 | Spoofing | push to wrong/typo remote | accept | origin already verified = canonical `https://github.com/agenticapps-eu/agenticapps-shared.git`; HTTPS over org auth |
| T-28-08 | Info disclosure | CHANGELOG exposes internal line ranges | accept | Repo is org-private; line ranges are non-sensitive INFO at ASVS L1 |

No HIGH-severity threats.
</threat_model>

<verification>
1. `bash ~/Sourcecode/agenticapps/agenticapps-shared/tests/run-tests.sh` exits 0 (standalone suite GREEN).
2. `git describe --tags --exact-match HEAD` == `v1.0.0` and tree is clean.
3. CHANGELOG provenance corrected (no false filter-repo claim) and cites source SHA 5aff1b1.
4. Tag pushed to origin (`git ls-remote --tags`).
5. No observability/GSD leakage in committed lib (re-run the 28-01 grep gate over committed tree).
</verification>

<success_criteria>
- agenticapps-shared has an independent GREEN test suite (SC-1 verifiability).
- v1.0.0 tag exists, is annotated, points at the carve commit, and is pushed (the 28-03 pin source).
- CHANGELOG records the extraction with accurate D-28b provenance (SC-7).
- README documents the submodule consumption pattern (SC-7).
</success_criteria>

<output>
After completion, create `.planning/phases/28-split-01-agenticapps-shared/28-02-SUMMARY.md`.
Record the exact v1.0.0 commit SHA in the SUMMARY — 28-03 needs it to confirm the submodule pin.
</output>
