---
phase: 27
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - add-observability/templates/ts-cloudflare-worker/lib-observability.test.ts
  - add-observability/templates/ts-cloudflare-pages/lib-observability.test.ts
  - add-observability/templates/openrouter-monitor/src/observability/index.test.ts
autonomous: true
requirements: [WR-03]
must_haves:
  truths:
    - "Direct buildSentryOptions(env) unit tests exist in all 3 stacks that export the helper"
    - "Test sensitivity was proven via a TEMPORARY, uncommitted local mutation of the implementation (run log shows the new tests catching it), NOT via a deliberately-false assertion committed as RED (codex review: avoid TDD theater)"
    - "Assertions match the RESEARCH Blocker-C corrected set (TRACE_SAMPLE_RATE is a baked constant, NOT a runtime env override)"
    - "openrouter's existing D-02a determinism block stays decoupled from buildSentryOptions (codex MED-4)"
  artifacts:
    - path: "add-observability/templates/openrouter-monitor/src/observability/index.test.ts"
      provides: "WR-03 openrouter buildSentryOptions tests (directly testable, baked values)"
      contains: "buildSentryOptions"
    - path: "add-observability/templates/ts-cloudflare-worker/lib-observability.test.ts"
      provides: "WR-03 cf-worker buildSentryOptions tests (token-template, run via harness)"
      contains: "buildSentryOptions"
    - path: "add-observability/templates/ts-cloudflare-pages/lib-observability.test.ts"
      provides: "WR-03 cf-pages buildSentryOptions tests (token-template, run via harness)"
      contains: "buildSentryOptions"
  key_links:
    - from: "lib-observability.test.ts / index.test.ts"
      to: "buildSentryOptions(env)"
      via: "direct unit test invocation"
      pattern: "buildSentryOptions\\("
---

<objective>
WR-03 (COVERAGE ADDITION): add direct unit tests for the `buildSentryOptions(env)` helper across
the three stacks that export it (cf-worker, cf-pages, openrouter). The helper already exists
(Phase 26 DEF-1) but has no direct unit coverage — its behavior was only exercised indirectly.

Purpose: Test-lock the helper's contract BEFORE WR-04 (Plan 05) rewires the openrouter entry
to consume it.
Output: 3 test additions, committed in their correct GREEN state.

NOTE (codex review finding): this is NOT framed as strict RED→GREEN TDD. The implementation
already exists and already passes, so a "RED" produced by committing a deliberately-false
assertion (e.g. `.not.toBe(0.1)`) is theater — it proves only that vitest can fail on command,
not that the tests discover behavior. Instead, prove the tests are SENSITIVE (not vacuous) via a
TEMPORARY, uncommitted local mutation of the helper during execution, captured in the run log;
revert it before committing. The committed artifact is the coverage addition in its GREEN state.

CRITICAL CORRECTION (RESEARCH Blocker-C): `TRACE_SAMPLE_RATE` is a scaffold-time module
constant (`const TRACE_SAMPLE_RATE = 0.1` / `{{TRACE_SAMPLE_RATE}}`), NOT a runtime env
override. The helper has NO env-parse behavior for the sample rate. Do NOT assert env-var
override behavior that does not exist.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/27-1-21-0-stable-baseline-split-00-gate-close-wr-01-04-minimum-/27-CONTEXT.md
@.planning/phases/27-1-21-0-stable-baseline-split-00-gate-close-wr-01-04-minimum-/27-RESEARCH.md
@.planning/phases/27-1-21-0-stable-baseline-split-00-gate-close-wr-01-04-minimum-/27-VALIDATION.md

<interfaces>
<!-- Helper signature (all 3 stacks): buildSentryOptions(env: InitEnv): SentryOptions -->

openrouter (DIRECTLY TESTABLE — baked literal values), from src/observability/index.ts:154:
```typescript
export function buildSentryOptions(env: InitEnv): SentryOptions {
  return {
    dsn: env.SENTRY_DSN,
    environment: env.DEPLOY_ENV ?? "dev",
    release: env.SERVICE_NAME ?? SERVICE_DEFAULT,   // SERVICE_DEFAULT = "openrouter-monitor"
    tracesSampleRate: TRACE_SAMPLE_RATE,            // const = 0.1  (NOT env-derived)
    sendDefaultPii: false,
  };
}
```

cf-worker / cf-pages (TOKEN TEMPLATES — only testable AFTER harness materializes them),
from lib-observability.ts:154:
```typescript
export function buildSentryOptions(env: InitEnv): SentryOptions {
  return {
    dsn: env.{{ENV_VAR_DSN}},
    environment: env.{{ENV_VAR_ENV}} ?? "dev",
    release: env.{{ENV_VAR_SERVICE}} ?? SERVICE_DEFAULT,   // SERVICE_DEFAULT = "{{SERVICE_NAME}}"
    tracesSampleRate: {{TRACE_SAMPLE_RATE}},               // baked constant
    sendDefaultPii: false,
  };
}
```
The harness substitutes tokens into a temp dir before running vitest. In the test FILE you
reference the materialized identifiers — e.g. `env.SENTRY_DSN` after `{{ENV_VAR_DSN}}` →
`SENTRY_DSN` substitution. Confirm the exact substituted token names from the stack's meta.yaml
during execution (the harness materialization map is documented at run-template-tests.sh:149).

<!-- CORRECTED ASSERTION SET (RESEARCH Blocker-C) — exactly these 5: -->
1. tracesSampleRate === TRACE_SAMPLE_RATE  (baked constant; 0.1 for openrouter)
2. environment === env.DEPLOY_ENV ?? "dev"  (test BOTH set and unset)
3. release === env.SERVICE_NAME ?? SERVICE_DEFAULT  (test BOTH set and unset)
4. sendDefaultPii === false
5. dsn === env.SENTRY_DSN

<!-- DECOUPLING FIREWALL (codex MED-4) -->
openrouter's existing `index.test.ts` D-02a determinism block must stay FREE of
buildSentryOptions. The new WR-03 tests go in a SEPARATE describe/test block. MED-4 only
forbids the *determinism proof* from depending on the helper — a separate direct-helper
block does NOT violate it.
</interfaces>
</context>

<feature>
  <name>buildSentryOptions direct unit coverage × 3 stacks</name>
  <files>
    add-observability/templates/openrouter-monitor/src/observability/index.test.ts,
    add-observability/templates/ts-cloudflare-worker/lib-observability.test.ts,
    add-observability/templates/ts-cloudflare-pages/lib-observability.test.ts
  </files>
  <behavior>
    For each stack, a new `describe("buildSentryOptions", …)` block (vitest) covering the 5
    corrected assertions:
    - Test A (env all set): given env = { SENTRY_DSN: "dsn-x", DEPLOY_ENV: "staging",
      SERVICE_NAME: "svc-x" } → returns { dsn: "dsn-x", environment: "staging",
      release: "svc-x", tracesSampleRate: <baked const>, sendDefaultPii: false }.
    - Test B (env unset defaults): given env = { SENTRY_DSN: "dsn-y" } (DEPLOY_ENV +
      SERVICE_NAME absent) → environment === "dev"; release === SERVICE_DEFAULT
      (openrouter: "openrouter-monitor"; cf-worker/cf-pages: the materialized {{SERVICE_NAME}});
      tracesSampleRate === baked const; sendDefaultPii === false; dsn === "dsn-y".
    - Test C (sample rate is a constant, NOT env-derived): assert tracesSampleRate equals the
      baked constant EVEN WHEN a bogus `TRACE_SAMPLE_RATE` key is added to env
      (e.g. env = { SENTRY_DSN: "d", TRACE_SAMPLE_RATE: "0.99" } as any) → tracesSampleRate
      STILL === baked const (proves no env-override path; this is the meaningful RED for
      stacks whose other assertions already pass — and the explicit Blocker-C correction).
  </behavior>
  <implementation>
    The helper already exists and satisfies all assertions, so write Tests A/B/C with their
    CORRECT expectations from the start (no deliberately-wrong assertion). Concretely:
      1. Add the `describe("buildSentryOptions", …)` block with Tests A/B/C (correct
         expectations, including Test C `expect(opts.tracesSampleRate).toBe(<baked const>)` even
         when a bogus `TRACE_SAMPLE_RATE` env key is present — proving the rate is a constant,
         not an env override). Confirm GREEN.
      2. SENSITIVITY PROOF (not committed): to demonstrate the new tests are meaningful rather
         than vacuous, TEMPORARILY mutate the helper locally — e.g. flip `sendDefaultPii` to
         `true`, or make `tracesSampleRate` read from env — re-run the suite, and CONFIRM the new
         tests FAIL. Capture that failing run output as the §06 sensitivity evidence. Then REVERT
         the mutation (the helper is unchanged in the commit). Do NOT commit the mutated helper
         and do NOT commit a deliberately-false assertion.
      3. Commit ONCE per the coverage addition: `test(27-02): add buildSentryOptions coverage × 3 stacks`.
    cf-worker/cf-pages tests are token templates — they run THROUGH the materialization
    harness (`run-template-tests.sh cf-worker` / `cf-pages`), which substitutes tokens into a
    temp dir then runs vitest. openrouter is directly testable. Keep the new block SEPARATE
    from openrouter's existing D-02a determinism block (MED-4). Confirm the exact baked
    `tracesSampleRate` value (0.1) and SERVICE_DEFAULT per stack during execution.
  </implementation>
</feature>

<tasks>

<task type="auto">
  <name>Task 1: Add buildSentryOptions coverage (correct assertions) in all 3 stacks + prove sensitivity locally</name>
  <read_first>
    - add-observability/templates/openrouter-monitor/src/observability/index.test.ts (existing D-02a block — your new block must NOT touch or depend on it; MED-4)
    - add-observability/templates/openrouter-monitor/src/observability/index.ts (lines 154-180 — confirm the exact returned shape and SERVICE_DEFAULT/TRACE_SAMPLE_RATE constants)
    - add-observability/templates/ts-cloudflare-worker/lib-observability.test.ts (existing structure + how it imports the helper)
    - add-observability/templates/ts-cloudflare-pages/lib-observability.test.ts (existing structure)
    - add-observability/templates/run-template-tests.sh (lines 140-200 — the cf-worker/cf-pages materialization map + token substitution, so you reference the correct materialized env-var identifiers)
  </read_first>
  <files>
    add-observability/templates/openrouter-monitor/src/observability/index.test.ts,
    add-observability/templates/ts-cloudflare-worker/lib-observability.test.ts,
    add-observability/templates/ts-cloudflare-pages/lib-observability.test.ts
  </files>
  <action>
    In EACH of the 3 test files, add a NEW separate `describe("buildSentryOptions", () => { … })`
    block (do NOT modify openrouter's existing D-02a block — MED-4). Include Tests A, B, C from
    <behavior>, each with its CORRECT expectation from the start (NO deliberately-wrong
    assertion). Test C asserts the constant-not-override behavior correctly:
      it("tracesSampleRate is a baked constant, ignoring any env override", () => {
        const opts = buildSentryOptions({ SENTRY_DSN: "d", TRACE_SAMPLE_RATE: "0.99" } as any);
        expect(opts.tracesSampleRate).toBe(0.1);  // baked const; the bogus env key is ignored
      });
    For cf-worker/cf-pages use the materialized identifiers (e.g. `env.SENTRY_DSN`) — these run
    via the harness, not directly. Baked constant for openrouter is `0.1`; confirm cf-worker/
    cf-pages baked value from the harness materialization.
    SENSITIVITY PROOF (local, NOT committed): after the block is GREEN, temporarily mutate the
    helper (e.g. flip `sendDefaultPii` to `true`) and re-run the affected stack to CONFIRM the
    new tests FAIL — capture that failing output as the §06 sensitivity evidence — then REVERT
    the mutation. Do NOT commit the mutated helper or any deliberately-false assertion.
    Commit ONCE: `test(27-02): add buildSentryOptions coverage × 3 stacks`.
  </action>
  <verify>
    <automated>cd add-observability/templates && bash run-template-tests.sh openrouter 2>&1 | tail -3 && for f in openrouter-monitor/src/observability/index.test.ts ts-cloudflare-worker/lib-observability.test.ts ts-cloudflare-pages/lib-observability.test.ts; do grep -q 'describe("buildSentryOptions"' "$f" && echo "BLOCK PRESENT: $f" || (echo "MISSING in $f"; exit 1); done</automated>
  </verify>
  <acceptance_criteria>
    - EXACTLY the 3 named target files each contain a `describe("buildSentryOptions"` block (assert per-file, not a broad recursive grep — codex: `buildSentryOptions` already appears in comments elsewhere): openrouter-monitor/src/observability/index.test.ts, ts-cloudflare-worker/lib-observability.test.ts, ts-cloudflare-pages/lib-observability.test.ts
    - The block contains the three named tests A/B/C (e.g. `grep -q 'tracesSampleRate is a baked constant' <file>` for Test C)
    - openrouter's existing D-02a block does NOT reference `buildSentryOptions` (MED-4): `awk '/D-02a/,/^})/' add-observability/templates/openrouter-monitor/src/observability/index.test.ts | grep -c buildSentryOptions` → 0
    - NO deliberately-false assertion committed: `grep -rn '\.not\.toBe(0\.1)' add-observability/templates --include='*.test.ts'` returns NOTHING
    - Single coverage commit exists: `git log --oneline -1 --grep 'add buildSentryOptions coverage'`
  </acceptance_criteria>
  <done>3 separate buildSentryOptions describe blocks added with correct assertions; sensitivity proven locally (uncommitted mutation, reverted); MED-4 decoupling intact; no false assertion committed; single coverage commit.</done>
</task>

<task type="auto">
  <name>Task 2: Verify all 3 stacks GREEN through the materialization harness</name>
  <read_first>
    - The 3 test files edited in Task 1 (re-read your own additions)
  </read_first>
  <files>
    add-observability/templates/openrouter-monitor/src/observability/index.test.ts,
    add-observability/templates/ts-cloudflare-worker/lib-observability.test.ts,
    add-observability/templates/ts-cloudflare-pages/lib-observability.test.ts
  </files>
  <action>
    Verification only — no source edits. Confirm the full corrected 5-assertion set is covered in
    each stack:
      A (all set): dsn===env.SENTRY_DSN; environment===env.DEPLOY_ENV; release===env.SERVICE_NAME;
        tracesSampleRate===<baked>; sendDefaultPii===false.
      B (unset): environment==="dev"; release===SERVICE_DEFAULT; others as above.
      C: tracesSampleRate===<baked const> even with a bogus env key (constant-not-override).
    cf-worker/cf-pages run THROUGH the materialization harness (token substitution into a temp
    dir); openrouter is directly testable. Confirm all three stacks GREEN. No commit (Task 1
    already committed the coverage).
  </action>
  <verify>
    <automated>cd add-observability/templates && bash run-template-tests.sh openrouter 2>&1 | tail -3 && bash run-template-tests.sh cf-worker 2>&1 | tail -3 && bash run-template-tests.sh cf-pages 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `bash add-observability/templates/run-template-tests.sh openrouter` exits 0
    - `bash add-observability/templates/run-template-tests.sh cf-worker` exits 0
    - `bash add-observability/templates/run-template-tests.sh cf-pages` exits 0
    - No assertion claims `TRACE_SAMPLE_RATE` is read from env at runtime (Blocker-C honored): the only env-override test asserts the rate STAYS the baked constant
  </acceptance_criteria>
  <done>All 3 stacks GREEN through the harness; 5-assertion corrected set covered; sample-rate-is-constant proven.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Test-only additions. No production code changes, no request path, no untrusted input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-02-01 | Information Disclosure | buildSentryOptions(sendDefaultPii) | mitigate | Test B/A explicitly assert `sendDefaultPii === false`, pinning the no-PII-to-Sentry contract so a future regression that flips it to true is caught. |
| T-27-02-02 | (n/a) | test correctness | accept | Tests do not change the helper; they lock its existing contract. No attack surface added. |

No HIGH-severity threats. Coverage strengthens an existing security-relevant invariant (sendDefaultPii=false).
</threat_model>

<verification>
- Each of the 3 named target files contains a `describe("buildSentryOptions"` block (assert per-file, not a broad recursive grep)
- All 3 stacks GREEN via `run-template-tests.sh <stack>`
- No deliberately-false assertion committed (`grep -rn '\.not\.toBe(0\.1)' … --include='*.test.ts'` empty); single coverage commit present in git log
</verification>

<success_criteria>
- buildSentryOptions has direct unit coverage in cf-worker, cf-pages, openrouter.
- Assertions match the Blocker-C corrected set; sample rate proven to be a baked constant.
- Test sensitivity proven via a temporary, uncommitted local impl mutation (not via a committed false assertion); committed as a single coverage addition.
- openrouter MED-4 decoupling preserved.
</success_criteria>

<output>
After completion, create `.planning/phases/27-1-21-0-stable-baseline-split-00-gate-close-wr-01-04-minimum-/27-02-SUMMARY.md`
</output>
