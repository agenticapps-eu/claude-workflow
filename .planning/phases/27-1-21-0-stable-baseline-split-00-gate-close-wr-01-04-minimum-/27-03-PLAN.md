---
phase: 27
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/PROJECT.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [PROJECT-MD, STATE-ROADMAP-DRIFT]
must_haves:
  truths:
    - "A canonical .planning/PROJECT.md exists describing product identity (not a phase-by-phase retro)"
    - "STATE.md no longer points at 'PROJECT.md does not yet exist' and reflects Phase 26 MERGED + current position Phase 27"
    - "STATE.md's stale 'Next action' line (pointing at /gsd-discuss-phase 26) is corrected"
    - "ROADMAP.md marks Phase 26 Complete/merged and the milestone framing reflects the shipped 1.20.0 reality"
  artifacts:
    - path: ".planning/PROJECT.md"
      provides: "Canonical product identity doc (D-05 minimum-viable)"
      contains: "spec-first"
      min_lines: 30
    - path: ".planning/STATE.md"
      provides: "Drift-refreshed state (Phase 26 merged, position Phase 27)"
      contains: "Phase 27"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 26 marked complete/merged; milestone framing corrected"
      contains: "Phase 26"
  key_links:
    - from: ".planning/STATE.md"
      to: ".planning/PROJECT.md"
      via: "Project Reference pointer (now resolves)"
      pattern: "PROJECT.md"
---

<objective>
Create the canonical minimum-viable `.planning/PROJECT.md` and refresh the drifted
STATE.md / ROADMAP.md tracking after Phase 26's merge (PR #60, `46bb394`).

Purpose: SPLIT-00 requires a PROJECT.md (STATE.md currently points at "does not yet exist")
and a drift-clean STATE/ROADMAP before the 1.21.0 baseline. This satisfies that gate item
WITHOUT a full retroactive bootstrap of Phases 01-24 (D-05: minimum-viable, forward-looking).
Output: PROJECT.md created; STATE.md + ROADMAP.md drift-corrected.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-1-21-0-stable-baseline-split-00-gate-close-wr-01-04-minimum-/27-CONTEXT.md
@SPLIT-00-PREREQUISITES.md

<interfaces>
<!-- D-05 PROJECT.md required sections (minimum-viable, forward-looking) -->
PROJECT.md MUST contain:
  - What claude-workflow IS (one-paragraph product identity)
  - Core value: "spec-first, migration-driven workflow scaffolder for AgenticApps projects"
  - Who uses it (AgenticApps repos incl. factiv downstreams: cparx, callbot, fx-signal-agent)
  - Key constraints: migration-driven versioning (user rule `versioning-tracks-migrations`);
    spec-first via GSD
  - Current milestone (v1.20.x worker-template hardening; 1.21.0 stable baseline)
  - The impending 3-repo split → point at SPLIT-00 / SPLIT-01 / SPLIT-02
  - A link/pointer to .planning/phases/ + git for the Phases 01-26 history
    (PROJECT.md LINKS there, it does NOT reconstruct it)

<!-- STATE.md current drift (to correct) -->
STATE.md line 21: "See: .planning/PROJECT.md (does not yet exist …)" → must resolve (PROJECT.md now exists)
STATE.md lines 25, 29-34: "Current focus: Phase 26", "Phase: 26", "Status: Executing Phase 26" → Phase 26 MERGED, position Phase 27
STATE.md line 74 "Next action: `/gsd-discuss-phase 26` …" → STALE; correct to reflect Phase 27 planning/execution

<!-- ROADMAP.md current drift (to correct) -->
ROADMAP.md line 15: "🚧 v1.20.x … Phases 25-26 (in progress)" → Phase 26 shipped 1.20.0/merged
ROADMAP.md Phase 26 section + Progress table: mark Complete/merged (PR #60, 46bb394)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create canonical minimum-viable .planning/PROJECT.md (D-05)</name>
  <read_first>
    - .planning/STATE.md (read lines 19-25 — the "Project Reference" + "Core value" lines that PROJECT.md formalizes and that currently point at a nonexistent file)
    - .planning/phases/27-1-21-0-stable-baseline-split-00-gate-close-wr-01-04-minimum-/27-CONTEXT.md (D-05 section, lines 115-124 — the exact required sections)
    - SPLIT-00-PREREQUISITES.md (the end-state repo map + split rationale to link from PROJECT.md)
  </read_first>
  <files>.planning/PROJECT.md</files>
  <action>
    Create `.planning/PROJECT.md` as a forward-looking product-identity document (NOT a
    phase-by-phase retro). Include these sections, in this order:
      1. `# claude-workflow` + one-paragraph "What this is": the AgenticApps Claude Workflow —
         a spec-first, migration-driven workflow scaffolder for AgenticApps projects.
      2. `## Core value` — spec-first (GSD-driven planning) + migration-driven versioning.
      3. `## Who uses it` — AgenticApps repos including the factiv downstreams (cparx, callbot,
         fx-signal-agent) that upgrade to tagged releases.
      4. `## Key constraints` — (a) migration-driven versioning: per user rule
         `versioning-tracks-migrations`, the SKILL.md version tracks the latest migration's
         `to_version`; engine/test fixes to an existing migration get NO version bump; no
         migration → no SKILL.md bump. (b) spec-first via GSD (discuss → research → plan →
         execute → verify).
      5. `## Current milestone` — v1.20.x worker-template hardening; Phase 27 ships the 1.21.0
         stable baseline (tag-only, SKILL.md trails at 1.20.0 — see CHANGELOG note).
      6. `## The 3-repo split` — short paragraph: claude-workflow is splitting into
         claude-workflow (slimmed) + agenticapps-shared + agenticapps-observability. Link
         `SPLIT-00-PREREQUISITES.md`, `SPLIT-01-agenticapps-shared.md`,
         `SPLIT-02-agenticapps-observability.md`.
      7. `## History` — one line: Phases 01-26 history lives in `.planning/phases/` and the git
         log; this file does not reconstruct it.
    Keep it concise (target ~40-70 lines). Do NOT enumerate phases 01-24.
  </action>
  <verify>
    <automated>test -f .planning/PROJECT.md && grep -q 'spec-first' .planning/PROJECT.md && grep -q 'migration-driven' .planning/PROJECT.md && grep -qi 'SPLIT-00' .planning/PROJECT.md && echo "PROJECT.md OK" || (echo "FAIL"; exit 1)</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/PROJECT.md` succeeds
    - `grep -q 'spec-first' .planning/PROJECT.md` matches
    - `grep -q 'migration-driven' .planning/PROJECT.md` matches
    - `grep -q 'versioning-tracks-migrations' .planning/PROJECT.md` matches (constraint cited)
    - `grep -Eqi 'SPLIT-00|SPLIT-01|SPLIT-02' .planning/PROJECT.md` matches (split linked)
    - `grep -Eqi 'cparx|callbot|fx-signal-agent' .planning/PROJECT.md` matches (downstreams named)
    - File does NOT enumerate "Phase 01" through "Phase 24" individually (no retro): `grep -c 'Phase 0[1-9]' .planning/PROJECT.md` is low (history is a single pointer line, not a list)
  </acceptance_criteria>
  <done>PROJECT.md exists with all 7 D-05 sections; cites the versioning rule; links the split; names downstreams; no phase retro.</done>
</task>

<task type="auto">
  <name>Task 2: Refresh STATE.md + ROADMAP.md drift (D-08)</name>
  <read_first>
    - .planning/STATE.md (full file — the frontmatter, Project Reference, Current Position, Session Continuity / Next action lines all drift)
    - .planning/ROADMAP.md (lines 12-16 milestone header; Phase 26 section ~50-83; Progress table ~105-109)
    - .planning/phases/27-1-21-0-stable-baseline-split-00-gate-close-wr-01-04-minimum-/27-CONTEXT.md (D-08 section, lines 184-189)
  </read_first>
  <files>.planning/STATE.md, .planning/ROADMAP.md</files>
  <action>
    STATE.md edits:
      - Line 21 "See: .planning/PROJECT.md (does not yet exist …)" → "See: .planning/PROJECT.md"
        (drop the "does not yet exist" parenthetical; it now exists).
      - "Current focus: Phase 26 — worker-template-hardening" → "Current focus: Phase 27 —
        1.21.0 stable baseline (SPLIT-00 gate)".
      - Current Position block: `Phase: 26` → `Phase: 27`; `Status: Executing Phase 26` →
        `Status: Planning/executing Phase 27`; update the Progress line to note Phase 26
        MERGED (PR #60, `46bb394`).
      - Frontmatter: bump `last_updated` to today (2026-06-02) and set
        `stopped_at` / Session Continuity to reflect Phase 27 planning. Update `progress`
        block counts if appropriate (3 phases tracked, Phase 26 complete+merged).
      - Line 74 "Next action: `/gsd-discuss-phase 26` …" (the stale long line) → replace with
        a Phase-27-correct next action, e.g. "Next action: execute Phase 27 plans
        (27-01..27-06) — WR-01..04, PROJECT.md, STATE/ROADMAP refresh, boundary ADR-0035,
        SPLIT doc fixes, CHANGELOG [1.21.0] + tag v1.21.0."
    ROADMAP.md edits:
      - Milestone header line 15: reflect that Phases 25-26 SHIPPED 1.20.0 (not "in progress").
        Correct the "v1.19.0 / migration" framing drift noted in D-08 — either relabel the
        milestone to its true shipped version or add a parenthetical noting 1.20.0 shipped.
      - Phase 26 section "**Plans:** 3/3 plans complete" already says complete — ALSO add a
        merged marker (PR #60, `46bb394`) to the Phase 26 heading/depends line.
      - Progress table row "26. worker-template hardening | 3/3 | Complete" → append merged/
        PR reference in the Completed or Status column.
    Do NOT touch the Phase 27 section's plan list here — Plan 06 / the orchestrator's
    update_roadmap step finalizes plan placeholders.
  </action>
  <verify>
    <automated>grep -q 'Phase 27' .planning/STATE.md && ! grep -q 'does not yet exist' .planning/STATE.md && ! grep -q 'gsd-discuss-phase 26' .planning/STATE.md && grep -qi '46bb394' .planning/ROADMAP.md && echo "DRIFT REFRESHED" || (echo "FAIL"; exit 1)</automated>
  </verify>
  <acceptance_criteria>
    - STATE.md: `grep -q 'Phase 27' .planning/STATE.md` matches; `grep -q 'does not yet exist' .planning/STATE.md` returns NOTHING (pointer resolved)
    - STATE.md: `grep -q 'gsd-discuss-phase 26' .planning/STATE.md` returns NOTHING (stale Next action removed)
    - STATE.md: `grep -qi '46bb394' .planning/STATE.md` matches (Phase 26 merge SHA recorded)
    - ROADMAP.md: `grep -qi '46bb394' .planning/ROADMAP.md` matches (Phase 26 marked merged)
    - ROADMAP.md milestone header no longer labels Phases 25-26 as only "in progress" (the 1.20.0 ship is reflected)
  </acceptance_criteria>
  <done>STATE.md points at existing PROJECT.md, reflects Phase 27 + Phase 26 merge, stale Next action gone; ROADMAP marks Phase 26 merged and corrects milestone framing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Pure planning-documentation edits. No code, no runtime, no input handling. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-03-01 | (n/a) | planning docs | accept | Documentation-only. No attack surface. Accuracy is verified by grep-checkable acceptance criteria. |

No new attack surface.
</threat_model>

<verification>
- `test -f .planning/PROJECT.md` and required-section greps pass
- STATE.md + ROADMAP.md drift greps pass (Phase 27 present, "does not yet exist" gone, 46bb394 recorded)
</verification>

<success_criteria>
- Canonical minimum-viable PROJECT.md exists and satisfies all D-05 sections.
- STATE.md and ROADMAP.md are drift-clean post-Phase-26-merge.
</success_criteria>

<output>
After completion, create `.planning/phases/27-1-21-0-stable-baseline-split-00-gate-close-wr-01-04-minimum-/27-03-SUMMARY.md`
</output>
