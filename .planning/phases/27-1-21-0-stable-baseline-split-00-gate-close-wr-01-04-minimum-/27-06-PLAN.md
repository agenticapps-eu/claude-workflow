---
phase: 27
plan: 06
type: execute
wave: 2
depends_on: [01, 02, 03, 04, 05]
files_modified:
  - CHANGELOG.md
autonomous: false
requirements: [CHANGELOG-1210, RELEASE-TAG]
must_haves:
  truths:
    - "Root CHANGELOG.md has a new ## [1.21.0] section promoting Phase 26's [Unreleased] entry plus Phase 27's WR-01..04 + baseline changes"
    - "The CHANGELOG makes the SKILL.md-version-vs-release-tag distinction explicit (release tagged v1.21.0 while skill version trails at 1.20.0)"
    - "skill/SKILL.md version stays 1.20.0 (A2 — no migration, no SKILL bump)"
    - "The migration drift test stays GREEN (SKILL.md 1.20.0 == highest migration 0021 to_version 1.20.0)"
    - "A git tag v1.21.0 is created at ship time (manual release action, NOT during execute-phase)"
  artifacts:
    - path: "CHANGELOG.md"
      provides: "## [1.21.0] release section (A2 tag-only)"
      contains: "## [1.21.0]"
  key_links:
    - from: "CHANGELOG.md ## [1.21.0]"
      to: "git tag v1.21.0"
      via: "release marker (tag, not SKILL.md version)"
      pattern: "1\\.21\\.0"
---

<objective>
Promote the parked Phase 26 [Unreleased] CHANGELOG entry and add Phase 27's changes into a new
`## [1.21.0]` section, then (as a MANUAL release action) tag the merge commit `v1.21.0`.

A2 (tag-only) is LOCKED: NO migration, NO SKILL.md bump. "1.21.0" is a git tag + CHANGELOG
release marker ONLY. `skill/SKILL.md` STAYS at 1.20.0 so the drift test
(`test_skill_md_version_matches_latest_migration_to_version`) stays GREEN (highest migration
0021 to_version = 1.20.0). add-observability STAYS at 0.10.0 (D-07e). No VERSION file exists or
is created.

Purpose: Mark the 1.21.0 stable baseline that SPLIT-00 gates on (pin-by-tag per Plan 04's
SPLIT-00 fix).
Output: CHANGELOG ## [1.21.0] section; git tag v1.21.0 flagged for manual creation at ship.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/27-1-21-0-stable-baseline-split-00-gate-close-wr-01-04-minimum-/27-CONTEXT.md
@CHANGELOG.md

<interfaces>
<!-- Current CHANGELOG.md head: the parked Phase 26 entry to PROMOTE -->
CHANGELOG.md:7  "## [Unreleased] — Phase 26 engine + harness + fixture hardening (2026-06-01)"
  + a "Versioning note" blockquote explaining why Phase 26 parked at [Unreleased] (drift-test
    invariant; no migration → SKILL.md stays 1.20.0).
  + "### Fixed (Phase 26 …)" entries (CR-D firewall, harness DUAL pin, 0021/04 TS1038, fail-fast).

<!-- A2 facts (LOCKED, D-07): -->
- skill/SKILL.md version field: STAYS 1.20.0 (line 3). Do NOT edit it.
- Highest migration: migrations/0021-with-cron-and-queue-updates.md, to_version 1.20.0.
- Drift test: migrations/run-tests.sh:2210 — asserts SKILL.md version == highest migration
  to_version. 1.20.0 == 1.20.0 → GREEN. Must STAY green.
- No VERSION file in repo root or add-observability/.
- add-observability stays 0.10.0 (D-07e).
- Release convention: git tag (e.g. v1.21.0). No prior v1.* tags exist yet.

<!-- Phase 27 changes to document in [1.21.0]: -->
  WR-01 (go-test counter double-count fix), WR-02 (supabase-edge _resetForTest cleanup),
  WR-03 (buildSentryOptions direct unit tests × 3 stacks), WR-04 (openrouter entry uses helper),
  PROJECT.md created, STATE/ROADMAP drift refresh, boundary ADR-0035 + run-tests.sh SHARED/
  WORKFLOW annotations (audit-only), SPLIT-01 correction + SPLIT-00 pin-by-tag gate fix.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the ## [1.21.0] CHANGELOG section (A2 tag-only); leave SKILL.md at 1.20.0</name>
  <read_first>
    - CHANGELOG.md (full head — the [Unreleased] block, its Versioning note blockquote, and the Phase 26 Fixed entries you will fold into [1.21.0])
    - skill/SKILL.md (line 3 — confirm version: 1.20.0; you must NOT change it)
    - .planning/phases/27-1-21-0-stable-baseline-split-00-gate-close-wr-01-04-minimum-/27-CONTEXT.md (D-07, D-07a, D-07b, D-07e — the exact A2 framing)
  </read_first>
  <files>CHANGELOG.md</files>
  <action>
    Insert a new `## [1.21.0] — stable baseline (SPLIT-00 gate) — 2026-06-02` section ABOVE the
    Phase 26 [Unreleased] block (or convert [Unreleased] into [1.21.0] and fold its entries in).
    AFTER cutting [1.21.0], PRESERVE an empty `## [Unreleased]` header at the TOP of the changelog
    for the next cycle (codex review finding — avoids a messy next release). Also add a one-line
    note that SPLIT-00 remains BLOCKED until the `v1.21.0` tag actually exists on `main` (the
    CHANGELOG section landing in the PR does not by itself satisfy the gate).
    The section MUST:
      1. Promote the Phase 26 [Unreleased] "### Fixed" entries (CR-D content-marker firewall,
         DUAL harness pin, 0021/04 TS1038 + honest fail-fast, idempotency ADR-0034) into the
         [1.21.0] release.
      2. Add Phase 27's changes under appropriate Keep-a-Changelog headings:
         ### Fixed — WR-01 (run-template-tests.sh go-test counter double-count, lines 633-634;
            firewall lines 128/130/558/559 preserved); WR-02 (supabase-edge D-02a test
            _resetForTest cleanup in finally — test isolation).
         ### Added — WR-03 (direct buildSentryOptions unit tests × 3 stacks: cf-worker, cf-pages,
            openrouter); .planning/PROJECT.md (canonical product identity);
            docs/decisions/0035-shared-extraction-boundaries.md (split-prep boundary ADR);
            migrations/run-tests.sh # SHARED / # WORKFLOW boundary annotations (audit-only).
         ### Changed — WR-04 (openrouter entry routes Sentry options through buildSentryOptions(env)
            — DEF-1 wiring complete in the worked example); STATE.md + ROADMAP.md drift refresh;
            SPLIT-01 gsd-tools.cjs premise correction; SPLIT-00 gate changed to pin-by-tag.
      3. Include an EXPLICIT versioning note in the [1.21.0] section, using the STANDARDIZED
         terminology from PROJECT.md's Versioning policy section: the **release/baseline tag**
         `v1.21.0` leads the **skill version** (`skill/SKILL.md`), which is migration-coupled and
         TRAILS at 1.20.0 until the next migration catches it up — the documented
         migration-locked-version behavior (user rule `versioning-tracks-migrations`; A2 decision
         D-07; handoff open-question #70). Frame this as deliberate policy, NOT inconsistency, and
         cross-reference PROJECT.md. Note that add-observability stays 0.10.0 (its template fixes
         are internal until the deferred DEF-1/DEF-2 re-rev migration, D-07d).
    Do NOT edit skill/SKILL.md. Do NOT create a VERSION file. Do NOT add a migration.
  </action>
  <verify>
    <automated>grep -q '## \[1.21.0\]' CHANGELOG.md && grep -q '1.20.0' CHANGELOG.md && [ "$(grep -m1 'version:' skill/SKILL.md)" = "version: 1.20.0" ] && bash migrations/run-tests.sh 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q '## \[1.21.0\]' CHANGELOG.md` matches (release section exists)
    - An empty `## [Unreleased]` header is preserved at the top for the next cycle: `grep -q '## \[Unreleased\]' CHANGELOG.md` matches AND it appears ABOVE the `## [1.21.0]` line
    - The [1.21.0] section references all four WR items (WR-01..WR-04), PROJECT.md, ADR-0035, and the SPLIT doc fixes: `grep -E 'WR-0[1-4]' CHANGELOG.md` finds all four
    - The [1.21.0] section contains the explicit SKILL.md-1.20.0-vs-tag-v1.21.0 distinction: `grep -A40 '## \[1.21.0\]' CHANGELOG.md | grep -qi '1.20.0'`
    - `grep -m1 'version:' skill/SKILL.md` is exactly `version: 1.20.0` (UNCHANGED — A2)
    - `bash migrations/run-tests.sh` exits 0 with the drift test PASSING (SKILL.md 1.20.0 == migration 0021 to_version)
    - No VERSION file created: `! test -f VERSION` and `! test -f add-observability/VERSION`
    - add-observability version unchanged at 0.10.0
  </acceptance_criteria>
  <done>## [1.21.0] section added with all WR + baseline changes and the tag-vs-skill-version note; SKILL.md still 1.20.0; drift test GREEN; no VERSION file; no migration.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: MANUAL — create git tag v1.21.0 at ship time</name>
  <what-built>
    The 1.21.0 stable baseline: WR-01..04 closed, PROJECT.md created, STATE/ROADMAP refreshed,
    boundary ADR-0035 + run-tests.sh annotations, SPLIT-00/01 doc fixes, and the
    CHANGELOG ## [1.21.0] section. SKILL.md intentionally stays at 1.20.0 (A2 tag-only).
  </what-built>
  <files>(none — git tag is a release action, not a file edit)</files>
  <action>
    MANUAL release action (autonomous: false). After this phase's PR merges to main, create the
    annotated git tag `v1.21.0` on the merge commit and push it. Do NOT run during
    execute-phase and do NOT bump skill/SKILL.md. See <how-to-verify> for the exact commands.
  </action>
  <how-to-verify>
    Tag creation is a RELEASE action — it happens AFTER this phase's PR merges to main, NOT
    during execute-phase. When ready to ship (PR #for-phase-27 merged to main):
      1. Confirm the full suite is GREEN on the merge commit:
         `bash add-observability/templates/run-template-tests.sh && bash migrations/run-tests.sh`
      2. Create the tag on the merge commit (matching the repo's release convention):
         `git tag -a v1.21.0 -m "claude-workflow 1.21.0 — stable baseline (SPLIT-00 gate)"`
         `git push origin v1.21.0`
      3. Verify: `git tag --list 'v1.21.0'` shows the tag, and
         `git describe --tags` / `git show v1.21.0 --stat` points at the 1.21.0 merge commit.
      4. After tagging, the SPLIT-00 cooling-off clock (≥7 days on main, no follow-up patches)
         starts. Downstreams pin to the tag `v1.21.0` (NOT SKILL.md version — per Plan 04's
         pin-by-tag gate fix).
    Do NOT bump skill/SKILL.md to 1.21.0 — the drift test would FAIL (A2 invariant).
  </how-to-verify>
  <resume-signal>Type "tagged" once v1.21.0 is created and pushed (or "defer" to ship the tag later — the CHANGELOG section can land in the PR ahead of the tag).</resume-signal>
  <verify>
    <automated>git tag --list 'v1.21.0'</automated>
  </verify>
  <done>git tag v1.21.0 exists on the merge commit and is pushed; skill/SKILL.md still 1.20.0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | CHANGELOG edit + a manual git tag. No code, no runtime, no input handling. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-06-01 | Tampering | version/drift invariant | mitigate | The acceptance criterion pins SKILL.md at 1.20.0 and requires the drift test to PASS, preventing an accidental SKILL bump that would break the migration-locked-version invariant (the exact failure A2 avoids). |
| T-27-06-02 | (n/a) | release tag | accept | Tag creation is a deliberate manual release step; no attack surface. |

No HIGH-severity threats.
</threat_model>

<verification>
- `grep '## \[1.21.0\]' CHANGELOG.md` present; all WR items referenced; tag-vs-skill note present
- `skill/SKILL.md` version still 1.20.0; `bash migrations/run-tests.sh` drift test PASS
- git tag v1.21.0 created manually at ship (checkpoint)
</verification>

<success_criteria>
- CHANGELOG ## [1.21.0] section documents the WR closures + baseline + the tag-vs-skill-version distinction.
- A2 invariants intact: SKILL.md 1.20.0, drift test GREEN, no migration, no VERSION file, add-observability 0.10.0.
- v1.21.0 tag flagged as a manual release action (autonomous: false).
</success_criteria>

<output>
After completion, create `.planning/phases/27-1-21-0-stable-baseline-split-00-gate-close-wr-01-04-minimum-/27-06-SUMMARY.md`
</output>
