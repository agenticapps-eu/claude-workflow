---
phase: 27
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - add-observability/templates/run-template-tests.sh
  - add-observability/templates/ts-supabase-edge/index.test.ts
autonomous: true
requirements: [WR-01, WR-02]
must_haves:
  truths:
    - "The go-test counter in run-template-tests.sh renders a single integer (not '0\\n0') on zero matches"
    - "The four `grep -oE … || echo \"0\"` firewall expressions in run-template-tests.sh are unchanged (verified by content, not by line number)"
    - "The supabase-edge D-02a determinism test resets singleton state via _resetForTest() in its finally block"
    - "The supabase-edge template suite is GREEN through the harness"
  artifacts:
    - path: "add-observability/templates/run-template-tests.sh"
      provides: "WR-01 go-test counter fix (lines 633-634)"
      contains: "grep -c '^--- PASS'"
    - path: "add-observability/templates/ts-supabase-edge/index.test.ts"
      provides: "WR-02 _resetForTest() cleanup in finally"
      contains: "_resetForTest()"
  key_links:
    - from: "run-template-tests.sh:633-634"
      to: "go-test pass/fail reporting"
      via: "grep -c without double-count"
      pattern: "grep -c '\\^--- PASS'"
---

<objective>
Close PR #60 deferred items WR-01 and WR-02 — two surgical, independent test-harness/test-file
fixes that carry zero new attack surface.

WR-01: the go-test counter at `run-template-tests.sh:633-634` double-counts on zero matches.
WR-02: the supabase-edge D-02a determinism test leaks singleton state because its `finally`
restores `console.log` but never calls the existing `_resetForTest()` seam.

Purpose: Stabilize the template test harness ahead of the 1.21.0 baseline tag.
Output: Two corrected files, supabase-edge suite GREEN, firewall lines provably untouched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-1-21-0-stable-baseline-split-00-gate-close-wr-01-04-minimum-/27-CONTEXT.md
@.planning/phases/27-1-21-0-stable-baseline-split-00-gate-close-wr-01-04-minimum-/27-VALIDATION.md

<interfaces>
<!-- WR-01: the two buggy lines (633-634) and the four DO-NOT-TOUCH firewall lines -->

Buggy (WR-01 target) — run-template-tests.sh:633-634:
```bash
  PASSED=$(echo "$OUTPUT" | grep -c '^--- PASS' || echo "0")
  FAILED=$(echo "$OUTPUT" | grep -c '^--- FAIL' || echo "0")
```
Root cause: `grep -c` ALWAYS prints a count (`0` on no match) AND exits 1 on no match.
`|| echo "0"` therefore appends a SECOND `0`, yielding the captured string `"0\n0"`.

FIREWALL — DO NOT TOUCH — run-template-tests.sh:128, 130, 558, 559:
```bash
128:    PASSED=$(echo "$LINE" | grep -oE '[0-9]+ passed' | grep -oE '^[0-9]+' || echo "0")
130:    F=$(echo "$LINE" | grep -oE '[0-9]+ failed' | grep -oE '^[0-9]+' || echo "0")
558:  PS=$(echo "$OUTPUT" | grep -oE '[0-9]+ passed' | tail -1 | grep -oE '^[0-9]+' || echo "0")
559:  FS=$(echo "$OUTPUT" | grep -oE '[0-9]+ failed' | tail -1 | grep -oE '^[0-9]+' || echo "0")
```
These use `grep -oE`, which prints NOTHING on no match, so `|| echo "0"` is the CORRECT
fallback. A fix that alters any of 128/130/558/559 is WRONG.

<!-- WR-02: existing test seam (zero-arg call resets _testEnv=null; initialized=false; registry=null) -->
From ts-supabase-edge/index.ts:145:
```typescript
export function _resetForTest(env?: InitEnv): void
```
The D-02a test (index.test.ts, Deno.test block starting line 209) currently has:
```typescript
  } finally {
    console.log = origLog;
  }
```
Its own docstring (index.test.ts:206) documents `_resetForTest()  // cleanup` that the code omits.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: WR-01 — drop the double-count on the go-test counter (lines 633-634 only)</name>
  <read_first>
    - add-observability/templates/run-template-tests.sh (read lines 620-650 to see current go-test reporting AND lines 120-135 + 550-565 to confirm the firewall idiom you must NOT touch)
  </read_first>
  <files>add-observability/templates/run-template-tests.sh</files>
  <action>
    Edit ONLY lines 633-634. Change:
      PASSED=$(echo "$OUTPUT" | grep -c '^--- PASS' || echo "0")
      FAILED=$(echo "$OUTPUT" | grep -c '^--- FAIL' || echo "0")
    to:
      PASSED=$(echo "$OUTPUT" | grep -c '^--- PASS' || true)
      FAILED=$(echo "$OUTPUT" | grep -c '^--- FAIL' || true)
    Rationale: `grep -c` already emits `0` on no match; `|| true` only suppresses grep's
    exit-1 (which would otherwise abort under `set -e`) WITHOUT appending a second `0`.
    This is the Claude's-discretion idiom choice per CONTEXT (`|| true` over capture-then-default).
    Do NOT modify lines 128, 130, 558, 559 — those use `grep -oE` (prints nothing on no
    match) where `|| echo "0"` is the correct fallback. Do NOT change any surrounding
    pass/fail reporting logic (the `if [[ $EXIT_CODE -eq 0 ]]` block stays as-is).
  </action>
  <verify>
    <automated>test "$(grep -c '|| echo "0"' add-observability/templates/run-template-tests.sh)" = "4" && [ "$(grep -c 'grep -c .* || echo "0"' add-observability/templates/run-template-tests.sh)" = "0" ] && [ "$(grep -c 'grep -oE.*|| echo "0"' add-observability/templates/run-template-tests.sh)" = "4" ] && grep -Eq "grep -c '\^--- PASS' \|\| true" add-observability/templates/run-template-tests.sh && grep -Eq "grep -c '\^--- FAIL' \|\| true" add-observability/templates/run-template-tests.sh && echo "FIREWALL INTACT (content-based), 633-634 CLEAN" || (echo "FAIL: firewall/counter content mismatch"; exit 1)</automated>
  </verify>
  <acceptance_criteria>
    - CONTENT-BASED (not line-number-dependent): exactly 4 lines contain `|| echo "0"` (`grep -c '|| echo "0"'` = 4), and ALL 4 are `grep -oE …` firewall lines (`grep -c 'grep -oE.*|| echo "0"'` = 4)
    - ZERO `grep -c … || echo "0"` lines remain (`grep -c 'grep -c .* || echo "0"'` = 0) — the buggy double-count idiom is gone
    - `grep -Eq "grep -c '\^--- PASS' \|\| true"` matches (the PASS counter now uses `|| true`)
    - `grep -Eq "grep -c '\^--- FAIL' \|\| true"` matches (the FAIL counter now uses `|| true`)
    - The four `grep -oE … || echo "0"` firewall expressions are unchanged in content (firewall preserved — immune to line-number shifts)
  </acceptance_criteria>
  <done>Lines 633-634 use `|| true`; the four `grep -oE … || echo "0"` firewall expressions unchanged (verified by content); exactly four `|| echo "0"` lines remain and all are `grep -oE`.</done>
</task>

<task type="auto">
  <name>Task 2: WR-02 — add _resetForTest() to the supabase-edge D-02a finally block</name>
  <read_first>
    - add-observability/templates/ts-supabase-edge/index.test.ts (read lines 200-245 — the full D-02a Deno.test block including its docstring at line 206 documenting the omitted cleanup and the finally block at ~231-233)
    - add-observability/templates/ts-supabase-edge/index.ts (read lines 140-160 to confirm the `_resetForTest(env?: InitEnv)` signature — zero-arg call resets `_testEnv=null; initialized=false; registry=null`)
  </read_first>
  <files>add-observability/templates/ts-supabase-edge/index.test.ts</files>
  <action>
    In the `Deno.test("D-02a init() repeated-init determinism: …")` block (starts line 209),
    locate the `finally` block that currently reads:
      } finally {
        console.log = origLog;
      }
    Add a zero-arg `_resetForTest();` call AFTER restoring console.log, so the block becomes:
      } finally {
        console.log = origLog;
        _resetForTest();  // cleanup: reset initialized + singletons so state doesn't bleed (matches docstring line 206)
      }
    Use the EXISTING `_resetForTest` import/helper (already referenced at the top of the test
    via the env-arg call `_resetForTest({ SENTRY_DSN: "dsn-a", … })` on entry). Do NOT add a
    new test-only seam (carries forward Phase 26 codex HIGH-3). Do NOT change any assertions
    in the block. This is the ONLY edit — ~1 line added.
  </action>
  <verify>
    <automated>cd add-observability/templates && bash run-template-tests.sh supabase-edge 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - The D-02a `finally` block contains `_resetForTest()` on a line AFTER `console.log = origLog`
    - `grep -A3 'console.log = origLog;' add-observability/templates/ts-supabase-edge/index.test.ts` shows `_resetForTest()` within the finally
    - `bash add-observability/templates/run-template-tests.sh supabase-edge` exits 0 (supabase-edge suite GREEN)
    - No new `_set*`/`_test*` export added to index.ts (HIGH-3 honored)
  </acceptance_criteria>
  <done>`_resetForTest()` called in the D-02a finally; supabase-edge harness run GREEN; no new test seam.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | WR-01 edits a test harness shell script; WR-02 edits a test file. No production request path, no untrusted input crosses either change. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-01-01 | Tampering | run-template-tests.sh counter | accept | Test-harness reporting only; no consumer-facing behavior. The firewall-line acceptance check (grep returns exactly 128/130/558/559) prevents an accidental over-broad edit that could mask a real counter regression. |
| T-27-01-02 | (n/a) | supabase-edge test isolation | accept | Change strengthens test isolation (prevents state bleed). No attack surface; no new seam (HIGH-3 honored). |

No HIGH-severity threats. No new attack surface introduced.
</threat_model>

<verification>
- Content-based firewall check: exactly 4 `|| echo "0"` lines, all `grep -oE`; zero `grep -c … || echo "0"`; lines 633-634 use `|| true` (immune to line-number shifts)
- `bash add-observability/templates/run-template-tests.sh supabase-edge` → exit 0
</verification>

<success_criteria>
- WR-01: go-test counter no longer double-counts; firewall lines provably untouched.
- WR-02: supabase-edge D-02a test resets singleton state; supabase-edge suite GREEN.
</success_criteria>

<output>
After completion, create `.planning/phases/27-1-21-0-stable-baseline-split-00-gate-close-wr-01-04-minimum-/27-01-SUMMARY.md`
</output>
