---
phase: 27
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - migrations/run-tests.sh
  - docs/decisions/0035-shared-extraction-boundaries.md
  - SPLIT-01-agenticapps-shared.md
  - SPLIT-00-PREREQUISITES.md
autonomous: true
requirements: [SPLIT-PREP-AUDIT, SPLIT-00-GATE-FIX]
must_haves:
  truths:
    - "migrations/run-tests.sh logical sections/helpers are annotated # SHARED or # WORKFLOW (annotation only, zero behavior change)"
    - "The migration drift test still PASSES after annotation (no behavior change)"
    - "ADR-0035 records the shared/workflow boundary as the canonical reference SPLIT-01 Phase C executes against"
    - "SPLIT-01 carries a correction note: the extraction target is migrations/run-tests.sh + migration content, NOT bin/gsd-tools.cjs (which is the GSD framework, not this repo)"
    - "SPLIT-00's downstream gate is changed from pin-by-SKILL-version (1.21.0) to pin-by-tag (git tag v1.21.0 / commit SHA)"
  artifacts:
    - path: "migrations/run-tests.sh"
      provides: "# SHARED / # WORKFLOW boundary annotations (audit-only)"
      contains: "# SHARED"
    - path: "docs/decisions/0035-shared-extraction-boundaries.md"
      provides: "ADR-0035 boundary record (Status: Accepted)"
      contains: "Accepted"
    - path: "SPLIT-01-agenticapps-shared.md"
      provides: "gsd-tools.cjs premise correction note"
      contains: "run-tests.sh"
    - path: "SPLIT-00-PREREQUISITES.md"
      provides: "pin-by-tag gate fix (D-07c)"
      contains: "v1.21.0"
  key_links:
    - from: "docs/decisions/0035-shared-extraction-boundaries.md"
      to: "migrations/run-tests.sh annotations"
      via: "canonical boundary reference for SPLIT-01 Phase C"
      pattern: "run-tests.sh"
---

<objective>
Split-prep groundwork — AUDIT + ANNOTATE + DOCUMENT ONLY. No code is moved this phase; the
7-day cooling-off baseline must stay stable (any behavior change resets the SPLIT-00 clock).

Per RESEARCH Blocker B (resolved B1): `bin/gsd-tools.cjs` does NOT exist in this repo — it's
the GSD framework at `~/.claude/get-shit-done/`. The repo's real shared-able migration infra
is `migrations/run-tests.sh` (~2500 lines). This plan:
  1. Annotates `migrations/run-tests.sh` sections/helpers `# SHARED` or `# WORKFLOW`.
  2. Writes ADR-0035 capturing the boundary (Accepted).
  3. Adds a correction note to SPLIT-01 (its gsd-tools.cjs premise is wrong).
  4. Fixes SPLIT-00's gate from pin-by-SKILL-version to pin-by-tag (D-07c) — required because
     under A2 the SKILL.md version stays 1.20.0, making the original gate unsatisfiable.

Purpose: Pre-decide the SPLIT-01 boundary so the future extraction is mechanical; unblock the
SPLIT-00 checklist.
Output: 1 annotated script (no behavior change), 1 new ADR, 2 corrected SPLIT docs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-1-21-0-stable-baseline-split-00-gate-close-wr-01-04-minimum-/27-CONTEXT.md
@.planning/phases/27-1-21-0-stable-baseline-split-00-gate-close-wr-01-04-minimum-/27-RESEARCH.md
@SPLIT-00-PREREQUISITES.md
@SPLIT-01-agenticapps-shared.md

<interfaces>
<!-- Boundary test (from SPLIT-01): "if agenticapps-observability would need it to apply a
     migration → SHARED; if only useful for THIS repo's specific migrations or GSD planning
     → WORKFLOW." -->

migrations/run-tests.sh known top-level helpers/sections (verify exact set during execution):
  _runtests_do_cleanup()  (line ~76)   → SHARED (generic harness lifecycle)
  extract_to()            (line ~92)   → SHARED (generic fixture util)
  setup_fixture()         (line ~105)  → SHARED (fixture-runner harness)
  run_check()             (line ~133)  → SHARED (generic pass/fail helper)
  assert_check()          (line ~144)  → SHARED (generic assertion helper)
  test_preflight_verify_paths()        (line ~1746) → classify per boundary test
  test_meta_destinations_consistency() (line ~1942) → classify per boundary test
  test_skill_md_version_matches_latest_migration_to_version() (line ~2210) → MECHANISM is SHARED
    (the drift-test RUNNER is generic framework machinery), but the COUPLING POLICY it enforces
    ("SKILL.md version == latest migration to_version") is repo-specific WORKFLOW policy, not
    repo-agnostic infra (codex review finding). Annotate so the two are distinguishable — e.g.
    `# SHARED — drift-test runner mechanism` on the harness/runner, with an inline note that the
    specific version-coupling rule is workflow-owned policy (see ADR-0035).
  test_sigterm_mid_apply_preserves_state() (line ~2243) → classify per boundary test
  <plus per-migration apply/verify bodies tied to specific 00NN migrations> → WORKFLOW

ADR numbering: highest existing = docs/decisions/0034-observability-init-singleton-invariant.md.
Next free = 0035.

<!-- SPLIT-01 wrong premise (lines ~44-55, "Shared GSD-tools subset"): claims bin/gsd-tools.cjs
     is split between shared vs workflow. That file is the GSD FRAMEWORK, not this repo. -->

<!-- SPLIT-00 gate to fix (line ~54): "Workflow installed marker matches:
     .claude/skills/agentic-apps-workflow/SKILL.md version: 1.21.0" → unsatisfiable under A2
     (SKILL.md stays 1.20.0). Change to pin-by-tag. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Annotate migrations/run-tests.sh with # SHARED / # WORKFLOW (audit-only, zero behavior change)</name>
  <read_first>
    - migrations/run-tests.sh (read the structure: the helper functions at lines ~76-150, the drift test at ~2210, and a sampling of the per-migration test bodies — enough to classify each logical section per the boundary test)
    - SPLIT-01-agenticapps-shared.md (the "What lives in agenticapps-shared" section + the boundary test definition, to align annotations with the SPLIT-01 contract)
    - .planning/phases/27-1-21-0-stable-baseline-split-00-gate-close-wr-01-04-minimum-/27-CONTEXT.md (D-06 RETARGETED, lines 126-141 — the exact SHARED vs WORKFLOW definitions)
  </read_first>
  <files>migrations/run-tests.sh</files>
  <action>
    Add COMMENT-ONLY annotations above each logical section/helper in migrations/run-tests.sh.
    Use the exact marker strings `# SHARED` and `# WORKFLOW` (with a short rationale clause),
    e.g.:
      # SHARED — generic fixture-runner harness; agenticapps-observability needs this to apply its migrations
      setup_fixture() { … }
      # WORKFLOW — verify body specific to migration 0019 content; stays in claude-workflow
      <per-0019 test function>
    Classification rule (boundary test): SHARED = the dispatcher, the drift-test RUNNER
    mechanism (`test_skill_md_version_matches_latest_migration_to_version` as generic
    machinery), the fixture-runner harness (`setup_fixture`, `extract_to`, `run_check`,
    `assert_check`, `_runtests_do_cleanup`), logging/pass-fail helpers, generic apply/verify
    utils. WORKFLOW = per-migration (00NN) setup/verify bodies tied to specific migration content
    + any GSD-planning-specific checks + the drift-test's version-COUPLING POLICY (the specific
    rule "SKILL.md == latest migration to_version" is repo-owned, not repo-agnostic — codex
    finding). For the drift test, annotate the runner `# SHARED` and add an inline note that its
    coupling rule is workflow-owned policy (cross-reference ADR-0035).
    Annotate at minimum every top-level function and every major section header.
    CRITICAL: COMMENTS ONLY. Do NOT change any executable line, function body, control flow,
    or test logic. The drift test and full suite MUST stay GREEN. This is the cooling-off
    invariant — zero behavior change.
  </action>
  <verify>
    <automated>bash migrations/run-tests.sh > /tmp/rts-out.txt 2>&1; RTSEX=$?; tail -5 /tmp/rts-out.txt; [ "$RTSEX" -eq 0 ] && grep -c '# SHARED' migrations/run-tests.sh && grep -c '# WORKFLOW' migrations/run-tests.sh</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '# SHARED' migrations/run-tests.sh` ≥ 5 (harness helpers + drift test + dispatcher annotated)
    - `grep -c '# WORKFLOW' migrations/run-tests.sh` ≥ 1 (at least one migration-specific body annotated)
    - `bash migrations/run-tests.sh` exits 0 — full suite GREEN (zero behavior change)
    - The drift test specifically passes: run-tests.sh output shows `test_skill_md_version_matches_latest_migration_to_version` PASS (SKILL.md still 1.20.0)
    - `git diff --stat migrations/run-tests.sh` shows only added comment lines (no executable-line deletions): verify the diff hunks are `+# SHARED`/`+# WORKFLOW`-only additions
  </acceptance_criteria>
  <done>run-tests.sh annotated with ≥5 # SHARED and ≥1 # WORKFLOW markers; full suite + drift test GREEN; diff is comment-only additions.</done>
</task>

<task type="auto">
  <name>Task 2: Write ADR-0035 + correct SPLIT-01 premise + fix SPLIT-00 gate to pin-by-tag</name>
  <read_first>
    - docs/decisions/0034-observability-init-singleton-invariant.md (read for ADR format/structure to mirror: title, Status, Context, Decision, Consequences)
    - migrations/run-tests.sh (the annotations you added in Task 1 — the ADR references them)
    - SPLIT-01-agenticapps-shared.md (lines ~44-55 "Shared GSD-tools subset" — the wrong gsd-tools.cjs premise to correct)
    - SPLIT-00-PREREQUISITES.md (line ~54 — the "SKILL.md version: 1.21.0" gate to change to pin-by-tag)
    - .planning/phases/27-1-21-0-stable-baseline-split-00-gate-close-wr-01-04-minimum-/27-CONTEXT.md (D-06a, D-06b, D-07c)
  </read_first>
  <files>docs/decisions/0035-shared-extraction-boundaries.md, SPLIT-01-agenticapps-shared.md, SPLIT-00-PREREQUISITES.md</files>
  <action>
    (a) Create `docs/decisions/0035-shared-extraction-boundaries.md` mirroring the ADR-0034
        format. Status: Accepted. Content:
          - Context: the impending 3-repo split (SPLIT-00/01/02); the need to pre-decide the
            shared/workflow boundary so SPLIT-01 Phase C is mechanical; the Blocker-B
            correction (the real shared artifact is `migrations/run-tests.sh`, NOT
            `bin/gsd-tools.cjs` which is the GSD framework not in this repo).
          - Decision: the SHARED set (dispatcher, drift-test runner, fixture-runner harness,
            pass/fail + logging helpers, generic apply/verify utils) vs the WORKFLOW set
            (per-migration 00NN setup/verify bodies, GSD-planning-specific checks), with the
            boundary test verbatim ("if agenticapps-observability would need it to apply a
            migration → SHARED; else WORKFLOW"). Reference the `# SHARED`/`# WORKFLOW`
            annotations now in run-tests.sh as the canonical line-level map.
          - MECHANISM vs POLICY (codex review finding — call this out explicitly in the ADR):
            the drift-test *runner* is a SHARED mechanism, but the version-coupling *policy* it
            enforces ("SKILL.md version == latest migration to_version") is repo-specific WORKFLOW
            policy. SPLIT-01 may extract the runner, but the coupling rule stays owned by the
            consumer repo (it encodes that repo's `versioning-tracks-migrations` discipline, not a
            generic invariant). The ADR must not let SPLIT-01 extract the policy as if it were
            shared infra.
          - Consequences: SPLIT-01 Phase C extracts the SHARED set; no code moves in Phase 27
            (annotation only, cooling-off preserved). Link SPLIT-00 and SPLIT-01.
    (b) Edit `SPLIT-01-agenticapps-shared.md`: add a clearly-marked CORRECTION note (e.g. a
        `> **CORRECTION (Phase 27, ADR-0035):**` blockquote) at the top of the "Shared
        GSD-tools subset" section stating: the extraction target is `migrations/run-tests.sh`
        + migration content, NOT `bin/gsd-tools.cjs`. `bin/gsd-tools.cjs` is the GSD framework
        (`~/.claude/get-shit-done/bin/`), installed independently, NOT claude-workflow's code;
        its function list (`phase-plan-index`, `state begin-phase`, `init phase-op`, etc.)
        belongs to the framework and is out of scope for the claude-workflow repo split. Point
        readers at ADR-0035. Do NOT delete the original section — annotate it as superseded.
    (c) Edit `SPLIT-00-PREREQUISITES.md`: change the downstream gate condition (the
        "Workflow installed marker matches: `.claude/skills/agentic-apps-workflow/SKILL.md`
        `version: 1.21.0`" line under "cparx upgraded to 1.21.0") to PIN-BY-TAG: downstreams
        pin to the git tag `v1.21.0` / commit SHA, NOT the SKILL.md version (which stays
        1.20.0 under A2 — no migration, no SKILL bump). Add a one-line rationale referencing
        D-07/A2 and the migration-locked-version behavior. Apply the same pin-by-tag framing
        to the callbot and fx-signal-agent gate lines if they reference SKILL.md version.
        ALSO add a DOWNSTREAM-EVIDENCE RULE (codex review finding — SC-8/SC-9 are governance
        criteria, not just doc edits): each downstream must RECORD the source tag `v1.21.0` +
        commit SHA of the installed workflow in a durable place (its phase doc, STATE file, or
        upgrade PR body) as the proof-of-baseline. Explicitly state that the installed
        `SKILL.md` version (1.20.0) is NOT acceptable evidence of the 1.21.0 baseline — because
        under A2 it does not advance. This closes the "what version is installed?" gap where an
        auditor reading only SKILL.md would conclude 1.20.0.
  </action>
  <verify>
    <automated>test -f docs/decisions/0035-shared-extraction-boundaries.md && grep -qi 'Accepted' docs/decisions/0035-shared-extraction-boundaries.md && grep -q 'run-tests.sh' docs/decisions/0035-shared-extraction-boundaries.md && grep -qi 'CORRECTION' SPLIT-01-agenticapps-shared.md && grep -qi 'pin-by-tag\|git tag\|v1.21.0' SPLIT-00-PREREQUISITES.md && echo "DOCS OK" || (echo "FAIL"; exit 1)</automated>
  </verify>
  <acceptance_criteria>
    - `test -f docs/decisions/0035-shared-extraction-boundaries.md` succeeds
    - `grep -qi 'Status.*Accepted\|Accepted' docs/decisions/0035-shared-extraction-boundaries.md` matches
    - ADR-0035 references `migrations/run-tests.sh` and the boundary test, and notes the gsd-tools.cjs-is-framework correction
    - ADR-0035 explicitly separates SHARED mechanism (drift-test runner) from repo-specific POLICY (the version-coupling rule): `grep -qi 'mechanism' docs/decisions/0035-shared-extraction-boundaries.md` AND `grep -qi 'policy' docs/decisions/0035-shared-extraction-boundaries.md` both match
    - SPLIT-01: `grep -qi 'CORRECTION' SPLIT-01-agenticapps-shared.md` matches AND the note says the target is `run-tests.sh` + migration content, not `bin/gsd-tools.cjs`
    - SPLIT-00: the cparx gate line no longer requires `SKILL.md version: 1.21.0`; instead references the git tag `v1.21.0` / commit pin (`grep -qi 'git tag\|v1.21.0' SPLIT-00-PREREQUISITES.md` matches in the gate context)
    - SPLIT-00 carries a downstream-evidence rule: downstreams record source tag + commit SHA as proof-of-baseline, and SKILL.md version is explicitly NOT acceptable evidence (`grep -qi 'commit SHA\|commit pin' SPLIT-00-PREREQUISITES.md` AND a statement that installed SKILL.md is not proof of 1.21.0)
    - ADR-0035 is the next free number (0035; no clash with 0034)
  </acceptance_criteria>
  <done>ADR-0035 Accepted and referencing the annotations; SPLIT-01 corrected; SPLIT-00 gate now pin-by-tag.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Comment-only annotations to a test script + new/edited documentation. No executable change, no runtime, no input handling. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-04-01 | Tampering | run-tests.sh annotations | accept | Comment-only; the acceptance criterion (`git diff` shows only `+# SHARED`/`+# WORKFLOW` additions, full suite GREEN) guards against an accidental executable-line change that could weaken the drift test or harness. |
| T-27-04-02 | (n/a) | SPLIT docs | accept | Documentation accuracy. No attack surface. |

No HIGH-severity threats. The cooling-off invariant (zero behavior change) is itself the safety property, enforced by the comment-only-diff acceptance check.
</threat_model>

<verification>
- `bash migrations/run-tests.sh` exits 0 (drift test + suite GREEN; annotation introduced no behavior change)
- ADR-0035 exists + Accepted + references run-tests.sh
- SPLIT-01 correction present; SPLIT-00 gate is pin-by-tag
</verification>

<success_criteria>
- run-tests.sh annotated (SHARED/WORKFLOW) with zero behavior change; suite GREEN.
- ADR-0035 captures the canonical boundary for SPLIT-01 Phase C.
- SPLIT-01 premise corrected; SPLIT-00 gate satisfiable under A2 (pin-by-tag).
</success_criteria>

<output>
After completion, create `.planning/phases/27-1-21-0-stable-baseline-split-00-gate-close-wr-01-04-minimum-/27-04-SUMMARY.md`
</output>
